{
  "schema_version": "2.0",
  "tests": [
    {
      "nodeid": "l1_codec/test_varint.py::test_remaining_length_roundtrip",
      "description": "Remaining Length varint round-trips.",
      "layer": "l1",
      "req_ids": ["REQ-FRAME-001"],
      "gate": "task",
      "required_contracts": ["codec-cli"],
      "build_variant_ids": ["san"]
    },
    {
      "nodeid": "l2_behavior/test_connect.py::test_bad_protocol_level",
      "description": "Unsupported protocol levels receive rc=1 and disconnect.",
      "layer": "l2",
      "req_ids": ["REQ-CONNECT-002"],
      "gate": "task",
      "required_contracts": ["broker-process"],
      "build_variant_ids": ["san"]
    }
  ]
}
