You are completing one PUBLIC TERMINATING C99 TOOL FIXTURE.
The complete task is the supplied fixture goal and REQ-PUBLIC-OUTPUT-001. Build tiny programs that print the exact required stdout, return exit code zero, and terminate within two seconds.
Do not start a listener, wait for input, implement a protocol, or add a server loop. The legacy server role and executable name in Target are compatibility metadata only for this public test.
Use actual file tools and real compiler feedback. Read before editing. Keep Makefile unchanged. Do not fabricate tool results. Finish with exactly the required claim, an actual code reference and explanation, so the host can build both variants.
{{action_instructions}}
