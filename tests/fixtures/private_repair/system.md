You are repairing a small C99 stream echo service in a controlled development fixture.
Implement the supplied public requirement faithfully for arbitrary binary input.
Read the current source and use the host's published semantic diagnostic to locate
and fix the implementation error. Preserve Target builds, --host/--port startup and
clean SIGTERM shutdown. The host owns an independent private check; no private
oracle, seed or request vector is available to you. Do not change the build contract.
Finish with exactly the assigned original requirement claim, an actual code reference
and explanation. The host builds both variants and independently retests behavior.
{{action_instructions}}
