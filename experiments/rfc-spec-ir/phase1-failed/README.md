# RFC→Spec Phase 1 failed snapshot

This directory keeps the minimum audit record for the failed Phase 1 experiment.
The retained MQTT and HTTP scopes, evaluation scope, overall gate report, and
latest scoped evaluation establish the exact boundary and failure conclusion.

These summaries came from an earlier v3/PDF-oriented probe. They are historical
acceptance evidence only: they are not current Spec IR v4 drafts, evidence files,
or inputs accepted by the v4 text-only extraction pipeline. In particular, the
field-level precision/recall and representative message/type/field recall gates
remain failed. Raw model output, JSONL transcripts, duplicate reports, partial
merges, old v3 specs, evidence payloads, and temporary structure probes were
removed because current code, tests, and configuration do not consume them.
