# M1-4a2 基准提示词实验（设计 7.0.0）

本目录是设计 7.0.0 下唯一有效的 ArchitecturePlanner 基准提示词实验记录入口。

- 旧实验全部保留原文件和原运行叶子，只作为诊断历史，不进入新实验的 2/3 统计、版本比较、选择或交接。
- 旧规则下尚未开始的 `attempt_005` 已停止，不再执行。
- 新实验只使用配置中的一个逻辑模型槽位；当前配置把该槽位指向 Claude，但模型名称不是设计门槛。
- V0～V2 每版各三条样本，总上限 9 条初始生成。普通失败、截断或服务不可用只影响当前样本。
- 当前有效运行根为 `ee5a23a8fcbaa5dc273f36c0365707fac5a9684f050463fc32ec7fd6bc3b67a5`。V0 已完成但未达门槛；V1 在明确授权的样本级基础设施重试后达到 p2 3/3 并被选中；按提前停止规则未运行 V2。
- 负责人已批准 V1，M1-4c 交接已发布。运行证据位于 `runs/_calibration/s4-architecture/<运行根>/prompt-development/`，版本控制内的汇总与报告位于 `../m1-4a2-patch-calibration-rerun/results/`。
- 本实验只证明基准可用性，不声明生产质量；生产质量证明仍由后续 D1.3 范围承担。

初始化时的根标识、哈希和旧记录范围见 `00-design-7-reset.json`；该文件保留为当时的不可变初始化记录。
