# P0 串行基线固化

记录日期：2026-09-13。原始运行、源码、交付与报告保持不变。
只读汇总位于 `runs/p0-baseline-inventory/`；上一轮最终交付与候选摘要见
`project_docs/protocol_expansion.md`、`experiments/protocol-expansion/generation-results.json`。

| 样本 | 实际结果 | 墙钟分钟 | 模型等待秒 | 智能体命令秒 | 主机任务构建／检查秒 | 最终导出构建／检查秒 |
|---|---|---:|---:|---:|---:|---:|
| 首次 DeepSeek MQTT 最小成功 56f67d18 | 通过其原始检查 | 147.36 | 7521.75 | 137.70 | 18.29 | 3.88 |
| 扩展 MQTT e4b27709 | 20 场景、双变体通过 | 69.39 | 3758.38 | 261.06 | 61.19 | 46.58 |
| HTTP d0b839c4 | 12 场景、双变体通过 | 19.74 | 1053.25 | 95.88 | 16.91 | 9.54 |

墙钟从原 Run 的创建到最终状态更新时间，包含暂停或恢复；模型等待累加每次调用的真实 elapsed，
含失败尝试。命令时间来自持久化 ExecResult.duration_ms；不把嵌套客户端时间重复加入容器总时间。
历史记录没有分别计时的 Python 文件操作、checkpoint、控制器开销保留为未分配时间，不能臆造。
后续优化使用同一口径，并可新增单调时钟事件；新增计时字段不能反填历史记录。

| 当前 CNY 样本／模型 | 调用数 | 模型等待秒 | 输入／输出 token | 已结算 CNY |
|---|---:|---:|---:|---:|
| MQTT Flash | 678 | 2531.72 | 15314349／495240 | 4.56980712 |
| MQTT Pro | 72 | 1226.65 | 2480031／78435 | 5.34977040 |
| HTTP Flash | 170 | 738.25 | 2658370／149076 | 1.01218312 |
| HTTP Pro | 38 | 315.00 | 601120／18476 | 1.03963080 |

MQTT 占用 ¥9.91957752；HTTP 已结算 ¥2.05181392，另保留 ConnectTimeout 未知调用
¥0.236486，占用 ¥2.28829992。首次最小样本是 USD 历史账本：已结算 $21.49246926、
占用 $21.78629862；缺少原请求时间与缓存细分，不能把旧账本金额改标人民币。
它作为链路与时间证据保留；人民币比较使用后续两份已完整记录的 CNY 样本。

MQTT 最耗时任务为 requirements:002：模型等待 1064.42 秒、68 次调用、2 个会话、
¥3.96247906，约占该运行已结算费用 40%。其次 requirements:003 等待 348.66 秒、
requirements:001 等待 331.87 秒。Flash 承担初始普通任务；Pro 承担 shared-wire、
final-integration 及会话耗尽后的任务。HTTP 没有会话耗尽重试；最耗时的 final-integration
与 shared-wire 分别等待 160.86、154.14 秒。每任务原始计数与费用在汇总 JSON 中保留。

MQTT 格式无效 192 次，占用模型等待 675.92 秒；HTTP 无效 52 次、233.42 秒。
XML 分别 152／41，其他类型及模型细分见上一轮结构化结果。格式错误不执行工具；
“再次决策”、供应商网络重试和任务新会话分别记录，不合并成一个无定义的重试数。

这三个样本的检查脚本曾对代码智能体可读。它们证明该版本链路和规定场景可行，
不能作为私有验收隔离或三次同候选稳定性证据。首次样本曾获授权切换运行配置；
后两份样本共享最终 runtime，但协议输入不同，不能互称重复样本。

冻结规则：后续对比同时记录 runtime、提示词、配置、Spec、Target、完整 Acceptance 资产、
镜像 ID 和实验驱动器摘要；任一不同均标记为不同候选。每次私有验收种子与实际交互独立记录，
不把不同合法随机向量误称为修改了冻结检查标准。

P0 剩余门槛：公开通用工具会话夹具和私有 MQTT／HTTP 验收实现后冻结其摘要；
汇总脚本固化，并验证按任务的构建与检查耗时细分。当前文件不是 P0–P2 完成声明。
