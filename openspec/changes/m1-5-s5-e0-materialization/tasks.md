## 1. Baseline and scope lock

- [ ] 1.1 Re-read `implementation-brief.md` and the referenced design sections, inspect the current diff without modifying `project_docs/`, and run the recorded M1-4d/S4 baseline before code changes. Requirements: all three delta capabilities' prerequisite and preservation clauses. Design: Context and decisions 1/7. Paths: existing `nepa/run_store.py`, `nepa/stages/s4_planning.py`, `nepa/speclib/plan_revision.py`, `nepa/speclib/plan_state.py`, and their tests only. Verify: `uv run pytest -q tests/test_run_store.py tests/test_plan_revision.py tests/test_plan_revision_state.py tests/test_plan_activation.py tests/test_s4_publication.py tests/test_s4_checkpoint_resume.py tests/test_s4_commitment.py`; record any failure as an unverified prerequisite or concrete §10.2.1 blocker before continuing.
- [ ] 1.2 Confirm the existing MQTT and non-MQTT S4 inputs can traverse the current deterministic completion/publication helpers without changing the ArchitecturePlanner prompts, Schema input contract, selected bundle, or experiment lineage. Requirement: `s5-e0-materialization / The S5 rendering view is complete, canonical, and derived without Plan mutation`. Design: Context, decision 7. Paths: `tests/conftest.py`, `tests/fixtures/non_mqtt_application/`, existing S4 helpers/tests. Verify: focused fixture-generation test command added for this check plus `git diff -- nepa/agents/prompts nepa/calibration project_docs` showing no implementation change; stop and report if either real S4-produced fixture exposes a principle-level missing input that S5 cannot derive.

## 2. Fresh-run v2 persistence contract

- [ ] 2.1 Add closed Run v4, file-ledger v2, and revision-ledger v2 Schemas/examples, including all eight typed payload variants, independent `event_seq`/`revision_seq`, S5-frozen versus S6-owned realized-row conditions, and an empty initial ledger. Requirements: `plan-revision-infrastructure / File and revision ledgers are closed append-only contracts` and `s4-initial-plan-publication / Initial publication artifacts are closed and canonical`. Design: decisions 1/6. Paths: `nepa/schemas/run.schema.json`, `file-ledger.schema.json`, `revision-ledger.schema.json`, affected examples and schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s4_contracts.py tests/test_plan_revision.py -k 'schema or ledger or event or file'` with negative cases for aliases, illegal conditional fields, payload cross-contamination, gaps, and future event refs.
- [ ] 2.2 Convert canonical revision helpers to typed event envelopes, add latest-activation lookup and idempotent E0 `epoch_materialized` append, and retain complete-chain tamper/aggregate validation without enabling later runtime producers. Requirement: `plan-revision-infrastructure / File and revision ledgers are closed append-only contracts`. Design: decisions 1/6. Paths: `nepa/speclib/plan_revision.py`, `tests/test_plan_revision.py`. Verify: `uv run pytest -q tests/test_plan_revision.py -k 'event or chain or materialized or activation or tamper'` including a non-activation tail and conflicting duplicate E0 event.
- [ ] 2.3 Update the dormant M1-4d activation/recovery path to read/write the typed `revision_activated` envelope and latest-activation semantics, solely to keep the fresh-run contract coherent; do not add triggers, candidate generation, or production revision admission. Requirements: modified `plan-revision-infrastructure` plus preserved existing activation/recovery requirements. Design: decision 1. Paths: `nepa/run_store.py`, activation Schema/example if directly affected, `tests/test_plan_activation.py`, `tests/test_run_store.py`. Verify: `uv run pytest -q tests/test_plan_activation.py tests/test_run_store.py -k 'revision or activation or recovery or event'` and assertions that no non-M1-5 producer becomes reachable.
- [ ] 2.4 Cut new spec runs and S4 publication over to Run v4 plus empty v2 ledgers while preserving the delivered Plan bytes/semantics, 1.0.0/E0/0 pointer, S4 logical commit order, and absence of materialization facts. Requirements: `s4-initial-plan-publication / Initial publication artifacts are closed and canonical`. Design: decision 1. Paths: `nepa/run_store.py`, `nepa/stages/s4_planning.py`, current config/example fixtures, `tests/test_run_initialization.py`, `tests/test_s4_publication.py`, `tests/test_s4_contracts.py`. Verify: `uv run pytest -q tests/test_run_initialization.py tests/test_s4_publication.py tests/test_s4_contracts.py tests/test_s4_checkpoint_resume.py` with explicit rejection of old-version automatic conversion.
- [ ] 2.5 Generalize completed-S4 verification to compare the pointer with the most recent accepted activation, allow a valid E0 materialization or other non-activation tail, and validate current epoch/binding facts without rewriting the immutable initial Plan anchor. Requirement: `s4-initial-plan-publication / The S4 seal carries independent typed anchors`. Design: decisions 1/6. Paths: `nepa/stages/s4_planning.py`, `nepa/speclib/plan_state.py` only if its direct reader changes, focused S4/revision tests. Verify: `uv run pytest -q tests/test_s4_publication.py tests/test_s4_checkpoint_resume.py tests/test_plan_revision_state.py -k 'completed or anchor or event or materialized'` for no-activation and non-activation-tail cases.

## 3. Pure Blueprint expansion and renderability compiler

- [ ] 3.1 Promote concrete `file_rules[]` expansion to one public deterministic Delivery Compiler helper and replace S4's private path expansion with it, preserving canonical order and all path/message/type closure errors. Requirement: `s5-e0-materialization / S5 admission validates the complete sealed input before workspace effects`. Design: decision 2. Paths: `nepa/speclib/delivery.py`, `nepa/stages/s4_planning.py`, `tests/test_delivery_blueprint.py`, S4 ledger tests. Verify: `uv run pytest -q tests/test_delivery_blueprint.py tests/test_s4_publication.py -k 'expand or ledger or path'` including none/per-message/per-type and collision cases.
- [ ] 3.2 Implement the finite, full-consumption C99 export parser and normalizer for authorized function/type/constant declarations, declared-symbol dependencies, fixed arrays, structs/enums/typedefs, and explicit rejection of directives, bodies, extern objects, VLA, function pointers, variadics, or unconsumed tokens. Requirement: `s5-e0-materialization / The S5 rendering view is complete, canonical, and derived without Plan mutation`. Design: decision 2. Paths: new `nepa/speclib/materialization.py`, new focused `tests/test_s5_rendering_view.py`. Verify: `uv run pytest -q tests/test_s5_rendering_view.py -k 'declaration or parser or type or function or constant'` with one positive and one negative case for every grammar boundary.
- [ ] 3.3 Implement `derive_rendering_view` with unique symbol/header binding, dependency DAG and include projection, normalized parameters/return/stub rule, provider-task implementation-source intersection, and Blueprint entry binding; ensure the function is pure, canonical, and never mutates Plan or consults paths/prose/protocol identity heuristically. Requirement: `s5-e0-materialization / The S5 rendering view is complete, canonical, and derived without Plan mutation`. Design: decision 2. Paths: `nepa/speclib/materialization.py`, `tests/test_s5_rendering_view.py`. Verify: `uv run pytest -q tests/test_s5_rendering_view.py -k 'view or implementation or dependency or entry or deterministic'` including ambiguous/no implementation, undeclared symbol, cycle, no main, duplicate main, and replay cases.
- [ ] 3.4 Implement the side-effect-free completed-E0 semantic validator over Plan/Blueprint/rendering view/manifest/map/ledger/receipts/event/workspace facts so later stage and replay checks use one closed cross-artifact rule set. Requirements: `s5-e0-materialization / E0 publishes closed manifests, ledgers, receipts, and stage anchors` and `Completed E0 replay is a zero-change operation`. Design: decisions 2/6. Paths: `nepa/speclib/materialization.py`, focused validator tests. Verify: `uv run pytest -q tests/test_s5_artifacts.py -k 'complete or binding or drift or workspace'` with each anchor independently corrupted.

## 4. Artifact projections and deterministic templates

- [ ] 4.1 Add closed artifact-manifest v2 and contract-map v2 Schemas/examples matching the exact expanded file, artifact graph, export/declaration, implementation-file, creator, mutability, owner, Plan, Blueprint, version, and epoch projections. Requirement: `s5-e0-materialization / E0 publishes closed manifests, ledgers, receipts, and stage anchors`. Design: decisions 2/6. Paths: new files under `nepa/schemas/` and `nepa/schemas/examples/`, `tests/test_schema_examples.py`, new `tests/test_s5_artifacts.py`. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s5_artifacts.py -k 'manifest or contract_map or schema'` with missing/extra/wrong-owner negative rows.
- [ ] 4.2 Implement pure manifest/map and E0 file-ledger projection, keeping every S6-owned stub slot-only and marking only accepted S5-frozen files realized with checkpoint/build/epoch proof and no task owner. Requirements: `s5-e0-materialization / E0 publishes closed manifests, ledgers, receipts, and stage anchors` and modified file-ledger behavior. Design: decisions 2/6. Paths: `nepa/speclib/materialization.py`, `tests/test_s5_artifacts.py`, relevant ledger tests. Verify: `uv run pytest -q tests/test_s5_artifacts.py tests/test_plan_revision.py -k 'manifest or contract_map or file_ledger or frozen or slot_only'` and canonical replay assertions.
- [ ] 4.3 Add the exact packaged templates already named by the Delivery Compiler and package-data configuration: layout header, build file, documentation, mechanical types/codec, task-owned source stub, and explicit POSIX entry source, all using strict closed contexts and protocol-neutral source. Requirement: `s5-e0-materialization / E0 rendering is deterministic and protocol neutral`. Design: decision 3. Paths: new `nepa/templates/`, package metadata in `pyproject.toml`, static neutrality tests. Verify: package-wheel resource test plus `uv run pytest -q tests/test_s5_templates.py tests/test_planning_protocol_neutrality.py -k 'template or package or neutral'`; static scans find no MQTT identifiers, packet names, topic/subscription semantics, port 1883, or protocol-name branch.
- [ ] 4.4 Implement `render_e0_files` as a pure complete path-to-bytes map from expanded rules/rendering view, enforce bidirectional path closure and fixed UTF-8/LF output, and render compilable declaration/stub/entry/build relationships without suffix inference. Requirement: `s5-e0-materialization / E0 rendering is deterministic and protocol neutral`. Design: decision 3. Paths: `nepa/speclib/materialization.py`, templates, `tests/test_s5_templates.py`. Verify: `uv run pytest -q tests/test_s5_templates.py -k 'render or deterministic or closure or stub or entry or build'` including byte replay, unused-parameter casts, derived NOT_IMPLEMENTED, no abort/assert, and undeclared-path rejection.

## 5. Configuration, sandbox, build, and smoke

- [ ] 5.1 Add closed positive `smoke.dwell_seconds` and `smoke.term_grace_seconds` configuration, defaults 2/5 seconds, and snapshot/drift coverage without adding revision or test-runner settings. Requirement: `s5-e0-materialization / E0 build and smoke are hard acceptance gates`. Design: decision 4. Paths: `nepa/config.py`, `configs/default.yaml`, config examples/tests. Verify: `uv run pytest -q tests/test_config.py -k smoke` for defaults, overrides, non-positive values, unknown fields, and snapshot hashing.
- [ ] 5.2 Implement the network-disabled `SandboxExecutor.exec` boundary using argument vectors, workspace-only mount, configured image/CPU/memory limits, timeout cleanup, bounded complete result capture, and stage observation; generated code must have no host-execution fallback. Requirement: `s5-e0-materialization / E0 build and smoke are hard acceptance gates`. Design: decision 4. Paths: new `nepa/tools/__init__.py`, `nepa/tools/sandbox.py`, `tests/test_sandbox.py`. Verify: `uv run pytest -q tests/test_sandbox.py` with command construction, path confinement, timeout, unavailable-Docker, network, and explicit no-host-fallback cases.
- [ ] 5.3 Add closed build-result and smoke-result Schemas/examples plus `run_build_variants` and `run_smoke_checks`, covering release/SAN commands, clean boundaries, all declared artifacts, dwell/early-exit/SIGTERM/grace/SIGKILL cleanup, normalized 143, and sanitizer rejection. Requirement: `s5-e0-materialization / E0 build and smoke are hard acceptance gates`. Design: decisions 4/6. Paths: new Schema/examples, `nepa/tools/build.py`, `tests/test_s5_build.py`, schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s5_build.py` using a fake executor for every result branch and proving Test Bundle tests are never requested.
- [ ] 5.4 Build the frozen rendered fixtures in `docker/sandbox.Dockerfile` and run real smoke checks for release and SAN, persisting evidence bound to the exact candidate tree; clean build outputs afterward. Requirement: `s5-e0-materialization / E0 build and smoke are hard acceptance gates`. Design: decision 4. Paths: sandbox integration test and fixture outputs only; do not change Docker contents unless a concrete required C99 runtime is missing. Verify: build `nepa-sandbox:latest` from the existing Dockerfile and run `uv run pytest -q -m s5_epoch -k real_sandbox`; failure is not replaceable by fake-executor success.

## 6. E0 receipt publication and stage integration

- [ ] 6.1 Add closed epoch-receipt 1.0, binding-receipt 1.0, and S5 pending-state Schemas/examples with the exact no-cycle order and typed Run v4 S5 output refs. Requirements: `s5-e0-materialization / E0 publishes closed manifests, ledgers, receipts, and stage anchors` and `S5 publication and recovery converge on one accepted checkpoint`. Design: decisions 5/6. Paths: new Schema/examples, `nepa/schemas/run.schema.json`, `tests/test_schema_examples.py`, `tests/test_s5_artifacts.py`. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s5_artifacts.py -k 'epoch or binding or pending or run'` including future-binding/file-ledger cycle attempts and pending-group rejection for E0 ready.
- [ ] 6.2 Implement RunStore E0 staging/publication operations: persist expected inputs/view/files before workspace writes, atomically write only recorded paths, initialize git once, verify a clean exact source tree, create one fixed-identity checkpoint with Plan/Epoch trailers, and publish immutable evidence/manifest/map/epoch/binding followed by mutable ledger/current copies in the designed order. Requirements: `s5-e0-materialization / E0 publishes closed manifests, ledgers, receipts, and stage anchors` and `S5 publication and recovery converge on one accepted checkpoint`. Design: decision 5. Paths: `nepa/run_store.py`, new `nepa/tools/git_ops.py` if the focused git boundary remains needed, `tests/test_s5_publication.py`. Verify: `uv run pytest -q tests/test_s5_publication.py -k 'publish or git or order or immutable or trailer or tree'` with fault hooks after every write and assertions for one commit/no build outputs.
- [ ] 6.3 Implement `S5MaterializationController.run` admission, preflight, rendering, build/smoke, publication and result verification on the existing registered-stage path; map only input/seal/Blueprint/lint/renderability rejection to `ControlledStageFailure` and let template/tool/git/build/smoke defects route to `internal_error`, with no LLM/Fixer/test assets/Plan State/S6 effects. Requirements: S5 admission and failure-semantics requirements. Design: decisions 2-5. Paths: new `nepa/stages/s5_materialization.py`, stage registration used by tests/application assembly, `tests/test_s5_controller.py`, `tests/test_s5_failures.py`. Verify: `uv run pytest -q tests/test_s5_controller.py tests/test_s5_failures.py tests/test_s9_partial_report.py` including Blueprint drift with internal-error count 0 and template/build/smoke internal-error cases.
- [ ] 6.4 Add the minimal optional Orchestrator post-commit and resume-reconciliation hooks needed for S5: atomically commit typed epoch/binding refs first, append the E0 event afterward, and reconcile before terminal/done short-circuiting without reacquiring the run lock or changing other stage behavior. Requirements: `s5-e0-materialization / S5 publication and recovery converge on one accepted checkpoint` and modified S4 typed-anchor behavior. Design: decision 5. Paths: `nepa/orchestrator.py`, `nepa/stages/s5_materialization.py`, `tests/test_orchestrator.py`, `tests/test_runtime_resume.py`, S5 controller tests. Verify: `uv run pytest -q tests/test_orchestrator.py tests/test_runtime_resume.py tests/test_s5_controller.py -k 's5 or post_commit or reconcile or lock'` and existing S4/S6 fake-controller regressions.

## 7. Recovery, event completion, and idempotence

- [ ] 7.1 Implement pre-checkpoint E0 recovery that validates the pending record, removes only matching uncommitted attempted bytes/build outputs, rejects unrelated/conflicting paths, and rematerializes without deleting broad directories. Requirement: `s5-e0-materialization / S5 publication and recovery converge on one accepted checkpoint`. Design: decision 5. Paths: `nepa/run_store.py`, `nepa/stages/s5_materialization.py`, `tests/test_s5_recovery.py`. Verify: `uv run pytest -q tests/test_s5_recovery.py -k 'before_checkpoint or dirty or conflict or confined'` with interruption after pending write, each rendered write, build result, smoke result, and git init.
- [ ] 7.2 Implement post-checkpoint forward recovery that verifies and reuses the exact commit/tree/trailers and completes only missing evidence, manifest/map, epoch receipt, binding receipt, file ledger/current copies, or S5 Run suffix without creating another commit. Requirement: `s5-e0-materialization / S5 publication and recovery converge on one accepted checkpoint`. Design: decision 5. Paths: same recovery path and tests. Verify: `uv run pytest -q tests/test_s5_recovery.py -k 'after_checkpoint or receipt or binding or ledger or run'` with a fault at every publication boundary and a second resume proving idempotence.
- [ ] 7.3 Implement accepted-stage reconciliation for a missing E0 event, exact-event deduplication, pending cleanup, and read-only completed-E0 verification; reject any conflicting accepted receipt/binding/current copy/ledger/event/workspace/git fact. Requirements: `s5-e0-materialization / Completed E0 replay is a zero-change operation` and modified typed-ledger requirement. Design: decisions 5/6. Paths: `nepa/stages/s5_materialization.py`, `nepa/speclib/plan_revision.py`, `nepa/speclib/materialization.py`, `tests/test_s5_recovery.py`, `tests/test_s5_idempotence.py`. Verify: `uv run pytest -q tests/test_s5_recovery.py tests/test_s5_idempotence.py -k 'event or completed or replay or drift'`, snapshot all files/refs/timestamps before a second invocation, and assert zero changes.

## 8. Frozen fixtures and end-to-end M1-5 acceptance

- [ ] 8.1 Add a deterministic developer fixture generator that runs both MQTT and non-MQTT inputs through the existing S4 completion/publication path, then freeze the resulting Plan/Spec/Target/Test Bundle/Blueprint/S4 anchors under `tests/fixtures/s5/`; CI consumes but does not silently regenerate them. Requirements: two-fixture scenarios under S5 rendering/protocol-neutrality requirements. Design: decision 7. Paths: fixture generator under `tests/fixtures/s5/` or `tests/tools/`, frozen fixture files, provenance README limited to generation command and source refs. Verify: run generator twice in temporary directories and byte-compare semantic fixture outputs; `uv run pytest -q tests/test_s5_fixtures.py` rejects hand-edited drift and proves each Plan came through S4 helpers.
- [ ] 8.2 Add `pytest.mark.s5_epoch` end-to-end cases for both frozen fixtures from S5 admission through ready E0, exact workspace/Blueprint closure, release/SAN build and smoke, one checkpoint, manifest/map/ledger/receipt/event bindings, protocol-neutrality audit, and repeated zero-change invocation. Requirements: all `s5-e0-materialization` positive scenarios and M1-5 acceptance. Design: decision 7. Paths: `tests/test_s5_epoch.py`, pytest marker configuration, frozen fixtures. Verify: `uv run pytest -q -m s5_epoch` and `uv run pytest -q tests/test_planning_protocol_neutrality.py tests/test_s5_templates.py tests/test_s5_epoch.py`.
- [ ] 8.3 Add end-to-end negative/fault cases for no main, duplicate main, missing definition, ambiguous implementation, frozen-input/Blueprint drift, early smoke exit, template/tool failure, and every pre/post checkpoint/receipt/Run/event interruption, proving controlled versus internal error routing and no false ready state. Requirements: all negative scenarios and M1-5 task-card evidence. Design: decisions 5-7. Paths: `tests/test_s5_failures.py`, `tests/test_s5_recovery.py`, `tests/test_s5_epoch.py`, partial-report assertions. Verify: `uv run pytest -q -m s5_epoch -k 'negative or recovery or failure'` plus `uv run pytest -q tests/test_s9_partial_report.py`.

## 9. CI and final scope acceptance

- [ ] 9.1 Add locked development-only ruff/mypy dependencies and the first repository CI workflow that runs schema examples, ruff, mypy, full pytest, gold lint, strict OpenSpec validation, and the Docker-backed S5 marker where Docker is available; do not add runtime dependencies. Requirement: M1-5 first-CI delivery in the proposal and §10.8. Design: decisions 4/7. Paths: `pyproject.toml`, `uv.lock`, type/lint configuration, new `.github/workflows/ci.yml`, pytest marker registration. Verify locally with the exact workflow commands and inspect the lock diff to confirm only development tools and their transitive packages were added.
- [ ] 9.2 Inspect the complete diff and remove unrelated refactoring, speculative fallback/compatibility paths, duplicated expansion/validation, unused extension points, and any E1+/S6/revision-controller/Test Bundle asset behavior; confirm no `project_docs/`, architecture prompt, calibration lineage, or selected bundle file is in the implementation diff. Then run all change acceptance: `uv run ruff check .`, `uv run mypy nepa`, `uv run pytest -q`, `uv run pytest -q -m s5_epoch`, `uv run python -m nepa lint spec gold_file/specIR.json`, focused S4/revision/RunStore/protocol-neutrality regressions, the actual sandbox integration, `openspec validate m1-5-s5-e0-materialization --strict`, `openspec validate --all --strict`, and `git diff --check`. Requirements: all three delta capability areas. Design: all decisions/risks/migration steps. Paths: complete implementation diff excluding the user's pre-existing `project_docs/` changes.
