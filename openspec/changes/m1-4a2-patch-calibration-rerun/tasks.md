## 1. Entry Gates and Experiment Freeze

- [x] 1.1 Reverify and record the current design-6.0.1 hash, M1-4a1 free-layout/fifteen-gate/three-slot component hashes, frozen gold-input hashes, relevant configuration and historical experiment/lineage hashes before implementation; record the 6.0.0 value-hash lineage as provenance-only and distinguish verified facts from archived claims in a fresh experiment `00-implementation-brief.md`. (Requirements: Development starts only on a complete patch-capable lineage; New development evidence is recomputable and scope-limited. Design: Context, Decisions 4/8. Paths: `project_docs/system_design.md`, `gold_file/`, `configs/`, `nepa/calibration/`, `experiments/<new-m1-4a2-directory>/00-implementation-brief.md`. Verify: repository hash script or `sha256sum` over the recorded explicit files, `git status --short`, and compare historical directory manifests without modifying them.)
- [x] 1.2 Write and freeze the new experiment preregistration before Provider I/O, specifying V0～V4, N=3 per slot/version, at most four evidence-backed prompt revisions, semantic depth two, value-hash-free patch-only repairs, per-model initial-generation ceiling 15, separate repair/format accounting, three-model `p2 ≥ 0.60`, early stop, fixed fallback/tie and non-scope boundaries. (Requirements: all `architecture-prompt-development` requirements. Design: Decisions 5/6/8. Paths: `experiments/<new-m1-4a2-directory>/01-preregistration.md`. Verify: static assertions in `tests/test_prompt_development_config.py` and `tests/test_prompt_development_scope.py`, plus manual comparison to `system_design.md` §6.4.8.2.)
- [x] 1.3 Run the no-I/O preflight over the three logical slots, context limits, the value-hash-free patch contract, current prompt neutrality, gold inputs and required environment-variable presence, recording only names/presence and no secret values; block all live commands until the deterministic suites in task group 5 pass. (Requirements: Development starts only on a complete patch-capable lineage; Every version uses coherent isolated N3 three-model evidence. Design: Decision 8. Paths: `configs/m1-4a2-live.yaml`, `configs/m1-4a2-context-limits.json`, `nepa/calibration/s4_prompt_development.py`, `run_m1_calibration.sh`, new experiment provenance artifact. Verify: `uv run python -m pytest -q tests/test_prompt_development_config.py tests/test_prompt_development_neutrality.py tests/test_prompt_development_scope.py`.)
- [x] 1.4 Amend and freeze a post-fix preregistration before any new Provider I/O, recording the validator/locality blocker, both immutable pre-fix lineages and their actual usage, the explicit owner-authorized budget reset, and a zero-start ceiling of 15 initial-generation trials per model for the replacement lineage. (Requirements: Development starts only on a complete patch-capable lineage; Every version uses coherent isolated N3 three-model evidence. Design: Context, Decisions 3a/5/8. Paths: `experiments/<post-fix-m1-4a2-directory>/01-preregistration.md`, prior experiment/lineage refs. Verify: compare the preregistration to both pre-fix batch declarations, assert no pre-fix leaf is admitted, and run prompt-development scope/config tests.)
- [x] 1.5 Amend the implementation brief and post-fix preregistration to authoritative design 6.1.0 before any new Provider I/O: record the shared `initial`/`repair` bundle, single-stage-per-revision rule, single-template lineage exclusion and no-new-hash constraint; use the existing design/artifact references without adding a design hash gate, bundle digest or template digest fields. (Requirements: Development starts only on a complete patch-capable lineage; One shared two-stage prompt bundle is developed through V0 to V4. Design: Context, Decisions 1/4/8. Paths: existing post-fix experiment `00-implementation-brief.md` and `01-preregistration.md`. Verify: compare both documents to `system_design.md` 6.1.0, assert no Provider calls occurred after the design change, and scan the planned fields for newly introduced SHA-256/hash requirements.)

## 2. Patch Contract and Deterministic Application

- [x] 2.1 Revise the strict versioned ArchitecturePlanner repair-patch Schema and examples so closed add/replace/remove JSON-Pointer operations retain explicit presence state and add/replace value rules but contain no `expected_value_sha256`, prior-value hash or other value digest; reject those removed fields, extra fields, root replacement and complete ArchitectureDraft-shaped repair payloads. (Requirements: M1-3 provides the required built-in role skeletons; Semantic repair is explicit, fresh, and bounded by the declared protocol. Design: Decision 2. Paths: `nepa/schemas/`, `nepa/schemas/examples/`, `tests/test_schema_examples.py`, `tests/test_architecture_patch.py`. Verify: `uv run python -m pytest -q tests/test_schema_examples.py tests/test_architecture_patch.py`.)
- [x] 2.2 Remove canonical prior-value hashing from deterministic patch validation and atomic in-memory application; enforce `add`-absent and `replace`/`remove`-present semantics, ordered operations, overlap/conflict and array-address rejection, unchanged persisted parent candidates, and no full-draft fallback. (Requirements: Semantic repair is explicit, fresh, and bounded by the declared protocol. Design: Decision 2. Paths: `nepa/calibration/s4_architecture.py`, `tests/test_architecture_patch.py`. Verify: `uv run python -m pytest -q tests/test_architecture_patch.py`.)
- [x] 2.3 Add a separately versioned M1-4a2 failure-to-concrete-path mapping that prefers exact validator issue paths, resolves permitted fallback prefixes against the current candidate, and produces recomputable allowed-path and before/after locality evidence without changing the existing recovery impact policy. (Requirements: Lineage identity freezes every non-prompt comparison variable; Each trial uses at most two locality-constrained patch repairs. Design: Decision 3. Paths: `nepa/calibration/s4_architecture.py`, patch/locality Schemas and examples, `tests/test_architecture_patch_locality.py`, existing `tests/test_architecture_repair_locality.py`. Verify: `uv run python -m pytest -q tests/test_architecture_patch_locality.py tests/test_architecture_repair_locality.py`.)
- [x] 2.4 Update the ArchitecturePlanner invocation contract so depth zero binds ArchitectureDraft while M1-4a2 repair depths bind the value-hash-free patch Schema and render only the current candidate, exact failures and allowed paths; remove prompt instructions to calculate or return value hashes while preserving the seven-role catalog and existing non-development invocation behavior. (Requirements: M1-3 provides the required built-in role skeletons. Design: Decisions 1/2. Paths: `nepa/agents/base.py`, `nepa/agents/prompts/architecture_planner.md`, `nepa/calibration/s4_architecture.py`, `tests/test_agent_invoker.py`, `tests/test_prompt_development_neutrality.py`. Verify: `uv run python -m pytest -q tests/test_agent_invoker.py tests/test_prompt_development_neutrality.py`.)
- [x] 2.5 Correct `arch_15` token derivation so `{message_id}` and `{type_id}` plus exact frozen Spec-derived identifiers are recognized atomically before existing generic literal tokenization, without changing the ArchitectureDraft Schema or generic responsibility whitelist; retain rejection of partial matches, adjacent undeclared text and protocol-specific literals. (Requirements: Path-neutrality token derivation preserves legal placeholders and derived identifiers. Design: Decision 3a. Paths: `nepa/speclib/architecture.py`, architecture-validation tests and neutral/non-neutral fixtures. Verify: focused `arch_15` tests covering both placeholders, separator-bearing derived identifiers, partial/adjacent negative cases and existing path-neutrality negatives.)
- [x] 2.6 Implement deterministic exact coupled-reference projection for an admitted layout `path`/`path_pattern` patch: derive a unique old-to-new expansion, substitute only exact matches in `modules[].owns_files` and `work_packages[].allowed_files`, persist model versus controller-derived operations separately, apply them atomically, and reject ambiguity, domain changes, missing closure or any broader edit. (Requirements: Semantic repair is explicit, fresh, and bounded by the declared protocol; Each trial uses at most two locality-constrained patch repairs. Design: Decision 3. Paths: `nepa/calibration/s4_architecture.py`, patch application/locality evidence Schemas and examples, `tests/test_architecture_patch.py`, `tests/test_architecture_patch_locality.py`. Verify: positive static-path and expanded-pattern projection tests, unrelated-list-entry preservation, ambiguity/rejection tests, full Schema/all-fifteen-gate revalidation and deterministic replay.)
- [x] 2.7 Replace the runtime `architecture_planner.md` template with `architecture_planner_initial.md` and `architecture_planner_repair.md`; keep one ArchitecturePlanner registry entry and model route, render only initial at depth zero and only repair at every semantic-repair depth, preserve M1-4a2 patch versus M1-4a2r/formal full-draft output contracts, and provide no legacy-template fallback. (Requirements: M1-3 provides the required built-in role skeletons; Each trial uses at most two locality-constrained patch repairs. Design: Decision 1. Paths: `nepa/agents/prompts/`, Agent invocation/rendering code, `nepa/calibration/s4_architecture.py`, invocation/recovery/formal-calibration tests. Verify: tests prove stage selection, fresh no-history repair rendering, unchanged seven-role catalog/route, patch/full-draft contract separation, and absence of `architecture_planner.md` from runtime selection.)

## 3. Trial Engine, Lineage and Recomputable Evidence

- [x] 3.1 Extend calibration declarations and lineage manifests with explicit repair mode, output-contract references, patch Schema/applier/locality hashes and semantic depth two, rejecting implicit mode selection and any attempt to combine new patch evidence with old full-draft lineages. (Requirements: Lineage identity freezes every non-prompt comparison variable; Development starts only on a complete patch-capable lineage. Design: Decisions 1/4. Paths: `nepa/calibration/s4_architecture.py`, lineage/batch Schemas and examples, `tests/test_calibration_lineage.py`, `tests/test_architecture_calibration.py`. Verify: `uv run python -m pytest -q tests/test_calibration_lineage.py tests/test_architecture_calibration.py`.)
- [x] 3.2 Close the per-trial depth loop end to end: produce the complete initial draft, render/apply/validate the first patch, conditionally render/apply/validate the second patch, stop after p0/p1 success, and persist immutable request/response/patch/application/candidate/validation/trace refs and separate usage at every attempted depth. (Requirements: Each trial uses at most two locality-constrained patch repairs; Trial artifacts and calibration reports are hash-bound and recomputable. Design: Decisions 1/2/7. Paths: `nepa/calibration/s4_architecture.py`, trial Schemas/examples, `tests/test_architecture_calibration.py`, `tests/test_calibration_artifacts.py`. Verify: `uv run python -m pytest -q tests/test_architecture_calibration.py tests/test_calibration_artifacts.py tests/test_architecture_patch.py`.)
- [x] 3.3 Update report recomputation for fixed N=3 p0/p1/p2, all `arch_01`～`arch_15` gates at each depth, p0→p1 and p1→p2 transitions/regressions, patch locality/rejections, and separate initial/format/patch usage; reject missing or mutated patch leaves and undeclared-depth fabrication. (Requirements: Trial artifacts and calibration reports are hash-bound and recomputable; Calibration metrics preserve fixed denominators and failure evidence. Design: Decisions 4/6/7. Paths: `nepa/calibration/s4_architecture.py`, report/trial Schemas and examples, `tests/test_calibration_reporting.py`, `tests/test_calibration_artifacts.py`. Verify: `uv run python -m pytest -q tests/test_calibration_reporting.py tests/test_calibration_artifacts.py`.)
- [x] 3.4 Prove the explicit full-draft mode still preserves M1-4a2r and formal calibration behavior, including semantic depth one/two metrics where declared, without allowing those protocols to consume patch-mode candidates or artifacts. (Requirements: Semantic repair is explicit, fresh, and bounded by the declared protocol; Calibration metrics preserve fixed denominators and failure evidence. Design: Decisions 1/4 and Non-Goals. Paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery*.py`, `tests/test_architecture_calibration.py`. Verify: `uv run python -m pytest -q tests/test_architecture_calibration.py tests/test_prompt_recovery*.py`.)
- [x] 3.5 Version and bind the corrected path-neutrality derivation, coupled-reference projection and derived-operation evidence into lineage/recompute contracts; reject pre-fix lineage leaves from the post-fix denominator while preserving their immutable usage and provenance. (Requirements: Lineage identity freezes every non-prompt comparison variable; Trial artifacts and calibration reports are hash-bound and recomputable. Design: Decisions 4/7. Paths: lineage/trial/application/locality Schemas and examples, `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, lineage/artifact/report tests. Verify: component-drift creates a different lineage id, post-fix recompute rejects pre-fix leaves, derived projections replay byte-identically, and historical hashes remain unchanged.)
- [x] 3.6 Revise prompt snapshot/version/revision/selection/handoff and lineage projections for one two-stage bundle version containing both prompt sources; remove parent/same-prompt hash prerequisites and any dedicated patch-component, bundle or template digest fields introduced only as defensive checks, while retaining the pre-existing generic trace/artifact references and lineage/evidence traceability explicitly required by design. (Requirements: Lineage identity freezes every non-prompt comparison variable; New development evidence is recomputable and scope-limited. Design: Decision 4. Paths: calibration development/lineage Schemas and examples, `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, snapshot/lineage/revision/report tests. Verify: byte comparisons and existing references detect cross-bundle mixing; schema/tests reject new digest fields; historical artifacts remain readable and unchanged.)

## 4. V0～V4 Prompt-Development Coordinator

- [x] 4.1 Replace the development protocol/version/attempt Schemas and coordinator constants with V0～V4, N=3, semantic depth two, three fixed slots and a per-model 15-initial-trial ceiling; remove N=10 extension and single-slot retry from the current M1-4a2 command surface while leaving historical files untouched. This completed state-machine work is retained and the later two-stage version-unit delta is task 4.6. (Requirements: Every version uses coherent isolated N3 three-model evidence. Design: Decision 5. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/calibration-development-*.schema.json`, examples, `run_m1_calibration.sh`, `tests/test_prompt_development_config.py`, `tests/test_prompt_development_extension.py`. Verify: `uv run python -m pytest -q tests/test_prompt_development_config.py tests/test_prompt_development_extension.py tests/test_prompt_development_schema.py`.)
- [x] 4.2 Implement coherent nine-trial version attempts, complete three-slot retry after any infrastructure-invalid slot, early-stop rejection of later versions and depth-zero-only initial-generation accounting; prevent replacement samples, partial-slot assembly and exceeding 15 initial trials per model. (Requirements: Every version uses coherent isolated N3 three-model evidence; Development screening requires every model to reach p2 at 0.60. Design: Decisions 5/7. Paths: `nepa/calibration/s4_prompt_development.py`, attempt/outcome Schemas and examples, `tests/test_prompt_development_attempts.py`, `tests/test_prompt_development.py`. Verify: `uv run python -m pytest -q tests/test_prompt_development_attempts.py tests/test_prompt_development.py`.)
- [x] 4.3 Generalize evidence-backed revision records and prompt snapshot/source checks through V4 for the then-current single-template protocol, enforcing one distinct falsifiable hypothesis and exact parent/evidence/diff binding while rejecting model/provider/protocol-specific branches and non-prompt control changes. This completed historical step is superseded for the active two-stage contract by tasks 3.6, 4.6 and 4.7. (Requirements: New development evidence is recomputable and scope-limited. Design: Decisions 4/5/8. Paths: `nepa/calibration/s4_prompt_development.py`, legacy revision Schema/example, `nepa/agents/prompts/architecture_planner.md`, existing revision/snapshot/neutrality tests. Verify: retained historical test evidence; active two-stage verification is defined by tasks 4.6/4.7.)
- [x] 4.4 Implement three-slot p2 screening at two-of-three per slot, required validity/truncation checks, diagnostic-only p0/p1/Schema/gate/locality fields, first-pass early stop, and the exact p2→p1→semantic-first-pass→Schema→cost fallback/tie order over complete V0～V4 assessments. (Requirements: Development screening requires every model to reach p2 at 0.60; Terminal fallback and tie use complete V0 to V4 assessments. Design: Decision 6. Paths: `nepa/calibration/s4_prompt_development.py`, assessment/selection/outcome Schemas and examples, `tests/test_prompt_development_selection.py`, `tests/test_prompt_development_reporting.py`. Verify: `uv run python -m pytest -q tests/test_prompt_development_selection.py tests/test_prompt_development_reporting.py`.)
- [x] 4.5 Update development recomputation, machine summary, Markdown report and terminal handoff/tie publication to bind every new-lineage patch leaf, show each model and fixed denominator separately, verify selected prompt source bytes, and prohibit recovery, production qualification, formal Run or downstream Plan claims. (Requirements: New development evidence is recomputable and scope-limited. Design: Decisions 4/7. Paths: `nepa/calibration/s4_prompt_development.py`, development artifact Schemas/examples, `run_m1_calibration.sh`, `tests/test_prompt_development_artifacts.py`, `tests/test_prompt_development_reporting.py`, `tests/test_prompt_development_scope.py`. Verify: `uv run python -m pytest -q tests/test_prompt_development_artifacts.py tests/test_prompt_development_reporting.py tests/test_prompt_development_scope.py`.)
- [x] 4.6 Change the V0～V4 coordinator's version unit from one prompt to one `initial`/`repair` bundle: require identical bundle bytes for Qwen/Claude/DeepSeek, reject cross-version mixing, allow exactly one changed stage per V1～V4 transition, preserve the other stage byte-for-byte, and keep four total revision opportunities across the bundle. (Requirements: One shared two-stage prompt bundle is developed through V0 to V4; Every version uses coherent isolated N3 three-model evidence. Design: Decisions 4/5. Paths: `nepa/calibration/s4_prompt_development.py`, development protocol/version/revision/selection Schemas and examples, `tests/test_prompt_development_revisions.py`, `tests/test_prompt_development_snapshot.py`, `tests/test_prompt_development_selection.py`. Verify: positive initial-only and repair-only revisions, rejection of dual-stage edits/cross-version pairs/model-specific pairs, unchanged V0～V4/N=3/15-trial behavior, and no bundle/template hash field.)
- [x] 4.7 Update neutrality, reporting and terminal publication for both prompt stages: scan both files for protocol/model/provider specialization, record the selected bundle version and both source artifacts, compare selected repository bytes with the snapshot, and publish both stages together without adding hashes beyond the existing per-call trace and artifact-reference mechanisms. (Requirements: One shared two-stage prompt bundle is developed through V0 to V4; New development evidence is recomputable and scope-limited. Design: Decisions 4/7/8. Paths: `nepa/calibration/s4_prompt_development.py`, report/handoff code and Schemas, both prompt files, neutrality/snapshot/reporting/scope tests. Verify: both-stage neutrality tests, byte-mismatch rejection for either stage, machine/Markdown bundle reporting parity, and static rejection of bundle/template digest fields.)

## 5. Deterministic Verification Before Live Calls

- [x] 5.1 Revise positive and negative Schema/example coverage for every changed patch, trial, protocol, assessment, selection and handoff artifact, including rejection of removed value-hash fields, full-draft-in-patch-slot, extra-field, presence mismatch, overlap, out-of-path and cross-lineage evidence. (Requirements: all delta requirements. Design: Decisions 2～4. Paths: `nepa/schemas/`, `nepa/schemas/examples/`, `tests/test_schema_examples.py`, focused patch tests. Verify: `uv run python -m pytest -q tests/test_schema_examples.py tests/test_architecture_patch.py tests/test_architecture_patch_locality.py tests/test_prompt_development_schema.py`.)
- [x] 5.2 Rerun and update the deterministic fake-provider M1-4a2 suite for value-hash-free p0/p1/p2 early stop, two patch depths, per-depth regression, all-fifteen-gate locality, V0～V4 revision limits, N=3 coherence, 15-initial ceiling, one-slot failure, truncation, coherent retry, three-model p2=2/3 pass, p2=1/3 failure, fallback/tie and rejection of the historical value-hash lineage. (Requirements: all `architecture-prompt-development` requirements. Design: Decisions 3/5/6/7. Paths: `tests/test_architecture_*.py`, `tests/test_prompt_development*.py`. Verify: `uv run python -m pytest -q tests/test_architecture_calibration.py tests/test_architecture_patch.py tests/test_architecture_patch_locality.py tests/test_prompt_development*.py`.)
- [x] 5.3 Rerun unchanged recovery/formal-calibration, Agent invocation, protocol-neutrality and historical-evidence regression suites after removing the value-hash patch precondition; prove no shared-prompt model/provider branch, MQTT constant, recovery patch-mode migration or historical artifact rewrite was introduced. (Requirements: M1-3 provides the required built-in role skeletons; Development starts only on a complete patch-capable lineage. Design: Non-Goals, Decisions 1/8. Paths: `nepa/agents/`, `nepa/calibration/`, `tests/test_agent_invoker.py`, `tests/test_prompt_recovery*.py`, neutrality and lineage tests. Verify: `uv run python -m pytest -q tests/test_agent_invoker.py tests/test_prompt_recovery*.py tests/test_prompt_development_neutrality.py tests/test_calibration_lineage.py`.)
- [x] 5.4 Pass the complete pre-live gate for the value-hash-free implementation and record the exact command results in the new implementation brief: focused suites, full repository pytest, strict OpenSpec validation, gold/design lints and diff hygiene; if any gate fails, make zero Provider calls. (Requirements: New development evidence is recomputable and scope-limited. Design: Decision 8. Paths: repository-wide and new experiment brief. Verify: `uv run python -m pytest -q`, `openspec validate --all --strict`, repository design/gold lint commands documented by the brief, and `git diff --check`.)
- [x] 5.5 Rerun the complete no-I/O gate after the validator/locality and two-stage-prompt corrections, including legal-placeholder positives, derived-identifier boundaries, exact coupled closure, preservation of unrelated correct values, initial/repair stage selection, one-role routing, one-stage-only revisions, cross-bundle rejection, both-template neutrality, unchanged recovery/formal full-draft contracts, no-new-hash assertions, full pytest, strict OpenSpec, gold/design lints and diff hygiene; make zero new Provider calls unless tasks 1.5, 2.7, 3.6, 4.6 and 4.7 and the reset-budget declaration pass. (Requirements: all updated delta requirements. Design: Decisions 1/3/3a/4/5/8. Paths: repository-wide, corrected experiment brief and post-fix preregistration. Verify: focused Agent/architecture/patch/lineage/prompt-development/recovery suites, `uv run python -m pytest -q`, `openspec validate --all --strict`, documented lint commands, a scan proving no new SHA-256/hash fields or gates, and `git diff --check`.)

## 6. Fresh Live M1-4a2 Experiment

- [x] 6.1 After task 5.4 only, initialize the first 6.0.1 value-hash-free patch lineage and, after its replay defect, one replacement lineage; verify their frozen component/evidence refs and preserve both as pre-fix audit provenance without admitting any 6.0.0 value-hash evidence. (Requirements: Development starts only on a complete patch-capable lineage; New development evidence is recomputable and scope-limited. Design: Decisions 4/8. Paths: pre-fix lineages `2d297e…` and `a85f28…`, experiment provenance, `run_m1_calibration.sh`. Verify: recorded initialization/recompute evidence, historical exclusion and immutable pre-fix leaves.)
- [x] 6.2 Complete and recompute the pre-fix diagnostic execution through active-lineage V0/V1, record the supported prompt improvement and the subsequent `{type_id}`/coupled-locality blocker, and stop before V2 without representing those results as a post-fix selection or handoff. (Requirements: Every version uses coherent isolated N3 three-model evidence; New development evidence is recomputable and scope-limited. Design: Context, Decisions 5～8. Paths: pre-fix lineage `v0/` and `v1/`, prompt snapshots, optimizer/blocker records. Verify: complete/source-match recomputation recorded before component correction, no V2 leaves and no terminal selection.)
- [x] 6.3 Preserve the infrastructure-invalid, complete V0/V1 and all other single-template lineages as audit-only, publish the owner-authorized budget-reset record, then initialize a fresh design-6.1.0 post-fix lineage whose existing component references/versions and recorded bytes match the corrected validator/locality/projection and two-stage invocation sources, whose bundle is the selected restart baseline split into `initial`/`repair`, whose admitted trial set is empty and whose per-model initial-generation counter is zero of 15. (Requirements: Development starts only on a complete patch-capable lineage; Every version uses coherent isolated N3 three-model evidence. Design: Context, Decisions 1/3/3a/4/5/7/8. Paths: old lineage refs, `runs/_calibration/s4-architecture/<post-fix-lineage>/`, post-fix experiment provenance. Verify: existing historical evidence comparison, explicit actual pre-reset usage, lineage-id recomputation through the existing mechanism, byte-for-byte bundle snapshot review, zero trial leaves, no new bundle/template/component digest fields and zero new Provider calls during initialization.)
- [ ] 6.4 Run one coherent design-6.1.0 post-fix V0 N=3 attempt for Qwen, Claude and DeepSeek using identical bundle bytes; render only `initial` for each depth-zero generation and only `repair` for patch depths, recompute all reports and assessment exclusively from replacement-lineage leaves, and either stop on three-slot p2 screening success or admit exactly one evidence-backed V1 single-stage prompt hypothesis. Do not treat validator/locality fixes or the mechanical template split as prompt improvement. (Requirements: Every version uses coherent isolated N3 three-model evidence; Each trial uses at most two locality-constrained patch repairs; Development screening requires every model to reach p2 at 0.60. Design: Decisions 1/3/3a/5～8. Paths: post-fix lineage `v0/`, bundle snapshot and post-fix experiment records. Verify: complete/source-match recomputation, per-model counters equal 3/15, per-call stage selection, three-slot bundle equality, all-fifteen-gate and coupled-projection evidence audit.)
- [ ] 6.5 If still required, execute post-fix V1～V4 sequentially under the same rules—one admitted revision changing exactly one of `initial` or `repair`, byte-identical preservation of the other stage, then one coherent N=3 three-slot attempt—stopping at the first screening pass; never mix bundle versions, issue a later-version call after success or exceed the reset lineage's 15 initial-generation trials per model. (Requirements: One shared two-stage prompt bundle is developed through V0 to V4; Every version uses coherent isolated N3 three-model evidence. Design: Decision 5. Paths: post-fix lineage `v1/`～`v4/`, revision records, bundle snapshots. Verify: recompute each completed version and audit reset-lineage counters, existing component refs, parent bundle version, selected-stage diff and unchanged-stage bytes before admitting the next.)
- [ ] 6.6 Recompute and atomically publish the terminal bundle selection/handoff or fixed fallback/tie, then generate the machine summary and Markdown report only from post-fix lineage leaves, with a separate non-comparative pre-reset usage appendix and all three slots, selected `initial`/`repair` artifacts, p0/p1/p2, fifteen gates, patch/model-derived outcomes, usage, model strings, limitations and exact decision; make no M1-4a3/B1～B4 or production claim and add no bundle/template digest fields. (Requirements: Terminal fallback and tie use complete V0 to V4 assessments; New development evidence is recomputable and scope-limited. Design: Decisions 4/6/7. Paths: post-fix lineage terminal artifacts, `experiments/<post-fix-m1-4a2-directory>/results/`, both repository ArchitecturePlanner prompt files when selected. Verify: rerun terminal recompute/report commands, compare Markdown to the canonical machine summary, compare both repository files byte-for-byte with the selected snapshot, and prove no pre-fix leaf enters a comparison tuple.)

## 7. Boundary Gate and Final Validation

- [ ] 7.1 Record the owner-only boundary in the post-fix terminal experiment result: a normal selection permits only an M1-4a3 technical admission handoff; a tie permits no handoff and this change SHALL stop without creating or authorizing M1-4a2r. Do not mark any M1-4a2r authorization, M1-4a3 qualification or production signature as approved. (Requirements: Terminal fallback and tie use complete V0 to V4 assessments; New development evidence is recomputable and scope-limited. Design: Non-Goals, Decision 6. Paths: post-fix terminal outcome/handoff or tie and experiment report. Verify: `tests/test_prompt_development_scope.py` and inspection of terminal artifacts for absence of recovery/qualification/signature claims.)
- [ ] 7.2 Rerun focused and full tests, strict validation, post-fix report recomputation, both-prompt source-byte checks, protocol-neutrality scans, existing historical-evidence integrity checks and `git diff --check`; explicitly scan code/Schemas/tasks for any newly added bundle/template/component SHA-256/hash field or gate and remove it unless an existing authoritative-design contract requires it. Preserve every pre-fix call and infrastructure-invalid artifact without representing it as a post-fix assessment or charging it to the reset lineage. (Requirements: all updated delta requirements. Design: Migration Plan. Paths: repository-wide, post-fix lineage and experiment directory. Verify: `uv run python -m pytest -q`, `openspec validate m1-4a2-patch-calibration-rerun --strict`, `openspec validate --all --strict`, implemented recompute/report commands, budget/lineage audit, no-new-hash scan and `git diff --check`.)
- [ ] 7.3 Inspect the complete code, Schema, both prompts, configuration, experiment and evidence diff; remove incidental changes, legacy runtime selection, cross-version bundle assembly, new defensive hash checks and any broad model-editable closure while retaining value-hash-free patch semantics plus only the pre-existing lineage/evidence traceability required by design. Verify placeholder handling does not expand the whitelist, exact projection preserves unrelated entries, archived changes and all pre-fix/single-template lineages remain unchanged, and M1-4a2r/M1-4a3 thresholds/output semantics are unaffected by their repair-template migration. Record remaining limitations without claiming production qualification. (Requirements: all updated delta requirements. Design: Goals/Non-Goals, Risks, Migration Plan. Paths: full `git diff`, `openspec/changes/m1-4a2-patch-calibration-rerun/`, post-fix experiment directory and lineage refs. Verify: `git status --short`, `git diff --stat`, `git diff --check`, all task-listed validation commands, comparison to existing historical evidence, no-new-hash scan and inspection that no non-scope capability was implemented.)
