## 1. Agent Package Foundations

- [x] 1.1 Add the design-required Jinja2 dependency to `pyproject.toml` and refresh `uv.lock` without changing unrelated dependencies. Covers “Prompt rendering is strict, deterministic, and self-contained” and Design §3. Verify with `uv lock --check` and `uv run python -c "import jinja2"`.
- [x] 1.2 Create `nepa/agents/__init__.py` and the typed role, invocation-contract, resolved-route, result, and Agent-owned error models in `nepa/agents/base.py`, keeping provider/runtime errors owned by `nepa.llm`. Covers “Agent roles and output contracts are explicit” and Design §§1, 5. Add focused construction/invalid-input cases in `tests/test_agent_roles.py`; verify with `uv run pytest tests/test_agent_roles.py -q`.

## 2. Role Registry, Routing, and Availability

- [x] 2.1 Implement the closed seven-role registry in `nepa/agents/roles.py` with the exact identifiers, stage associations, packaged template paths, and required input names defined by “M1-3 provides the required built-in role skeletons” and Design §§1, 6. Test complete catalog equality, lookup, and unknown-role rejection in `tests/test_agent_roles.py`; verify with `uv run pytest tests/test_agent_roles.py -q`.
- [x] 2.2 Implement static route resolution in `nepa/agents/base.py` from `ResolvedConfig`: merge tier defaults with role overrides, validate the referenced tier/final provider, enforce PlanCritic temperature `0` and Coder/Fixer temperature `≤ 0.2`, and expose but never execute `escalate_to`. Covers “Agent routes are resolved statically from project configuration” and Design §2. Test inheritance, partial overrides, invalid references, temperature violations, the default Diagnoser/Fixer escalation metadata, and the default PlanCritic model differing from ArchitecturePlanner in `tests/test_agent_roles.py`; verify with `uv run pytest tests/test_agent_roles.py -q`.
- [x] 2.3 Implement the FlatPlanBaseline availability rule in `nepa/agents/roles.py` and enforce it before rendering/provider work in `nepa/agents/base.py`. Covers “FlatPlanBaseline is an explicit comparison strategy only” and Design §7. Test explicit `flat`, rejection under `layered`, and absence of fallback invocation in `tests/test_agent_roles.py`; verify with `uv run pytest tests/test_agent_roles.py -q`.

## 3. Contract Binding and Prompt Rendering

- [x] 3.1 Implement packaged UTF-8 template loading, the exact exported `AGENT_SYSTEM_INSTRUCTION`, `StrictUndefined` rendering, exact missing/extra input checks, canonical JSON serialization, named delimiters, and raw-byte SHA-256 in `nepa/agents/base.py`. Covers “Prompt rendering is strict, deterministic, and self-contained” and Design §§3, 4. Add exact system-instruction/message-placement, deterministic, undeclared-variable, missing/extra-input, and delimiter-preservation tests in `tests/test_agent_rendering.py`; verify with `uv run pytest tests/test_agent_rendering.py -q`.
- [x] 3.2 Validate each caller-supplied JSON Schema and minimal example, then embed their canonical representations in the rendered output-contract section from `nepa/agents/base.py`. Covers “Agent roles and output contracts are explicit” and Design §§1, 3. Test malformed schemas, non-conforming examples, and a conforming contract in `tests/test_agent_rendering.py`; verify with `uv run pytest tests/test_agent_rendering.py -q`.
- [x] 3.3 Add the seven English-body prompt skeletons under `nepa/agents/prompts/` with the ordered Role and Goal, Inputs, Output Contract, Rules, and Counterexamples sections, numbered weak-model rules, the exact trust-injected-artifacts/do-not-trust-protocol-memory sentence, exactly-one-JSON instruction, and role-specific input delimiters; keep maintenance comments in Chinese and omit all M1-4a production schemas/calibration content. Covers “Prompt rendering is strict, deterministic, and self-contained” and “M1-3 provides the required built-in role skeletons”; implements Design §§3, 6. Test every packaged template’s structure, required trust sentence, and renderability in `tests/test_agent_rendering.py`; verify with `uv run pytest tests/test_agent_rendering.py -q`.
- [x] 3.4 Add deterministic source scans and a synthetic non-MQTT render fixture in `tests/test_agent_protocol_neutrality.py` for the shared `nepa/agents/**/*.py` and `nepa/agents/prompts/*.md` assets. Covers “Shared Agent assets remain protocol and provider neutral” and Design §6. Verify with `uv run pytest tests/test_agent_protocol_neutrality.py -q`.

## 4. One-Call Agent Invocation

- [x] 4.1 Implement the exact keyword-only `AgentInvoker.invoke` contract from Design §5 in `nepa/agents/base.py`, taking explicit `role`, `inputs`, output contract, `run_id`, `stage`, optional `task_id`, `attempt`, and `use_cache`; validate role/stage and trace identity, resolve availability/route, render/hash, construct `LLMRequest` plus `LLMCallContext`, call `LLMClient.complete` once, and return parsed data with response, route, and template metadata. Covers “Each Agent invocation delegates one logical completion.” Add fake-provider success and invalid identity/stage tests that assert the exact request, target, context including caller-supplied run id, result, and one logical client call in `tests/test_agent_invoker.py`; verify with `uv run pytest tests/test_agent_invoker.py -q`.
- [x] 4.2 Close the failure boundary in `nepa/agents/base.py`: reject all preflight errors before cache/provider work and propagate M1-2 typed provider/schema/budget/configuration failures without Agent repair, retry, escalation, role fallback, or pipeline-state mutation. Covers “Each Agent invocation delegates one logical completion,” the escalation scenario under static routing, and Design §§2, 5, 8. Add spy-based failure cases plus a case proving M1-2's internal schema repair still corresponds to exactly one Agent call to `LLMClient.complete` in `tests/test_agent_invoker.py`; verify with `uv run pytest tests/test_agent_invoker.py -q`.

## 5. Durable Template Provenance

- [x] 5.1 Add a narrow `LLMClient.complete` preflight in `nepa/llm/client.py` that validates `context.trace_fields["prompt_template_sha256"]` when present and raises `LLMRequestError` before cache/provider work; add that key to the existing success allowlist and make `publish_failure` in `nepa/llm/telemetry.py` copy only this key, not all pre-existing optional context fields. Covers “Agent template provenance is durable” and Design §4. Add valid, invalid, omitted, cache-hit, success, provider-failure, and legacy-failure-field-omission cases to `tests/test_llm_client.py` and `tests/test_llm_telemetry.py`; verify with `uv run pytest tests/test_llm_telemetry.py tests/test_llm_cache.py tests/test_llm_client.py -q`.
- [x] 5.2 Add an end-to-end fake-provider Agent test in `tests/test_agent_invoker.py` that verifies the raw packaged-template SHA-256 and effective prompt SHA-256 are distinct trace fields and that the raw hash is present on cached and uncached Agent call records as applicable. Covers “Agent template provenance is durable” and “Each Agent invocation delegates one logical completion”; implements Design §§4, 5. Verify with `uv run pytest tests/test_agent_invoker.py tests/test_llm_telemetry.py -q`.

## 6. Acceptance and Scope Audit

- [x] 6.1 Run all focused M1-3 and affected M1-2 regression tests, confirming role/route behavior, strict rendering, contract validation, one-call delegation, flat-only gating, protocol neutrality, and trace provenance. Covers every requirement and Design §§1-8. Verify with `uv run pytest tests/test_agent_roles.py tests/test_agent_rendering.py tests/test_agent_invoker.py tests/test_agent_protocol_neutrality.py tests/test_llm_telemetry.py tests/test_llm_cache.py tests/test_llm_client.py -q`.
- [x] 6.2 Inspect the complete diff and remove unrelated or speculative code, then run `uv run pytest -q` and `openspec validate --all --strict`. Confirm the implementation adds no production ArchitecturePlanner schema/prompt calibration, stage controller, plan compiler, Agent-layer retry/escalation, coding/repair loop, protocol-specific branch, CLI behavior, or owner-approval claim; M1-4a3 remains the later responsible-owner freeze gate. Covers the full change scope, all requirements, and Design Goals / Non-Goals plus §§1-8.
