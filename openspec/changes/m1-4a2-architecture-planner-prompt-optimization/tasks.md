## 1. Correct the Lineage and Batch Boundary

- [x] 1.1 Refine `build_lineage_manifest(...)` and the calibration-lineage Schema/example so `lineage_id` binds the metric-definition contract, resolved non-secret provider/pricing configuration, exact provider/model/request parameters, explicit per-model context-window limits, the fixed §8.3 key-variable-name mapping, and all existing controlled components but excludes `trial_count` and `semantic_depth`; preserve prompt exclusion, reject unknown/missing controlled fields, and exclude secret values and value hashes from lineage/hash inputs. Covers `planning-architecture-infrastructure` “Lineage identity freezes every non-prompt comparison variable” and Design Decisions 1/10. Expected paths: `nepa/calibration/s4_architecture.py`, `nepa/schemas/calibration-lineage.schema.json`, `nepa/schemas/examples/calibration-lineage.example.json`, `tests/test_calibration_lineage.py`. Verify with `uv run pytest tests/test_calibration_lineage.py tests/test_schema_examples.py -q`.
- [x] 1.2 Keep `trial_count`, `semantic_depth`, exact trial ids, requested parameters, model/provider identity, lineage id, and prompt identity mandatory and immutable in each batch; update reload/report validation so one ordinary report still consumes exactly one compatible batch and rejects direct cross-protocol aggregation. Covers the incompatible-batch scenarios and Design Decision 1. Expected paths: `nepa/calibration/s4_architecture.py`, `nepa/schemas/calibration-batch.schema.json`, `tests/test_architecture_calibration.py`, `tests/test_calibration_reporting.py`. Verify with `uv run pytest tests/test_architecture_calibration.py tests/test_calibration_reporting.py -q`.
- [x] 1.3 Add regression cases proving N=5/N=10 and depth-one/depth-two declarations retain the same lineage when all controlled inputs are identical, while any Schema/validator/serializer/input/compiler/model-config/metric-definition change creates a new lineage; prove roots created by the pre-change lineage projection are never rewritten or silently admitted. Covers all modified-lineage scenarios and Design Decision 1/Migration Plan 1. Expected paths: `tests/test_calibration_lineage.py`, `tests/test_calibration_artifacts.py`. Verify with `uv run pytest tests/test_calibration_lineage.py tests/test_calibration_artifacts.py -q`.
- [x] 1.4 Add an explicit calibration-configuration preflight used by `init`: require a caller-supplied configuration path and exact two-model context-window declaration, resolve provider/model/request parameters/pricing, require `max_tokens=65536` for both models, require Qwen/DeepSeek `api_key_env` to equal `NEPA_QWEN_API_KEY`/`NEPA_DS_API_KEY`, and require those fixed variables to be non-empty in the current process environment before provider I/O. Bind canonical non-secret projections and fixed names into lineage/protocol evidence; prohibit user-supplied alternate names, model-name/network inference, a default-config fallback, and reading `.bashrc`, `.profile`, dotenv, or other shell configuration. Cover successful initialization, missing pricing, missing/invalid context limits, configuration drift, all fixed variables present, each variable missing/empty, and non-authorized `api_key_env`. Covers “Live development initialization uses explicit complete calibration inputs” and Design Decision 10. Expected paths: `nepa/calibration/s4_prompt_development.py`, the existing configuration loader only where reuse requires a narrow extension, `tests/test_prompt_development_config.py`. Verify with `uv run pytest tests/test_prompt_development_config.py tests/test_calibration_lineage.py -q`.
- [x] 1.5 Inject distinct sentinel values into the two fixed variables and prove no secret value or value hash enters CLI/configuration input, JSON artifacts, lineage/hash input, protocol, trace, logs, diagnostics, exception text, or command output; only fixed environment-variable names may persist. Covers the configuration secret boundary and Design Decision 10. Expected paths: `tests/test_prompt_development_config.py`, `tests/test_prompt_development_artifacts.py`, provider trace/log capture tests only where already established. Verify with `uv run pytest tests/test_prompt_development_config.py tests/test_prompt_development_artifacts.py -q`.

## 2. Define Closed Prompt-Development Evidence Contracts

- [x] 2.1 Add closed draft-2020-12 Schemas and generic examples for the development protocol, prompt version, prompt snapshot, revision, base/extension attempt declaration and outcome, extension, version assessment, outcome, and final selection records, including safe relative path/SHA-256 refs, explicit configuration/context refs, closed enums for the two §8.3 key-variable names, exact model/version/attempt sets and states, screening fields, ambiguity enums, batch controls, comparison tuples, and terminal reasons; provide no field capable of carrying a key value or value hash. Covers `architecture-prompt-development` immutable/recomputable evidence and Design Decisions 3/10/11/12. Expected paths: `nepa/schemas/`, `nepa/schemas/examples/`, `nepa/schemas/__init__.py`, `tests/test_schema_examples.py`, `tests/test_prompt_development_schema.py`. Verify with `uv run pytest tests/test_schema_examples.py tests/test_prompt_development_schema.py -q`.
- [x] 2.2 Implement canonical load/serialize validation for the new records and confined immutable publication below `<lineage>/prompt-development/`, with staged/fsynced atomic commits, byte-identical replay, conflicting replay rejection, and no timestamp or secret environment value/hash as canonical input; the fixed environment-variable names remain permitted non-secret protocol fields. Covers development-evidence persistence and Design Decisions 3/10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/calibration/__init__.py`, `tests/test_prompt_development_artifacts.py`. Verify with `uv run pytest tests/test_prompt_development_artifacts.py tests/test_run_store.py -q`.
- [x] 2.3 Implement evidence reload/reconciliation that walks only committed protocol/version/prompt-snapshot/revision/attempt/extension/assessment/outcome/selection parents, verifies every relative ref/hash and exact state transition, ignores orphan staging/trace data, rejects invalid or cross-attempt assessment inputs, and returns the single valid next attempt/action on resume. Covers deterministic resume/tamper behavior and Design Decisions 3/11/12. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_artifacts.py`, `tests/test_prompt_development_attempts.py`. Verify with `uv run pytest tests/test_prompt_development_artifacts.py tests/test_prompt_development_attempts.py -q`.

## 3. Implement the Bounded Development State Machine

- [x] 3.1 Implement protocol initialization that fixes one corrected lineage, exactly `qwen`/`deepseek`, the explicitly resolved non-secret provider/pricing/model/request/context configuration and §8.3 fixed key-variable-name mapping, V0/V1/V2 order, `semantic_depth=1`, N=5/N=10 rules, screening definitions, fallback order, and development component/Schema hashes before V0; reject reuse after any controlled protocol, configuration, context-limit, fixed-name mapping, or returned model-identity drift. Covers the shared-version and bounded-sequence requirements and Design Decisions 2-3/6/10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development.py`, `tests/test_prompt_development_config.py`. Verify with `uv run pytest tests/test_prompt_development.py tests/test_prompt_development_config.py tests/test_calibration_lineage.py -q`.
- [x] 3.2 Implement V0 admission through the existing `ArchitectureCalibrationDriver` with N=5 and one semantic repair for both isolated models, first publish immutable `versions/v0/prompt.md`, require every real provider render and raw-template hash to consume/match those exact snapshot bytes, and prevent any V1/V2, extra model, cache reuse, mutable source-template reload, or alternative validator/call path. Covers V0 sequence and shared-prompt scenarios and Design Decisions 2/5/12. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/calibration/s4_architecture.py` only where reuse requires a narrow snapshot-input extension point, `tests/test_prompt_development.py`, `tests/test_prompt_development_snapshot.py`. Verify with `uv run pytest tests/test_prompt_development.py tests/test_prompt_development_snapshot.py tests/test_architecture_calibration.py -q`.
- [x] 3.3 Implement V1/V2 revision admission with one hypothesis, exact prior report/failing-trial refs, expected gate/metric set, computed unified prompt diff, previous/new raw hashes, neutrality preflight, and proof that no lineage-controlled file or value changed; publish pre-run `revision.json` and post-run `outcome.json` separately. Covers “Every prompt edit has one immutable evidence-backed revision record” and Design Decision 5. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_revisions.py`. Verify with `uv run pytest tests/test_prompt_development_revisions.py tests/test_calibration_lineage.py -q`.
- [x] 3.4 Enforce V1 only after complete V0 failure evidence, V2 only after a complete failing final V1 assessment and a second distinct evidence-backed hypothesis, no skipped/duplicate V3 version, and no edit/sample admission after `selection.json`. Covers bounded ordering/stopping scenarios and Design Decision 5. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development.py`, `tests/test_prompt_development_revisions.py`. Verify with `uv run pytest tests/test_prompt_development.py tests/test_prompt_development_revisions.py -q`.
- [x] 3.5 Implement attempt-qualified base N=5 coordination: publish a monotonic attempt declaration before calls, require both reports in one attempt, mark the whole attempt infrastructure-invalid when any model is infrastructure-invalid, retain invalid evidence permanently, and admit only a fresh same-version/same-prompt/same-lineage/same-protocol both-model attempt; prohibit failed-model-only replacement and cross-attempt assembly. Covers “Every base N=5 version uses one coherent two-model attempt” and Design Decision 11. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_attempts.py`. Verify with `uv run pytest tests/test_prompt_development_attempts.py tests/test_prompt_development_reporting.py -q`.
- [x] 3.6 Enforce prompt-snapshot integrity before provider I/O and before attempt outcome/assessment publication: fail on source/snapshot mismatch or snapshot tamper, invalidate an in-flight attempt on source drift, and prove both models' trace/raw-template/provider inputs reconstruct from the same evidence-bound snapshot without changing the Agent call shape. Covers “Admitted prompt snapshots are immutable execution inputs” and Design Decision 12. Expected paths: `nepa/calibration/s4_prompt_development.py`, the narrow existing calibration-driver entry point, `tests/test_prompt_development_snapshot.py`. Verify with `uv run pytest tests/test_prompt_development_snapshot.py tests/test_agent_rendering.py -q`.
- [x] 3.7 Expose the narrow internal module runner for `init`, `run-version`, `record-revision`, `expand`, and `recompute`, requiring explicit `--config` and `--context-limits` inputs for `init` plus explicit input/development-root paths elsewhere and delegating only to the tested coordinator; do not register a public `nepa` CLI command or infer defaults. Covers reproducible live execution and Design Decisions 2/10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development.py`, `tests/test_prompt_development_config.py`. Verify with `uv run python -m nepa.calibration.s4_prompt_development --help` and `uv run pytest tests/test_prompt_development.py tests/test_prompt_development_config.py -q`.

## 4. Implement Evidence-Gated N=10 Expansion

- [x] 4.1 Recompute the allowed N=5 ambiguity predicates from committed trial evidence: leave-one-trial-out sensitivity of a numeric screening result and conflicting declared numeric screening metrics; reject free-form, infrastructure-only, V0, already-stable, or post-selection expansion requests. Covers “N=10 expansion is evidence-gated” and Design Decision 4. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_extension.py`. Verify with `uv run pytest tests/test_prompt_development_extension.py -q`.
- [x] 4.2 Add one append-only extension path for V1/V2 that preserves trials 001-005 by hash reference and runs exactly fresh trials 006-010 per model with the same lineage/prompt/configuration, depth one, no history/cache, and no replacement sampling; reuse the existing trial/trace/parser/validator implementation instead of copying it. Covers the extension sample contract and Design Decisions 2/4. Expected paths: `nepa/calibration/s4_prompt_development.py`, a minimal reusable entry point in `nepa/calibration/s4_architecture.py`, `tests/test_prompt_development_extension.py`. Verify with `uv run pytest tests/test_prompt_development_extension.py tests/test_architecture_calibration.py -q`.
- [x] 4.3 Recompute each extended model report over exactly trials 001-010 using the same M1-4a1 metric functions, publish denominator-ten reports and one `assessment-n010.json`, and reject missing/duplicate/replaced/cross-model/cross-prompt/cross-lineage trials or mixed semantic depth. Covers recomputable N=10 evidence and Design Decision 4. Expected paths: `nepa/calibration/s4_prompt_development.py`, minimal shared aggregation helpers in `nepa/calibration/s4_architecture.py`, `tests/test_prompt_development_extension.py`, `tests/test_prompt_development_reporting.py`. Verify with `uv run pytest tests/test_prompt_development_extension.py tests/test_prompt_development_reporting.py tests/test_calibration_reporting.py -q`.
- [x] 4.4 Apply the same explicit attempt declaration/outcome state to extension runs: a failed extension attempt invalidates the whole two-model attempt, the fresh attempt reruns both models with identical version/prompt/lineage/batch protocol, invalid evidence remains audit-only, deterministic resume selects one new attempt, and no individual/cross-attempt sample is replaced or cherry-picked into the final ten. Covers extension failure semantics and Design Decisions 4/11/Risks. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_extension.py`, `tests/test_prompt_development_attempts.py`. Verify with `uv run pytest tests/test_prompt_development_extension.py tests/test_prompt_development_attempts.py tests/test_llm_retry.py -q`.

## 5. Recompute Screening and Final Selection

- [x] 5.1 Build the per-version two-model assessment only from one complete valid attempt by reloading and recomputing each final N=5 or authorized N=10 sample set; reject invalid or cross-attempt inputs and report every model separately, exact trial ids, Schema/p1/first-pass values, truncation/infrastructure status, initial-candidate per-gate failures, returned model/version identity, parameter-support states, and usage. Covers per-model screening and immutable evidence requirements and Design Decisions 6/11. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_reporting.py`, `tests/test_prompt_development_attempts.py`. Verify with `uv run pytest tests/test_prompt_development_reporting.py tests/test_prompt_development_attempts.py tests/test_calibration_reporting.py -q`.
- [x] 5.2 Apply the exact per-model screening gate, including the explicitly authorized rule that the same initial-candidate `arch_01`-`arch_10` gate failing in at least two trials is a repeated systematic failure, forbid cross-model averages, reject returned identity/parameter-support drift, and atomically select the first passing final version while blocking all later provider I/O. Add boundary cases for one versus two failures of the same gate and failures of different gates. Covers “Screening is per model and stops at the first passing version” and Design Decision 6. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_reporting.py`, `tests/test_prompt_development.py`. Verify with `uv run pytest tests/test_prompt_development_reporting.py tests/test_prompt_development.py -q`.
- [x] 5.3 Implement the post-V2-only lexicographic fallback over minimum per-model `p1`, minimum first semantic-pass rate, minimum Schema-after-format-repair rate, and lower total cost; store every comparison tuple, reject averages/extra samples, and return `PROMPT_SELECTION_TIE` without an invented tiebreaker. Covers “V2 fallback selection follows the fixed lexicographic rule” and Design Decision 7. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_selection.py`. Verify with `uv run pytest tests/test_prompt_development_selection.py -q`.
- [x] 5.4 Bind `selection.json` to the selected prompt/report/assessment and require `nepa/agents/prompts/architecture_planner.md` to be byte-identical before technical completion; prove fallback restoration selects exact earlier evidence bytes and that no production model, B1-B4 result, repair budget, call-shape freeze, or signature is emitted. Covers selected-prompt handoff and scope boundary and Design Decisions 7/9. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/agents/prompts/architecture_planner.md`, `tests/test_prompt_development_selection.py`, `tests/test_prompt_development_scope.py`. Verify with `uv run pytest tests/test_prompt_development_selection.py tests/test_prompt_development_scope.py -q`.

## 6. Preserve Prompt Contract and Neutrality

- [x] 6.1 Extend deterministic source scanning over every ArchitecturePlanner candidate and development record input to reject configured model/provider names, protocol names/branches, target messages/fields, REQ ids, paths, interface constants, ports, and topic/subscription semantics while allowing concrete values only inside rendered frozen-input delimiters. Covers “Every candidate version is one shared protocol-neutral prompt” and Design Decision 8. Expected paths: `tests/test_agent_protocol_neutrality.py`, `tests/test_planning_protocol_neutrality.py`, `tests/test_prompt_development_neutrality.py`, and the smallest shared scan helper already used by those tests. Verify with `uv run pytest tests/test_agent_protocol_neutrality.py tests/test_planning_protocol_neutrality.py tests/test_prompt_development_neutrality.py -q`.
- [x] 6.2 Prove every candidate retains the M1-3 five-section structure, exact `planning_index`/`delivery_constraints`/`repair_context` inputs, production Schema/example binding, initial `repair_context=null`, and exact fresh semantic-repair context; no Agent runtime, input name, Schema, validator, or repair semantics change is permitted as prompt optimization. Covers shared prompt and revision-scope requirements and Design Decision 8. Expected paths: `nepa/agents/prompts/architecture_planner.md`, `tests/test_agent_rendering.py`, `tests/test_agent_roles.py`, `tests/test_prompt_development_neutrality.py`. Verify with `uv run pytest tests/test_agent_rendering.py tests/test_agent_roles.py tests/test_prompt_development_neutrality.py -q`.
- [x] 6.3 Drive the existing non-MQTT fixture through each candidate render, development binding, Schema path, and `ARCH_VALIDATE`, proving all concrete identifiers remain attributable to frozen fixture inputs or documented language/role rules and no model/provider condition appears. Covers the neutrality fixture scenario and Design Decision 8. Expected paths: `tests/fixtures/non_mqtt_application/`, `tests/test_planning_protocol_neutrality.py`, `tests/test_prompt_development_neutrality.py`. Verify with `uv run pytest tests/test_planning_protocol_neutrality.py tests/test_prompt_development_neutrality.py tests/test_architecture_validation.py -q`.
- [x] 6.4 Add snapshot-bound execution regressions proving Qwen/DeepSeek consume one identical admitted prompt snapshot and rejecting source drift, snapshot tamper, source/snapshot/provider-prompt mismatch, and check/use races before valid assessment publication. Covers prompt execution integrity without changing the Agent call shape and Design Decision 12. Expected paths: `tests/test_prompt_development_snapshot.py`, `tests/test_prompt_development_neutrality.py`, `tests/test_agent_rendering.py`. Verify with `uv run pytest tests/test_prompt_development_snapshot.py tests/test_prompt_development_neutrality.py tests/test_agent_rendering.py -q`.

## 7. Execute the Bounded Two-Model Development Protocol

- [x] 7.1 Before live calls, run all deterministic M1-4a2 tests; set `NEPA_M1_4A2_CONFIG` to an explicit complete two-model calibration-config path and `NEPA_M1_4A2_CONTEXT_LIMITS` to an explicit canonical context-limit declaration path; verify `NEPA_QWEN_API_KEY` and `NEPA_DS_API_KEY` are already non-empty in the current process environment without printing their values; and initialize a fresh corrected lineage from the two non-secret inputs and signed gold inputs. Preflight must verify pricing/provider/model/request parameters/context limits, require the config's `api_key_env` mapping to match those two fixed names, publish only names/non-secret projections in `lineage.json` and `protocol.json`, and admit no prior or cross-lineage evidence. The task does not ask the user to supply variable names, does not accept key values via CLI/configuration, and does not source or parse shell/dotenv files. Covers the development-evidence entry gate and Design Decisions 1/3/9/10. Expected evidence: `runs/_calibration/s4-architecture/<lineage_id>/lineage.json` and `prompt-development/protocol.json`. Verify with §8.1 followed by `uv run python -m nepa.calibration.s4_prompt_development init --config "${NEPA_M1_4A2_CONFIG}" --context-limits "${NEPA_M1_4A2_CONTEXT_LIMITS}" --spec gold_file/specIR.json --target gold_file/target.json --test-bundle gold_file/test_bundle.json`, without printing secrets.
- [x] 7.2 Run the actual V0 N=5/depth-one `attempt-001` for Qwen and DeepSeek from the single immutable `versions/v0/prompt.md` snapshot; require both raw-template hashes and provider renders to match it; recompute two reports from that same valid attempt; publish `assessment-n005.json`; and either select V0 immediately if it passes or record the exact failing evidence that admits one V1 hypothesis. If any model is infrastructure-invalid, retain the entire invalid attempt and rerun both models under the uniquely admitted next attempt; fake-provider output, failed-model-only retry, or cross-attempt splicing cannot satisfy this task. Set task-specific `NEPA_M1_4A2_ROOT` to the exact emitted prompt-development root after validating it is below `runs/_calibration/s4-architecture/`. Covers V0, attempt, snapshot, and first-passing stop requirements and Design Decisions 5/9/11/12. Verify with `uv run python -m nepa.calibration.s4_prompt_development run-version --development-root "${NEPA_M1_4A2_ROOT}" --version v0` followed by `uv run python -m nepa.calibration.s4_prompt_development recompute --development-root "${NEPA_M1_4A2_ROOT}"`.
- [x] 7.3 Execute the single state-machine-authorized post-V0 branch: if V0 selected, record that V1/V2 are not admitted; otherwise publish one V1 revision from an explicit closed input record, edit only the shared prompt, immutably admit `versions/v1/prompt.md`, run one coherent actual two-model V1 N=5 attempt, rerun both under a new attempt after any infrastructure-invalid result, and publish the final same-attempt N=5 assessment. Set task-specific `NEPA_M1_4A2_REVISION` to the validated revision-input path. Covers V1 revision/sequence/attempt/snapshot requirements and Design Decisions 5/11/12. Verify with `uv run python -m nepa.calibration.s4_prompt_development record-revision --development-root "${NEPA_M1_4A2_ROOT}" --version v1 --input "${NEPA_M1_4A2_REVISION}"` and `uv run python -m nepa.calibration.s4_prompt_development run-version --development-root "${NEPA_M1_4A2_ROOT}" --version v1`; when V0 selected, the same commands must reject before provider I/O.
- [x] 7.4 Resolve V1 only through the specified state: perform the append-only N=10 extension when and only when its recomputed ambiguity predicate qualifies; then select the first passing final V1, or record one second distinct evidence-backed hypothesis that alone admits V2. Covers N=10 and V2 admission requirements and Design Decisions 4-6. Verify an admitted extension with `uv run python -m nepa.calibration.s4_prompt_development expand --development-root "${NEPA_M1_4A2_ROOT}" --version v1` and always rerun `uv run python -m nepa.calibration.s4_prompt_development recompute --development-root "${NEPA_M1_4A2_ROOT}"`; when expansion is not admissible, `expand` must fail before provider I/O and recompute must report the exact next state.
- [x] 7.5 If V2 is admitted, edit only the shared prompt once more, immutably admit its snapshot, run one coherent actual two-model N=5 attempt, rerun both under a fresh attempt after infrastructure invalidity, perform only an evidence-qualified attempt-qualified N=10 extension, and either select the first passing V2 or execute the fixed fallback tuple after complete failing evidence; if V2 is not admitted, verify the terminal selection prevents these calls. Covers optional V2, attempt, snapshot, and fallback requirements and Design Decisions 5-7/11/12. Verify with `record-revision --development-root "${NEPA_M1_4A2_ROOT}" --version v2 --input "${NEPA_M1_4A2_REVISION}"`, `run-version --development-root "${NEPA_M1_4A2_ROOT}" --version v2`, the conditionally admitted `expand --development-root "${NEPA_M1_4A2_ROOT}" --version v2`, and final `recompute --development-root "${NEPA_M1_4A2_ROOT}"`, each prefixed by `uv run python -m nepa.calibration.s4_prompt_development`; every forbidden command must stop before provider I/O.
- [ ] 7.6 Ensure the repository ArchitecturePlanner source template bytes still equal the selected immutable prompt ref/hash and that no source drift, snapshot tamper, or source/snapshot/provider-prompt mismatch occurred during its valid attempt; publish the final technical handoff containing only lineage/version/prompt/assessment/selection identity, and explicitly leave M1-4a3 N=20, B1-B4, production model/repair/call-shape freezes, and responsible-owner signature unsatisfied. Covers the M1-4a2 completion boundary and Design Decisions 9/12. Verify with `uv run python -m nepa.calibration.s4_prompt_development recompute --development-root "${NEPA_M1_4A2_ROOT}" --require-source-match` and validate the resulting selection/handoff against their closed Schemas.

## 8. Acceptance and Scope Audit

- [x] 8.1 Run all focused deterministic regressions for lineage/batch/configuration binding, fixed two-key-name/presence/non-disclosure and `max_tokens=65536` behavior, development Schemas/artifacts/resume, base and extension attempt state, immutable prompt snapshots/TOCTOU, bounded ordering, revisions, report recomputation, screening/selection, scope, and neutrality: `uv run pytest tests/test_calibration_lineage.py tests/test_calibration_artifacts.py tests/test_architecture_calibration.py tests/test_calibration_reporting.py tests/test_prompt_development_schema.py tests/test_prompt_development_artifacts.py tests/test_prompt_development_config.py tests/test_prompt_development_attempts.py tests/test_prompt_development_snapshot.py tests/test_prompt_development.py tests/test_prompt_development_revisions.py tests/test_prompt_development_extension.py tests/test_prompt_development_reporting.py tests/test_prompt_development_selection.py tests/test_prompt_development_scope.py tests/test_prompt_development_neutrality.py tests/test_agent_rendering.py tests/test_agent_roles.py tests/test_agent_protocol_neutrality.py tests/test_planning_protocol_neutrality.py tests/test_architecture_validation.py -q`. Covers every delta requirement and Design Decisions 1-12.
- [ ] 8.2 Recompute the complete actual development evidence from lineage/configuration/prompt-snapshot/attempt/trial/trace/report/revision/extension records, verify the selected source/snapshot/provider-prompt hashes and one valid same-attempt two-model assessment per executed version, and confirm every conditionally required live task in §7 has either its required immutable evidence or a terminal state that mechanically forbids it; no invalid/cross-attempt/fake evidence or unverified model prose may satisfy this gate. Covers M1-4a2 technical DoD and Design Decisions 9-12. Verify with `uv run python -m nepa.calibration.s4_prompt_development recompute --development-root "${NEPA_M1_4A2_ROOT}" --require-complete --require-source-match`.
- [x] 8.3 Inspect the complete diff and evidence boundary, remove unrelated/speculative code, then run `uv run pytest -q`, `openspec validate --all --strict`, and `git diff --check`. Confirm the authorized `project_docs/system_design.md` v3.0.0 dual-model/max-token update is the only design-document edit; confirm no N=20 qualification, `model_comparison.json`, B1-B4/production selection, owner signature/freeze, M1-4b compiler asset, M1-4c/S5/S6 behavior, public CLI, formal Run/S4 receipt/Plan/Blueprint/Report v2, or protocol/model-specific prompt branch was added; M1-4a3 remains the next serial change.
