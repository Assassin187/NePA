## 1. Configuration and Runtime Contracts

- [ ] 1.1 Add the design-required Pydantic v2 runtime dependency, create the closed §8.3 configuration models and `configs/default.yaml`, and reject undeclared or ill-typed settings without interpreting M1-2 provider behavior. **Requirements:** Configuration is resolved once and sealed without secrets. **Design:** §8.1-§8.3; `design.md` Decisions 1 and 10. **Paths:** `pyproject.toml`, `uv.lock`, `configs/default.yaml`, `nepa/config.py`, `tests/test_config.py`. **Verify:** `uv run pytest tests/test_config.py -q -k 'model or invalid'`.
- [ ] 1.2 Implement deterministic defaults → YAML → explicit override merging, secret-free public snapshots, canonical snapshot hashing, and hash-drift rejection; cover precedence, stable hashes, environment-secret non-persistence, and the fact that M1-4a3 has not yet qualified S4 budget defaults. **Requirements:** Configuration is resolved once and sealed without secrets. **Design:** §5.6.2, §8.3; `design.md` Decision 1. **Paths:** `nepa/config.py`, `tests/test_config.py`. **Verify:** `uv run pytest tests/test_config.py -q -k 'precedence or snapshot or secret or drift'`.
- [ ] 1.3 Audit the existing Run v3 and Report v2 Schemas/examples against the M1-1 initial, non-terminal, planned-stop, controlled-exit, internal-error, availability-envelope, and request-stage binding cases; add only fields or constraints directly required by the cited design and add evidence-backed negative tests. **Requirements:** Run state and output anchors are atomically durable; Termination branches have distinct persisted semantics; Controlled exits produce a conditionally valid partial report. **Design:** §5.4, §5.6.2, §6.9, §9.1.2; `design.md` Decisions 5, 7, 8, and 10. **Paths:** `nepa/schemas/run.schema.json`, `nepa/schemas/report.schema.json`, `nepa/schemas/examples/run.example.json`, `nepa/schemas/examples/report.example.json`, `tests/test_schema_examples.py`, focused runtime Schema tests under `tests/`. **Verify:** `uv run pytest tests -q -k 'schema_examples or runtime_schema or run_terminal or report_availability'`.

## 2. Durable Run Store and Spec-Run Initialization

- [ ] 2.1 Implement `RunStore` canonical JSON/raw-byte hashing, same-filesystem temporary publication, file and directory `fsync`, atomic replacement, immutable identical-byte replay, artifact-reference verification, path confinement to the run root, and append-only stage events. **Requirements:** Run state and output anchors are atomically durable. **Design:** §4.4, §4.8, §5 general conventions; `design.md` Decision 3 and Interfaces and Ownership. **Paths:** `nepa/run_store.py`, `tests/test_run_store.py`. **Verify:** `uv run pytest tests/test_run_store.py -q -k 'atomic or immutable or ref or event or path'`.
- [ ] 2.2 Add the non-blocking process-held advisory controller lock and prove a competing run/resume session is rejected without mutating `run.json`; ensure every future orchestration entry uses this lock rather than a separate PID-file path. **Requirements:** Resume reconciles durable facts before continuing. **Design:** §4.8; `design.md` Decision 4. **Paths:** `nepa/run_store.py`, `tests/test_run_store.py`, `tests/test_runtime_resume.py`. **Verify:** `uv run pytest tests/test_run_store.py tests/test_runtime_resume.py -q -k 'lock or concurrent or live_controller'`.
- [ ] 2.3 Implement staged spec-run initialization: reuse M0 Spec/Target/Test Bundle validation, freeze the documented run-local input files with the correct raw/canonical hash semantics, construct and validate initial Run v3 with S1-S3 skipped and S4-S9 pending as applicable, then atomically publish the complete run directory. **Requirements:** Spec-run initialization freezes the authoritative inputs; M0 public behavior and frozen inputs remain unchanged. **Design:** §4.2, §4.4, §5.6.2, §5.6.5.5; `design.md` Decision 2 and Persistent Commit and Recovery Sequence. **Paths:** `nepa/run_store.py`, reused `nepa/speclib/lint.py`, `tests/test_run_initialization.py`. **Verify:** `uv run pytest tests/test_run_initialization.py -q`.
- [ ] 2.4 Add initialization failure tests for missing/invalid source inputs, non-canonical Test Bundle bytes, unsupported Target roles, source drift during staging, and a crash before final run-directory rename; assert no resumable half-run is published and all three source inputs remain byte-identical. Separately prove that missing or hash-drifting committed run-local inputs are routed as a controlled failure before S4 admission. **Requirements:** Spec-run initialization freezes the authoritative inputs; M0 public behavior and frozen inputs remain unchanged. **Design:** §4.2, §4.4, §4.7, §4.8; `design.md` Decisions 2, 3, and 7. **Paths:** `tests/test_run_initialization.py`, `tests/test_termination.py`, any narrowly required test hooks in `nepa/run_store.py`. **Verify:** `uv run pytest tests/test_run_initialization.py tests/test_termination.py -q -k 'invalid or drift or crash or unchanged or frozen_input'`.

## 3. Stage State Machine and Budget Accounting

- [ ] 3.1 Define the typed runtime records and `StageController` boundary, then implement the allowed Run v3 stage transitions, upstream commit checks, validated `output_refs`, serial S4→S5→S6 admission, and verified-done no-op behavior using deterministic fake controllers. **Requirements:** The deterministic orchestrator owns stage progression; Run state and output anchors are atomically durable. **Design:** §4.2, §4.8; `design.md` Decisions 4 and 5 and Interfaces and Ownership. **Paths:** `nepa/orchestrator.py`, `tests/test_orchestrator.py`. **Verify:** `uv run pytest tests/test_orchestrator.py -q -k 'sequence or transition or output_ref or idempotent'`.
- [ ] 3.2 Implement monotonic active-time synchronization and persisted provider-usage deltas at stage, pre-call, post-call, and normal-finalization boundaries; enforce configured global wall-clock/cost limits, charge returned usage before stopping, and keep cached provider increments at zero. **Requirements:** Global budgets persist across resume and stop work at boundaries. **Design:** §4.7, §5.6.2; `design.md` Decision 6. **Paths:** `nepa/orchestrator.py`, `tests/test_budget.py`. **Verify:** `uv run pytest tests/test_budget.py -q`.
- [ ] 3.3 Add multi-session budget tests with injected UTC/monotonic clocks proving offline time is excluded, active time accumulates across fresh orchestrator instances, a pre-stage exhausted budget leaves the stage pending, and a post-call overrun is persisted before any later action. **Requirements:** Global budgets persist across resume and stop work at boundaries. **Design:** §4.7; `design.md` Decision 6 and Validation Strategy. **Paths:** `tests/test_budget.py`, narrowly required clock hooks in `nepa/orchestrator.py`. **Verify:** `uv run pytest tests/test_budget.py -q -k 'resume or offline or pre_stage or post_call or cached'`.

## 4. Termination Routing and Minimal S9 Core

- [ ] 4.1 Implement typed controlled-exit persistence and the disjoint planned-stop, controlled-exit, and internal-error branches: persist the affected S4-S6 stage plus the single request before S9, derive planned stop only from the sealed snapshot, and never attach an outcome to planned stop or internal error. **Requirements:** Termination branches have distinct persisted semantics. **Design:** §4.7, §5.6.2, §8.7, §9.1.2; `design.md` Decision 7. **Paths:** `nepa/orchestrator.py`, `tests/test_termination.py`. **Verify:** `uv run pytest tests/test_termination.py -q`.
- [ ] 4.2 Implement the deterministic controlled-exit Report v2 builder and minimal Markdown rendering using only validated available facts; copy the persisted reason exactly, classify the process outcome by §9.1.2, use availability envelopes/null reasons for missing downstream artifacts, validate both outputs, and commit their refs as the S9 output receipt before finalization. **Requirements:** Controlled exits produce a conditionally valid partial report. **Design:** §5.4, §6.9, §9.1.2; `design.md` Decision 8. **Paths:** `nepa/stages/__init__.py`, `nepa/stages/s9_report.py`, `tests/test_s9_partial_report.py`. **Verify:** `uv run pytest tests/test_s9_partial_report.py -q`.
- [ ] 4.3 Add fault-injection tests for global budget exhaustion and expected structured-stage failures at S4/S6, proving request-before-S9 ordering, `enforce=False` S9 entry/final budget sync, Schema-valid partial reports, correct 10/20 exit codes, and no `internal_error` misclassification; separately test deterministic tool/invariant failures as exit code 1 with no process report. **Requirements:** Global budgets persist across resume and stop work at boundaries; Termination branches have distinct persisted semantics; Controlled exits produce a conditionally valid partial report. **Design:** §4.7, §6.9, §9.1.2, D1.10; `design.md` Decisions 6-8. **Paths:** `tests/test_termination.py`, `tests/test_s9_partial_report.py`, narrowly required injection hooks in runtime modules. **Verify:** `uv run pytest tests/test_termination.py tests/test_s9_partial_report.py -q -k 'budget or structured or ordering or internal_error'`.

## 5. Resume and Crash Reconciliation

- [ ] 5.1 Implement resume validation of Run v3, configuration snapshot hash, stage refs, and terminal state; after acquiring the controller lock, atomically convert orphaned `running` stages to `failed` with `process crashed mid-stage` without creating a request, then retry the first eligible ordinary stage through the shared transition path. **Requirements:** Resume reconciles durable facts before continuing; The deterministic orchestrator owns stage progression. **Design:** §4.8; `design.md` Decision 9. **Paths:** `nepa/orchestrator.py`, `tests/test_runtime_resume.py`. **Verify:** `uv run pytest tests/test_runtime_resume.py -q -k 'orphaned or retry or config_hash'`.
- [ ] 5.2 Implement request-aware S9 reconciliation: bypass all non-S9 stages when a request exists, finalize from an already valid S9 commit, retry an orphaned/incomplete S9, and fail-stop as `internal_error` without reopening a `done` S9 when its report/ref/Schema/reason binding is corrupt. **Requirements:** Resume reconciles durable facts before continuing; Controlled exits produce a conditionally valid partial report. **Design:** §4.8, §6.9; `design.md` Decisions 8 and 9. **Paths:** `nepa/orchestrator.py`, `nepa/stages/s9_report.py`, `tests/test_runtime_resume.py`. **Verify:** `uv run pytest tests/test_runtime_resume.py -q -k 'request or s9 or corrupt or fail_stop'`.
- [ ] 5.3 Add crash-window tests using fresh `RunStore`/`Orchestrator` instances after interruption around stage `running`, output publication, stage `done`, request persistence, S9 report publication, S9 commit, and terminal finalization; prove recovery uses only disk facts and does not duplicate committed immutable outputs. **Requirements:** Run state and output anchors are atomically durable; Resume reconciles durable facts before continuing. **Design:** §4.8, D1.4; `design.md` Persistent Commit and Recovery Sequence and Validation Strategy. **Paths:** `tests/test_runtime_resume.py`, narrowly required injection hooks in `nepa/run_store.py` and `nepa/orchestrator.py`. **Verify:** `uv run pytest tests/test_runtime_resume.py -q -k 'crash_window or replay or finalization'`.

## 6. Change-Level Acceptance and Scope Audit

- [ ] 6.1 Run the focused M1-1 runtime suite and the full existing test suite; run all three M0 lint commands and verify the current gold bytes against the confirmed freeze YAML, without rewriting any gold or freeze asset. **Requirements:** M0 public behavior and frozen inputs remain unchanged; all other run-lifecycle requirements. **Design:** §10.2 M1-1, §10.8; `design.md` Validation Strategy. **Paths:** all files changed by this change plus `gold_file/*` and `configs/m0-default-inputs.freeze.yaml` as read-only evidence. **Verify:** `uv run pytest tests/test_config.py tests/test_run_store.py tests/test_run_initialization.py tests/test_orchestrator.py tests/test_budget.py tests/test_termination.py tests/test_s9_partial_report.py tests/test_runtime_resume.py -q && uv run pytest tests -q && uv run nepa lint spec gold_file/specIR.json --gold --manifest gold_file/test_bundle.json && uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json && uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json` plus a scripted SHA-256 comparison with `configs/m0-default-inputs.freeze.yaml`.
- [ ] 6.2 Inspect the complete diff and remove unrelated refactoring, speculative validation, unused extension points, or any M1-2+ implementation; confirm `project_docs/system_design.md`, the three gold files, freeze records, and the pre-existing `openspec/config.yaml` user modification were not changed by implementation. Re-run `openspec validate m1-1-runtime-framework --strict` and the complete task 6.1 verification set. **Requirements:** M0 public behavior and frozen inputs remain unchanged; scope and completion rules for all run-lifecycle requirements. **Design:** §10.0, §10.2 M1-1, §10.8; `proposal.md` Out of scope; `design.md` Goals / Non-Goals. **Paths:** repository-wide diff inspection, with implementation limited to the paths declared in this change. **Verify:** `git diff --check && openspec validate m1-1-runtime-framework --strict` followed by all task 6.1 commands; confirm no responsible-owner signature task is required for M1-1 and do not claim M1/D1 completion.
