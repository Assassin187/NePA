## 1. Prerequisite and contract baseline

- [x] 1.1 Verify the exact committed M1-8 baseline without rewriting it: confirm every archived OpenSpec task is complete, validate implementation commit `3a249de` and finalization commit `292c632`, inspect its acceptance record and checked-in E1+/F2 fixtures, and run focused S5 multi-epoch, ordinary S6 and F1 lease tests. Requirements: all M1-9 prerequisite clauses. Design: Context, decisions 1/8. Paths: `openspec/changes/archive/2026-09-08-m1-8-s5-multi-epoch-materialization/`, `tests/test_s5_multi_epoch.py`, existing S6 tests. Verify: archived tasks/acceptance inspection; `git merge-base --is-ancestor 3a249de 292c632`; `uv run pytest -q tests/test_s5_multi_epoch.py tests/test_s6_execution.py tests/test_s6_lease_execution.py`.
- [x] 1.2 Map every target persisted field and producer/consumer from active Plan and M1-8 activation/epoch/binding through State, Task/Joint Evidence, verification WAL, revision event, file ledger, Run/receipt and execution lint; record concrete mismatches without modifying `project_docs/` or reopening prior changes. Requirements: successful migration publication and WAL completeness. Design: decisions 1/2/5/6. Paths: `nepa/schemas/`, `nepa/speclib/`, `nepa/stages/s6_execution.py`. Verify: focused Schema collection plus a requirement-to-contract checklist in the implementation brief or acceptance record.

## 2. Persisted migration and joint-verification contracts

- [x] 2.1 Extend Task Evidence as one closed discriminated contract for current Plan version/epoch and normal, revalidate, amend, lease and group execution: allow attempt zero only for REVALIDATE, require the exact migration ref for migrated forms, require independent amendment use for AMEND, and allow empty changed files only for accepted revalidation. Requirements: `s6-migration-joint-verification / Successful migration verification publishes one complete transaction`; `plan-state-validation / Execution lint verifies external evidence separately from snapshot shape`. Design: decision 5. Paths: `nepa/schemas/task-evidence.schema.json`, Schema examples/tests and S6 evidence builders. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s6_artifacts.py -k 'evidence or schema'` with positive and cross-form negative cases.
- [x] 2.2 Generalize Joint Evidence to explicit lease/group forms, preserving every F1 bound while adding frozen `group_id`, unrestricted exact group membership and per-member migration evidence for group results. Requirements: `plan-revision-infrastructure / Joint S6 verification records every lease member`. Design: decision 5. Paths: `nepa/schemas/joint-evidence.schema.json`, S6 artifact and lease tests. Verify: focused Schema tests accept valid lease/group examples and reject mixed lease/group fields, missing members and wrong ordering.
- [x] 2.3 Extend `verification_committed` and typed revision-ledger validation with `kind=group`, exact group id/member/evidence/current-revision bindings and deterministic replay/conflict behavior while retaining ordinary and lease events byte-for-byte. Requirements: modified joint-ledger requirement. Design: decisions 5/6. Paths: `nepa/schemas/revision-ledger.schema.json`, `nepa/speclib/plan_revision.py`, `tests/test_s6_artifacts.py`. Verify: `uv run pytest -q tests/test_plan_revision.py tests/test_s6_artifacts.py -k 'verification or group or lease'`.
- [x] 2.4 Extend the verification WAL Schema to closed normal/lease/group variants with complete group activation, baseline, candidate/failure, result, evidence, commit and old/allocated/new snapshot fields; remove no existing F0/F1 invariant. Requirements: `plan-revision-infrastructure / Migration verification WAL binds every pre-commit and post-commit fact`. Design: decisions 5/6. Paths: `nepa/schemas/verification-pending.schema.json`, RunStore/S6 WAL readers and Schema tests. Verify: valid examples for all three kinds plus negatives for partial members, incompatible discriminators, absent baseline and snapshot drift.
- [x] 2.5 Add or update attempt/validation records and Run/S6-related Schema references only as required to represent revalidation allocation, AMEND's independent call and group candidates/failures across current versions/epochs. Requirements: migration-mode bounded semantics and recovery. Design: decisions 3/6. Paths: `nepa/schemas/`, `nepa/run_store.py`, Schema example tests. Verify: all touched Schema examples validate and old E0/F1 examples remain accepted without compatibility fallbacks.

## 3. Migration State projection and validation

- [x] 3.1 Correct complete activation-time State projection: INHERIT preserves accepted proof, REVALIDATE becomes pending without future proof, AMEND preserves even full ordinary attempts with `amendment_used=0`, REGENERATE starts pending/normal at zero, and every changed row receives the exact accepted activation migration ref and frozen group id. Requirements: modified Plan-State Schema/snapshot and transition requirements. Design: decision 2. Paths: `nepa/speclib/plan_revision.py`, `nepa/schemas/plan-state.schema.json`, `tests/test_plan_revision_state.py`. Verify: focused projection tests cover all four classifications, full-attempt AMEND, absent/incorrect activation refs and no premature done/evidence.
- [x] 3.2 Implement deterministic revision-based dependency reopening in the complete migration projection, preserving usage/mode and reopening only when the current graph and migrated ancestor State prove the prior block is gone. Requirements: `plan-state-validation / State transitions are derived from the closed event table`. Design: decisions 2/7. Paths: plan-revision/state helpers and focused State tests. Verify: legal removed/changed dependency reopens pending; unchanged blocked ancestry, caller-only claims and unrelated terminal rows are rejected.
- [x] 3.3 Add pure State transitions for AMEND start/success/failure and REVALIDATE start/pass/fail with exact evidence-sequence and global-call accounting; keep normal and F1 tables unchanged. Requirements: migration-mode bounded semantics and modified State transition requirement. Design: decision 3. Paths: `nepa/speclib/plan_state.py`, `nepa/schemas/plan-event.schema.json`, State tests. Verify: full-attempt AMEND uses one amendment/global call but no fifth attempt; REVALIDATE uses no coding/global call and failure blocks with typed proof.
- [x] 3.4 Add complete all-member `group_verified`, group exhaustion and reconciled-group transitions that consume exact frozen membership and one commit/tree/evidence set, never a partial row update. Requirements: group publication/failure and modified State transition requirement. Design: decisions 4/6/7. Paths: Plan-State projector/event Schema and tests. Verify: exact group pass/failure derives one complete State; omitted, duplicated, wrong-group or mixed-migration members fail without mutating input.
- [x] 3.5 Extend snapshot lint for current binding/epoch migration forms, group ids and mode-specific counters, then prove deterministic issue ordering and no mutation. Requirements: modified Plan-State Schema/snapshot and deterministic validation requirements. Design: decisions 1/2. Paths: `nepa/speclib/plan_state.py`, State lint tests. Verify: valid F2/F3 snapshots pass; premature success, stale Plan/binding, illegal fields and old-major mixes fail identically on replay.

## 4. Current-version S6 admission and singleton modes

- [x] 4.1 Replace E0 constants in S6 admission with one current execution context validated against the active pointer, latest activation, current Plan/binding/epoch, immutable/current manifest/map, State/ledgers, recomputed Blueprint, workspace checkpoint/HEAD and frozen config; retain the unique fresh-E0 initialization rule. Requirements: modified S6 admission and migration work ordering. Design: decision 1. Paths: `nepa/stages/s6_execution.py`, materialization/revision validators, S6 admission tests. Verify: ready E0 regression, accepted F2/current-epoch and ready F3 admission pass; stale/missing/mixed refs fail before allocation.
- [x] 4.2 Admit `pending_repair` only for the exact F3 groups common to activation, epoch receipt and State, process migration work before ordinary selection, and prevent ordinary exit/admission from treating pending repair as ready. Requirements: migration work ordering and modified S6 admission. Design: decisions 1/4. Paths: S6 controller/orchestrator and admission tests. Verify: exact group is selected first; missing/extra/resolved/mismatched group ids and ordinary selection under unresolved pending repair fail.
- [x] 4.3 Implement singleton REVALIDATE through the existing build/smoke and evidence path with zero Agent/global attempt use and a legal same-tree evidence commit when content is unchanged. Requirements: migration-mode semantics and successful publication. Design: decisions 3/5/6. Paths: S6 controller, RunStore allocation, Git helpers and execution tests. Verify: current-version attempt-zero evidence, result refs, commit/tree, State/ledger/event and interrupted replay all agree; no LLM trace row is added.
- [x] 4.4 Implement singleton AMEND allocation/context/execution/failure through exactly one T1 Fixer call, preserving ordinary attempts including four and consuming one amendment/global allowance before provider I/O. Requirements: migration-mode semantics. Design: decision 3. Paths: S6 controller, RunStore allocator, Agent context tests and execution tests. Verify: full-history AMEND succeeds once; interruption remains spent; second amendment, T2/Coder routing or ordinary-attempt increment is rejected.
- [x] 4.5 Route REGENERATE into the existing normal Coder/Fixer pipeline as a zero-attempt new generation with mandatory migration lineage, preserving prior-generation audit facts and all F0/F1 bounds. Requirements: migration-mode semantics. Design: decisions 2/3. Paths: S6 selection/allocation/evidence and State tests. Verify: first call is Coder/T2, retries follow normal limits, evidence binds current generation and no historical allowance is silently copied.
- [x] 4.6 Generalize successful singleton evidence, task commit trailers, file-ledger projection, verification event and failure artifacts for current Plan version/epoch and each migration kind without weakening existing ordinary/lease validation. Requirements: successful migration publication and external execution lint. Design: decisions 3/5/6. Paths: S6 evidence/publication helpers, Git operations, file-ledger helpers and execution tests. Verify: normal E0 plus revalidate/amend/regenerate positive and cross-kind corruption cases.

## 5. F3 cumulative repair-group execution

- [x] 5.1 Build and validate one immutable group descriptor from the accepted activation/Plan/State/Blueprint/contract map, including stable topological member order, exact affected paths/symbols/build artifacts, external dependency readiness and epoch-checkpoint baseline. Requirements: migration work ordering and group assembly. Design: decision 4. Paths: S6 controller plus revision/materialization helpers and group tests. Verify: valid group replay is deterministic; inferred, stale, duplicate, unsafe, non-member or unready inputs fail before allocation.
- [x] 5.2 Implement persisted cumulative group candidate assembly: traverse members in stable order, reuse the existing closed task context against the accumulated candidate tree, allocate each applicable mode before I/O, and store candidate/failure refs without formal State readiness. Requirements: group assembly without intermediate success. Design: decisions 3/4. Paths: S6 controller, candidate workspace/store and group tests. Verify: two members' sequential responses compose on one tree; intermediate State/ledger/Git/event remain unchanged and out-of-group writes/declarations fail.
- [x] 5.3 Run every default build and smoke gate after each bounded traversal and deterministically map structured compiler/linker diagnostics through the frozen group closure; retry only implicated members with allowance and use the full group when attribution is not strict. Requirements: group assembly and failure semantics. Design: decision 4. Paths: S6 build/smoke integration, M1-8 diagnostic structures and group tests. Verify: strict one/two-member attribution, unlocatable failure, warnings, timeout, smoke failure and no-allowance cases take the specified path without an extra budget.
- [x] 5.4 Implement group success projection: allocate one evidence sequence per exact member, publish current-version Task Evidence and sorted `kind=group` Joint Evidence, prepare one joint commit including empty-change legality only for migration verification, and derive one complete State/file-ledger/event result. Requirements: successful migration transaction and group ledger requirement. Design: decisions 5/6. Paths: S6 controller, RunStore, Git helpers and artifact tests. Verify: the two-member incompatible fixture yields one tree/commit/event and every member proof; strict subsets, wrong order and mixed lease fields fail.
- [x] 5.5 Implement group exhaustion: restore the accepted epoch baseline, block all unresolved members with `GROUP_VALIDATION_EXHAUSTED`, preserve consumed calls/sequences/candidates/failures, propagate proven dependency blocks and allow only independently fully verifiable branches. Requirements: group failure and recovery. Design: decision 7. Paths: S6 controller, State dependency logic and failure tests. Verify: no intermediate done/commit, dependent rows block, legal independent branch runs, and globally broken build routes `EXECUTION_UNRESOLVED` without M1-12 events.

## 6. Atomic publication, recovery and execution lint

- [x] 6.1 Implement group WAL preparation/publication in the fixed order from allocated facts through candidates/results, immutable member/joint evidence, exact tree installation, legal commit, all-member State/file-ledger/event publication and terminal record cleanup. Requirements: migration WAL completeness and successful migration transaction. Design: decision 6. Paths: `nepa/stages/s6_execution.py`, `nepa/run_store.py`, Git helpers and fault tests. Verify: inspect every durable boundary and assert no done row exists before the legal commit.
- [x] 6.2 Implement pre-commit group reconciliation that verifies/restores only WAL-listed baseline paths, preserves spent work and evidence gaps, rejects unrelated dirty bytes, and deterministically reassembles persisted candidates when work remains. Requirements: group failure/recovery and migration WAL. Design: decision 6. Paths: S6/RunStore reconciliation and fault tests. Verify: faults after each allocation/candidate/result/evidence/install boundary converge to the same baseline/candidate set without refund, duplicate call or lost prior content.
- [x] 6.3 Implement post-commit forward reconciliation that verifies parent/tree/trailers, current activation/group, every evidence/result ref and complete snapshot derivation before publishing all missing members/event once. Requirements: group failure/recovery and modified joint-ledger requirement. Design: decision 6. Paths: S6 reconciliation, Plan-State/revision helpers and corruption tests. Verify: faults after commit and each State/ledger/event/terminal write converge without Agent/build/smoke/commit replay; any partial/conflicting accepted fact fail-stops.
- [x] 6.4 Extend execution-state lint across current versions/epochs and migration singleton/group commits, including same-tree REVALIDATE, full-attempt AMEND, current binding/S5 ancestry, exact migration lineage and complete Joint Evidence/WAL/event membership; retain ordinary and lease checks. Requirements: modified execution lint. Design: decisions 1/5/6. Paths: `nepa/speclib/plan_state.py`, execution lint tests. Verify: positive normal/lease/revalidate/amend/regenerate/group histories plus one corruption test per binding/ref/trailer/counter/member boundary.
- [x] 6.5 Integrate verification reconciliation before every S6 admission/resume and completed-stage check, and keep final S6 receipt/exit validation anchored to the current binding, epoch, complete State, file ledger, immutable revision-ledger prefix, clean HEAD, builds and smoke. Requirements: modified admission and all recovery requirements. Design: decisions 1/6/7. Paths: orchestrator, S6 controller/receipt and runtime-resume tests. Verify: revised run resumes in order, completed replay is zero-action, and unresolved groups cannot seal a successful S6 receipt.

## 7. Frozen dual-protocol fixtures and M1-9 scenarios

- [x] 7.1 Extend the developer fixture generator to derive complete MQTT and non-MQTT M1-9 F2/F3 packages from accepted M1-8 artifacts and real M1-6/M1-7-shaped history, including deterministic frozen responses, activation migration refs, group closures and expected evidence. Requirements: all legal-input/protocol-neutral clauses. Design: decision 8. Paths: `tests/tools/`, `tests/fixtures/s6/`, fixture provenance README/tests. Verify: generation into two temporary roots is byte-stable against checked-in assets, every nested ref/Schema validates and CI never regenerates in place.
- [x] 7.2 Add `s6_execution` end-to-end singleton cases for same-tree zero-attempt REVALIDATE, full-four-attempt AMEND and zero-attempt REGENERATE on both protocol families, asserting exact roles/budgets/evidence/commits/State/ledger/events. Requirements: migration-mode and singleton publication requirements. Design: decisions 3/5/8. Paths: frozen S6 fixtures and `tests/test_s6_execution.py` or focused migration test module. Verify: `uv run pytest -q -m s6_execution -k 'revalidate or amend or regenerate'` collects and passes intended cases.
- [x] 7.3 Add the required two-member incompatible F3 group case for both protocols, proving sequential candidate assembly, temporary readiness only, one whole-group validation result and one atomic evidence/commit/State publication. Requirements: group assembly and successful transaction. Design: decisions 4-6/8. Paths: group fixtures/tests. Verify: `uv run pytest -q -m s6_execution -k 'migration_group and success'` plus assertions that no member success artifact predates the joint commit.
- [x] 7.4 Add dependency reopening, group exhaustion and independent-branch cases, including a default-build closure that forces controlled degradation. Requirements: migration ordering, State reopening and group failure. Design: decisions 2/7/8. Paths: group fixtures/tests and State tests. Verify: focused selectors assert exact pending/blocked transitions, no partial done and no trigger/circuit-breaker event.
- [x] 7.5 Add markerized faults at every pre/post-group-commit boundary and corruption fixtures for member omission, wrong group/revision/mode, stale candidate/tree, invalid trailer, damaged evidence/result and partial mutable projections. Requirements: WAL/recovery and D1.15. Design: decisions 5/6/8. Paths: S6 fault fixtures/tests. Verify: `uv run pytest -q -m s6_execution -k 'group and (recovery or corruption)'`; each case converges uniquely or fail-stops without duplicated external work.
- [x] 7.6 Add protocol-neutrality and predecessor regression checks proving MQTT/non-MQTT use the same controller/templates and all existing F0/F1/S5 multi-epoch fixtures retain their expected semantics. Requirements: preservation clauses. Design: decisions 1/3/8. Paths: fixture tests, neutrality scans and existing S5/S6 suites. Verify: focused neutrality, `s5_epoch`, ordinary S6 and lease markers all pass.

## 8. Scope, acceptance and owner gate

- [x] 8.1 Audit requirement→Schema-valid fixture→controller→persisted evidence/commit/State/ledger/event→corruption-test closure and remove E0 constants, fake lease reuse, speculative compatibility/fallbacks, duplicate execution frameworks and any reachable M1-10+ producer. Requirements: all M1-9 requirements. Design: all decisions. Paths: complete M1-9 implementation diff. Verify: recorded closure matrix plus searches for stale `1.0.0`/`E0` contract constants and group fields outside discriminated forms.
- [x] 8.2 Run focused Schema, plan-revision/state, RunStore/Git/orchestrator, S5 multi-epoch, S6 ordinary/F1/migration/group and D1.15 fault tests in the real sandbox. Requirements: all M1-9 requirements and predecessor preservation. Design: migration plan steps 1-6. Paths: affected implementation/tests. Verify: all focused commands recorded with nonzero intended collection and no modified `project_docs/`, prompt or calibration asset.
- [x] 8.3 Run full automated acceptance: `uv run pytest -q -m s6_execution`, `uv run pytest -q -m s5_epoch`, `uv run pytest -q`, `uv run ruff check .`, `uv run mypy nepa`, public Schema/gold/Plan lint commands, `openspec validate m1-9-s6-migration-joint-verification --strict`, `openspec validate --all --strict`, and `git diff --check`; write an exact acceptance record for D1.15 and remaining later-milestone boundaries. Requirements: proposal acceptance and all delta capabilities. Design: migration plan. Paths: complete repository/change diff and acceptance record. Verify: every command is green with exact counts/evidence refs.
- [x] 8.4 Obtain and record the responsible owner's explicit review of the M1-9 acceptance package before archive; leave this task unchecked until that external confirmation exists, and do not treat it as D1.14 parameter approval or final M1-15 acceptance. Requirements: project OpenSpec owner-gate rule. Design: Goals/Non-Goals. Paths: M1-9 acceptance record only. Verify: dated owner decision names the reviewed evidence and scope.
- [x] 8.5 Inspect the final diff, remove unrelated refactoring/configurability and rerun the complete M1-9 acceptance after any review correction; confirm M1-8 was neither archived nor rewritten, all required tasks and D1.15 pass, the owner gate is truthful, and M1-10 triggers/operators, M1-11 activation, M1-12 circuit breakers, M2 tests, public CLI, design docs and prompt/calibration changes remain absent. Requirements: all change requirements and non-scope. Design: all decisions/risks. Paths: complete repository/change diff. Verify: final command transcript is green and every remaining limitation is explicitly reported before marking the change complete.

## 9. Review corrections (supersedes the original acceptance result)

- [x] 9.1 Correct current F2 admission so the active Plan is bound to its current Blueprint/manifest/map/metadata while a reused epoch receipt remains bound to its materialized Plan; verify latest F2 activation, epoch, old receipt binding and checkpoint ancestry with real task renumbering and owner/provider changes. Requirements: current-version S6 admission. Design: decision 1 and review correction 1. Paths: `nepa/stages/s6_execution.py`, focused S5/S6 tests.
- [x] 9.2 Separate group and lease cardinality contracts: frozen groups allow one or more members while leases retain two or more, consistently across Task/Joint Evidence, verification WAL, revision events and pure validators. Requirements: migration group publication and joint-ledger validation. Design: decisions 4-6 and review correction 2. Paths: `nepa/schemas/`, `nepa/speclib/`, Schema and revision tests.
- [x] 9.3 Implement chained group transaction baselines: retain the epoch checkpoint as immutable ancestry anchor, bind each group WAL/evidence/commit/recovery to the clean accepted current HEAD/tree, preserve earlier verified groups, and prove resolved group commits are ancestors of current HEAD. Requirements: group assembly, publication and recovery. Design: decisions 4/6/7 and review correction 2. Paths: S6 controller, WAL Schema and group recovery tests.
- [x] 9.4 Freeze each member's writable set to `deliverable_files ∩ affected_paths`; pass only that set to Agents and normalizer/binding/commit validation, accept non-empty legal output subsets for normal/AMEND/REGENERATE, reject empty non-REVALIDATE changes, and resume from candidate manifests without duplicate calls. Requirements: bounded migration execution and cumulative candidates. Design: decisions 3/4 and review correction 3. Paths: S6 controller, candidate persistence, evidence validators and focused tests.
- [x] 9.5 Replace path-substring-only retry attribution with deterministic frozen path-owner, contract symbol/provider and Blueprint build-artifact mapping; fall back to the whole group only for smoke, timeout or non-strict diagnostics and preserve every mode/run-wide budget. Requirements: group failure attribution. Design: decisions 4/7 and review correction 3. Paths: S6 diagnostics and group tests.
- [x] 9.6 Add the review regression matrix for real F2 rebinding, singleton and chained groups, subset/empty candidates, candidate-manifest recovery, strict path/symbol/build attribution, WAL-anchor corruption and shared MQTT/non-MQTT execution. Requirements: D1.15 and all corrected clauses. Design: decision 8. Paths: focused Schema/State/revision/S6 tests and fixtures.
- [x] 9.7 Rerun focused and full acceptance, public Spec/Target/Test Bundle/Plan lint, strict change/all OpenSpec validation and `git diff --check`; inspect the final diff for F0/F1 and M1-8 preservation and non-scope, then append the new acceptance result and obtain the existing owner-gate confirmation. Requirements: all change requirements. Design: risks/migration plan. Paths: complete M1-9 diff and `acceptance-record.md`.
