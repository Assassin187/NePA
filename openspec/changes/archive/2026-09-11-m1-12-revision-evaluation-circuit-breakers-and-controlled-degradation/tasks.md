## 1. Entry conditions and derived implementation contract

- [x] 1.1 Verify the archived M1-11 superseding owner approval, active-change isolation, current strict OpenSpec baseline and current focused `revision_mechanism` collection; record exact commands/results before implementation. Requirements: proposal prerequisite and acceptance boundary. Design: Context/Goals. Paths: archived M1-11 acceptance record, current OpenSpec state and test collection only. Verify: `openspec list --json`, `openspec validate --all --strict`, and `uv run pytest -q -m revision_mechanism --collect-only` succeed with recorded counts.
- [x] 1.2 Derive the mandatory M1-12 implementation brief with only input artifacts/Schemas, outputs/acceptance commands, intended function/class signatures and every governing section; introduce no decision beyond this change and the authoritative design. Requirements: all M1-12 requirements. Design: decisions 1-8 and Migration step 1. Paths: this change's `implementation-brief.md` only. Verify: manual four-section audit against system design §10.8 and source-section references.
- [x] 1.3 Add positive and negative v2 revision-ledger Schema examples for activation-bound terminal evaluations, including positive revision sequence, canonical non-empty anchors and the three legal truth combinations; retain schema version 2.0 and reject `resolved=true, ineffective=true`. Requirements: unique activation-bound ledger facts. Design: decision 4. Paths: `nepa/schemas/revision-ledger.schema.json`, its example and Schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py -k revision` passes with each negative case exercised.

## 2. Pure revision availability and circuit-breaker projection

- [x] 2.1 Implement canonical trigger lookup and attempted-pair derivation by resolving each `candidate_rejected.trigger_event_seq` to its accepted trigger signature and including each accepted activation pair; fail closed on missing, future, wrong-route or inconsistent references. Requirements: pure ledger projection and gate admission. Design: decision 1. Paths: `nepa/speclib/revision_mechanism.py` and focused revision tests. Verify: `uv run pytest -q -m revision_mechanism -k 'attempted_pair or trigger_reference'` passes.
- [x] 2.2 Implement independent F2/F3 successful-activation counters and level limits from the validated ledger plus frozen configuration, including legal zero limits and exact equality exhaustion. Requirements: pure ledger projection and RG-3 independent allowances. Design: decisions 1/5. Paths: revision mechanism projection and config/ledger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'level_limit or activation_count'` passes.
- [x] 2.3 Implement one rejection streak per level: ignore other-level events, require different signatures, reset only on same-level activation and close at two accepted rejections; do not persist counters. Requirements: level-local rejection streaks. Design: decision 1. Paths: revision mechanism projection and breaker matrix tests. Verify: `uv run pytest -q -m revision_mechanism -k 'rejection_streak or level_closed'` passes.
- [x] 2.4 Derive `revision_locked` for an accepted ineffective evaluation, accepted F4/F5 diagnosis, or closure of both supported levels while leaving all Plan/State/Run bytes unchanged. Requirements: ineffective/unsupported-level lock. Design: decision 1. Paths: revision mechanism projection and side-effect tests. Verify: `uv run pytest -q -m revision_mechanism -k 'revision_locked or unsupported_level'` passes and snapshot assertions show no writable lock field.
- [x] 2.5 Replace the existing partial attempted-signature logic with the shared projection in trigger evaluation/selection, preserving canonical TR ordering, record-only events and untried higher-level escalation. Requirements: availability projection and gate admission. Design: decisions 1/5. Paths: `nepa/speclib/revision_mechanism.py`, S6 trigger seam and existing M1-10 regressions. Verify: focused trigger tests plus `uv run pytest -q -m revision_mechanism -k trigger` pass.

## 3. Terminal evaluation fact and ledger closure

- [x] 3.1 Build the canonical affected-obligation anchor set from immutable from/to Plans, patch targets, every non-INHERIT migration row, pending-group members and explicit lineage/dependency closure; reject empty, ambiguous or missing mappings. Requirements: evidence-bound terminal evaluation. Design: decision 2. Paths: revision/Plan helpers and anchor tests. Verify: split, merge, move, rewrite, F2 and F3 fixtures produce exact sorted anchors under `uv run pytest -q -m revision_mechanism -k obligation_anchor`.
- [x] 3.2 Implement the transaction-free evaluation-readiness query over accepted State/history, activation/materialization/verification/attempt/lease/group state and hard-budget availability; pending/in-progress migration work with legal budget must defer evaluation. Requirements: evidence-bound evaluation and S6 evaluation boundary. Design: decision 3. Paths: revision mechanism, S6 transaction readers and readiness tests. Verify: `uv run pytest -q -m revision_mechanism -k evaluation_readiness` covers every in-flight transaction and terminal/budget boundary.
- [x] 3.3 Recompute the activation's original trigger predicate on the frozen signature/anchor domain and implement the exact resolved, ineffective and unresolved truth table without using task-count, denominator or blocked-count changes as proof. Requirements: evidence-bound evaluation. Design: decision 3. Paths: existing fact projector/evaluator and outcome tests. Verify: `uv run pytest -q -m revision_mechanism -k 'evaluation_outcome or denominator'` passes resolved, persistent-signature, deleted-node, renumbered and insufficient-evidence cases.
- [x] 3.4 Build the deterministic `evaluated_at` boundary identity and canonical evidence refs from accepted State-history/ledger facts; collect stable trace `output_path` call refs through candidate/critic/activation/group/verification identities, deduplicate them and calculate exact non-negative cost including the valid zero-cost case. Requirements: terminal evaluation payload and metric source. Design: decision 3. Paths: revision mechanism, trace/metric association helpers and cost tests. Verify: fixed hand-calculation, duplicate-ref, zero-cost and resume-replay cases pass.
- [x] 3.5 Implement `append_revision_evaluated` and strengthen v2 ledger validation for prior activation, one evaluation per positive revision sequence, sorted unique non-empty anchors, legal truth combinations and intact hash/event sequencing; byte-equivalent replay is a no-op and conflicting replay is artifact damage. Requirements: unique fact and idempotent recovery. Design: decision 4. Paths: `nepa/speclib/plan_revision.py`, revision-ledger Schema and ledger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'revision_evaluated or evaluation_replay'` passes all positive, duplicate, conflict and malformed-history cases.
- [x] 3.6 Integrate evaluation before ordinary trigger selection at each legal S6 boundary and on resume; append or reuse the unique event without changing Plan, pointer, State, file ledger or workspace, and do not introduce an evaluation WAL. Requirements: S6 evaluates before another revision cycle. Design: decisions 3/4. Paths: `nepa/stages/s6_execution.py`, revision helpers and resume tests. Verify: crash-before-append, replay, pending traversal and completed traversal cases converge with one event and no duplicate call.

## 4. Gate and hard-budget integration

- [x] 4.1 Make candidate generation and RG-1 consume the shared availability projection: reject a tried pair or locked run, defer an activated signature awaiting evaluation, and admit only an independently applicable untried higher level. Requirements: gate admission. Design: decision 5. Paths: revision gate controller/S6 handoff and gate tests. Verify: `uv run pytest -q -m revision_mechanism -k 'rg1 and availability'` covers rejection, escalation, pending evaluation and locked histories.
- [x] 4.2 Make RG-3 consume the same level-local closure/count result and prove both cross-level directions: F2-full/F3-legal and F3-full/unrelated-F2-legal; retain existing preservation, rework and remaining-call checks. Requirements: RG-3 independent allowances. Design: decision 5. Paths: revision budget evaluator/gate controller and RG-3 tests. Verify: `uv run pytest -q -m revision_mechanism -k 'rg3 or cross_level'` passes equality, exhaustion and both independent-level cases.
- [x] 4.3 Audit every revision-side Agent/critic/candidate call boundary so synchronized global wall-clock/cost is checked before the call and actual post-call usage is retained even when it triggers exit; no later gate or activation begins after exhaustion. Requirements: hard global exhaustion and RG-3 hard limits. Design: decisions 5/7. Paths: revision controller, Agent invocation seam and orchestrator budget tests. Verify: focused fault tests prove zero calls after pre-call exhaustion and retained usage after post-call exhaustion.
- [x] 4.4 Recheck the projection and hard budgets immediately before candidate publication and activation, preserving M1-11 frozen-boundary and pointer-commit semantics; a newly closed/locked/budget-exhausted state publishes no formal version or activation. Requirements: gate admission/hard limits. Design: decisions 5/7. Paths: revision gate/activation controller and M1-11 activation recovery regressions. Verify: late-exhaustion and late-ledger-change fault cases fail without authoritative mutation, while all existing activation crash-window tests pass.

## 5. S6 branch continuation and controlled degradation

- [x] 5.1 Preserve group baseline, member terminal failures, attempts, build/smoke refs and accepted code when a registered repair group exhausts; complete any ready terminal evaluation before further selection. Requirements: independent work and S6 evaluation. Design: decisions 3/6. Paths: S6 group exhaustion path, State/history and group tests. Verify: `uv run pytest -q -m revision_mechanism -k 'group_exhaustion or group_evaluation'` proves no fabricated done, rollback or evidence loss.
- [x] 5.2 Add the deterministic complete-independence check over task dependencies, build artifacts and runtime/smoke prerequisites before selecting work outside a failed group; do not weaken the existing full acceptance gate. Requirements: S6 independent branches. Design: decision 6. Paths: S6 scheduler/Blueprint dependency readers and branch tests. Verify: task-disjoint/build-independent passes, task-disjoint/shared-build fails, and no Agent call occurs in the latter case.
- [x] 5.3 Integrate level/global closure with the S6 selection loop so an exhausted F2 can still route legal F3, no closure refreshes F0/AMEND/REGENERATE history, and accepted independent tasks retain their existing stable order and budgets. Requirements: level-local closure and independent work. Design: decisions 5/6. Paths: S6 revision handoff/scheduler and migration regressions. Verify: focused cross-level, attempt-history and independent-order tests pass.
- [x] 5.4 When no completely verifiable work or legal revision path remains, raise the existing `EXECUTION_UNRESOLVED` controlled failure with all authoritative artifacts intact; distinguish deterministic static-contract/artifact invalidity as failed and implementation invariant errors as internal_error. Requirements: controlled degradation. Design: decisions 6/7. Paths: S6 final/no-work path, orchestrator and termination tests. Verify: degraded/10, failed/20 and internal_error/1 fixtures each take only their documented route.
- [x] 5.5 Add an explicit S6 total-call-cap pre-selection/pre-allocation query and translate exhaustion into controlled degradation without allocating a call, changing an attempt row or surfacing `RunValidationError` as internal_error. Requirements: hard global exhaustion and S6 hard exit. Design: decision 7. Paths: S6 scheduler, RunStore allocation boundary and budget tests. Verify: exact-cap normal, AMEND, group and F1-related cases show no cap+1 call and preserve counters.
- [x] 5.6 Verify global budget and `EXECUTION_UNRESOLVED` requests use the existing termination transaction: request precedes S9, S9 uses `enforce=false`, report reason matches request, and `--until s6`/resume cannot convert the result to planned_stop or restart work. Requirements: hard exit and controlled degradation. Design: decision 7. Paths: orchestrator/S9 integration and runtime-resume/termination tests. Verify: focused budget, termination, partial-report and resume suites pass with exit code 10.

## 6. Protocol-neutral fixtures and acceptance matrix

- [x] 6.1 Extend the existing revision fixture generator in dependency order for both MQTT and non-MQTT with evaluation outcomes, rejection streaks, independent level exhaustion, F4/F5 lock, group termination, global-call exhaustion and independent-branch descriptors bound to source hashes. Requirements: all fixture-backed M1-12 behavior. Design: decision 8. Paths: `tests/tools/` and `tests/fixtures/revision/`. Verify: two temporary generations are byte-identical to each other and checked-in outputs, with no protocol-name branch in core code.
- [x] 6.2 Add a strict ledger/evaluation matrix covering prior-activation binding, canonical anchors, legal truth combinations, one terminal evaluation, append replay/conflict, broken refs/hash chain and crash-before-append resume. Requirements: plan-revision-infrastructure deltas. Design: decisions 2-4. Paths: revision ledger tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k revision_evaluated` has nonzero intended collection and passes.
- [x] 6.3 Add the complete circuit-breaker matrix: same-pair rejection, higher-level escalation, two different-signature rejections, same-level reset, other-level non-reset, independent configured limits, ineffective lock, F4/F5 lock and both-level closure. Requirements: availability/closure and gate deltas. Design: decisions 1/5. Paths: revision mechanism tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k 'circuit or level_closed or revision_locked'` passes.
- [x] 6.4 Add evaluation-timing/outcome integration cases for pending-after-activation, partial affected set, resolved signature, persistent ineffective signature, deleted/renumbered task, budget-unresolved and resume, asserting evaluation precedes any later candidate. Requirements: terminal evaluation and S6 delta. Design: decisions 2-4. Paths: S6 revision integration tests and fixtures. Verify: focused selection passes with exactly one evaluation and no premature event.
- [x] 6.5 Add the §10.2.2 cross-level acceptance case proving F2 full still permits a legal F3, plus the symmetric F3-full/unrelated-F2 case, without refunding attempts or changing production 0/0 defaults. Requirements: independent level allowances. Design: decisions 1/5. Paths: gate/S6 integration tests and config fixtures. Verify: focused cross-level selection passes and default-config regression still pauses/declines revision calls.
- [x] 6.6 Add the §10.2.2 unbuildable-group and independent-branch cases: one branch completes only when its full build/smoke gate is independent; a shared broken build causes immediate controlled degradation with no speculative call. Requirements: independent work/controlled exit. Design: decision 6. Paths: S6 group/branch integration tests and fixtures. Verify: focused group selector passes with exact State, call count, reason and exit code.
- [x] 6.7 Add wall-clock, cost and S6-call-cap exhaustion cases before/after relevant calls and across resume, asserting request-before-S9 ordering, no subsequent Agent role and no planned_stop masking. Requirements: hard global exhaustion. Design: decision 7. Paths: budget/orchestrator/termination integration tests. Verify: affected budget, termination, report and runtime-resume suites pass.
- [x] 6.8 Extend metric regressions with Schema-valid unique `revision_evaluated` histories, resolved/ineffective/unresolved results, deduplicated associated costs and zero-cost reason; change metric implementation only if required to consume `revision_seq` identity correctly. Requirements: existing metric consumer compatibility. Design: decision 8. Paths: `nepa/metrics.py` only if necessary and `tests/test_metrics_contract.py`. Verify: `uv run pytest -q -m metric_contract` passes all fixed cases without formula changes.
- [x] 6.9 Run predecessor regressions for M1-8 materialization, M1-9 migration/groups, M1-10 triggers/operators, M1-11 gates/activation/recovery, F0/F1, RunStore, public CLI and protocol neutrality; assert no PlanReviser, prompt/calibration, new CLI/config, S7/S8/M2 or protocol-specific production branch was added. Requirements: proposal non-scope and public-behavior preservation. Design: Goals/Non-Goals and decision 8. Paths: existing predecessor suites and final source scans. Verify: all selected suites/scans pass with production revision defaults still 0/0.

## 7. Closure, public CI and responsible-owner gate

- [x] 7.1 Audit requirement → Schema-valid fixture → pure projection/evaluation → ledger append → gate/S6 consumer → controlled exit/metric → replay/conflict test closure; remove duplicated counters, persisted lock flags, speculative compatibility/fallbacks and unrelated refactoring. Requirements: all change requirements. Design: all decisions/risks. Paths: complete M1-12 implementation diff. Verify: record a closure matrix and scope scan in `acceptance-record.md`.
- [x] 7.2 Run focused Schema, revision ledger/projection, gate, activation/recovery, S5/S6, group/branch, budget/termination/resume and metric tests, then the full intended `uv run pytest -q -m revision_mechanism` collection. Requirements: M1-12 §10.2.2 acceptance. Design: Migration steps 1-4. Paths: affected implementation/tests. Verify: every focused command is green with exact counts recorded.
- [x] 7.3 Run the focused §10.8 acceptance: the complete `revision_mechanism` marker and only affected Schema/config/ledger, activation/S5/S6/RunStore, budget/termination/resume, metric and public-contract test modules; do not run unselected repository-wide pytest. Also run `uv run ruff check .`, `uv run mypy nepa`, public Spec/gold/Target/Test Bundle/Plan lints, strict current/all OpenSpec validation and `git diff --check`; publish the exact M1-12 acceptance record, state that full pytest was intentionally not run, and state that production limits remain 0/0. Requirements: proposal acceptance and all delta capabilities. Design: Migration step 5. Paths: complete repository/change diff and `acceptance-record.md`. Verify: all focused commands are green and evidence/limitations are recorded.
- [x] 7.4 Publish an immutable evaluation-bound State-history snapshot at the deterministic revision path only when evaluation is ready, bind `revision_evaluated.evidence_refs` to it, and make pre-append crash/resume byte-idempotent with conflicts routed as artifact damage. Requirements: activation-bound accepted evidence and idempotent recovery. Design: decisions 3/4. Paths: S6 revision handoff and focused execution tests. Verify: later State transitions do not invalidate the old evaluation ref; pending evaluation publishes no snapshot; replay reuses bytes; conflicting snapshot follows failed/20.
- [x] 7.5 Bind evaluation calls to the current activation by selecting only the accepted last S6 trace row for each activation-derived `(task_id, attempt)` key, while retaining unique critic and group/AMEND/REGENERATE identities. Requirements: associated actual call refs and costs. Design: decisions 3/8. Paths: S6 evidence collector and focused tests. Verify: a pre-activation row with the same task id and reset attempt is excluded, post-activation cost is exact, and critic/group/zero-call cases remain correct.
- [x] 7.6 Make metric telemetry comparison distinguish an absent fallback from an explicit zero cost, so every supplied complete call set must agree with `revision_evaluated.cost_usd`; retain payload fallback when telemetry is absent and keep the existing formula. Requirements: code-generation metric compatibility. Design: decision 8. Paths: metric calculator and metric-contract tests. Verify: zero/zero, zero/non-zero conflict, non-zero conflict, missing telemetry and duplicate-ref cases pass.
- [x] 7.7 Run the affected S6, metric and `revision_mechanism` suites, then the complete §10.8 public CI including repository-wide pytest, ruff, mypy, Schema example validation, public lints, strict current/all OpenSpec validation and diff hygiene; update the acceptance record with exact results and confirm no new scope or blocker. Requirements: all M1-12 requirements. Design: Migration step 5. Paths: complete repository/change diff and acceptance record. Verify: every command is green and the full-suite count is recorded.
- [x] 7.8 Obtain and record the responsible owner's explicit dated review of the final corrected M1-12 diff and acceptance package; leave this task unchecked until the external decision names the reviewed evidence and do not treat machine checks as M1-13 parameter selection or production approval. Requirements: project owner gate. Design: Migration step 5. Paths: M1-12 acceptance record only. Verify: the owner decision records scope, evidence, corrections and approval status.
- [x] 7.9 After owner review, inspect the complete final diff, apply only requested in-scope corrections, rerun the same M1-12 focused and complete public-CI acceptance, and confirm `project_docs/`, archived changes, prompts, public CLI/config surface and production 0/0 defaults are unchanged; verify every task/requirement passes and M1-13/M1-14/M1-15, PlanReviser calls, TR-9, S7/S8/M2 and historical migration remain absent before marking the change complete. Requirements: all change requirements and non-scope. Design: all decisions/risks and Migration step 5. Paths: complete final diff. Verify: final command transcript is green, owner gate remains truthful and every remaining limitation is explicitly reported.
