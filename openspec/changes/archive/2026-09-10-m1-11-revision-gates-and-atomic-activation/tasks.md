## 0. Review remediation (reopened 2026-09-10)

- [x] 0.1 Reopen and correct 2.1/2.7: enforce strict revision configuration types and remove the reachable v1 activation/recovery implementation while preserving predecessor coverage through v2 fixtures.
- [x] 0.2 Reopen and correct 3.1/3.5: re-read the frozen boundary before publication and recompute RG-3 from synchronized current usage immediately before activation; a late failure must converge to one RG-3 rejection without refunding evidence or usage.
- [x] 0.3 Reopen and correct 5.4-6.4: require the controller lock, close all WAL/binding/reference relations, validate the complete new view before reconciliation, and reconcile only the unique unfinished activation so historical reconciled WALs cannot block later revisions.
- [x] 0.4 Reopen and correct 7.3-7.5: add consecutive activation, late-budget rejection, strict-type, lock, v1-removal, drift and historical-WAL regression coverage without weakening the existing crash-window matrix.
- [x] 0.5 Reopen 8.1-8.3 and rerun the complete focused/public acceptance; append a superseding correction section to the acceptance record while preserving the earlier evidence as history.
- [x] 0.6 Reopen 8.4-8.5: obtain a new responsible-owner decision for the corrected final diff, then rerun the post-decision acceptance. Keep this unchecked until that external decision exists.

## 1. Prerequisite audit and implementation brief

- [x] 1.1 Re-read the authoritative M1-11 sections, this change's proposal/specs/design, the archived M1-10 acceptance/handoff and current revision/S5/S6/RunStore code; verify the M1-10 final owner approval, clean baseline behavior and absence of another active change that overlaps this path. Requirements: all prerequisite and scope clauses. Design: Context, Goals/Non-Goals. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, this change, archived M1-10 and current implementation (read-only audit). Verify: record exact prerequisite refs, baseline commit/status and any mismatch before code edits.
- [x] 1.2 Create the §10.8 four-part implementation brief containing only exact input artifacts and Schema refs, output artifacts and acceptance commands, intended function/class signatures, and governing section numbers; map M1-10 handoff → RG gates → rejection/activation → M1-8/M1-9 consumer without adding decisions. Requirements: all M1-11 requirements. Design: decisions 1-9. Paths: this change and `implementation-brief.md`. Verify: review the brief against §10.8 and confirm it names no M1-12 evaluator/circuit breaker or M1-14 PlanReviser producer.
- [x] 1.3 Run the focused predecessor baseline before implementation, including strict current/all OpenSpec validation and the existing M1-8/M1-9/M1-10 revision, materialization, migration, S6 and orchestrator tests; preserve the exact results in the future acceptance record. Requirements: prerequisite preservation. Design: Context and decision 9. Paths: existing tests and archived change evidence. Verify: `uv run pytest -q -m revision_mechanism`, focused S5/S6 predecessor suites, `openspec validate m1-11-revision-gates-and-atomic-activation --strict`, and `openspec validate --all --strict` pass before implementation.

## 2. Configuration and persistent artifact contracts

- [x] 2.1 Extend the frozen configuration model with non-negative `budgets.revision_f2_limit/revision_f3_limit` capped at 3/1 and defaulting to 0/0; require a complete revision section whenever either limit is enabled and preserve absent revision configuration for the disabled default. Requirements: RG-3 and non-scope configuration clauses. Design: decision 3. Paths: `nepa/config.py`, default/example configs and config tests. Verify: disabled defaults parse unchanged; explicit 3/1 trial config passes; over-limit, enabled-with-missing-revision and invalid-type cases fail before run creation; no public CLI option is added.
- [x] 2.2 Add `revision.rho_min_f2/rho_min_f3` in `[0,1]` and `revision.cost_rates.build_usd >= 0`, keeping existing `theta2/theta6` and requiring all enabled-revision values explicitly with no suggested-value fallback. Requirements: RG-3 exact frozen inputs. Design: decision 3. Paths: configuration model/snapshot tests. Verify: boundary values and explicit zero build cost pass; missing, negative, out-of-range and post-snapshot mutation cases fail deterministically.
- [x] 2.3 Define a closed gate-result Schema/example binding candidate/trigger/source/current hashes, ordered RG-1…RG-5 rows, evidence refs, first failed gate, critic/rehearsal refs and disposition; enforce the exact pass/fail/not_applicable/not_evaluated state machine. Requirements: frozen boundary and ordered-gate requirements. Design: decision 2. Paths: new revision gate-result Schema/example and Schema tests. Verify: all-pass F2/F3 and each first-failure example pass; reordered, skipped, multiple-failure, illegal not-applicable and trailing-evaluated cases fail.
- [x] 2.4 Define a closed F3 rehearsal Schema/example binding both isolated runs, baseline/candidate refs, trees, file differences, build/smoke refs and complete pending-group attribution; ensure F2 cannot publish this artifact. Requirements: RG-5 rehearsal. Design: decisions 2/4. Paths: new rehearsal Schema/example and tests. Verify: ready and registered-pending-repair F3 examples pass; divergent trees, unregistered/partial group failure, missing result and F2-level examples fail.
- [x] 2.5 Replace the incomplete activation-WAL contract with fresh-run `schema_version="2.0"`, containing complete old/new pointer, State, file ledger, revision ledger, Run active ref/current copies, Plan, F2 binding or F3 pending intent, gate/rehearsal refs, hashes and transaction phase; add no converter for v1. Requirements: activation and crash-recovery requirements. Design: decisions 2/6-8. Paths: `nepa/schemas/plan-activation.schema.json`, examples and Schema tests. Verify: legal F2/F3 WALs pass; missing old/new bytes, mixed binding state, bad hashes/phases and v1 input fail.
- [x] 2.6 Tighten typed ledger validation/examples for the M1-11 `candidate_rejected` and `revision_activated` producers without changing unrelated event payloads or metric formulas; require the exact F2 binding versus F3 pending-materialization conditions. Requirements: failed-gate rejection and activation contracts. Design: decisions 5-7. Paths: revision-ledger Schema/validator, examples and ledger tests. Verify: rejection and both activation levels validate; unrelated fields, illegal sequence/reference, F2-null-binding and F3-nonnull-binding cases fail; F0/F1/M1-8/M1-9 events remain valid.
- [x] 2.7 Wire all new/changed Schemas and examples into package data and mutual validation without modifying historical accepted run formats or adding compatibility conversion. Requirements: all artifact contracts. Design: decisions 2/8 and Non-Goals. Paths: package metadata, Schema loader/index and Schema-example tests. Verify: all Schema/example checks pass and unsupported aliases/old activation WAL remain rejected.

## 3. Boundary revalidation and RG-1 through RG-4

- [x] 3.1 Implement one internal gate/activation outcome contract and controller entry that consumes the accepted `revision_handoff`, reconciles candidate publication, activation/materialization/verification in authoritative order, and validates the transaction-free S6 boundary before gates. Requirements: frozen authoritative boundary and S6 handoff. Design: decision 1. Paths: `nepa/speclib/revision_mechanism.py`, `nepa/stages/s6_execution.py`, RunStore/orchestrator seams and boundary tests. Verify: current handoff is admitted; stale pointer/ledger/tree/ref and every in-flight transaction are rejected before gate or Agent side effects.
- [x] 3.2 Implement RG-1 by rerunning the existing M1-10 predicate and selection against current accepted facts, proving the same signature/level remains selected and is neither tried nor activated. Requirements: ordered gates RG-1. Design: decision 3. Paths: revision mechanism and gate tests. Verify: current, predicate-cleared, same-level-rejected, higher-level-eligible, already-activated and ledger-tail replay cases select the exact RG-1 result.
- [x] 3.3 Implement RG-2 by reusing candidate, patch-operator, INV-1/2/3, Linker/full-lint, Blueprint and migration validators and, for F3, only the declared affected architecture closure; do not trust stored pass flags. Requirements: ordered gates RG-2. Design: decision 3. Paths: revision mechanism, existing Plan/Delivery/migration validators and gate tests. Verify: each invariant/lint/mapping/architecture failure independently rejects at RG-2, while legal M1-10 F2/F3 candidates replay byte-identically.
- [x] 3.4 Implement the canonical rework-cost estimator over REVALIDATE builds, one AMEND Fixer, REGENERATE Coder plus remaining Fixers, new-file tasks and affected-group maximum validation, using frozen route token/pricing bounds and explicit build cost exactly once per execution unit. Requirements: RG-3 budget. Design: decision 3. Paths: revision mechanism/config pricing helpers and estimator tests. Verify: fixed hand calculations, zero build cost, shared multi-file task, grouped members, absent price and overflow cases produce exact values or deterministic rejection.
- [x] 3.5 Implement RG-3 as independent preservation, half-remaining-global-cost, level-success-count and remaining-run-call-cap checks, re-reading current usage before result publication and consuming no successful-activation allowance on rejection. Requirements: ordered gates RG-3. Design: decision 3. Paths: revision mechanism, Run budget readers and gate tests. Verify: equality boundaries pass; each one-unit overrun fails; F2/F3 counters remain independent; rejected attempts preserve counters while actual critic usage remains charged.
- [x] 3.6 Extend the existing PlanCritic binding so initial S4 retains the full compact graph while RG-4 supplies only the candidate delta and complete affected closure through the same three declared inputs/output Schema/temperature-zero route. Requirements: modified Agent contract and RG-4. Design: decision 3. Paths: Agent role/binding, S4 regression and revision mechanism tests. Verify: exact input projection contains no unrelated Plan/workspace/test implementation, existing S4 snapshots remain unchanged, and no ArchitecturePlanner/TaskPlanner/FlatPlanBaseline/PlanReviser call is reachable.
- [x] 3.7 Implement RG-4 invocation, response validation, durable call/evidence binding and usage charging; pass only with no blocker/major and reject invalid/contradictory/replacement-Plan responses without semantic retry. Requirements: RG-4 critic. Design: decision 3. Paths: revision mechanism, Agent/orchestrator usage boundary and frozen-response tests. Verify: stub pass/minor, blocker, major, malformed, typed provider failure and crash-after-call cases have exact gate/usage results and at most one logical completion.
- [x] 3.8 Implement canonical ordered `gates.json` publication after each accepted result and byte-equivalent replay/conflict detection; later gates remain not_evaluated after the first failure. Requirements: ordered gate evidence. Design: decisions 2/3. Paths: RunStore candidate artifact helpers, revision mechanism and replay tests. Verify: each RG-1…RG-4 interruption resumes from accepted evidence without duplicating calls or accepting stale bytes, while conflicts fail as artifact damage.

## 4. RG-5 rehearsal and rejection publication

- [x] 4.1 Refactor only the minimum existing S5 multi-epoch primitives needed to run against an explicit temporary root without publishing a live commit, receipt, ledger event or Run update; retain one rendering/materialization algorithm. Requirements: RG-5 and live-workspace isolation. Design: decision 4. Paths: `nepa/stages/s5_materialization.py`, `nepa/speclib/materialization.py` and S5 regression tests. Verify: the isolated entry returns the same canonical difference/build/smoke projection as the existing path while a live-run hash/HEAD/status audit remains unchanged.
- [x] 4.2 Implement two fresh F3 rehearsals from the identical accepted HEAD, compare canonical results/trees, enforce realized-file preservation and explicit retirement/re-adoption, and publish the closed rehearsal record into the candidate area. Requirements: RG-5 structural rehearsal. Design: decisions 2/4. Paths: revision mechanism, S5 helpers, sandbox/build/smoke paths and rehearsal tests. Verify: ready and byte-identical cases pass; divergent render/tree, lost realized content and undeclared file changes fail at RG-5.
- [x] 4.3 Enforce exact registered-group attribution for rehearsal build failures and reject unrelated, partially mapped or smoke failures; accept `pending_repair` only when every failure belongs to the frozen candidate groups. Requirements: RG-5 group boundary. Design: decision 4. Paths: revision mechanism, existing M1-8 attribution helper and focused tests. Verify: disjoint/shared groups, partial symbol/path/artifact mappings, unregistered failures and smoke failures yield exact outcomes without expanding a group.
- [x] 4.4 Implement the F2 RG-5 `not_applicable` path with no temporary workspace, S5 call, build/smoke artifact or epoch mutation. Requirements: RG-5 F2 behavior. Design: decisions 2/4. Paths: revision mechanism and negative side-effect tests. Verify: an all-pass F2 candidate records RG-5 not_applicable and source scans/spies show zero materialization invocation.
- [x] 4.5 Implement the atomic idempotent first-failure `candidate_rejected` append under the run lock, including exact payload/evidence, same-candidate replay and conflicting-payload damage detection. Requirements: failed gate produces one rejection. Design: decision 5. Paths: revision mechanism, plan revision ledger helpers, RunStore and ledger tests. Verify: one case per RG gate advances only event_seq; replay is byte-identical; conflict fails; no `revision_activated` is appended.
- [x] 4.6 Add a rejection side-effect audit that snapshots formal versions, active pointer, State, file ledger, binding/current copies, Run active ref and workspace HEAD/tree before gates and asserts exact preservation after every RG failure while retaining spent trace/build evidence. Requirements: rejection non-mutation. Design: decision 5. Paths: gate controller and end-to-end rejection tests. Verify: RG-1…RG-5 failure fixtures differ only in allowed `_s4r` evidence, ledger rejection and actual usage records.

## 5. F2/F3 successor projection and atomic activation

- [x] 5.1 Build the unique legal successor from current pointer/level and recompute candidate hashes, migration State/file ledger and `revision_activated` payload only after all applicable gates pass; reject any version/epoch/revision skip or drift. Requirements: activation successor and locked boundary. Design: decision 6. Paths: `nepa/speclib/plan_revision.py`, revision mechanism and successor tests. Verify: F2 P increment/same epoch and F3 A increment/P reset/new epoch pass; regression/skip/mixed source fail before formal publication.
- [x] 5.2 Implement F2 metadata binding projection and immutable publication against the current epoch receipt plus candidate-specific immutable manifest/map, without changing workspace bytes, S5 receipt or epoch. Requirements: F2 activation/binding. Design: decision 6. Paths: existing binding/materialization helpers, RunStore and F2 tests. Verify: owner/task/contract metadata updates bind the new Plan; source tree/HEAD and old epoch/binding bytes remain unchanged; conflicting binding replay fails.
- [x] 5.3 Implement F3 pending-materialization projection with null activation binding, next epoch, migrated State/file ledger and current S5 instance pending while preserving historical epoch/binding receipts. Requirements: F3 activation route. Design: decision 6. Paths: plan revision helpers, Run/S5 projection and F3 tests. Verify: no premature binding/epoch receipt exists, prior receipts remain hash-identical and the new S5 admission consumes exactly the activation migration.
- [x] 5.4 Implement complete v2 activation-WAL preparation from the same old/new canonical values, gate/rehearsal evidence and current hashes under the run lock, with byte-equivalent replay and conflict rejection. Requirements: activation WAL. Design: decisions 2/6/7. Paths: revision mechanism, RunStore and WAL tests. Verify: F2/F3 expected refs all close; stale current values and altered WAL bytes fail before any live write.
- [x] 5.5 Implement activation publication in the exact order WAL → immutable Plan/F2 binding → State → file ledger → revision ledger → atomic active pointer → Run/current copies, making pointer replacement the sole logical commit and retaining immutable S4 `output_refs.plan`. Requirements: locked activation commit point. Design: decision 7. Paths: RunStore atomic helpers, revision mechanism and transaction tests. Verify: an uninterrupted F2/F3 activation yields one self-consistent chain and exactly one revision_seq increment; old Plan/receipt/event bytes are unchanged.
- [x] 5.6 Add stable fault hooks before/after WAL, successor Plan, F2 binding, State, file ledger, revision ledger, active pointer, Run active ref/current copies and WAL finalization without changing production behavior. Requirements: all activation crash windows. Design: decisions 7/8. Paths: revision controller/RunStore and fault-injection tests. Verify: every named hook leaves a reproducible WAL-bound state handled by the reconciliation matrix.

## 6. Pointer-driven recovery and downstream routing

- [x] 6.1 Implement pointer-old recovery for every pre-commit interruption, restoring exact WAL-bound old State/file ledger/revision ledger/Run/current copies and isolating unactivated Plan/F2 binding bytes even when the new activation entry was already written. Requirements: crash recovery pointer-old rule. Design: decision 8. Paths: RunStore/revision mechanism and recovery tests. Verify: each fault before pointer replacement returns to byte-identical old authority with no accepted activation and no deleted realized content.
- [x] 6.2 Implement pointer-new forward reconciliation, validating every new reference and completing only missing Run/current-copy/F3-pending projection work without rolling back or duplicating the activation. Requirements: crash recovery pointer-new rule. Design: decision 8. Paths: RunStore/revision mechanism and recovery tests. Verify: every fault after pointer replacement converges to the same accepted F2/F3 result and one ledger activation.
- [x] 6.3 Fail closed when the pointer is neither WAL value, WAL is missing, or any Plan/State/ledger/binding/hash/version/epoch/sequence byte conflicts; do not admit materialization, verification or S6 work afterward. Requirements: recovery corruption behavior. Design: decision 8. Paths: RunStore admission/reconciliation and corruption tests. Verify: isolated mutation cases all raise artifact damage with no speculative repair or additional ledger event.
- [x] 6.4 Integrate activation reconciliation before current-epoch materialization and verification reconciliation at every S4/S5/S6 admission, preserving the design's one-transaction-at-a-time order. Requirements: frozen boundary and recovery ordering. Design: decisions 1/8. Paths: orchestrator, S4/S5/S6 controllers and ordering tests. Verify: mixed pending activation/materialization/verification fixtures settle in the documented order and cannot start a second transaction early.
- [x] 6.5 Complete the F2 handoff so S6 resumes from the accepted same-epoch binding and migrated INHERIT/REVALIDATE/AMEND/REGENERATE view through only the M1-9 execution paths, without rematerialization or attempt refresh. Requirements: successful F2 routing and modified S6 behavior. Design: decisions 1/6/8. Paths: `nepa/stages/s6_execution.py`, orchestrator/application wiring and F2 integration tests. Verify: renumber/move/split fixtures select exact migrated modes, preserve attempts/history and finish through existing verification transactions.
- [x] 6.6 Complete the F3 handoff so resume selects the new pending S5 epoch, consumes the accepted activation migration, materializes once, executes registered groups before ordinary work and then returns to S6; never mark S6 done before this chain closes. Requirements: successful F3 routing and modified S6 behavior. Design: decisions 1/6/8. Paths: orchestrator, S5/S6 controllers and F3 integration tests. Verify: ready and pending-repair F3 fixtures follow exact stage projections, reuse existing commits/receipts on resume and do not fabricate a binding before S5.
- [x] 6.7 Preserve rejection routing and exhaustion history: after a rejection, resume the same active view, retain blocked/dependency-blocked facts and consumed attempts, and continue only independently legal work or the existing controlled exit. Requirements: gate failure routing and modified exhaustion behavior. Design: decisions 1/5. Paths: S6 controller/orchestrator and integration tests. Verify: RG rejection neither loops into the same candidate nor reopens tasks; an independent branch remains executable and unresolved execution keeps its existing exit semantics.

## 7. Fixtures and independent acceptance coverage

- [x] 7.1 Extend the deterministic fixture generator in dependency order from the existing M1-10 MQTT/non-MQTT candidates to gate results, rehearsal inputs and F2/F3 activation/recovery states; bind every generated file to its source hashes. Requirements: protocol neutrality and all fixture-backed requirements. Design: decision 9. Paths: `tests/tools/`, `tests/fixtures/` and generator replay tests. Verify: two temporary generations are byte-identical and match checked-in outputs with zero protocol-name branch in core code.
- [x] 7.2 Add a gate matrix under `revision_mechanism` covering RG-1…RG-5 pass plus every isolated first failure, later-gate non-evaluation, F2 not-applicable rehearsal, PlanCritic frozen response/usage and rejection replay/conflict. Requirements: ordered gates, critic, rehearsal and rejection. Design: decisions 2-5/9. Paths: focused revision-gate tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k 'gate or critic or rehearsal or rejected'` has nonzero intended collection and all cases pass.
- [x] 7.3 Add F2/F3 success tests covering version/epoch/revision monotonicity, F2 metadata binding, F3 pending materialization, migrated State/file ledger, Run/current copies, unchanged historical artifacts and exact downstream route. Requirements: successful activation/routing. Design: decisions 6/7/9. Paths: activation/integration tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k 'activation and not recovery'` passes with both levels and no M1-12 event.
- [x] 7.4 Add one fault-injection case for every activation publication boundary and both pointer branches, including the formerly incorrect new-ledger/old-pointer window, plus missing-WAL/third-pointer/hash-chain/artifact-byte corruption. Requirements: all crash-recovery scenarios. Design: decisions 7-9. Paths: activation recovery tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k 'activation and recovery'` proves each valid window converges uniquely and each conflict fails without mutation.
- [x] 7.5 Add predecessor and protocol-neutrality regressions proving initial S4 PlanCritic, ordinary F0/F1, M1-8 multi-epoch materialization, M1-9 migration/group execution, M1-10 candidate replay, metrics and public CLI remain unchanged. Requirements: modified-capability preservation and non-scope. Design: decision 9 and Risks. Paths: existing S4/S5/S6/metric/CLI suites plus neutrality scans. Verify: focused predecessor suites pass and searches find no PlanReviser, `revision_evaluated`, circuit-breaker, prompt/calibration, public CLI, M2/S7/S8 or protocol-specific path added.

## 8. Closure, public CI and responsible-owner gate

- [x] 8.1 Audit requirement → Schema-valid fixture → boundary/gate/critic/rehearsal → rejection-or-WAL/activation → F2/F3 consumer → replay/corruption-test closure; remove duplicate planners/materializers/executors, speculative compatibility/fallbacks and any reachable M1-12/M1-14 behavior. Requirements: all change requirements and non-scope. Design: all decisions/risks. Paths: complete M1-11 implementation diff. Verify: record a closure matrix and source/scope scans in the acceptance record.
- [x] 8.2 Run focused Schema/config, Agent/S4, revision/State/RunStore, S5/S6/orchestrator, gate/rehearsal/rejection, activation/recovery and predecessor tests, then the full intended `uv run pytest -q -m revision_mechanism` collection. Requirements: all M1-11 requirements. Design: migration steps 2-4. Paths: affected implementation/tests. Verify: every focused command is green with exact counts recorded.
- [x] 8.3 Run the public §10.8 CI: full `uv run pytest -q`, `uv run ruff check .`, `uv run mypy nepa`, Schema examples, public Spec/gold-coverage/Target/Test Bundle/Plan lint, strict current/all OpenSpec validation and `git diff --check`; write the exact M1-11 acceptance record and state that it completes only the mechanism portion of D1.13. Requirements: proposal acceptance and all delta capabilities. Design: migration step 5. Paths: complete repository/change diff and `acceptance-record.md`. Verify: all commands are green and exact evidence/remaining boundaries are recorded.
- [x] 8.4 Obtain and record the responsible owner's explicit review of the final M1-11 diff and acceptance package before archive; leave this task unchecked until the external dated decision exists and do not treat machine checks as D1.14 parameter or M1-15 production approval. Requirements: project OpenSpec owner-gate rule. Design: decision 9 and migration step 5. Paths: M1-11 acceptance record only. Verify: the owner decision names the reviewed implementation, evidence, corrections and approval scope.
- [x] 8.5 After owner review, inspect the complete final diff, apply only requested in-scope corrections, remove unrelated refactoring/configurability, and rerun the entire M1-11 focused/public acceptance; confirm M1-10 and archived changes are unchanged, `project_docs/` is untouched, all tasks/requirements pass, the owner gate is truthful, and M1-12 evaluation/circuit breaking, M1-13 parameter selection, M1-14 PlanReviser, public CLI and M2/S7/S8 remain absent. Requirements: all change requirements and non-scope. Design: all decisions/risks and migration step 5. Paths: complete repository/change diff. Verify: final command transcript is green and every remaining limitation is explicitly reported before marking the change complete.
