# M1-10 technical acceptance record

> **Superseded acceptance notice (2026-09-09):** the earlier acceptance and
> owner decision below predate the review-correction implementation. They are
> retained as historical evidence, but no longer approve the current diff.
> Tasks 8.4/8.5 were reopened; a new responsible-owner decision is required
> after the replacement evidence is complete.

This record covers only the trigger/operator portion of D1.13. It does not
approve or implement M1-11 revision gates/activation, M1-12 circuit breakers,
M1-14 PlanReviser, D1.14 parameter calibration, or M1-15 production acceptance.

## Requirement-to-artifact closure

| Requirement | Schema-valid fixture and implementation path | Durable result, replay and corruption evidence |
| --- | --- | --- |
| Plan local identity | Plan 5.0 requires `local_task_id`; MQTT/non-MQTT S5/S6 fixtures use the shared Linker and `plan_to_draft_ir` | uid stability, renumbering, exact Plan→IR→Plan replay and current v4 rejection are covered without a compatibility converter |
| Stable S6 boundary and TR-1…TR-8 | Closed trigger-evaluation/ledger payloads; `project_revision_boundary` and `evaluate_revision_triggers` consume accepted Plan/State/workspace/Blueprint/map/ledger anchors | all hits are canonical and unique, F2 precedes F3, record-only/F1/F4/submission rejection remain unselected, TR-9 has no producer, and replay/conflicting-prefix cases are tested |
| Atomic F2/F3 language | Closed discriminated patch Schema; `apply_revision_patch` clones the source and dispatches only stable semantic ids | every operator family has positive/negative coverage; first/middle/final failure leaves source bytes unchanged; forbidden or unrelated cross-level companions fail |
| Contract/slot/build closure | contract additions/extensions preserve old exports; slot/re-adoption operations explicitly bind link-source slots to build artifacts | missing provider/consumer/implementation/build binding, published-interface retirement, unknown successor and wrong quarantine hash fail before candidate publication |
| Shared candidate completion and INV-1/2/3 | patched IR uses shared Plan completion, full lint, Linker, Blueprint, manifest/map and migration classifier | commitment refs/content, unique normative primary owners, explicit lineage and non-weaker task/file/test obligations are recorded; missing lineage/mapping and weakened acceptance fail |
| M1-9 migration compatibility | F3 completion adds deterministic pending-group and re-adoption inputs after lint/invariants | group members/paths/symbols/artifacts and quarantine owner/content bindings validate through existing M1-8/M1-9 contracts |
| Event-scoped publication | `RunStore.stage_revision_candidate`, `commit_revision_candidate` and `reconcile_revision_candidate` validate the closed flat bundle and selected trigger ancestry | no-event pending state is inert; accepted event converges by rename; identical staged/final copies converge; mismatched trigger, marker, file set or content hash fails as artifact damage |
| Internal S6 handoff | optional frozen `revision_patch_provider`; `StagePause(kind="revision_handoff", ...)`; orchestrator restores S6 to pending | trigger-only and completed-candidate replay keep the ledger idempotent, produce no termination/S9 output, and do not change the active Plan/revision, State, file ledger or workspace |
| Protocol neutrality and predecessor preservation | one deterministic generator binds MQTT/non-MQTT Plan 5.0 fixtures; core source has no protocol-name branch | regenerated bytes match checked-in fixtures; ordinary F0/F1, M1-8 materialization, M1-9 migration/group, metrics and public CLI regression suites pass |

## Focused evidence

- Accepted M1-9 baseline before M1-10 edits: `174 passed in 434.63s`; baseline implementation commit `d77bc83`.
- Combined Schema, Plan/Linker/Delivery, revision/State, RunStore, S5/S6, orchestrator and M1-10 collection: `302 passed in 491.74s`.
- Final predecessor/materialization/S6/F1/metrics/CLI collection: `211 passed in 498.32s`.
- `uv run pytest -q -m revision_mechanism`: `34 passed, 696 deselected in 8.31s`.
- Trigger/ledger selector: `11 passed, 716 deselected`.
- Operator/patch selector: `8 passed, 719 deselected`.
- Candidate/invariant/recovery selector: `6 passed, 721 deselected`.
- F1 lease regression after the internal TR-3 handoff input was added: `41 passed in 101.58s`.

## Scope audit

- `project_docs/`, the M1-9 archive, prompts, calibration assets, public CLI and M2/S7/S8 implementation files are unchanged.
- New production behavior is confined to Plan 5.0 compilation, the revision mechanism speclib, event-scoped RunStore candidate storage, and internal S6/orchestrator seams.
- Source scans found no added PlanReviser, RG/critic/rehearsal/activation producer or circuit-breaker path. Reads of accepted `candidate_rejected`/`revision_activated` events exist only for deduplication and replay ancestry.
- The build-artifact operand added to `add_file_slot`/`re_adopt` closes a concrete candidate-completion counterexample; it avoids guessing a build-graph edit and is reflected in the current change Schema/design/spec/brief only.

## Public acceptance and owner gate

Final technical run on 2026-09-09:

- `uv run pytest -q` — `730 passed in 662.67s`.
- Schema examples — `20 passed in 1.51s`.
- Public Spec, gold-coverage Spec, Target and Test Bundle CLI lints — valid with
  zero errors and warnings.
- Matched non-MQTT public basic Plan lint — valid with zero errors and warnings.
- `uv run ruff check .` — passed.
- `uv run mypy nepa` — passed with no issues in 4 source files.
- `openspec validate m1-10-revision-triggers-and-patch-operators --strict` —
  valid.
- `openspec validate --all --strict` — `17 passed, 0 failed`.
- `git diff --check` — passed.

The first full-suite pass exposed stale S6 provider/lease provenance hashes
after the required Plan 5.0 fixture rewrite. Regenerating those fixtures then
exposed their downstream migration-fixture hash. The existing generators were
run in dependency order (S5 → provider/lease → migration); the complete
generator/source-binding selector then passed `7 passed in 0.94s`, and the
final full suite above passed without failures.

At the end of this technical run, responsible-owner review and the post-review
rerun were still pending. The decision and its subsequent final verification
are recorded below.

## Responsible-owner decision (2026-09-09)

- **Decision:** approved. In response to the request to review the current
  final diff and acceptance package, the responsible owner confirmed the other
  presented acceptance contents and instructed the implementation agent to
  determine the exact implementation boundary from the authoritative design
  and current change, then continue execution.
- **Reviewed implementation:** the final M1-10 diff implementing Plan 5.0 local
  task identity, deterministic TR-1 through TR-8 evaluation and ledger facts,
  the closed F2/F3 patch operators, complete non-authoritative revision
  candidates and recovery, and the internal S6 `revision_handoff`.
- **Reviewed evidence:** the requirement-to-artifact closure and scope audit in
  this record, focused revision/predecessor evidence, `730 passed` full-suite
  result, public lint, Ruff, mypy, strict OpenSpec and diff checks above.
- **Approval scope:** M1-10 and only the trigger/operator portion later composed
  into D1.13, including the necessary Plan 5.0 persistence correction. It does
  not approve M1-11 RG/rehearsal/rejection/activation, M1-12 circuit breakers,
  M1-14 PlanReviser, the remaining D1.13 chain, D1.14 calibration or M1-15
  production acceptance.
- **Review corrections:** none requested. Task 8.5 must still rerun the final
  acceptance after recording this decision.

## Post-review final verification

No review correction was requested. After recording the approval, the complete
final diff was inspected and acceptance was rerun against the reviewed package:

- Combined affected Schema, Plan/Linker/Delivery, revision/State, RunStore,
  S5/S6, lease, orchestrator and M1-10 focused collection —
  `338 passed in 506.35s`.
- `uv run pytest -q -m revision_mechanism` —
  `34 passed, 696 deselected in 7.90s`.
- Schema examples — `20 passed in 1.40s`.
- Final `uv run pytest -q` — `730 passed in 664.45s`.
- Public Spec, gold-coverage Spec, Target and Test Bundle CLI lints, plus the
  matched non-MQTT public basic Plan lint — valid with zero errors and warnings.
- Ruff — passed; mypy — passed with no issues in 4 source files.
- Current-change strict OpenSpec validation — valid; all-change strict
  validation — `17 passed, 0 failed`.
- `git diff --check` — passed.

The final scope inspection found no changed `project_docs/`, M1-9 archive,
public CLI, prompt/calibration or M2/S7/S8 implementation file and no
protocol-name branch in core code. The only new reads of accepted
`candidate_rejected`/`revision_activated` events support trigger deduplication
and replay ancestry; this change adds no producer for those events. M1-11
gates/rehearsal/rejection/activation, M1-12 circuit breakers and M1-14
PlanReviser remain outside the implementation. This acceptance remains only
the M1-10 trigger/operator portion later composed into D1.13.

## Review-correction replacement evidence (2026-09-09)

This section supersedes the earlier technical acceptance and owner decision.
The historical text above is retained, but it does not approve the corrected
diff. The replacement responsible-owner decision is recorded below.

Implemented corrections:

- Production TR facts now come from the current Plan/State/Blueprint/contract
  graph, hash-bound terminal attempts, controller-bound build refs, the file
  and revision ledgers, and typed provider metadata. Model diagnosis flags,
  stale revisions, started attempts and mismatched hashes do not create facts.
- `revision.theta2/theta6` is an optional closed pair with no default block or
  silent threshold. TR-4 uses the authorized deterministic output formula and
  the smaller effective Coder/Fixer output limit.
- INV-3 uses task-scoped source/target mappings and recomputes obligation,
  coverage-node, enabled-state and non-postponed gate preservation. Unrelated
  same-name obligations and implicit package ownership changes fail closed.
- F3 groups are exact connected affected components with attributable paths,
  symbols and build artifacts; disjoint artifacts remain separate and shared
  artifacts merge them. Unattributable structural paths are rejected.
- Candidate reconciliation covers interruption after staging, after accepted
  ledger replacement and after final rename. Selected-trigger source revision
  and pre-batch ledger-prefix bindings are revalidated before commit/replay.

Replacement verification against the corrected final diff:

- `uv run pytest -q -m revision_mechanism` — `43 passed, 696 deselected`.
- Final `uv run pytest -q` — `739 passed in 679.04s`.
- Focused F1/revision/Schema collection — `71 passed`; affected
  Schema/Plan/revision/S5/S6/orchestrator collection — `248 passed` before the
  final boundary-only additions, which are included in the final full run.
- Public Spec, gold-coverage Spec, Target and Test Bundle lints — valid with
  zero errors and warnings. A freshly linked matched non-MQTT Plan basic lint
  is also valid with zero errors and warnings.
- `uv run ruff check .`, `uv run mypy nepa` and `git diff --check` — passed.
- Strict current-change OpenSpec validation — valid; strict all-item validation
  — `17 passed, 0 failed`.

Scope and convergence inspection found no changed M1-9 archive, public CLI,
prompt/calibration or M2/S7/S8 implementation file. The only authoritative
design edits are the approved TR-4 formula/config clarification and their
version-history rows in the two named `project_docs` files. No M1-11 gate,
rehearsal, rejection or activation producer, M1-12 circuit breaker, or M1-14
PlanReviser was added. No repository candidate pending directory remains.

## Corrected responsible-owner decision (2026-09-09)

- **Decision:** approved. After being presented with the corrected M1-10
  implementation and replacement acceptance package, the responsible owner
  explicitly responded `批准` on 2026-09-09.
- **Reviewed implementation:** the corrected final M1-10 diff covering
  current/hash-bound TR-1 through TR-8 fact projection, stable trigger
  signatures and deduplication, the closed F2/F3 patch operators, task-scoped
  INV-3 validation, exact F3 affected groups, and convergent event-scoped
  candidate recovery.
- **Reviewed evidence:** the review-correction replacement evidence above,
  including the `43 passed` focused revision result, `739 passed` full-suite
  result, public lints, Ruff, mypy, strict current/all OpenSpec validation,
  diff check, scope inspection and absence of pending candidate directories.
- **Approval scope:** the corrected M1-10 implementation and replacement
  acceptance package only. This remains only the trigger/operator portion
  later composed into D1.13; it does not approve M1-11 RG/rehearsal/rejection/
  activation, M1-12 circuit breakers, M1-14 PlanReviser, full D1.13, D1.14
  calibration or M1-15 production acceptance.
- **Post-decision condition:** tasks 8.5 and 9.6 remain open until the complete
  focused/public acceptance and final scope/convergence inspection are rerun
  successfully against this approved diff.

## Corrected post-owner final verification (2026-09-09)

After recording the corrected responsible-owner approval, the complete
focused/public acceptance and scope inspection were rerun against the approved
diff:

- `uv run pytest -q -m revision_mechanism` — `43 passed, 696 deselected in
  14.63s`.
- `uv run pytest -q` — `739 passed in 668.81s`.
- Schema examples — `20 passed in 1.43s`.
- Public Spec, gold-coverage Spec, Target and Test Bundle CLI lints — valid
  with zero errors and warnings.
- A freshly generated non-MQTT Plan copy, rebound in a temporary directory to
  the canonical hashes of its exact companion Spec, Target and Test Bundle,
  passed public basic Plan lint with zero errors and warnings. The checked-in
  fixture generator preserves the raw source Spec hash while storing a
  canonicalized companion, so directly pairing those two representations
  reports `PLAN_INPUT_REF_DRIFT`; no repository artifact was changed to mask
  that pre-existing fixture-harness distinction.
- `uv run ruff check .` — passed; `uv run mypy nepa` — passed with no issues
  in 4 source files; `git diff --check` — passed.
- Strict current-change OpenSpec validation — valid; strict all-item
  validation — `17 passed, 0 failed`.

The final scope/convergence inspection found no changed M1-9 archive, public
CLI, prompt/calibration or S7/S8/M2 implementation file. The only changed
authoritative documents are the two approved TR-4 formula/config files. No
repository `.candidate_*.pending` directory remains. The only added reads of
`candidate_rejected` and `revision_activated` support accepted-ledger
deduplication; no M1-11 gate/rehearsal/rejection/activation producer, M1-12
circuit breaker or M1-14 PlanReviser was introduced. No additional M1-10
limitation or blocked task remains, and this approval still covers only the
trigger/operator portion later composed into D1.13.
