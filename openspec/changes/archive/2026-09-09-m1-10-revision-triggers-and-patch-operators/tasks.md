## 1. Prerequisite and implementation contract

- [x] 1.1 Verify the exact accepted M1-9 baseline without rewriting it: inspect the archived tasks, replacement acceptance and corrected owner decision; identify the committed implementation/finalization ancestry; run the focused revision, S5 multi-epoch, S6 execution and lease collections before M1-10 edits. Requirements: all prerequisite and preservation clauses. Design: Context, migration step 1. Paths: `openspec/changes/archive/2026-09-09-m1-9-s6-migration-joint-verification/`, current revision/S5/S6 tests. Verify: archive/commit inspection plus `uv run pytest -q tests/test_plan_revision.py tests/test_plan_revision_state.py tests/test_s5_multi_epoch.py tests/test_s6_execution.py tests/test_s6_lease_execution.py`.
- [x] 1.2 Create the §10.8 four-part implementation brief containing only the exact input artifacts and Schema refs, output artifacts and acceptance commands, intended function/class signatures, and governing section numbers; map source fact → evaluator → ledger/candidate → M1-9 consumer and record any concrete baseline mismatch without adding design decisions. Requirements: all M1-10 requirements. Design: decisions 1-8. Paths: this change, `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, `implementation-brief.md`. Verify: review the brief against system design §10.8 and confirm it names no M1-11 gate/activation, M1-12 circuit breaker or M1-14 Agent implementation.
- [x] 1.3 Upgrade the current formal Plan path to Schema 5.0: require/persist `local_task_id`, preserve `[work_package, local_task_id]` uid derivation, add lossless `plan_to_draft_ir`, exclude local identity from Blueprint semantics, and move candidate completion into the shared Plan speclib used by S4 and revisions. Update current fresh-run producers/consumers/examples/fixtures together; reject v4 without a converter and leave Run 4.0, archived changes and unrelated versioned contracts untouched. Requirements: formal Plan preserves package-local task identity. Design: decision 9. Paths: Plan Schema/speclib, S4, current examples/fixtures and Plan/S4/S5/S6 tests. Verify: Plan 5.0 local-id, uid stability, round-trip/relink, renumbering and v4-rejection tests plus predecessor regressions.

## 2. Closed trigger, patch and candidate contracts

- [x] 2.1 Add the closed `revision-trigger-evaluation` Schema/example and close the typed ledger trigger boundary payload needed for task/provider boundaries, normalized route/signature anchors, sorted evidence and selected state while preserving one event per hit. Requirements: trigger boundary and typed trigger-fact requirements. Design: decisions 1/3/4. Paths: `nepa/schemas/`, examples, `nepa/schemas/revision-ledger.schema.json`, Schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_plan_revision.py -k 'trigger or revision'` accepts canonical batches and rejects stale anchors, duplicate hits, multiple selections, extra fields and malformed refs.
- [x] 2.2 Add a closed discriminated `revision-patch` Schema/example for every F2/F3 operator with exact per-operation operands, source/selected-trigger binding and future rationale/expected-effect text that is never trusted as validation. Requirements: closed F2/F3 patch requirements. Design: decision 4. Paths: new patch Schema/example and Schema tests. Verify: one positive example per operator family and negatives for mixed operands, unknown operators, F3 operations at F2, implicit rename/delete/replace and additional properties.
- [x] 2.3 Add the closed `revision-candidate` Schema/example that binds event-scoped identity, source/patch/candidate and derived artifact refs, lineage/obligation mapping, migration, lint/invariant reports and content hashes without formal version or activation fields. Requirements: unactivated-candidate and no-activation requirements. Design: decisions 4/7. Paths: new candidate Schema/example and Schema tests. Verify: valid F2/F3 candidates pass; wrong event/source/hash, missing mapping/ref, formal version/RG/activation fields and mixed candidate ids fail.
- [x] 2.4 Wire all new Schemas into package data and mutual example validation without changing historical major-version acceptance or adding compatibility conversion. Requirements: all artifact-contract requirements. Design: decision 4 and Non-Goals. Paths: `pyproject.toml`, Schema loader/index paths and `tests/test_schema_examples.py`. Verify: Schema/example mutual validation is green and unsupported old/new aliases remain rejected.

## 3. Trigger evaluation, routing and ledger facts

- [x] 3.1 Implement the canonical boundary-fact projector and validator over the current active Plan/revision/epoch, State-history sequence, workspace commit/tree, Blueprint/map, accepted ledger prefix, thresholds and typed evidence; expose only stable task and provider-submission boundaries after existing WAL reconciliation. Requirements: trigger evaluation consumes validated facts. Design: decision 1. Paths: `nepa/speclib/revision_mechanism.py`, `nepa/stages/s6_execution.py`, focused trigger tests. Verify: canonical replay passes while stale/future/mixed Plan, State, tree, ledger and in-flight-work cases fail before a trigger append.
- [x] 3.2 Implement TR-1 and TR-2 from graph/build facts, including the multi-task undeclared-symbol/complete-consumes proof, single-task record-only behavior, unique task-ready provider closure, frozen `theta_2` comparison and zero denominator. Requirements: exact M1 predicates. Design: decision 2. Paths: revision mechanism and trigger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'tr1 or tr2'` covers every positive, near miss, stale evidence and denominator boundary.
- [x] 3.3 Implement TR-3 and TR-4, reusing the existing lease-authorization validator for eligible same-package/direct-provider F1 and selecting F2 only after the specified F1 and package constraints; implement two-truncation and preflight full-lint-output overflow without spending an extra call. Requirements: exact M1 predicates. Design: decision 2. Paths: revision mechanism, existing lease authorization/S6 path and trigger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'tr3 or tr4'` proves F1 precedence, F2 fallback, cross-package no-route, exact thresholds and no counter/attempt mutation.
- [x] 3.4 Implement TR-5 and TR-6 with independent Diagnoser identity, graph-confirmed cross-module gap, record-only routing, unique issue counting and exact frozen `theta_6` ratio. Requirements: exact M1 predicates. Design: decision 2. Paths: revision mechanism, diagnosis/evidence readers and trigger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'tr5 or tr6'` covers duplicate/non-independent findings, graph failures, record-only allowance behavior and threshold edges.
- [x] 3.5 Implement TR-7 and the TR-8 provider submission gate: distinguish an existing-architecture legal missing slot from F4, reject contract/export drift before candidate installation, require independent TR-1/7 evidence for F3, and keep TR-9 unreachable in M1. Requirements: exact M1 predicates and no-activation boundary. Design: decisions 1/2/8. Paths: revision mechanism, `nepa/stages/s6_execution.py`, contract/Blueprint validators and trigger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'tr7 or tr8 or tr9'` covers legal F3, F4, F0/F1 rejection, independent structural selection and explicit TR-9 non-production.
- [x] 3.6 Implement canonical signature construction, all-hit ordering, F2-before-F3/TR-number selection and accepted-ledger deduplication for record-only, same-level rejection, higher-level eligibility and prior activation. Requirements: canonical trigger signature/selection. Design: decisions 1/2. Paths: revision mechanism, `nepa/speclib/plan_revision.py`, selection tests. Verify: volatile timestamp/log/task-id/evidence-hash changes preserve signatures; byte replay is identical; at most one candidate is selected and rejected/activated signatures follow the exact retry rules.
- [x] 3.7 Implement atomic idempotent trigger-event batch append under the existing run lock, including common-boundary validation, canonical event order and conflict detection while preserving revision sequence, active pointer and metric formulas. Requirements: typed trigger ledger facts and no execution-state mutation. Design: decision 3. Paths: `nepa/speclib/plan_revision.py`, `nepa/run_store.py`, ledger/metric tests. Verify: focused tests cover no-hit no-write, multi-hit single replacement, replay, conflicting duplicate, interruption, non-activation tail pointer validation and unchanged trigger-histogram semantics.

## 4. F2 decomposition patch operators

- [x] 4.1 Implement one atomic semantic patch dispatcher that Schema-validates the complete list before cloning the source IR, dispatches only the declared operator enum by stable ids, applies operations in order and discards the whole result on any failure. Requirements: closed F2/F3 patches and complete candidate application. Design: decisions 4/5. Paths: `nepa/speclib/revision_mechanism.py`, patch tests. Verify: replay produces byte-identical IR and an error at the first/middle/final operation publishes no partial output.
- [x] 4.2 Implement `split_task` and `merge_tasks` with same-package scope, explicit new uid/source lineage, non-empty successors, at-most-four-file tasks and complete file/responsibility/acceptance mappings. Requirements: F2 patch operators and INV-3. Design: decisions 5/6. Paths: revision mechanism, Plan/linker helpers and operator tests. Verify: legal split/merge and renumbering pass; missing/duplicate lineage, empty/oversized tasks and lost obligations fail.
- [x] 4.3 Implement `move_responsibility`, `move_file_owner` and `insert_task` with same-package allowed-file constraints, atomic removal from the old owner, exact unique partitions and no premature REVALIDATE success claim. Requirements: F2 patch operators and INV-2/3. Design: decisions 5/6. Paths: revision mechanism and operator tests. Verify: legal moves/insertion pass; cross-package targets, double owners, unknown allowed files, orphaned requirements and runtime fields fail.
- [x] 4.4 Implement `rewrite_instructions` and `reorder_dependency`, restricting the former to guidance fields without usage reset and the latter to contract-proven package-local added edges followed by the existing deterministic Linker ordering. Requirements: F2 patch operators and revision candidate completion. Design: decisions 5/8. Paths: revision mechanism, `nepa/speclib/plan.py`, linker/lint tests. Verify: guidance digest/ordering changes are deterministic; attempts/budgets, architecture, unsupported fields, unproven/cross-package edges and cycles are rejected.

## 5. F3 architecture patch operators

- [x] 5.1 Implement `add_contract` and `extend_contract` with monotonic exports, byte-preserved published declarations/signatures, explicit implementation slots and closed provider/consumer bindings. Requirements: F3 patch operators and INV-1/3. Design: decisions 5/6. Paths: revision mechanism, architecture/contract validators and operator tests. Verify: legal additions pass; removal/signature mutation, missing slot/provider/consumer and commitment expansion fail.
- [x] 5.2 Implement `add_file_slot`, `add_work_package` and `move_file_across_wp` while retaining the module boundary and complete file/responsibility/provider/build partitions and emitting explicit obligation/file lineage. Requirements: F3 patch operators and candidate completion. Design: decisions 5/6. Paths: revision mechanism, architecture/Plan/Delivery helpers and operator tests. Verify: legal closed deltas pass; new modules, expanded commitments, duplicate/unowned files, dangling build refs and partial cross-package moves fail.
- [x] 5.3 Implement `retire_file_slot` and `re_adopt` as migration declarations only: retirement removes every live reference and supplies successor obligations/symbols plus quarantine mapping; re-adoption binds one registered quarantine source to an explicit slot/owner and later revalidation. Requirements: F3 patch operators and existing M1-8/M1-9 consumer compatibility. Design: decisions 5/6. Paths: revision mechanism, `nepa/speclib/materialization.py` contract helpers and operator tests. Verify: legal retirement/re-adoption outputs validate against existing migration consumers; deletion, collision, unknown source, wrong hash/owner and missing successor fail without touching workspace.
- [x] 5.4 Enforce F3-only structural operations, admit F2 operations inside F3 only when needed to close the same structural delta, and reject forbidden remove/shrink/delete/replace/implicit-rename semantics. Requirements: closed F3 operator set. Design: decisions 4/5. Paths: revision mechanism and negative operator matrix. Verify: every listed forbidden operation and unnecessary/cross-delta F2 companion fails; a complete explicit retire+add+migration path succeeds atomically.

## 6. Candidate completion, invariants and publication

- [x] 6.1 Route the patched IR through the existing contract closure, task uid/digest derivation, stable Linker numbering, coverage, basic/full lint and Delivery Blueprint/manifest/contract-map derivation without embedding State fields or inventing companion edits. Requirements: revision candidates use existing completion path. Design: decision 5. Paths: `nepa/speclib/plan.py`, `nepa/speclib/delivery.py`, `nepa/speclib/lint.py`, revision mechanism and candidate tests. Verify: legal candidates equal a direct deterministic completion; dangling references, inferred fixes, runtime fields and replay drift fail.
- [x] 6.2 Implement explicit INV-1/2/3 reports over canonical commitment equality, frozen normative requirement ownership and non-weaker old-to-new task/file/test obligations, using stable lineage rather than task numbers/text similarity. Requirements: complete invariant-valid candidate. Design: decision 6. Paths: revision mechanism, Plan/coverage validators and invariant tests. Verify: each invariant has passing split/merge/move/interface cases and isolated negative cases for commitment drift, missing/double owner and weakened/missing acceptance mapping.
- [x] 6.3 Invoke the existing migration classifier only after candidate lint/invariants pass and assemble complete task/file classifications, preservation data, retirement/re-adoption and affected-group inputs compatible with M1-8/M1-9; make only a proven §10.2.1 minimum fix if a concrete classifier counterexample appears. Requirements: complete migration candidate and prerequisite preservation. Design: decision 6. Paths: `nepa/speclib/plan_revision.py`, revision mechanism, migration/group tests. Verify: F2 renumber/move and F3 contract/slot cases cover every old/new task and old realized file and validate unchanged through existing materialization/S6 consumers.
- [x] 6.4 Implement event-scoped candidate staging, hash validation, trigger-batch append, atomic final-directory rename and reconciliation for no-event, selected-event, completed replay and conflicting-byte states. Requirements: unactivated candidate and no-activation/recovery requirements. Design: decision 7. Paths: `nepa/run_store.py`, revision mechanism, candidate Schemas and recovery tests. Verify: fault injection before/after staging, ledger replacement, rename and commit marker converges once; conflicts fail as damage with no Agent call, formal version, State/file-ledger/workspace change or duplicate event.
- [x] 6.5 Integrate legal S6 task boundaries, TR-3 lease handoff, TR-8 provider submission rejection and the optional frozen internal `revision_patch_provider` seam end to end; return `StagePause(kind="revision_handoff", selected_event_seq, candidate_ref)` for selected F2/F3, restore S6 to pending and produce no termination/S9 output. Ensure replay returns the same pause with no duplicate event and no M1-11/M1-14 producer is reachable. Requirements: all trigger/candidate flow, selected-revision handoff and non-scope clauses. Design: decision 8. Paths: `nepa/stages/s6_execution.py`, `nepa/application.py`, orchestrator/RunStore seams and integration tests. Verify: trigger-only, local F1, submission reject, F2/F3 handoff and pause/resume fixture flows have exact outputs while active Plan/revision, State, workspace, public CLI and Agent traces retain the specified behavior.

## 7. Fixtures and independent acceptance coverage

- [x] 7.1 Add a deterministic developer generator and checked-in MQTT/non-MQTT M1-10 fixtures covering every TR, every operator family, F2 renumbering/ownership and F3 interface/slot/retirement cases through the same code path. Requirements: protocol neutrality and full M1-10 acceptance. Design: decision 8. Paths: `tests/tools/`, `tests/fixtures/`, fixture replay tests. Verify: two generator runs are byte-identical and regenerated temporary outputs match checked-in fixtures with zero protocol-specific branch in core code.
- [x] 7.2 Add `revision_mechanism` marker registration and focused trigger/selection/ledger tests with nonzero collection for TR-1 through TR-8, TR-9 absence, signature normalization, route precedence and deduplication. Requirements: trigger requirements. Design: decisions 1-3/8. Paths: `pyproject.toml`, new trigger tests. Verify: `uv run pytest -q -m revision_mechanism -k 'trigger or ledger'` passes and reports every TR-1…TR-8 case.
- [x] 7.3 Add focused positive/negative tests for every F2/F3 operation, cross-level composition, forbidden operations, whole-patch atomicity and deterministic replay. Requirements: patch operator requirements. Design: decisions 4/5. Paths: new operator tests and fixtures. Verify: `uv run pytest -q -m revision_mechanism -k 'operator or patch'` passes with an explicit operator-to-case matrix.
- [x] 7.4 Add candidate-completion, INV-1/2/3, migration compatibility, event-scoped publication/recovery and corruption tests, including F2 task renumbering and F3 interface extension/retirement. Requirements: candidate and compilation requirements. Design: decisions 5-7. Paths: candidate/recovery tests and existing Plan/revision/S5/S6 tests. Verify: `uv run pytest -q -m revision_mechanism -k 'candidate or invariant or recovery'` passes and each fault either converges uniquely or fails before authoritative mutation.
- [x] 7.5 Add predecessor and protocol-neutrality regressions proving ordinary F0/F1, M1-8 materialization, M1-9 migration/group execution, metric formulas and public CLI remain unchanged. Requirements: preservation and non-scope clauses. Design: decision 8 and risks. Paths: existing S5/S6/metric/CLI suites plus neutrality scans. Verify: focused predecessor markers/suites pass and searches find no prompt, calibration, public CLI, M2/S7/S8 or protocol-name branch added by M1-10.

## 8. Scope, acceptance and owner gate

- [x] 8.1 Audit requirement → Schema-valid fixture → boundary/evaluator/operator/completion → ledger/candidate artifact → replay/corruption-test closure; remove duplicate planner/patch frameworks, speculative compatibility/fallbacks and any reachable RG/critic/rehearsal/activation/circuit-breaker/PlanReviser behavior. Requirements: all M1-10 requirements and non-scope. Design: all decisions/risks. Paths: complete M1-10 implementation diff. Verify: record a closure matrix and targeted source scans in the acceptance record.
- [x] 8.2 Run focused Schema, Plan/Linker/Delivery, revision/State, RunStore/S6, trigger/operator/candidate and predecessor tests in the real sandbox, then run `uv run pytest -q -m revision_mechanism` with nonzero intended collection. Requirements: all M1-10 requirements. Design: migration steps 2-4. Paths: affected implementation/tests. Verify: all focused commands are green and exact counts are recorded.
- [x] 8.3 Run the public §10.8 CI: full `uv run pytest -q`, `uv run ruff check .`, `uv run mypy nepa`, Schema examples, public Spec/gold-coverage/Target/Test Bundle/Plan lint, strict current/all OpenSpec validation and `git diff --check`; write the exact M1-10 acceptance record and state that this is only the trigger/operator portion of D1.13. Requirements: proposal acceptance and all delta capabilities. Design: migration plan. Paths: complete repository/change diff and `acceptance-record.md`. Verify: every command is green with exact counts/evidence and later milestone boundaries.
- [x] 8.4 Obtain and record the responsible owner's explicit review of the corrected final M1-10 diff and replacement acceptance package before archive; leave this task unchecked until the external decision exists and do not treat it as D1.13 full-chain, D1.14 parameter or M1-15 acceptance. Requirements: project OpenSpec owner-gate rule. Design: Goals/Non-Goals, migration step 5, decision 10. Paths: M1-10 acceptance record only. Verify: a dated owner decision names the corrected implementation, evidence and approval scope.
- [x] 8.5 Inspect the complete corrected diff, remove unrelated refactoring/configurability and rerun the entire M1-10 focused/public acceptance; confirm M1-9 and archived changes are unchanged, `project_docs/` contains only the authorized TR-4 formula/version-history edit, the owner gate is truthful, all tasks/requirements pass, and M1-11 gates/activation, M1-12 circuit breakers, M1-14 PlanReviser, public CLI and M2/S7/S8 remain absent. Requirements: all change requirements and non-scope. Design: all decisions/risks, decision 10. Paths: complete repository/change diff. Verify: final command transcript is green and every remaining limitation is explicitly recorded before marking the change complete.

## 9. Review corrections

- [x] 9.1 Replace production trigger fact collection with current-plan, current-attempt and hash-validated projection; add the optional exact `revision.theta2/theta6` config contract, typed truncation and authoritative TR-4 preflight calculation, and legal attempt-boundary semantics without adding a Diagnoser producer or public switch. Verify: real-artifact TR-2/TR-4 positives and stale/tampered/in-flight/model-flag negatives pass; default configuration remains unchanged.
- [x] 9.2 Replace global INV-3 obligation lookup with explicit source/target task mappings and controller-recomputed lineage, ownership and test-gate preservation; reject implicit ownership changes in `add_work_package`. Verify: unrelated cross-task mapping, removed/disabled/postponed tests and ownership collisions fail while legal split/merge/move mappings pass.
- [x] 9.3 Derive F3 affected groups as exact connected components of changed task/path/contract and Blueprint build dependencies. Verify: disjoint artifacts remain separate, shared artifacts merge, unrelated artifacts are absent and unattributable seeds fail closed.
- [x] 9.4 Reconcile a selected event's pending/final candidate before replaying the S6 handoff and cover interruptions after staging, ledger replacement and rename. Verify: every fault converges once or fails as corruption without duplicate Agent calls, events or authoritative mutations.
- [x] 9.5 Regenerate/update closed Schemas, examples and protocol-neutral fixtures and run the focused revision, predecessor, S6 and recovery suites after each correction group. Verify: all new counterexamples and existing M1-8/M1-9 consumers pass.
- [x] 9.6 Supersede the prior acceptance record with the correction evidence, run full pytest/Ruff/mypy/public lint/diff/OpenSpec validation, inspect scope, then obtain a new responsible-owner decision. Verify: no new blocked task or pending candidate, no later-milestone behavior and no undocumented limitation remains.
