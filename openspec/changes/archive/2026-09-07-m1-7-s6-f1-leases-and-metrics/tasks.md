## 1. Baseline and implementation brief

- [x] 1.1 Re-read the authoritative M1-7 sections, this change's proposal/specs/design, archived M1-6 handoff and current S6/State/ledger/Agent/Git code; write the §10.8 four-part implementation brief with exact input Schema refs, outputs and acceptance commands, required function/class signatures, and all section numbers, without adding decisions. Requirements: all prerequisite and scope clauses. Design: Context, decisions 1-9. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, this change, archived M1-6 and current `nepa/`/tests (read-only except the derived brief). Verify: review the brief against §10.8 and confirm it names no TR/F2/F3/E1+/M2 producer.
- [x] 1.2 Re-run the current M1-6 contract and execution baseline before editing: Schema examples, Plan State/revision tests, Agent S6 tests, `uv run pytest -q -m s6_execution`, ruff and mypy; record any concrete §10.2.1 blocker and stop for user direction if satisfying M1-7 would require changing `project_docs/` or the selected ArchitecturePlanner lineage. Requirements: prerequisite/preservation clauses. Design: Context/Migration 1. Paths: existing code and test outputs only. Verify: required M1-6 gates pass or the exact pre-existing failure is documented; confirm M1-7 has no independent owner-signature gate and does not mark M1-13/M1-14/M1-15 approvals.

## 2. Closed F1 artifact contracts

- [x] 2.1 Add a closed draft-2020-12 lease-authorization Schema and conforming example binding active Plan ref, baseline commit/tree, current uid, sorted lending uid/path rows and evidence refs; reject extra, duplicate, unsafe and unbound values. Requirements: `s6-f1-lease-execution / F1 lease authorization is deterministic and bounded`. Design: decision 1. Paths: `nepa/schemas/`, `nepa/schemas/examples/`, schema tests. Verify: focused Schema tests cover valid same-package/provider shapes and every missing/extra/duplicate/path/ref failure.
- [x] 2.2 Add closed Joint Evidence v2 Schema/example with deterministic verification id, `kind=lease`, Plan/tree/parent bindings and sorted unique per-member task-evidence refs, and extend Task Evidence validation for new per-member lease publications without weakening normal evidence. Requirements: successful F1 and joint verification requirements. Design: decision 4. Paths: `nepa/schemas/joint-evidence.schema.json`, example, `task-evidence.schema.json`, tests. Verify: tests reject wrong id, order, member cardinality, tree, parent, execution kind, reused ref and mismatched member sequence.
- [x] 2.3 Convert the verification-WAL Schema/example into a closed normal/lease union that preserves the existing normal contract and binds complete lease authorization, start event, member/evidence rows, Joint Evidence, changed-file ownership, expected joint trailers and complete old/new State/file/revision projections. Requirements: F1 all-member publication and recovery. Design: decision 5. Paths: `nepa/schemas/verification-pending.schema.json`, examples and artifact tests. Verify: normal examples remain byte-valid; lease positives pass; partial member sets, mixed trailers, missing event snapshots and cross-tree refs fail.
- [x] 2.4 Extend the S6 attempt and revision-ledger Schemas/examples with the exact optional lease binding, conditional start/finish payload rules, deterministic ids and lease-kind sorted multi-member verification while keeping later trigger/activation producers unreachable. Requirements: lease-call and typed-event requirements. Design: decisions 2/6. Paths: `s6-attempt`, `revision-ledger` Schemas/examples and tests. Verify: strict tests reject success without joint refs/commit, failure without reason, mismatched lease ids, unsorted/duplicate members, lease-as-revision and arbitrary later-event output.

## 3. Lease authorization, allocation and typed ledger projection

- [x] 3.1 Implement a pure authorization projector/validator over active Plan, State, file ledger, accepted ledger and config that proves done ownership, `s6_owned` class, same-work-package or direct-provider adjacency, exact ≤2 external paths and remaining κ/current/global allowance. Requirements: deterministic bounded authorization. Design: decision 1. Paths: existing S6/Plan helper modules and focused tests. Verify: positive same-package/direct-provider cases and negative frozen/not-done/non-neighbor/third-file/stale-plan/stale-baseline/exhausted-budget cases have no input mutation.
- [x] 3.2 Extend ordinary attempt allocation so only an already-legal Fixer attempt can carry an accepted lease, then append/replay `lease_started` and persist its deterministic `lease-<event_seq>` binding before provider I/O without changing lender State or adding an attempt. Requirements: F1 consumes one existing Fixer attempt; modified F0 routing. Design: decision 2. Paths: `nepa/run_store.py`, S6 controller, attempt/ledger helpers and tests. Verify: attempt 1 cannot lease, attempts 2-4 retain expected tier/role, current/global counters increment once, lender attempts stay fixed, and interruption never refunds or duplicates attempt/κ.
- [x] 3.3 Generalize typed-ledger validation and append idempotence to pair unique lease starts/finishes, enforce conditional payloads and call/evidence refs, and accept sorted lease verification at the latest unchanged revision sequence. Requirements: `plan-revision-infrastructure` lease and joint requirements. Design: decision 6. Paths: `nepa/speclib/plan_revision.py`, ledger tests. Verify: success/failure/pending histories validate; finish-before-start, double finish, conflicting replay, member omission and revision movement fail closed.
- [x] 3.4 Implement monotonic evidence-sequence allocation for every joint member after validation and before evidence publication, preserving legal crash gaps and the lender's ordinary attempts. Requirements: successful F1 execution and execution lint. Design: decision 4. Paths: `nepa/run_store.py`, `nepa/speclib/plan_state.py`, allocation tests. Verify: two-member allocation is uid-sorted, survives interruption without reuse, changes only counters and does not increment `s6_attempts_used` for lenders.

## 4. F1 Agent context and candidate validation

- [x] 4.1 Extend the production Fixer role input contract and prompt with conditionally present canonical lease authorization and sorted leased-file bytes while preserving the five sections, full-file output Schema and all protocol/provider/model-neutral visibility rules. Requirements: modified `agent-invocation-runtime`. Design: decision 3. Paths: `nepa/agents/s6.py`, `nepa/agents/roles.py`, Fixer prompt and tests. Verify: ordinary rendered prompts remain stable; F1 exposes only authorized files; missing/extra/mismatched lease fields fail before provider I/O; neutrality scan passes.
- [x] 4.2 Include leased files in required deterministic context accounting and fail before provider I/O when the combined required context exceeds `coder_context_max_tokens`; never trim current/leased files or failure feedback. Requirements: F1 context and overflow scenarios. Design: decision 3. Paths: `nepa/agents/s6.py`, context tests. Verify: boundary-size, non-ASCII accounting, guidance-only trimming and one-byte-overflow fixtures produce deterministic totals/results.
- [x] 4.3 Generalize candidate normalization to use exactly current-owned plus accepted leased paths for this invocation, preserving all safe-path, frozen-file, duplicate and full-file checks and preventing related-but-unleased writes. Requirements: modified candidate application and F1 candidate scope. Design: decision 3. Paths: Agent/S6 candidate helper and tests. Verify: current-only, mixed current/leased and leased-only changed subsets behave as specified; third-party, frozen, unsafe and unlisted neighbor paths fail before live mutation.
- [x] 4.4 Run declaration/implementation-binding checks over every changed current and leased file and reuse one joint candidate tree for all default builds and executable smoke checks with empty M1 tests. Requirements: successful joint validation and export-drift failure. Design: decision 3. Paths: `nepa/stages/s6_execution.py`, build/smoke helpers and sandbox tests. Verify: valid two-file repair passes release/SAN/smoke; current or lender export drift, warning, sanitizer failure, early exit, timeout and SIGTERM each preserve the baseline and publish no success.

## 5. Joint evidence, State and file-ledger projection

- [x] 5.1 Add pure per-member lease Task Evidence and Joint Evidence projection, sorting members by uid and deriving `v-<minimum uid>-<that uid evidence_seq>` while binding one Plan/tree/parent/result set and each member's exact changed files. Requirements: successful F1 execution. Design: decision 4. Paths: S6 evidence helpers, Schema loaders and artifact tests. Verify: canonical replay plus wrong member/path/tree/sequence/ref negative tests.
- [x] 5.2 Implement `amended_under_lease` as one complete State transition: current in-progress normal task and every lending done task become/remain done on the new joint proof, lender modes/attempts and all owners remain unchanged, unrelated rows are byte-preserved. Requirements: modified State transitions. Design: decision 6. Paths: `nepa/speclib/plan_state.py`, transition tests. Verify: valid two-member projection passes snapshot lint; partial proof, non-done lender, changed owner/attempt, duplicate uid and arbitrary replacement fail without mutation.
- [x] 5.3 Generalize the S6 file-ledger success projector to update only actual current/leased realized rows on the joint commit/tree, preserve class/ownership history and all unrelated rows, and reject leased paths not matching the accepted authorization. Requirements: F1 success and State/file-ledger atomicity. Design: decision 6. Paths: existing S6 projection helper, `nepa/speclib/plan_revision.py` as appropriate, tests. Verify: unchanged lender files remain valid, changed files get the new joint proof, and owner-history/hash drift fails.
- [x] 5.4 Extend full execution lint to verify lease start/finish, lease-kind member evidence, Joint Evidence, shared tree/commit, joint-only trailers, exact external paths, unchanged lender attempts/owner history and complete State/file-ledger/event agreement while leaving snapshot-only lint filesystem-neutral. Requirements: modified execution lint. Design: decisions 4/6. Paths: `nepa/speclib/plan_state.py`, Git/evidence readers and tests. Verify: independently corrupt every ref, member, trailer, tree, owner, attempt and event and prove no strict subset can validate.

## 6. Joint commit publication and recovery

- [x] 6.1 Generalize Git preparation/publication to stage exactly the accepted current/leased change set and create one joint commit with only `NePA-Verification-ID` and `NePA-Joint-Evidence-SHA256`, preserving the existing normal-task trailer path. Requirements: successful F1 execution. Design: decision 5. Paths: `nepa/tools/git_ops.py`, S6 publisher and Git tests. Verify: one commit has exact parent/tree/staged files/trailers; no single-task or unrelated trailer/path appears; ordinary commit regression remains green.
- [x] 6.2 Build and persist the complete lease verification WAL, immutable per-member/Joint Evidence and projected State/file/revision ledgers before installing candidate files; publish the legal joint commit and hand off to the shared reconciler. Requirements: all-member publication. Design: decisions 4-6. Paths: `nepa/stages/s6_execution.py`, `nepa/run_store.py`, artifact tests. Verify: successful fixture produces one WAL-bound result and every canonical ref/hash/tree is recomputable before commit.
- [x] 6.3 Implement pre-commit lease failure/recovery: restore only WAL-listed files to the common accepted parent, retain consumed attempt/κ/candidate/diagnostics/evidence gaps, verify the lender's prior done proof, append one failed `lease_finished`, and continue/exhaust only the current task. Requirements: F1 failure and pre-commit recovery. Design: decision 7. Paths: S6 reconciler/failure path and fault tests. Verify: faults after start, response, candidate, results, member evidence, Joint Evidence, WAL and install never accept a member, duplicate a call/event or broadly clean unrelated files.
- [x] 6.4 Implement post-commit forward reconciliation: verify exact joint commit/evidence/WAL, atomically complete all member State/file-ledger rows plus lease-kind verification and successful finish events, then remove the WAL; conflicting accepted facts fail as artifact damage. Requirements: F1 post-commit recovery and typed joint events. Design: decisions 5-7. Paths: S6 reconciler, RunStore and fault tests. Verify: faults after commit and after each mutable publication converge to one commit/start/verification/finish set with zero Agent/build/smoke replay and no half-done State.
- [x] 6.5 Integrate the explicit internal lease-authorization seam into the existing S6 loop so only an eligible next Fixer enters F1, default production execution remains ordinary when no authorization exists, and no TR, F2/F3, E1+ or group artifact is produced. Requirements: F1 authorization and modified F0 exhaustion. Design: decisions 1/9. Paths: `nepa/stages/s6_execution.py`, application assembly as necessary and controller tests. Verify: ordinary M1-6 fixtures are unchanged; authorized fixtures use F1; invalid/absent authorization follows remaining F0 or controlled exhaustion with no later-work-item event.

## 7. Pure M1 code-generation metrics

- [x] 7.1 Create the small standalone pure metrics module and common available/unavailable result helpers, with validated artifact inputs, byte-deterministic output and no run mutation or CLI/report dependency. Requirements: `code-generation-metrics / M1 metrics are pure projections`. Design: decision 8. Paths: new `nepa/metrics.py` (or the exact brief-approved equivalent), exports and unit tests. Verify: input deep-copy/replay tests, invalid/missing-ref envelopes, legal empty-ledger zeros and no filesystem write.
- [x] 7.2 Implement final equal-weight task completion/blocked/incomplete triplets and lineage-aware `@r0`/`ever_blocked_rate` projections using only explicit activation/migration obligations and valid completion proofs. Requirements: task-state rates. Design: decision 8. Paths: metrics module and `metric_contract` lineage fixtures. Verify: §9.1.4 split, regenerate-success, merge, move, empty-task and triplet-sum cases match exact fractions and reason codes.
- [x] 7.3 Implement original and post-REGENERATE first-pass calculations from immutable call/evidence history, excluding unexecuted, pure revalidation and AMEND-only rows. Requirements: first-pass rates. Design: decision 8. Paths: metrics module and history fixtures. Verify: A-first/B-second/C-new-generation fixed case yields 1/3 and 1/1; recovery gaps and later successes cannot rewrite the numerator.
- [x] 7.4 Implement S5/S6 `smoke.pass`, `s6_build_ok` and terminal `build_ok` projections from accepted receipt/ref facts, including artifact counts/failures, pending-repair exclusion and planned-stop/unavailable reasons. Requirements: build/smoke metrics. Design: decision 8. Paths: metrics module, receipt adapters and tests. Verify: the M1 planned-stop fixed case yields true S6 values and null `build_ok` with `PLANNED_STOP_NO_TERMINAL_ROUND`; latest-directory inference is rejected.
- [x] 7.5 Implement typed revision counts, rejection/trigger histograms, per-task migration mix, preservation sequence/mean/min, estimate/actual rework costs, effectiveness and ineffective count with immutable call-ref deduplication. Requirements: revision/lease metrics and denominator separation. Design: decision 8. Paths: metrics module and static future-event fixtures. Verify: §9.1.4 preservation and two-revision cost cases, empty activation `NO_REVISIONS`, duplicate call refs, record-only triggers and F1-not-activation all match exact results.
- [x] 7.6 Implement lease count, success rate, pending count and deterministic nearest-rank external-file p50/p95 from accepted start/finish pairs, freezing the percentile rule in the implementation brief and tests. Requirements: revision and lease metrics. Design: decision 8. Paths: metrics module and lease fixtures. Verify: two leases one success/one failure yields count 2/rate 0.5/pending 0; unfinished and zero-lease `NO_LEASES` cases are exact.
- [x] 7.7 Compose the public §9.1.4 M1 metric object and a read-only run-directory adapter that validates authoritative refs before calling pure functions; expose it for later M2-6 reuse without adding `nepa eval runs`. Requirements: all metric requirements. Design: decision 8. Paths: metrics module, optional focused loader and tests. Verify: fixed package replay is byte-identical, planned-stop fields have the prescribed envelopes, Agent `expected_effect` cannot affect output and no new CLI command exists.

## 8. Observability, fixtures and end-to-end acceptance

- [x] 8.1 Extend existing status and full Plan-State lint projections to report accepted/pending lease facts and validate joint results from durable artifacts, without adding a new command/flag or changing snapshot-only claims. Requirements: proposal observability and execution-lint clauses. Design: decision 9. Paths: `nepa/cli.py`, status/lint helpers and CLI tests. Verify: interrupted, failed, successful and completed-replay status is deterministic across processes; normal M1-6 CLI output remains compatible.
- [x] 8.2 Add frozen MQTT and non-MQTT lease authorization/provider fixtures for eligible success, validation failure, attempt/κ exhaustion and every recovery window, with provenance and deterministic regeneration checks; add static lineage/revision/receipt packages for every §9.1.4 fixed metric case. Requirements: all F1 and metric scenarios. Design: Migration 3-4/decision 9. Paths: `tests/fixtures/s6/`, metric fixtures, generators/readmes and tests. Verify: regenerate into two temporary directories and byte-compare semantic outputs; fixture editing or forbidden later producer code is detected.
- [x] 8.3 Register `pytest.mark.s6_lease` and add real-sandbox end-to-end tests from an M1-6 neighbor-done baseline through leased Fixer, two-member evidence, joint commit, State/file ledger/events and final S6 receipt for both protocols. Requirements: complete `s6-f1-lease-execution` capability. Design: decisions 1-7/9. Paths: pytest config, S6 tests and fixtures. Verify: build `nepa-sandbox:latest` and run `uv run pytest -q -m s6_lease`; audit one Fixer call, no extra attempt, unchanged owners, exact joint proof and no test runner.
- [x] 8.4 Register `pytest.mark.metric_contract` and implement all fixed, boundary, empty/unavailable and deterministic-replay metric tests without LLM calls. Requirements: complete `code-generation-metrics` capability. Design: decision 8/9. Paths: pytest config, metric tests and static fixtures. Verify: `uv run pytest -q -m metric_contract` passes and directly asserts every public key/formula/reason required by §9.1.4.

## 9. Scope and final acceptance

- [x] 9.1 Audit requirement-to-code/test closure and remove duplicate transaction paths, speculative fallback/compatibility behavior, protocol-specific branches, direct State edits, model-authorized leases, owner changes and any reachable TR/F2/F3/E1+/AMEND/REVALIDATE/group/M2 producer. Requirements: all scope and preservation clauses. Design: Goals/Non-Goals and all decisions. Paths: complete M1-7 diff. Verify: trace each delta requirement to implementation/tests, run neutrality scans, and confirm `project_docs/`, frozen inputs and ArchitecturePlanner lineage are untouched.
- [x] 9.2 Run focused regression and recovery gates: Schema examples, Plan State/revision/RunStore/Agent/Git/S4/S5/S6 tests, `uv run pytest -q -m s6_execution`, `uv run pytest -q -m s6_lease`, `uv run pytest -q -m metric_contract`, and every parameterized normal/joint fault window. Requirements: all six delta capabilities. Design: Risks/Migration. Paths: affected implementation and tests. Verify: every gate is green with one accepted result per transaction and byte/timestamp-stable completed replay.
- [x] 9.3 Inspect the complete diff and run the current change's full acceptance: `uv run pytest -q`, public §10.8 CI commands (ruff, mypy, schema examples, gold lint and target lint), strict current/all OpenSpec validation and `git diff --check`; confirm no non-scope behavior or unrecorded assumption remains, record limitations/prerequisite evidence, and do not mark the change complete while any required gate fails or any later owner approval is absent. Requirements: all requirements and M1-7 DoD. Design: Migration Plan. Paths: complete repository/change. Verify: all commands pass and the final acceptance record maps every artifact, metric fixture and recovery result to this change only.

## 10. Correctness repair

- [x] 10.1 Add sequential State history, integrate attempt/recovery and bind the S6 snapshot in its receipt.
- [x] 10.2 Permit legal non-empty F1 changed-file subsets and close lease verification/evidence bindings.
- [x] 10.3 Make the run metric adapter follow accepted refs/history and associate costs by trace output path.
- [x] 10.4 Add crash, corruption, subset and production-shaped metric regressions; rerun all acceptance gates and refresh the acceptance record.
