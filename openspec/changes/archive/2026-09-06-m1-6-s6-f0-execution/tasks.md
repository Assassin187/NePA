## 1. Baseline and contract inventory

- [x] 1.1 Re-read the authoritative M1-6 sections and inspect the archived M1-5 handoff plus current S4/S5/State/Agent/CLI code before editing; record only concrete §10.2.1 blockers. Requirements: all prerequisite clauses. Design: Context/decisions 1-2. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, archived M1-5 artifacts, existing `nepa/` and tests (read-only). Verify: run the current S4/S5/RunStore/Plan-State focused regressions and `uv run pytest -q -m s5_epoch`; confirm no `project_docs/`, selected ArchitecturePlanner prompt or calibration lineage edit is needed.
- [x] 1.2 Freeze the M1-6 implementation boundary and prerequisite evidence: M1-5 ready E0 is the only fresh baseline, M1-6 has no independent owner-signature gate, and F1/F2/F3/M2 producers remain disabled. Requirements: S6 admission and F0-only clauses. Design: Goals/Non-Goals, decisions 1/7. Paths: test fixtures/metadata and change evidence only. Verify: focused fixture test proves both MQTT and non-MQTT E0 receipts/checkpoints pass completed-S5 validation without regeneration.

## 2. Closed State and evidence contracts

- [x] 2.1 Replace the fresh-run Plan State contract with closed v2 Schema/examples containing active Plan identity, all task execution fields, evidence counters and `s6_attempts_used`, while rejecting old-major mixing and illegal status fields. Requirements: `plan-state-validation / Fresh S6 State uses the complete version-two execution contract`. Design: decision 2. Paths: `nepa/schemas/plan-state.schema.json`, examples and schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_plan_state.py -k 'schema or initial or status or version'` with positive initial/migrated shape and missing/extra/conditional/old-major negative cases.
- [x] 2.2 Add closed Task Evidence v2, attempt record/pending-state, verification-WAL and S6 receipt Schemas/examples with exact Plan/epoch/task/sequence/tree/build/smoke/commit/ref bindings and no cyclic refs. Requirements: successful transaction, failed-candidate and S6-seal requirements. Design: decisions 3/5-7. Paths: new files under `nepa/schemas/` and examples/tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s6_artifacts.py -k 'attempt or evidence or verification or receipt or schema'` including extra field, future ref, wrong task/tree and incomplete WAL cases.
- [x] 2.3 Extend Run v4 S6 output refs and file/revision-ledger payload closure for the exact S6 receipt and `verification_committed` producer without opening later event producers. Requirements: `plan-revision-infrastructure / Ordinary S6 verification appends the existing typed ledger fact` and S6 seal. Design: decisions 6-7. Paths: Run/file-ledger/revision-ledger Schemas/examples and schema tests. Verify: schema tests reject arbitrary S6 refs, partial realized rows, lease/trigger/activation payload production and wrong revision sequence.

## 3. Plan State projection and lint

- [x] 3.1 Update initial State projection and snapshot lint to produce/validate the unique v2 all-pending normal state, active anchors, task set, evidence counters, attempt limits and global usage without enabling migration modes. Requirements: fresh State requirement. Design: decision 2. Paths: `nepa/speclib/plan_state.py`, exports and focused tests. Verify: `uv run pytest -q tests/test_plan_state.py -k 'initialize or snapshot or counter or normal'` with byte-deterministic replay and no input mutation.
- [x] 3.2 Implement closed ordinary event derivation for attempt allocation, success, exhaustion, dependency blocking and reconciled commit; require complete proofs and preserve unrelated rows. Requirements: `plan-state-validation / Ordinary F0 transitions allocate attempts and derive terminal fields`. Design: decisions 3/6-7. Paths: Plan event Schema, `nepa/speclib/plan_state.py`, transition tests. Verify: `uv run pytest -q tests/test_plan_state.py -k 'attempt_started or succeeded or exhausted or dependency or reconciled'` including double allocation, cap, arbitrary replacement and incomplete-proof rejection.
- [x] 3.3 Complete execution lint for task evidence, commit ancestry/tree/trailers, changed-file ownership, E0 ancestry, realized ledger rows, sequence history and clean workspace. Requirements: `plan-state-validation / Evidence counters and execution lint close the ordinary task chain`. Design: decisions 2/6. Paths: `nepa/speclib/plan_state.py`, git boundary and execution-lint tests. Verify: focused tests independently corrupt every external anchor and prove JSON-only snapshot lint does not claim filesystem validation.
- [x] 3.4 Add monotonic per-uid evidence allocation that persists before attempt artifacts, permits crash gaps and never reuses a sequence. Requirements: evidence-counter requirement and ordinary routing. Design: decision 3. Paths: `nepa/run_store.py`, State helpers and allocation tests. Verify: fault after allocation followed by resume yields a greater sequence and unchanged `s6_attempts_used` accounting for the interrupted call.

## 4. Production Coder/Fixer boundary

- [x] 4.1 Add the closed shared Coder/Fixer output Schema/example and parser for unique non-empty full-file responses, preserving notes/micro-plan only as evidence. Requirements: `agent-invocation-runtime / Production S6 coding roles use one closed protocol-neutral contract`. Design: decision 4. Paths: new Schema/example, `nepa/agents/` and tests. Verify: focused tests reject diffs, empty/duplicate files, extra fields and plan-changing payloads before workspace effects.
- [x] 4.2 Implement the pure ordered S6 context projector for task/work-package, architecture/contracts, exact Spec slice, contract-map/interface bytes, advisory language guidance and task-owned files; enforce deterministic token accounting and required-input overflow failure. Requirements: production coding role requirement. Design: decision 4. Paths: new focused S6 context module and tests. Verify: byte replay, exact section/source assertions, optional guidance trimming and required-file/spec/feedback overflow cases.
- [x] 4.3 Bind Coder attempt 1 to T2 and Fixer attempts 2-3 to T2/attempt 4 to T1 through the existing static Agent/LLM boundary, without following `escalate_to` metadata or adding Agent-layer retries. Requirements: bounded role routing and production coding role requirements. Design: decisions 3-4. Paths: `nepa/agents/roles.py`, invoker call site, prompts and fake-provider tests. Verify: four-attempt trace audit proves role/tier/attempt identity, one logical completion per attempt and no fifth call.
- [x] 4.4 Update Coder/Fixer prompt assets to the five-section production contract and add protocol/provider/model-neutrality gates; ensure only injected inputs can contain target-protocol facts and no test implementation enters either prompt. Requirements: production coding role and M1 visibility clauses. Design: decision 4. Paths: `nepa/agents/prompts/coder.md`, `fixer.md`, shared scans/tests. Verify: static scan plus rendered MQTT/non-MQTT contexts; inspect trace prompts for required delimiters and absence of forbidden assets.

## 5. Candidate and failure execution path

- [x] 5.1 Implement confined immutable attempt directories and candidate normalization, including exact owner whitelist, duplicate/safe path checks, full-file UTF-8 handling and changed-file subset calculation before live workspace mutation. Requirements: candidate application requirement. Design: decision 5. Paths: focused S6 execution helper, `nepa/run_store.py`, candidate tests. Verify: tests reject escape, symlink, frozen, other-owner, undeclared and duplicate paths while accepted candidate bytes replay identically.
- [x] 5.2 Add sealed contract/declaration and implementation-binding validation against the current binding/contract map before sandbox execution and evidence acceptance. Requirements: candidate contract-drift scenario. Design: decision 5. Paths: materialization/C99 parser reuse or a minimal S6 validator, focused tests. Verify: valid full implementations pass; renamed/missing/extra/drifted exports fail without changing Plan, contract map or workspace.
- [x] 5.3 Execute all task-required default build variants and every Blueprint executable smoke check against the isolated candidate tree using the existing network-disabled sandbox, with an explicitly empty M1 applicable-test set. Requirements: controller-owned acceptance and failed-candidate requirements. Design: decision 5. Paths: `nepa/tools/build.py`, S6 execution helper and fake/real sandbox tests. Verify: release/SAN success, compile warning/error, sanitizer, early exit, timeout and SIGTERM branches; assert no Test Bundle nodeid/runner is requested.
- [x] 5.4 Persist each failed model output, candidate files/tree, build/smoke evidence, normalized error and any deterministic Diagnoser result before baseline restoration; build the next Fixer context from the latest matching immutable failure. Requirements: `s6-f0-execution / Failed candidates remain auditable and feed the next Fixer`. Design: decision 5. Paths: attempt store/context/controller and tests. Verify: failed Coder then Fixer test proves byte identity between failed candidate and Fixer input across process resume, while live workspace remains at accepted HEAD.

## 6. Successful publication and reconciliation

- [x] 6.1 Implement pure Task Evidence, realized S6 file-ledger and verification-event projections bound to exact Plan/epoch/task/attempt/tree/results, preserving S5 rows and owner history. Requirements: successful commit and typed verification requirements. Design: decision 6. Paths: S6 projection module, revision helper and artifact tests. Verify: canonical replay plus wrong owner, stale sequence, unrelated row mutation and revision advancement negative cases.
- [x] 6.2 Implement verification-WAL publication: persist complete old/new State/ledger/event and expected evidence/commit/tree/trailers, publish evidence, install exact candidate files, stage only actual task changes and create one normal-task commit. Requirements: successful recoverable boundary. Design: decision 6. Paths: `nepa/run_store.py`, `nepa/tools/git_ops.py`, S6 publication tests. Verify: one task produces one commit and one evidence ref; git staged set, trailers, ancestry and clean output tree match exactly.
- [x] 6.3 Implement pre-commit recovery that restores the WAL-bound accepted baseline while retaining consumed attempts, sequences and immutable failed/orphan evidence; reject unrelated workspace conflicts. Requirements: crash-before-commit and failed recovery scenarios. Design: decision 6. Paths: RunStore/S6 reconciliation and fault tests. Verify: inject faults after every candidate/evidence/workspace/staging boundary and prove no false done state or broad deletion.
- [x] 6.4 Implement post-commit forward reconciliation that verifies the legal commit and completes State, file ledger and idempotent `verification_committed` append, then removes the WAL; reject any conflicting fact. Requirements: crash-after-commit and verification replay scenarios. Design: decision 6. Paths: RunStore, S6 controller, revision helper and recovery tests. Verify: faults after commit and each mutable publication converge to one commit/event with no Agent/build/smoke replay.

## 7. F0 controller, exhaustion and final receipt

- [x] 7.1 Implement S6 admission and stable-topological ordinary task loop on the registered controller path, including prerequisite reconciliation, fresh initialization, clean gates and completed-stage read-only verification. Requirements: admission and routing requirements. Design: decisions 1/3/7. Paths: new `nepa/stages/s6_execution.py`, stage registration, orchestrator and controller tests. Verify: deterministic two-task ordering, valid resume, missing-State-after-commit rejection and completed replay with zero provider/tool/file/timestamp changes.
- [x] 7.2 Implement fourth-failure blocking, graph-proven dependency propagation and continued execution of independently admissible branches, with F1/F2/F3 and revision triggers closed. Requirements: exhaustion/dependency requirement. Design: decision 7. Paths: S6 controller/State transition and graph tests. Verify: full-budget failure makes no fifth call, dependent task consumes zero attempts, independent branch can commit, and no lease/trigger/candidate/activation artifact appears.
- [x] 7.3 Route hard global attempt/time/cost exhaustion and static-valid unresolved tasks through `EXECUTION_UNRESOLVED` degraded semantics, while proven static-contract invalidity uses the failed route and internal defects remain internal errors. Requirements: exhaustion requirement and CLI exit codes. Design: decisions 3/7. Paths: controller/orchestrator/termination tests. Verify: exact 10/20/1 exit classifications, durable termination request, and no false S6 receipt/done state.
- [x] 7.4 Implement final all-variant build/smoke plus snapshot/execution lint, immutable revision-ledger prefix and S6 receipt publication/verification; never reopen done tasks after exit validation failure. Requirements: S6 seal requirement. Design: decision 7. Paths: S6 controller, RunStore, receipt projection and tests. Verify: accepted success binds every ref and HEAD; exit smoke fault yields `S6_EXIT_VALIDATION_FAILED`/10; completed receipt replay is byte/timestamp stable.

## 8. CLI, fixtures and end-to-end acceptance

- [x] 8.1 Wire `run --until s6`, `resume`, durable `status`, Plan basic/full lint and Plan-State snapshot/execution lint through existing commands without adding S6-specific flags. Requirements: M1 S6 CLI requirement. Design: decision 8. Paths: `nepa/cli.py`, application assembly, status/lint helpers and CLI tests. Verify: subprocess CLI tests cover success/planned_stop, interrupted status, resume and 0/10/20/1 exits while proving S7/S9 are not run after successful `--until s6`.
- [x] 8.2 Add deterministic frozen-provider S6 fixtures derived from both archived ready-E0 inputs, including success, fail-then-fix, four-failure, dependency and independent-branch response sequences; CI consumes but does not regenerate them. Requirements: all positive/negative role and execution scenarios. Design: decisions 3-7. Paths: `tests/fixtures/s6/`, generator/provenance and fixture tests. Verify: regenerate into two temporary directories and byte-compare semantic outputs; reject edited fixture lineage.
- [x] 8.3 Add `pytest.mark.s6_execution` end-to-end tests from ready E0 through Coder/Fixer evidence, commits, State/ledger/event and final S6 receipt for MQTT and non-MQTT fixtures using the real sandbox. Requirements: all `s6-f0-execution` positive scenarios and D1.6/D1.8/D1.11. Design: all decisions. Paths: S6 epoch tests and pytest/CI marker configuration. Verify: build `nepa-sandbox:latest`, run `uv run pytest -q -m s6_execution`, audit traces and assert no protocol-specific shared code/prompt branch.
- [x] 8.4 Add fault injection at every attempt, candidate, evidence, commit, State, ledger, event, receipt and Run boundary plus corruption tests for every bound ref. Requirements: all recovery and fail-closed scenarios. Design: decisions 1/3/5-7. Paths: S6 recovery/idempotence tests. Verify: parameterized interruption suite converges to one accepted result or deterministic fail-stop; repeated accepted invocation changes no bytes, refs or timestamps.

## 9. Final scope and acceptance audit

- [x] 9.1 Inspect the complete diff and remove unrelated refactoring, compatibility conversion, speculative fallback, duplicate persistence/execution paths and all F1/F2/F3/M2 behavior; confirm no `project_docs/`, selected ArchitecturePlanner prompt/bundle, calibration lineage, gold input or Test Bundle implementation changed. Requirements: all scope/preservation clauses. Design: Goals/Non-Goals and all risks. Paths: complete implementation diff. Verify: targeted `git diff --name-only` checks, protocol-neutral scans and assertions that later event producers remain unreachable.
- [x] 9.2 Run the complete M1-6 acceptance suite: State/Schema/Agent/RunStore/S4/S5/S6 focused tests, `uv run pytest -q`, `uv run pytest -q -m s5_epoch`, real Docker `uv run pytest -q -m s6_execution`, CLI exit/status/lint tests, ruff, mypy, gold/target lint, strict current/all OpenSpec validation and `git diff --check`; record any remaining assumption or limitation and do not mark complete while a required gate fails. Requirements: all four delta capabilities. Design: Migration Plan. Paths: complete repository and CI workflow.
- [x] 9.3 Re-audit the completed repair for newly introduced blockers: map every delta requirement to implementation and tests, scan for direct State mutation, parallel S6 prompts, forbidden Agent context, later-mode producers, broad Git cleanup and duplicate billing, then replay the no-op, post-commit evidence corruption and conflicting verification identity regressions. Verify: all three regressions fail closed as designed, required gates remain green, and no `project_docs/` change is required.
