## 1. Architecture export contract

- [x] 1.1 Extend the closed ArchitectureDraft, architecture patch/application, flat-draft and validation Schemas plus examples/fixtures with exact `exports[{interface_file,symbol,signature}]` rows and no undeclared compatibility shape. Requirement: `planning-architecture-infrastructure / ArchitectureDraft is a closed production contract`. Design: decision 1 and decision 3. Paths: `nepa/schemas/architecture-*.schema.json`, `nepa/schemas/flat-plan-draft.schema.json`, `nepa/schemas/examples/`, `gold_file/`, `tests/fixtures/`. Verify: `uv run pytest -q tests/test_architecture_schema.py tests/test_architecture_patch.py tests/test_schema_examples.py`.
- [x] 1.2 Update the single architecture semantic validator and patch-locality path to check export file ownership, `(interface_file,symbol)` uniqueness, mechanically attributable symbols and implementation-free signatures while retaining complete canonically ordered ARCH_VALIDATE evidence. Requirements: `planning-architecture-infrastructure / ArchitectureDraft is a closed production contract` and `ARCH_VALIDATE is the single production S4-G2 validator`. Design: decision 1. Paths: `nepa/speclib/architecture.py`, `nepa/calibration/s4_architecture.py`, focused tests. Verify: `uv run pytest -q tests/test_architecture_validation.py tests/test_architecture_patch_locality.py tests/test_architecture_repair_locality.py tests/test_architecture_calibration.py`.
- [x] 1.3 Update ArchitecturePlanner initial/repair prompts and agent bindings to require closed exports without adding protocol/model/provider facts, and prove layered and flat callers still use the same validator. Requirements: `planning-architecture-infrastructure / ARCH_VALIDATE is the single production S4-G2 validator` and `architecture-prompt-development / New evidence is isolated from historical protocols`. Design: decisions 1 and 7. Paths: `nepa/agents/prompts/architecture_planner_initial.md`, `nepa/agents/prompts/architecture_planner_repair.md`, `nepa/agents/`, `nepa/stages/s4_planning.py`, focused tests. Verify: `uv run pytest -q tests/test_s4_agent_contracts.py tests/test_s4_controller.py tests/test_planning_protocol_neutrality.py tests/test_prompt_development_neutrality.py`.

## 2. Linked Plan identity and digests

- [x] 2.1 Extend the closed Plan Schema/example so every final task requires `task_uid`, `obligation_digest` and `guidance_digest`, while PlanDraftIR/task-shard Schemas reject those controller-owned fields. Requirement: `plan-compilation-validation / Plan and task-shard contracts are closed and state-free`. Design: decisions 1 and 3. Paths: `nepa/schemas/plan.schema.json`, `nepa/schemas/plan-draft-ir.schema.json`, related examples/fixtures and schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_plan_lint.py tests/test_s4_agent_contracts.py`.
- [x] 2.2 Extend the existing Linker after stable topology to derive the exact task uid and sorted obligation/guidance digests from architecture exports, reject in-Plan uid collisions, and recompute metadata after every repair through the common completion path. Requirements: `plan-compilation-validation / Final task identifiers use stable topological order` and `Candidate completion remains state-free and derives M1-4d metadata`. Design: decision 1. Paths: `nepa/speclib/plan.py`, `nepa/speclib/planning.py`, `nepa/stages/s4_planning.py`, focused tests. Verify: `uv run pytest -q tests/test_plan_linker.py tests/test_s4_candidate_completion.py tests/test_s4_controller.py`.
- [x] 2.3 Make the Blueprint input an explicit task semantic projection excluding all three derived metadata fields, and add replay/hash-cycle tests for array permutation, guidance-only change and consumed-signature change. Requirement: `plan-compilation-validation / Final Plan binds the exact Blueprint and frozen inputs without a hash cycle`. Design: decision 1. Paths: `nepa/speclib/plan.py`, Delivery Blueprint compiler call sites, `tests/test_plan_linker.py`, `tests/test_plan_coverage.py`. Verify: `uv run pytest -q tests/test_plan_linker.py tests/test_plan_coverage.py tests/test_s4_candidate_completion.py`.

## 3. Prompt lineage and production gate

- [x] 3.1 Version the ArchitecturePlanner development inputs as a new design-7.2.0 lineage, freeze the updated Schema/validator/serializer/patch contract and request configuration, and make selectors exclude all pre-export evidence while retaining legacy recomputation. Requirement: `architecture-prompt-development / New evidence is isolated from historical protocols`. Design: decision 7. Paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, prompt-development manifests/evidence Schemas and tests. Verify: `uv run pytest -q tests/test_calibration_lineage.py tests/test_prompt_development.py tests/test_prompt_recovery_lineage.py tests/test_architecture_calibration.py`.
- [x] 3.2 Execute the existing bounded single-slot V0-V2, N=3 development policy against the frozen lineage, persist all request/response/validation/neutrality evidence, and select only the first version with recomputable pass count at least 2/3; do not publish a handoff if no version qualifies. Requirement: `architecture-prompt-development / New evidence is isolated from historical protocols`. Design: decision 7 and migration step 2. Paths: the configured calibration output root and new-lineage report/decision artifacts only. Verify: run the repository prompt-development command for the frozen declaration, then `uv run pytest -q tests/test_prompt_development.py tests/test_prompt_development_attempts.py tests/test_prompt_development_neutrality.py` and independently recompute the selected report.

## 4. Revision artifact contracts

- [x] 4.1 Generalize the active-pointer, file-ledger and revision-ledger Schemas/examples: use top-level `files`, closed conditional file states, consecutive real revision entries and the 64-zero genesis predecessor; add a closed migration-report Schema/example with complete task/file rows, counts and preservation rate. Requirements: `plan-revision-infrastructure / File and revision ledgers are closed append-only contracts` and `Preservation rate is a file-level pre-activation fact`. Design: decision 3. Paths: `nepa/schemas/active-plan.schema.json`, `nepa/schemas/file-ledger.schema.json`, `nepa/schemas/revision-ledger.schema.json`, new migration-report Schema, examples and schema tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s4_contracts.py` plus focused negative cases for aliases, conditional-row drift and missing migration rows.
- [x] 4.2 Extend Plan State and S4 checkpoint/commitment contracts and examples so current Plan/sequence/epoch bindings can advance independently of immutable `output_refs.plan`, and encode the unique legal initial versus migrated task rows. Requirements: `plan-state-validation / Plan State Schema and snapshot lint enforce one unambiguous state` and `s4-initial-plan-publication / The S4 seal carries independent typed anchors`. Design: decisions 3 and 6. Paths: `nepa/schemas/plan-state.schema.json`, `nepa/schemas/s4-*.schema.json`, related examples and tests. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_plan_state.py tests/test_s4_commitment.py tests/test_s4_contracts.py`.

## 5. Pure migration and chain semantics

- [x] 5.1 Add the pure `nepa/speclib/plan_revision.py` classifier that accepts complete old/new Plans, old State, file ledger and explicit lineage; applies the ordered four-way predicates; accounts exactly once for every task and old realized file; and emits canonically sorted replay-stable rows without inferred ancestry. Requirement: `plan-revision-infrastructure / Migration classification is complete and deterministic`. Design: decision 2. Paths: `nepa/speclib/plan_revision.py`, `tests/test_plan_revision.py`. Verify: `uv run pytest -q tests/test_plan_revision.py -k 'classification or lineage or deterministic'`.
- [x] 5.2 Compute file-level preservation and task/file aggregate counts from report rows, including removed realized files as `REGENERATE` and exact `1.0` for a zero denominator; reject any supplied summary drift. Requirement: `plan-revision-infrastructure / Preservation rate is a file-level pre-activation fact`. Design: decision 2. Paths: `nepa/speclib/plan_revision.py`, `tests/test_plan_revision.py`. Verify: `uv run pytest -q tests/test_plan_revision.py -k 'preservation or counts or removed'`.
- [x] 5.3 Implement full revision-ledger entry construction/validation and canonical predecessor hashing, rejecting mutated history, gaps, duplicates, incomplete migration rows and aggregate drift. Requirement: `plan-revision-infrastructure / File and revision ledgers are closed append-only contracts`. Design: decisions 2 and 3. Paths: `nepa/speclib/plan_revision.py`, `tests/test_plan_revision.py`. Verify: `uv run pytest -q tests/test_plan_revision.py -k 'ledger or chain or tamper'`.
- [x] 5.4 Implement unique C.A.P/revision-sequence/epoch successor validation for F2 and F3, immutable version-path validation and C=1, without implementing trigger or candidate-generation policy. Requirement: `plan-revision-infrastructure / Plan versions and epochs advance monotonically`. Design: decision 2. Paths: `nepa/speclib/plan_revision.py`, `tests/test_plan_revision.py`. Verify: `uv run pytest -q tests/test_plan_revision.py -k 'version or epoch or successor'`.

## 6. Whole-state migration and evidence validation

- [x] 6.1 Extend snapshot lint to validate the current active Plan, pointer, immutable initial anchor and complete ledger lineage, including the ledger-proven exception for nonzero-attempt pending AMEND rows. Requirement: `plan-state-validation / Plan State Schema and snapshot lint enforce one unambiguous state`. Design: decisions 2 and 6. Paths: `nepa/speclib/plan_state.py`, `tests/test_plan_state.py`, new revision-state tests. Verify: `uv run pytest -q tests/test_plan_state.py tests/test_plan_revision_state.py -k 'snapshot or active or amend'`.
- [x] 6.2 Add one deterministic whole-State and file-ledger projection operation for INHERIT, typed-proof REVALIDATE, unexhausted AMEND, clean REGENERATE and removal from active State; reject partial reports and arbitrary replacement State while leaving ordinary execution transitions unchanged. Requirement: `plan-state-validation / State transitions are derived from the closed event table`. Design: decision 2. Paths: `nepa/speclib/plan_revision.py`, `nepa/speclib/plan_state.py`, `tests/test_plan_revision_state.py`, existing transition tests. Verify: `uv run pytest -q tests/test_plan_state.py tests/test_plan_revision_state.py -k 'projection or transition or exhausted'`.
- [x] 6.3 Extend execution lint to accept historical commit/evidence bindings only through an unbroken INHERIT/REVALIDATE ledger lineage and to reject bare id/hash matches or drifted active anchors. Requirement: `plan-state-validation / Execution lint verifies external evidence separately from snapshot shape`. Design: decisions 2 and 6. Paths: `nepa/speclib/plan_state.py`, `tests/test_plan_state.py`, `tests/test_plan_revision_state.py`. Verify: `uv run pytest -q tests/test_plan_state.py tests/test_plan_revision_state.py -k 'execution or evidence or inherit or revalidate'`.

## 7. Locked activation and crash recovery

- [x] 7.1 Add a RunStore activation API that, under the existing controller lock, revalidates all expected current/candidate hashes and writes canonical `_s4r/rev_NNN/activation.json` before changing any live artifact. Requirement: `plan-revision-infrastructure / Revision activation has one locked logical commit point`. Design: decision 4. Paths: `nepa/run_store.py`, activation WAL Schema/example if persisted as a public typed artifact, `tests/test_plan_activation.py`, `tests/test_run_store.py`. Verify: `uv run pytest -q tests/test_run_store.py tests/test_plan_activation.py -k 'wal or lock or precondition'`.
- [x] 7.2 Implement the ordered version, State, file-ledger, revision-ledger, pointer and Run-reference writes with atomic replacement; treat pointer advancement as the receipt-bound commit point; reject immutable version overwrite and preserve `output_refs.plan`. Requirement: `plan-revision-infrastructure / Revision activation has one locked logical commit point`. Design: decision 4. Paths: `nepa/run_store.py`, `nepa/stages/s4_planning.py` activation caller, `tests/test_plan_activation.py`. Verify: `uv run pytest -q tests/test_plan_activation.py -k 'activate or order or immutable or receipt'`.
- [x] 7.3 Implement idempotent locked recovery for ledger-old, ledger-new/pointer-old and pointer-new states using WAL hashes, restoring only pre-commit mutable bytes and failing closed on every conflicting or absent binding. Requirement: `plan-revision-infrastructure / Crash recovery converges to one legal revision state`. Design: decision 5. Paths: `nepa/run_store.py`, `tests/test_plan_activation.py`. Verify: `uv run pytest -q tests/test_plan_activation.py -k 'recovery or crash or damage'` with injected failure after every activation write boundary and a second recovery replay.

## 8. S4 publication and end-to-end admission

- [x] 8.1 Update initial S4 publication to require recomputable task metadata, emit the general pointer plus `files` slot rows and an empty general revision ledger, and reject a genesis revision or interim contract. Requirement: `s4-initial-plan-publication / Initial publication artifacts are closed and canonical`. Design: decision 6. Paths: `nepa/stages/s4_planning.py`, publication helpers, `tests/test_s4_publication.py`, `tests/test_s4_contracts.py`. Verify: `uv run pytest -q tests/test_s4_publication.py tests/test_s4_contracts.py tests/test_s4_checkpoint_resume.py`.
- [x] 8.2 Generalize S4 completion/resume verification so sequence zero requires both anchors at 1.0.0 and later sequences require terminal chain/pointer/Plan/State/file-ledger/Run agreement while keeping the immutable initial Plan ref unchanged. Requirement: `s4-initial-plan-publication / The S4 seal carries independent typed anchors`. Design: decision 6. Paths: `nepa/stages/s4_planning.py`, `nepa/run_store.py`, `tests/test_s4_publication.py`, `tests/test_s4_checkpoint_resume.py`, end-to-end revision tests. Verify: `uv run pytest -q tests/test_s4_publication.py tests/test_s4_checkpoint_resume.py tests/test_s4_commitment.py tests/test_plan_activation.py`.

## 9. Independent production approval

- [x] 9.1 Obtain the project owner's recorded approval for the selected design-7.2.0 ArchitecturePlanner bundle, explicitly binding the new lineage, protocol-neutrality result and recomputable 2/3 evidence; only after that human signature, publish the production M1-4c handoff/binding. Requirement: `architecture-prompt-development / Owner approval gates M1-4c handoff`. Design: decision 7 and migration step 5. Paths: new-lineage owner-approval and handoff artifacts governed by the existing prompt-development workflow. Verify: run the existing handoff verifier and `uv run pytest -q tests/test_prompt_development.py tests/test_prompt_recovery_artifacts.py`; this checkbox MUST remain unchecked until the owner has actually signed.

## 10. Final scope and acceptance

- [x] 10.1 Inspect the complete implementation diff, remove unrelated/refactoring/compatibility work, confirm no `project_docs/` file is part of the change implementation scope, confirm F1 trigger logic, M1-4e candidate generation/patching/RG orchestration, S5 quarantine operation, S6 execution loop, S9 metrics and CLI remain unimplemented, then run all change validation: `uv run pytest -q`, `uv run python -m nepa lint spec gold_file/specIR.json`, `uv run python -m nepa lint plan <linked-plan.json> --spec <matching-spec.json> --manifest <matching-manifest.json>` (prepare these three values together through the existing non-MQTT `_linked()` fixture; the schema-only Plan example is checked by `tests/test_schema_examples.py`), `uv run pytest -q tests/test_planning_protocol_neutrality.py tests/test_prompt_development_neutrality.py`, `openspec validate m1-4d-plan-version-and-migration-infrastructure --strict`, `openspec validate --all --strict`, and `git diff --check`. Requirements: all six change spec areas. Design: all decisions, risks and migration steps. Paths: complete implementation diff excluding `project_docs/`; verify the owner gate in 9.1 is genuinely satisfied rather than machine-marked.

## Completion evidence

Owner-authorized V0 handoff and final acceptance: `experiments/m1-4d-v0-calibration/4420296f4dead63a3119c49106e234147f4a69a1f4f4aecda8fa562af7944ab7/acceptance.md`. Full pytest: 465 passed. All tasks completed; OpenSpec archive: `openspec/changes/archive/2026-09-05-m1-4d-plan-version-and-migration-infrastructure/`.
