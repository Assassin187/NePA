## 1. Obtain Design Authority and Freeze the Recovery Entry

- [x] 1.1 Record the explicit responsible-owner decision approving the exact M1-4a2r design deltas: the post-tie recovery entry, R0/R1/optional R2 budget with at most two prompt edits, `p0` as diagnostic, repair-locality/no-regression gate, fresh-lineage boundary, M1-4a3 handoff and D1.0 update. This task is the independent owner gate and SHALL NOT be inferred by an implementation agent. Covers “Recovery activation requires explicit design authority” and Design Decision 1. Expected evidence: the owner-authored approval in the task history plus the recorded pre-edit `project_docs/system_design.md` hash/worktree baseline. Verify that the decision identifies version 3.1.0, the intended sections, the two-edit recovery protocol and minimal-scope constraint.
- [x] 1.2 Apply only the authorized `project_docs/system_design.md` 3.1.0 delta adding M1-4a2r to §6.4.8.2.1–§6.4.8.3, the M1 critical path/work-item table and D1.0/R-12/design-version records without changing ArchitectureDraft, `ARCH_VALIDATE`, M1-4a3 N=20/B1-B4 or production budgets. Covers the activation, recovery-screening and handoff requirements and Design Decisions 1/2/8/11. Expected path: `project_docs/system_design.md`. Verify the exact authorized diff with `git diff -- project_docs/system_design.md`, then run `openspec validate m1-4a2r-architecture-planner-calibration-recovery --strict`.
- [x] 1.3 Implement a fail-closed recovery authorization record and preflight that binds the responsible-owner decision plus approved design path/SHA-256, rejects missing/mismatched authority before root creation, prompt publication or Provider I/O, and invalidates further attempts/handoff after design drift. Covers “Recovery activation requires explicit design authority” and Design Decision 1. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/`, `nepa/schemas/examples/`, `tests/test_prompt_recovery_authorization.py`. Verify with `uv run pytest tests/test_prompt_recovery_authorization.py tests/test_prompt_development_artifacts.py -q`.
- [x] 1.4 Implement and publish a recovery-local predecessor attestation over the immutable M1-4a2 root: accept its stored `protocol.status = initialized`, validate the hash graph and complete V0/V1/V2 records against the Schemas/component bundles frozen in that root, recompute all three tuples as `[0,0,1,0]` and `PROMPT_SELECTION_TIE`, prove `selection.json`/handoff absence, and reject selected, incomplete, mutated or non-tie roots. The attester SHALL NOT invoke the ordinary current-lineage component-equality check, rerun the new validator or modify the old root; add a regression where current recovery components intentionally differ. Covers “A terminal M1-4a2 tie admits only a fresh isolated recovery lineage” and Design Decision 2. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery_lineage.py`; read-only fixtures reference the exact predecessor and its frozen bundles. Verify with `uv run pytest tests/test_prompt_recovery_lineage.py tests/test_prompt_development_selection.py tests/test_calibration_artifacts.py -q`.

## 2. Define Canonical Recovery Evidence and Lineage Isolation

- [x] 2.1 Add closed draft-2020-12 Schemas and protocol-neutral examples for recovery authorization/protocol/provenance, predecessor attestation/inventory, R0/R1/R2 prompt snapshots and revisions, coherent attempt declaration/outcome, repair diff/locality, per-model report, two-model assessment, quality audit, terminal selection/no-selection and M1-4a3 handoff. Ordinary refs SHALL be recovery-root-relative; a distinct closed workspace-source ref SHALL be permitted only in initialization provenance for the allowlisted predecessor, authorization/design and experiment seed/reports, with path confinement and SHA-256. No field may carry a secret value/hash. Covers the fresh-lineage, bounded-development, quality-audit, selection and reproducible-evidence requirements and Design Decisions 2/3/5/6/7/9/10/11. Expected paths: `nepa/schemas/`, `nepa/schemas/examples/`, `nepa/schemas/__init__.py`, `tests/test_schema_examples.py`, `tests/test_prompt_recovery_schema.py`. Verify with `uv run pytest tests/test_schema_examples.py tests/test_prompt_recovery_schema.py -q`.
- [x] 2.2 Extend canonical load/serialize/publication below a distinct `<lineage>/prompt-recovery/` namespace with staged/fsynced atomic commits, byte-identical idempotent replay, conflict rejection, append-only terminal records and no timestamps, key values or key-value hashes in canonical inputs. During initialization, copy verified authorization/design/report/seed bytes into local provenance snapshots and publish the predecessor inventory/attestation; require all later records to use only local refs and reject external trial/aggregate refs. Covers “Recovery evidence is reproducible, secret-free and non-downstream” and Design Decision 10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/calibration/__init__.py`, `tests/test_prompt_recovery_artifacts.py`. Verify with `uv run pytest tests/test_prompt_recovery_artifacts.py tests/test_run_store.py -q`.
- [x] 2.3 Extend lineage construction so the recovery metric contract, repair-impact policy, repair-diff implementation and quality-audit definition produce a genuinely new lineage while prompt bytes remain excluded; prove R0/R1/R2 share that lineage and any Schema/validator/serializer/input/model/request/metric/locality-policy drift creates another lineage. Covers the fresh isolated lineage and bounded-revision controlled-variable requirements and Design Decision 3. Expected paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/calibration-lineage.schema.json`, `tests/test_prompt_recovery_lineage.py`, `tests/test_calibration_lineage.py`. Verify with `uv run pytest tests/test_prompt_recovery_lineage.py tests/test_calibration_lineage.py -q`.
- [x] 2.4 Implement recovery reload/reconciliation that walks only committed hash-valid parents, recomputes state from leaves, returns one deterministic next action, resumes only complete children and rejects missing, substituted, duplicated, cross-lineage, cross-version or cross-attempt evidence without mutating audit roots. Covers the reproducible-evidence and unique-selection requirements and Design Decision 10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery_artifacts.py`, `tests/test_prompt_recovery.py`. Verify with `uv run pytest tests/test_prompt_recovery_artifacts.py tests/test_prompt_recovery.py -q`.

## 3. Make the Single Semantic Repair Local and Recomputable

- [x] 3.1 Implement canonical ArchitectureDraft pre/post comparison that publishes hashes and deterministically sorted changed JSON Pointer paths, comparing schema-identified set-like arrays by stable ids while retaining true semantic additions, removals and edits. Covers “Semantic repair is limited, issue-local and regression-free” and Design Decision 7. Expected paths: `nepa/calibration/s4_architecture.py`, `tests/test_architecture_repair_locality.py`. Verify with `uv run pytest tests/test_architecture_repair_locality.py tests/test_architecture_schema.py -q`.
- [x] 3.2 Define the closed, versioned `repair-impact-v1` issue-to-field/gate-dependency policy for every `arch_01`–`arch_10` issue and test its dependency closure, with explicit cases for `arch_03`, `arch_04`, `arch_09`, `arch_10` and `ARCH_TEST_READINESS_UNCLOSED`; bind the policy bytes/implementation into lineage identity. Covers repair attribution/locality and Design Decisions 3/7. Expected paths: `nepa/calibration/s4_architecture.py`, `tests/test_architecture_repair_locality.py`, `tests/test_architecture_validation.py`. Verify with `uv run pytest tests/test_architecture_repair_locality.py tests/test_architecture_validation.py tests/test_calibration_lineage.py -q`.
- [x] 3.3 Reuse the existing no-history full-ArchitectureDraft repair call with exactly the unchanged frozen inputs, prior Schema-valid candidate and canonical issue list; stop on p0 pass, permit only one semantic repair, rerun complete production `ARCH_VALIDATE`, and issue no retry for invalid, broad or still-failing repairs. Covers the single-repair invocation scenarios and Design Decision 7. Expected paths: `nepa/calibration/s4_architecture.py`, `tests/test_architecture_calibration.py`, `tests/test_architecture_repair_locality.py`, `tests/test_agent_rendering.py`. Verify with `uv run pytest tests/test_architecture_calibration.py tests/test_architecture_repair_locality.py tests/test_agent_rendering.py -q`.
- [x] 3.4 Publish and recompute per-repair issue attribution, admitted impact closure, improved/unchanged/regressed gates and locality verdict; count p1 success only when full validation passes, every changed path is attributable/in-scope and no initially passing gate regresses. Covers repair-locality/no-regression screening and Design Decisions 7/8. Expected paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, `tests/test_architecture_repair_locality.py`, `tests/test_prompt_recovery_reporting.py`. Verify with `uv run pytest tests/test_architecture_repair_locality.py tests/test_prompt_recovery_reporting.py tests/test_calibration_reporting.py -q`.

## 4. Implement the Bounded R0/R1/R2 Recovery Coordinator

- [x] 4.1 Implement recovery initialization that fixes exactly Qwen/DeepSeek, N=5, one semantic repair, `max_tokens=65536`, explicit non-secret provider/model/pricing/context configuration, frozen gold inputs, recovery metric/locality definitions and R0/R1/R2 order before any prompt or trial is admitted. Require only `NEPA_QWEN_API_KEY` and `NEPA_DS_API_KEY` names/presence and never source shell or dotenv files. Covers coherent N=5 and secret-free evidence requirements and Decisions 3/5/6/10. Expected paths: `nepa/calibration/s4_prompt_development.py`, existing configuration loader only for narrow reuse, `tests/test_prompt_recovery_config.py`, `tests/test_prompt_recovery.py`. Verify with `uv run pytest tests/test_prompt_recovery_config.py tests/test_prompt_recovery.py tests/test_calibration_lineage.py -q`.
- [x] 4.2 Implement R0 admission from the exact authorized seed `experiments/m1-4a2-architecture-planner-prompt-optimization/results/phase1/artifacts/prompt-exact-algorithm.md` with required SHA-256 `d5c24f1939f3a767f2cd1d7a116124d4b5ea32552391664052a93f24f1914b85`: first snapshot the verified seed locally, then publish one immutable shared prompt snapshot, require repository source/snapshot/raw-template/Provider render identity, run existing prompt structure and neutrality preflight, and reject wrong seed, target-protocol, model or Provider specialization before Provider I/O. Covers “Recovery uses one shared protocol-neutral exact-algorithm prompt” and Design Decision 4. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/agents/prompts/architecture_planner.md`, `tests/test_prompt_recovery_snapshot.py`, `tests/test_prompt_recovery_neutrality.py`. Verify with `uv run pytest tests/test_prompt_recovery_snapshot.py tests/test_prompt_recovery_neutrality.py tests/test_agent_rendering.py -q`.
- [x] 4.3 Implement the exact `design_blocked -> initialized -> r0_* -> selected_r0 | r1_* -> selected_r1 | r2_* -> selected_r2 | rollback_pending -> no_selection` transition graph, permanently reject skipped/duplicate versions, R3 and all edits/calls after a terminal state, and atomically restore/hash-check the immutable pre-recovery source bytes after any failed staged update and before terminal no-selection. Restoration failure must remain nonterminal and block calls/handoff. Covers bounded recovery ordering/stopping and Design Decisions 1/5/11. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery.py`, `tests/test_prompt_recovery_snapshot.py`. Verify with `uv run pytest tests/test_prompt_recovery.py tests/test_prompt_recovery_snapshot.py -q`.
- [x] 4.4 Implement attempt-qualified execution that declares one Qwen N=5 plus DeepSeek N=5 attempt before calls, keeps isolated no-history/cache-disabled trial roots and stable returned identities, marks both sides audit-only if either batch is infrastructure-invalid, and admits only a fresh whole-attempt rerun without replacement or cross-attempt assembly. Covers “Every recovery version uses one coherent fresh two-model N=5 attempt” and Design Decision 6. Expected paths: `nepa/calibration/s4_prompt_development.py`, narrow reuse in `nepa/calibration/s4_architecture.py`, `tests/test_prompt_recovery_attempts.py`, `tests/test_llm_retry.py`. Verify with `uv run pytest tests/test_prompt_recovery_attempts.py tests/test_architecture_calibration.py tests/test_llm_retry.py -q`.
- [x] 4.5 Implement R1 admission only from a complete failing R0 assessment and R2 admission only from a complete failing R1 assessment. Each admission requires one immutable falsifiable hypothesis with exact evidence refs, unified prompt diff and expected metric/gate effect; the R2 hypothesis must be distinct from R1. Recompute whether each expected defect improved, prove only prompt bytes changed, and terminate a failing version with no admissible next revision—or failing R2—as no-selection without fallback. Covers bounded R1/R2 and no-fallback scenarios and Design Decision 5. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery_revisions.py`, `tests/test_prompt_recovery_selection.py`. Verify with `uv run pytest tests/test_prompt_recovery_revisions.py tests/test_prompt_recovery_selection.py tests/test_calibration_lineage.py -q`.
- [x] 4.6 Expose a narrow internal module runner for `recovery-init`, `recovery-run-version`, `recovery-record-revision` and `recovery-recompute`, requiring explicit config/context/input/root paths and delegating to the tested coordinator; do not add a public `nepa` command or inferred defaults. Covers reproducible execution and Decisions 1/5/6/10. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery.py`, `tests/test_prompt_recovery_config.py`. Verify with `uv run python -m nepa.calibration.s4_prompt_development --help` and `uv run pytest tests/test_prompt_recovery.py tests/test_prompt_recovery_config.py -q`.

## 5. Recompute Screening, Quality Diagnostics and Handoff

- [x] 5.1 Build each version assessment only from one complete coherent attempt and recompute fixed-denominator per-model Schema, p0, p1, every gate/stage, issue co-occurrence, repair gain/regression/locality, calls, tokens, cost, latency, finish reason, truncation, requested parameters and returned identities; prohibit cross-model averaging. Covers recovery reporting and screening diagnostics and Design Decisions 6/8. Expected paths: `nepa/calibration/s4_prompt_development.py`, shared aggregation helpers in `nepa/calibration/s4_architecture.py`, `tests/test_prompt_recovery_reporting.py`, `tests/test_calibration_reporting.py`. Verify with `uv run pytest tests/test_prompt_recovery_reporting.py tests/test_calibration_reporting.py -q`.
- [x] 5.2 Apply the recovery hard gate independently to both models: Schema-after-format-repair 1.00, cumulative p1 1.00, zero truncation/infrastructure invalidity, zero repair regression and complete passing locality evidence; retain p0/repeated first-pass gate failures as diagnostics and prove low p0 alone passes while a failing model cannot be hidden. Covers “Recovery screening prioritizes bounded repair success and retains p0 as diagnostic” and Design Decision 8. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery_reporting.py`, `tests/test_prompt_recovery_selection.py`. Verify with `uv run pytest tests/test_prompt_recovery_reporting.py tests/test_prompt_recovery_selection.py -q`.
- [x] 5.3 Publish a separate canonical quality audit for ownership distribution/concentration, zero-responsibility work packages, consumer-less task-ready contracts, task-contract interface/file shape, repair size and final gate status; keep optional blinded judgments typed/provenanced or explicitly unavailable, and prove no audit value changes `ARCH_VALIDATE`, screening or the prompt. Covers “Architecture-quality audit remains separate from mechanical validation” and Design Decision 9. Expected paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_recovery_quality.py`, `tests/test_architecture_validation.py`. Verify with `uv run pytest tests/test_prompt_recovery_quality.py tests/test_architecture_validation.py -q`.
- [x] 5.4 Publish exactly one immutable selection for the first passing R0/R1/R2 version, require repository prompt/source/snapshot/Provider-render identity, and bind reports/assessment/repair diffs/quality audit plus the local predecessor attestation. On recovery failure, publish explicit terminal no-selection only after verified pre-recovery prompt restoration; never compute the old fallback tuple in this namespace. Covers unique recovery selection and Design Decisions 2/5/11. Expected paths: `nepa/calibration/s4_prompt_development.py`, `nepa/agents/prompts/architecture_planner.md`, `tests/test_prompt_recovery_selection.py`, `tests/test_prompt_recovery_snapshot.py`. Verify with `uv run pytest tests/test_prompt_recovery_selection.py tests/test_prompt_recovery_snapshot.py -q`.
- [x] 5.5 Emit a technical handoff whose only admitted consumer is M1-4a3 and prove it cannot satisfy N=20, p2, B1-B4, production model/repair/call-shape freeze, owner production signature, formal Run/S4 receipt or S5/S6 inputs; preserve quality red flags and the old tie unchanged. Covers recovery handoff and non-downstream boundaries and Design Decision 11. Expected paths: `nepa/calibration/s4_prompt_development.py`, affected handoff loader only where an explicit M1-4a3 admission check already belongs, `tests/test_prompt_recovery_scope.py`, `tests/test_prompt_recovery_selection.py`. Verify with `uv run pytest tests/test_prompt_recovery_scope.py tests/test_prompt_recovery_selection.py tests/test_prompt_development_scope.py -q`.

## 6. Materialize and Validate the Exact-Algorithm Prompt

- [x] 6.1 After task 1.2, update `nepa/agents/prompts/architecture_planner.md` with the protocol-neutral exact construction algorithm for exhaustive requirement ownership, s5/s6 file partitions, contract/module/work-package projection equality, contract-derived DAG, interface-slot closure, test-readiness convergence and issue-local self-check/repair; retain the established five-section contract and exact named inputs. Covers the shared exact-algorithm prompt requirement and Design Decision 4. Expected paths: `nepa/agents/prompts/architecture_planner.md`, `tests/test_agent_rendering.py`, `tests/test_agent_roles.py`, `tests/test_prompt_recovery_neutrality.py`. Verify with `uv run pytest tests/test_agent_rendering.py tests/test_agent_roles.py tests/test_prompt_recovery_neutrality.py -q`.
- [x] 6.2 Drive the existing non-MQTT fixture through R0 source scanning, snapshot binding, rendering, Schema and `ARCH_VALIDATE`, proving all concrete identifiers are input-attributable and no protocol/model/provider branch or changed Agent call shape exists; exercise initial and exact-issue repair contexts. Covers prompt neutrality/snapshot scenarios and Design Decision 4. Expected paths: `tests/fixtures/non_mqtt_application/`, `tests/test_planning_protocol_neutrality.py`, `tests/test_prompt_recovery_neutrality.py`, `tests/test_prompt_recovery_snapshot.py`. Verify with `uv run pytest tests/test_planning_protocol_neutrality.py tests/test_prompt_recovery_neutrality.py tests/test_prompt_recovery_snapshot.py tests/test_architecture_validation.py -q`.

## 7. Execute the Fresh Bounded Calibration Lineage

- [x] 7.1 Before any live call, pass all deterministic focused tests; verify tasks 1.1–1.4 and the approved design hash; set explicit task-local authorization/config/context/input paths; verify `NEPA_QWEN_API_KEY` and `NEPA_DS_API_KEY` are non-empty in the current process without printing values; then initialize one fresh recovery lineage from the exact predecessor attestation and seed, proving no predecessor/experiment trial was imported. Covers recovery activation, fresh lineage and secret-free preflight and Decisions 1/2/3/4/10. Expected evidence: recovery-local authorization/design/seed/report snapshots, `predecessor-attestation.json`, `protocol.json` and no external trial refs below `runs/_calibration/s4-architecture/<new-lineage-id>/prompt-recovery/`. Verify with the focused command in task 8.1 followed by `uv run python -m nepa.calibration.s4_prompt_development recovery-init --authorization "${NEPA_M1_4A2R_AUTHORIZATION}" --design project_docs/system_design.md --config "${NEPA_M1_4A2R_CONFIG}" --context-limits "${NEPA_M1_4A2R_CONTEXT_LIMITS}" --spec gold_file/specIR.json --target gold_file/target.json --test-bundle gold_file/test_bundle.json --predecessor-root "${NEPA_M1_4A2_ROOT}" --experiment-root experiments/m1-4a2-architecture-planner-prompt-optimization --seed experiments/m1-4a2-architecture-planner-prompt-optimization/results/phase1/artifacts/prompt-exact-algorithm.md --seed-sha256 d5c24f1939f3a767f2cd1d7a116124d4b5ea32552391664052a93f24f1914b85`.
- [x] 7.2 Run one actual coherent R0 attempt with fresh Qwen N=5 and DeepSeek N=5 trials, at most one issue-local repair per failing Schema-valid candidate, and no replacement sampling; rerun both full batches under a fresh attempt only after attempt-level infrastructure invalidity, then recompute reports, assessment and quality audit. Covers R0, coherent attempt, repair and assessment requirements and Decisions 4/6/7/8/9. Expected evidence: immutable R0 snapshot, one valid two-model attempt, ten trials and complete repair/quality records below the new recovery root. Verify with `uv run python -m nepa.calibration.s4_prompt_development recovery-run-version --recovery-root "${NEPA_M1_4A2R_ROOT}" --version r0` followed by `uv run python -m nepa.calibration.s4_prompt_development recovery-recompute --recovery-root "${NEPA_M1_4A2R_ROOT}"`.
- [x] 7.3 Follow the evidence-authorized branches in order: if R0 passes, prove R1/R2 commands fail before Provider I/O; otherwise admit R1 only from a closed revision input naming one falsifiable defect and expected gate/metric effect, edit only shared prompt bytes, run one fresh coherent two-model N=5 attempt, and recompute outcomes. If R1 passes, prove R2 commands fail; otherwise admit R2 only from a second closed revision input naming a distinct falsifiable defect supported by complete R1 evidence, then run and recompute one final fresh coherent N=5 attempt. Covers bounded R1/R2 and first-passing stop requirements and Decision 5. Expected evidence: terminal `selected_r0`, `selected_r1`, `selected_r2`, or the conditionally required revision/snapshot/attempt/assessment chain ending in `no_selection`. Verify each admitted revision by setting `NEPA_M1_4A2R_VERSION` to exactly `r1` or `r2` and running `uv run python -m nepa.calibration.s4_prompt_development recovery-record-revision --recovery-root "${NEPA_M1_4A2R_ROOT}" --version "${NEPA_M1_4A2R_VERSION}" --input "${NEPA_M1_4A2R_REVISION}"`, then `recovery-run-version` for that exact version and final `recovery-recompute`; inadmissible or post-selection calls must reject before Provider I/O.
- [x] 7.4 Require terminal recomputation to publish either one source-matching recovery selection and M1-4a3-only handoff or, after verified restoration to the pre-recovery prompt hash, explicit no-selection; verify all conditionally required live evidence is complete/coherent and confirm the old M1-4a2 lineage/recomputed tie/tasks remain byte-for-byte unchanged. Covers selection, rollback, handoff, reproducibility and predecessor preservation and Decisions 2/5/10/11. Expected evidence: terminal records below `${NEPA_M1_4A2R_ROOT}`, source hash equal to the selected snapshot or pre-recovery snapshot as appropriate, and unchanged predecessor hashes. Verify with `uv run python -m nepa.calibration.s4_prompt_development recovery-recompute --recovery-root "${NEPA_M1_4A2R_ROOT}" --require-complete --require-source-match` plus Schema validation of the terminal record/handoff.

## 8. Acceptance and Scope Audit

- [x] 8.1 Run all focused deterministic regressions for design authorization, predecessor/new-lineage isolation, recovery Schemas/artifacts/reload, prompt snapshots/neutrality, R0/R1/R2 ordering, coherent attempts, repair diff/impact/locality/no-regression, per-model metrics/screening, quality audit, terminal selection/no-selection rollback/handoff, configuration and secret non-disclosure. Covers every added requirement and Design Decisions 1–11. Expected paths: all implementation and focused test paths named in tasks 1.3–6.2. Verify with `uv run pytest tests/test_prompt_recovery_authorization.py tests/test_prompt_recovery_schema.py tests/test_prompt_recovery_artifacts.py tests/test_prompt_recovery_lineage.py tests/test_prompt_recovery_config.py tests/test_prompt_recovery.py tests/test_prompt_recovery_attempts.py tests/test_prompt_recovery_revisions.py tests/test_prompt_recovery_snapshot.py tests/test_prompt_recovery_neutrality.py tests/test_architecture_repair_locality.py tests/test_prompt_recovery_reporting.py tests/test_prompt_recovery_quality.py tests/test_prompt_recovery_selection.py tests/test_prompt_recovery_scope.py tests/test_calibration_lineage.py tests/test_architecture_calibration.py tests/test_calibration_reporting.py tests/test_agent_rendering.py tests/test_agent_roles.py tests/test_agent_protocol_neutrality.py tests/test_planning_protocol_neutrality.py tests/test_architecture_validation.py -q`.
- [x] 8.2 Recompute the complete actual recovery root from authorization/protocol/provenance/prompt/attempt/trial/repair/report/assessment/quality/terminal leaves, require exactly one valid same-attempt two-model assessment per executed version plus one first-passing selection and M1-4a3-only handoff, and prove every conditional live task has its required immutable evidence or a selected terminal state that mechanically forbids it; an explicit `no_selection` remains valid failure evidence but SHALL NOT satisfy this task or the change technical DoD, and no old, experimental, invalid, cross-attempt or fake-provider evidence may satisfy completion. Covers the recovery technical DoD and Design Decisions 2/5/6/7/8/9/10/11. Expected paths: the fresh gitignored calibration root only. Verify with `uv run python -m nepa.calibration.s4_prompt_development recovery-recompute --recovery-root "${NEPA_M1_4A2R_ROOT}" --require-complete --require-source-match`.
- [x] 8.3 Inspect the complete diff and evidence boundary, remove unrelated/speculative changes, and compare the M1-4a2r-specific `project_docs/system_design.md` delta from the recorded task 1.1 baseline byte-for-byte with the owner's approval, without attributing or rewriting older worktree edits. Confirm no `ARCH_VALIDATE`/ArchitectureDraft weakening, N=10 expansion, R3/fallback, fixed B3 staging, model-specific prompt, M1-4a3 N=20/B1-B4/production freeze, public CLI, formal Run/S4/S5/S6 artifact or old-lineage rewrite was added; all allowed design-document edits must be explicitly authorized and M1-4a3 remains the next serial change. Covers the complete change DoD, all requirements and Design Decisions 1–11. Expected paths: the complete M1-4a2r implementation/test/evidence diff, plus only the owner-authorized delta from the recorded design baseline. Verify with `uv run pytest -q`, `openspec validate --all --strict`, and `git diff --check`.
