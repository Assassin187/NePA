## 1. Baseline and implementation brief

- [x] 1.1 Re-read the authoritative M1-8 sections, this change's proposal/specs/design, archived M1-7 handoff and current S5/S6/materialization/RunStore/ledger/Git code; write the §10.8 four-part implementation brief with exact input Schema refs, output contracts, intended interfaces and acceptance commands, without adding decisions. Requirements: all prerequisite and scope clauses. Design: Context, decisions 1-8. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, this change, archived M1-7 and current implementation (read-only except the derived brief). Verify: review the brief against §10.8 and confirm it names no M1-9 group executor, M1-10 trigger/operator or M1-11 activation producer.
- [x] 1.2 Re-run and record the uncommitted M1-7 baseline before implementation: Schema examples, revision/file-ledger, S5, S6 ordinary/F1, metrics, RunStore/Git, ruff and mypy, plus the configured real-sandbox markers. Requirements: proposal prerequisite/preservation clauses. Design: Context/Risks/Migration 1. Paths: current code and test outputs only. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_plan_revision.py tests/test_s5_materialization.py tests/test_s6_execution.py tests/test_s6_lease_execution.py tests/test_metrics_contract.py`, `uv run pytest -q -m 's5_epoch or s6_execution or s6_lease or metric_contract'`, `uv run ruff check .`, and `uv run mypy nepa`; stop and report any pre-existing failure that would require a design-document or architecture-lineage change.

## 2. Epoch and binding artifact contracts

- [x] 2.1 Generalize the Run v4 S5 instance contract and example from E0-only to canonical `E<n>` current-instance values while preserving E0 initialization and existing stage lifecycle fields. Requirements: `s5-multi-epoch-materialization / Each accepted epoch has one immutable publication chain`. Design: decision 1. Paths: `nepa/schemas/run.schema.json`, examples and Run initialization/stage tests. Verify: focused Schema tests accept pending/done E0 and E1+, reject malformed or mismatched epoch ids, and prove new fresh runs still initialize E0 exactly.
- [x] 2.2 Generalize the S5 pending-state contract to bind target/predecessor epochs, active Plan/activation/migration refs, canonical action rows, expected pre/post path facts, result refs, checkpoint fields and output refs without weakening existing E0 validation. Requirements: multi-epoch recovery and admission. Design: decisions 1/2/5/6. Paths: `nepa/schemas/s5-pending-state.schema.json`, examples and artifact tests. Verify: E0 and E1 positives pass; missing predecessor, unsafe/colliding action, partial quarantine/re-adoption and unrelated fields fail.
- [x] 2.3 Extend epoch-receipt and binding-receipt validation/examples for arbitrary canonical epochs, `ready` versus `pending_repair`, conditional smoke refs, sorted pending group ids and version-to-existing-epoch F2 bindings while retaining the no-hash-cycle order. Requirements: immutable publication and F2 binding requirements. Design: decisions 5/7. Paths: epoch/binding Schemas/examples and tests. Verify: ready E0/E1, pending E1 and F2 same-epoch bindings pass; ready-with-groups, pending-with-success claim, mismatched Plan/epoch/version and cyclic refs fail.
- [x] 2.4 Extend closed file-ledger validation/examples for active/quarantined cross-epoch rows and re-adoption transitions without allowing invented verification, duplicate identities or lost realized history. Requirement: `plan-revision-infrastructure / Cross-epoch file-ledger transitions preserve realized evidence`. Design: decisions 2/3. Paths: file-ledger Schema/example, `nepa/speclib/plan_revision.py`, focused ledger tests. Verify: retained, owner-history append, frozen replacement, retirement and re-adoption cases validate; dropped evidence, duplicate active/quarantine path, bad epoch/path and premature verification fail.

## 3. Pure multi-epoch difference planning

- [x] 3.1 Define the immutable epoch context and pure old/new expanded-Blueprint inventory used by both E0 and E1+, validating active Plan/pointer, predecessor receipt/binding/checkpoint, frozen migration rows, State/ledgers and canonical epoch monotonicity before effects. Requirement: `s5-multi-epoch-materialization / E1+ admission consumes an already-activated structural version`. Design: decision 1. Paths: `nepa/speclib/materialization.py`, `nepa/stages/s5_materialization.py`, focused context tests. Verify: valid E0/F3-E1 contexts replay identically and stale pointer, non-F3, future/missing event, mismatched predecessor or unbound Blueprint cases fail with no input mutation.
- [x] 3.2 Implement the pure canonical action planner over old/new Blueprints, rendered candidates, current ledger and explicit migration rows for frozen render/replace, new owned stub, realized preservation and slot-only retirement. Requirement: `s5-multi-epoch-materialization / Multi-epoch materialization changes only the declared difference`. Design: decision 2. Paths: `nepa/speclib/materialization.py`, new focused multi-epoch projection tests. Verify: action order/replay, added/changed/unchanged paths, owner-only metadata changes and exact final active closure pass; implicit rename, overwrite of realized owned bytes, omitted/duplicate row and path collision fail.
- [x] 3.3 Implement realized retirement projection to the exact `_orphan/<epoch>/<old-path>` target and slot-only retirement removal, preserving all historical evidence and excluding only ledger-registered quarantine from active closure. Requirement: retirement/re-adoption and ledger requirements. Design: decision 3. Paths: materialization/ledger helpers and tests. Verify: realized bytes/history survive, slot-only removal invents no row, and unsafe/colliding/orphan-without-ledger cases fail.
- [x] 3.4 Implement explicit `re_adopt` projection from one named quarantine source to one declared active slot/owner, retaining bytes and lineage but leaving changed obligations unverified. Requirement: `s5-multi-epoch-materialization / Retirement and re-adoption preserve historical content`. Design: decision 3. Paths: materialization/ledger helpers and tests. Verify: valid later-epoch restoration passes; missing source, wrong hash/path/owner, duplicate target and fabricated done/verified state fail.
- [x] 3.5 Generalize manifest, contract-map and completed-materialization projections to arbitrary epochs using preserved workspace hashes for retained owned files and newly rendered hashes for changed frozen/new slots. Requirements: difference materialization, cross-epoch ledger and immutable publication. Design: decisions 1/2/5. Paths: `nepa/speclib/materialization.py`, artifact tests. Verify: E0 regression plus E1 ready/pending/re-adopt canonical snapshots and independently corrupted Plan/Blueprint/epoch/file bindings.

## 4. F2 metadata-only binding

- [x] 4.1 Implement a pure F2 compatibility and binding projector that recomputes Blueprint, owner/provider metadata, manifest and contract map for the accepted version while sourcing exact content facts from the unchanged workspace and existing epoch receipt. Requirement: `plan-revision-infrastructure / F2 versions receive metadata-only bindings`. Design: decision 7. Paths: materialization/delivery/revision helpers and focused F2 tests. Verify: owner/provider/task-id metadata changes produce deterministic new projections; structural path/class/build-graph changes, stale content or changed epoch are rejected without mutation.
- [x] 4.2 Implement idempotent F2 binding publication of immutable version manifest/map/receipt followed by atomic current-copy replacement, preserving S5 Run instance, workspace, Git, epoch receipt and event ledger. Requirement: F2 metadata-only binding. Design: decision 7. Paths: `nepa/run_store.py`, revision/binding helpers and publication tests. Verify: snapshot all excluded artifacts and timestamps; first publication changes only allowed binding/current-copy paths, exact replay changes nothing, and conflicting immutable bytes fail closed.
- [x] 4.3 Reconcile the dormant activation consumer contract with the F2 binding ref without enabling trigger, candidate, gate or pointer-advance production in M1-8. Requirements: F2 binding plus proposal scope. Design: decisions 1/7. Paths: `nepa/speclib/plan_revision.py`, dormant activation fixtures/tests only as directly required. Verify: a synthetic already-accepted F2 activation resolves its binding and same epoch; no new CLI/application route can create or activate a revision.

## 5. E1+ admission, application and readiness

- [x] 5.1 Generalize S5 admission and application assembly to select the current `E<n>` context after ordered reconciliation, preserve E0 behavior, and require an accepted F3 activation/migration for E1+. Requirement: E1+ admission. Design: decision 1. Paths: `nepa/stages/s5_materialization.py`, `nepa/application.py` only if registration needs adaptation, controller tests. Verify: E0 fixtures remain stable; valid synthetic E1 reaches planning; stale/unbound/non-F3 inputs fail before workspace effects; no LLM or public CLI option is introduced.
- [x] 5.2 Persist the epoch-keyed pending action plan, verify every path preimage, and apply exactly the declared writes, removals and moves with confined atomic operations while preserving unrelated and predecessor content. Requirements: declared difference and pre-checkpoint recovery. Design: decisions 2/3/5. Paths: `nepa/run_store.py`, S5 controller, Git/file helpers and fault tests. Verify: faults after every planned action can be reversed from recorded facts; a mismatched preimage or unregistered path fails without broad cleanup.
- [x] 5.3 Validate post-application Blueprint/worktree/ledger/manifest/map/declaration/build-graph closure, then reuse the existing sandbox to run every default build and ready-path smoke check. Requirements: difference closure and E1 readiness. Design: decision 4. Paths: materialization validators, `nepa/tools/build.py` only if result handling requires a minimal extension, S5 tests. Verify: ready MQTT/non-MQTT E1 fixtures pass all variants/smoke; missing/extra source, stale contract, warning, sanitizer, early exit and timeout are classified exactly.
- [x] 5.4 Implement pure attribution of failed compile/link evidence to frozen migration-group closures and publishable `pending_repair` facts only when every failure maps uniquely; preserve all failed build refs and run no smoke/Fixer/group execution. Requirement: `s5-multi-epoch-materialization / E1+ readiness distinguishes registered incompatibility from failure`. Design: decision 4. Paths: materialization/S5 helpers and focused readiness tests. Verify: registered one/multi-group failures yield sorted groups; ambiguous, partial, unregistered, template and sandbox/tool failures cannot produce pending repair.

## 6. Epoch publication, events and recovery

- [x] 6.1 Generalize Git checkpoint publication so E1+ creates exactly one ordinary descendant commit with exact Plan/Epoch trailers and never reinitializes Git, while retaining the E0 initialization path. Requirement: immutable epoch publication. Design: decision 5. Paths: `nepa/tools/git_ops.py`, `nepa/run_store.py`, Git tests. Verify: E0 has one init/checkpoint, E1 has one parented checkpoint, exact staged path set/tree/trailers hold, and a second init or unrelated staged path is rejected.
- [x] 6.2 Publish result evidence, epoch-scoped and version-scoped manifest/map copies, epoch receipt, binding receipt, projected ledger and current copies in the designed no-cycle order, then return exact refs for the current Run S5 instance. Requirement: `s5-multi-epoch-materialization / Each accepted epoch has one immutable publication chain`. Design: decision 5. Paths: `nepa/run_store.py`, S5 controller, Schemas and publication tests. Verify: ready and pending-repair E1 refs/hashes are recomputable, E0 remains immutable, and fault hooks cover every write boundary.
- [x] 6.3 Generalize `epoch_materialized` append/validation/deduplication to bind E1+ Run outputs to the latest active F3 revision without advancing revision sequence or pointer. Requirement: `plan-revision-infrastructure / E1+ materialization events bind the accepted active revision`. Design: decisions 1/5. Paths: `nepa/speclib/plan_revision.py`, S5 post-commit/reconcile path and ledger tests. Verify: exact append/replay, non-activation tails and missing-event recovery pass; early, stale, future or conflicting events fail closed.
- [x] 6.4 Implement pre-checkpoint recovery against the immediate predecessor, reversing only pending-record actions and preserving all accepted realized/quarantined/evidence content before deterministic retry. Requirement: multi-epoch recovery. Design: decision 6. Paths: RunStore/S5 recovery and fault tests. Verify: inject interruption after staging and every write/remove/move/build observation; resume never returns to E0 incorrectly, loses old content, duplicates work or alters unrelated paths.
- [x] 6.5 Implement post-checkpoint forward recovery and completed-instance verification for missing evidence/receipt/binding/ledger/current-copy/Run/event suffixes with no rerender, build, smoke or commit replay. Requirement: multi-epoch recovery and replay. Design: decisions 5/6. Paths: RunStore/S5/orchestrator reconciliation and idempotence tests. Verify: inject each post-commit window; resume converges to one accepted chain, while corrupt checkpoint/receipt/current-copy bytes fail as artifact damage.
- [x] 6.6 Update Orchestrator current-instance handling so a newly pending E1+ is processed without reopening a prior done epoch and accepted replay remains byte/timestamp/Git/event stable. Requirements: publication and replay. Design: decisions 1/5/6. Paths: `nepa/orchestrator.py`, S5 controller and runtime-resume tests. Verify: E0 done history remains immutable, E1 pending→done works under one lock, and repeated resume/invocation performs zero external actions.

## 7. Frozen fixtures and M1-8 acceptance cases

- [x] 7.1 Add a deterministic developer fixture generator that extends both existing S5 protocol families from accepted E0 through real M1-6/M1-7-shaped State/ledger/evidence/workspace history and frozen artificial F2/F3 Plan/activation/migration inputs; CI consumes but never silently regenerates them. Requirements: all legal-input and protocol-neutrality clauses. Design: decision 8. Paths: `tests/tools/`, `tests/fixtures/s5/`, provenance README and fixture-validation tests. Verify: generation into two temporary directories is semantically byte-identical, every activation/input ref validates, and edited fixtures are detected.
- [x] 7.2 Add `s5_epoch` end-to-end ready cases for MQTT and non-MQTT E1+ covering added/changed frozen files, new task-owned stubs, retained realized implementation, owner metadata, all builds/smoke, checkpoint/receipts/binding/ledger/event and repeated zero-change replay. Requirements: admission, declared difference, ready publication and replay. Design: decisions 1/2/4-6/8. Paths: S5 fixture and end-to-end tests. Verify: `uv run pytest -q -m s5_epoch -k 'multi_epoch and ready'` plus protocol-neutrality scans.
- [x] 7.3 Add `s5_epoch` retirement/re-adoption and registered pending-repair cases proving no old file is lost, quarantine/worktree closure holds, restored content is not prematurely verified, failed results/groups are exact and no repair execution occurs. Requirements: retirement/re-adoption, pending repair and ledger transitions. Design: decisions 3/4/8. Paths: S5 fixtures/tests. Verify: `uv run pytest -q -m s5_epoch -k 'quarantine or re_adopt or pending_repair'` with byte/history and no-LLM/no-done assertions.
- [x] 7.4 Add negative and fault fixtures for unbound activation/Blueprint, stale predecessor, unregistered or ambiguous build failure, unsafe/colliding paths, realized overwrite, orphan loss and every pre/post-checkpoint/Run/event interruption. Requirements: admission, readiness and recovery failure semantics. Design: decisions 1-6/8. Paths: S5 failure/recovery fixtures and tests. Verify: `uv run pytest -q -m s5_epoch -k 'negative or recovery or failure'` and assert controlled versus internal routing plus one unique legal terminal state.
- [x] 7.5 Add F2 receipt-reconciliation fixtures for owner/provider metadata changes, exact replay, structural rejection and immutable-byte conflict, snapshotting workspace/checkpoint/epoch/S5/event facts. Requirement: F2 metadata-only binding. Design: decisions 7/8. Paths: F2 fixture generator, binding/revision/RunStore tests. Verify: focused tests prove only the new version binding and current metadata copies change and no materialization operation is reachable.

## 8. Scope and final acceptance

- [x] 8.1 Audit requirement-to-code/test closure and remove duplicated E0/E1 paths, speculative compatibility/fallbacks, protocol-specific branches, workspace-derived migration inference, broad cleanup, LLM-based failure attribution and any reachable M1-9 group execution or M1-10/M1-11 producer. Requirements: all scope/preservation clauses. Design: Goals/Non-Goals and decisions 1-8. Paths: complete M1-8 diff. Verify: map every delta requirement to implementation/tests, run neutrality scans, and confirm `project_docs/`, architecture prompts, calibration lineage and prior accepted artifacts are untouched.
- [x] 8.2 Run focused regression and recovery gates: Schema examples; materialization/delivery/revision/RunStore/Git/orchestrator tests; existing S4/S5/S6 ordinary/F1/metrics markers; all M1-8 ready/pending/quarantine/F2/fault fixtures; and the configured real sandbox. Requirements: all M1-8 requirements and prerequisite preservation. Design: Risks/Migration. Paths: affected implementation/tests. Verify: all focused commands in the implementation brief pass with deterministic replay and one accepted result per transaction.
- [x] 8.3 Inspect the complete diff, remove unrelated/refactoring/configurability work, and run the full change acceptance: `uv run pytest -q`, `uv run pytest -q -m s5_epoch`, `uv run ruff check .`, `uv run mypy nepa`, Schema/gold/Plan lint commands from public CI, `openspec validate m1-8-s5-multi-epoch-materialization --strict`, `openspec validate --all --strict`, and `git diff --check`; confirm D1.12 and the M1-8 task-card cases pass, no M1-9+ behavior or public CLI was added, and no later owner approval is claimed. Requirements: all delta capabilities and proposal DoD. Design: all decisions, risks and migration steps. Paths: complete repository/change diff. Verify: every command is green and the final acceptance record identifies the exact baseline, fixtures, recovery windows and remaining later-milestone boundaries.

## 9. Post-review contract remediation

- [x] 9.1 Extend the closed migration contracts with canonical `pending_groups` and `re_adopt` rows, bind them unchanged through accepted F3 activation, reject them for F2, and remove aliases/top-level fallbacks. Requirements: E1+ admission, retirement/re-adoption and pending-repair attribution. Design: decisions 9/12. Paths: revision/migration Schemas, activation helpers, materialization context and Schema tests. Verify: legal rows pass while missing, aliased, unordered, duplicate, wrong-revision, unknown task/path/symbol/build-artifact/owner and F2 structural rows fail before workspace effects.
- [x] 9.2 Revalidate the original S4 immutable Plan/config anchors and the current accepted activation chain during E1 admission without comparing the mutable active pointer to its E0 hash. Requirement: E1+ admission. Design: decisions 1/9. Paths: S5 controller and admission tests. Verify: config/S4 Plan/activation drift fails before pending or workspace mutation; a legal F3 pointer advance remains admissible.

## 10. Strict readiness attribution

- [x] 10.1 Replace stderr substring attribution with a structured deterministic decision over eligible compiler/linker diagnostics and canonical frozen group closures. Requirement: E1+ readiness distinguishes registered incompatibility from failure. Design: decisions 4/12. Paths: materialization helper, S5 controller and readiness tests. Verify: exact one/multi-group failures publish sorted ids; partial, ambiguous, unparsed, timeout, sandbox, cleanup, command and template failures reject without smoke or checkpoint.

## 11. Recovery and accepted-chain verification

- [x] 11.1 Correct pre-checkpoint reversal for every action kind, restoring retire/replace preimages from the immediate predecessor and reversing quarantine/re-adopt moves target-to-source. Requirement: multi-epoch recovery. Design: decisions 6/10. Paths: RunStore recovery and fault tests. Verify: interruption after every action restores the exact predecessor active/quarantine bytes and rejects both-sided, unknown or missing preimages without deleting pending state.
- [x] 11.2 Validate the complete accepted E1+ chain before replay, pending deletion or missing-event append, including binding-named immutable manifest/map files. Requirements: publication, event and replay. Design: decisions 5/6/11. Paths: S5 reconcile/completed validation and corruption tests. Verify: event-only recovery appends once after validation; corruption of each receipt/copy/ref/checkpoint/ledger/workspace/evidence fails with no mutation.

## 12. Frozen end-to-end fixtures

- [x] 12.1 Regenerate and check in complete MQTT/non-MQTT E0→F2/F3 fixture packages and compare generator output byte-for-byte with checked-in assets. Requirements: frozen legal inputs and protocol neutrality. Design: decisions 8/9. Paths: fixture generator, fixture directories and fixture validation tests. Verify: every ref and Schema validates, edited or stale checked-in fixture bytes fail, and CI never regenerates in place.
- [x] 12.2 Drive ready structural E1, retirement E1, re-adopt E2, registered/unregistered/ambiguous failure and F2 metadata cases through accepted activation and the S5/RunStore controller path. Requirements: all M1-8 scenarios. Design: decisions 1-12. Paths: multi-epoch tests and frozen fixtures. Verify: no structural acceptance relies only on direct pure-function injection or an identical Plan with a changed version.

## 13. Remediation self-review and acceptance

- [x] 13.1 Audit requirement→Schema-valid fixture→controller→persisted output→corruption-test closure, search for unreachable migration fields/aliases and unverified nested refs, and confirm target pytest selectors collect nonzero intended cases. Requirements: all M1-8 requirements. Design: decisions 9-12. Paths: complete M1-8 diff and test collection. Verify: record the exact mapping and collection counts; retain any blocked task unchecked.
- [x] 13.2 Run focused Schema/S4/S5/S6/revision/RunStore/Git/orchestrator/fixture tests, all action and publication fault windows, MQTT/non-MQTT real sandbox, `pytest -m s5_epoch`, full pytest, ruff, mypy, public lint, strict current/all OpenSpec validation and `git diff --check`. Requirements: M1-8 DoD and regression preservation. Design: all decisions. Paths: complete repository. Verify: every command is green and `project_docs/`, M1-9+ behavior and unrelated user changes remain untouched.
- [x] 13.3 Replace the superseded acceptance record with exact post-remediation evidence and inspect the final diff for unrelated work. Requirements: truthful completion evidence. Design: migration step 8. Paths: `acceptance-record.md`, `tasks.md` and complete diff. Verify: the record names exact fixture cases, action/publication/corruption windows, command counts and remaining M1-9/M1-10/M1-11 boundaries before marking this task complete.
