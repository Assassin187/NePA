## 1. Configuration and Public LLM Contracts

- [x] 1.1 Add the design-required `httpx` dependency and the closed `pricing.models.<provider>/<model>` input/output-per-million-token configuration shape with an empty default table; cover snapshot persistence, non-negative rates, unknown fields, and missing-price preflight without adding guessed production rates. **Requirements:** Price-based usage and M1-1 budget integration; Configured provider routing and credential isolation. **Design:** §8.1, §8.3, §8.4 item 6; `design.md` Decisions 3 and 10. **Paths:** `pyproject.toml`, `uv.lock`, `configs/default.yaml`, `nepa/config.py`, `tests/test_config.py`, `tests/test_llm_pricing.py`. **Verify:** `uv run pytest tests/test_config.py tests/test_llm_pricing.py -q -k 'pricing or snapshot or missing_price'`.
- [x] 1.2 Create the `nepa.llm` package with Pydantic request/response/call-context/probe records, parameter and validation enums, the provider protocol, and the typed request/configuration/transport/provider/decoding/structured/storage error hierarchy; reject invalid requests and targets before I/O while keeping stage/termination codes absent. **Requirements:** Typed provider-neutral completion contract; LLM failures remain subordinate to stage control. **Design:** §8.4 unified interface; `design.md` Decisions 1 and 10 and Interfaces and Ownership. **Paths:** `nepa/llm/__init__.py`, `nepa/llm/client.py`, `tests/test_llm_client.py`. **Verify:** `uv run pytest tests/test_llm_client.py -q -k 'model or invalid or error_boundary'`.

## 2. Provider Adapters and Transport Policy

- [x] 2.1 Implement the single-attempt OpenAI-compatible adapter: resolve only its configured environment key at call time, build the selected model/messages/parameters, join the configured base prefix to `chat/completions`, normalize successful content/model/token usage, keep unsupported parameter states `unknown`, and return sanitized typed failures. **Requirements:** Configured provider routing and credential isolation; Typed provider-neutral completion contract; Honest parameter capability evidence. **Design:** §8.3, §8.4 items 1 and 4; `design.md` Decisions 1, 2, and 8. **Paths:** `nepa/llm/providers/__init__.py`, `nepa/llm/providers/openai_compat.py`, `tests/test_llm_providers.py`. **Verify:** `uv run pytest tests/test_llm_providers.py -q -k 'openai_compat or qwen or deepseek or secret'`.
- [x] 2.2 Implement the single-attempt Anthropic adapter for the configured chat-completions gateway, proving with mock transport that it uses `ProviderConfig.base_url` as the exact final URL, never appends or substitutes a path, resolves `NEPA_CLAUDE_API_KEY` only at call time, normalizes the response, and redacts credentials from all results/errors. **Requirements:** Exact Anthropic request destination; Configured provider routing and credential isolation. **Design:** §8.3, §8.4 item 1, §10.2 M1-2; `design.md` Decision 2 and stated gateway assumption. **Paths:** `nepa/llm/providers/anthropic.py`, `tests/test_llm_providers.py`. **Verify:** `uv run pytest tests/test_llm_providers.py -q -k 'anthropic and (exact_url or alternate_url or no_messages or secret or normalize)'`.
- [x] 2.3 Add the client-owned transport loop with injected sleeper/backoff, external-call admission immediately before every attempt, and exactly the initial request plus at most three retries for network/timeout, 429, and 5xx; make other 4xx and malformed successful responses fail without retry and record attempt metadata without touching stage repair counters. **Requirements:** Bounded transport retry; LLM failures remain subordinate to stage control. **Design:** §8.4 item 3, §4.7; `design.md` Decisions 4, 5, and 10. **Paths:** `nepa/llm/client.py`, provider modules as narrowly required, `tests/test_llm_retry.py`. **Verify:** `uv run pytest tests/test_llm_retry.py -q`.

## 3. Structured Output and Immediate Usage Accounting

- [x] 3.1 Implement deterministic price calculation from provider token counts, require the exact canonical provider/model price before credential lookup, and connect each successfully decoded provider response to `Orchestrator.record_external_usage` before parsing, repair, caching, or return; prove no `StageResult.usage` double-charge path is introduced. **Requirements:** Price-based usage and M1-1 budget integration; Typed provider-neutral completion contract. **Design:** §4.7, §8.4 item 6; `design.md` Decisions 3 and 4. **Paths:** `nepa/llm/telemetry.py`, `nepa/llm/client.py`, reused `nepa/orchestrator.py`, `tests/test_llm_pricing.py`, `tests/test_llm_budget.py`. **Verify:** `uv run pytest tests/test_llm_pricing.py tests/test_llm_budget.py -q -k 'cost or immediate or no_double_charge or missing_price'`.
- [x] 3.2 Implement the explicit-native-capability branch and the provider-neutral fallback that embeds canonical Schema instructions, extracts the first complete JSON value, validates through `jsonschema`, and returns a normalized sorted error list; cover prose, nested JSON, arrays, malformed text, and Schema violations. **Requirements:** Provider-independent structured output. **Design:** §8.4 item 2; `design.md` Decision 6. **Paths:** `nepa/llm/client.py`, `tests/test_llm_structured.py`. **Verify:** `uv run pytest tests/test_llm_structured.py -q -k 'native or fallback or first_json or validation_errors'`.
- [x] 3.3 Add exactly one schema-repair request using the invalid output and deterministic error list, aggregate usage/model metadata from the initial and repair responses, and raise `StructuredOutputError` after a second invalid result without changing Run stage/termination state. **Requirements:** Provider-independent structured output; Price-based usage and M1-1 budget integration; LLM failures remain subordinate to stage control. **Design:** §8.4 item 2, §4.7; `design.md` Decisions 4-6 and 10. **Paths:** `nepa/llm/client.py`, `tests/test_llm_structured.py`, `tests/test_llm_budget.py`. **Verify:** `uv run pytest tests/test_llm_structured.py tests/test_llm_budget.py -q -k 'repair or aggregate or structured_failure or stage_unchanged'`.
- [x] 3.4 Handle `BudgetExhausted` raised after a completed provider response by retaining and later publishing that response's evidence, suppressing repair and further HTTP attempts, and re-raising the original budget exception after usage is durable. **Requirements:** Price-based usage and M1-1 budget integration; Durable LLM call evidence. **Design:** §4.7, §5.5; `design.md` Decisions 4 and 9 and Persistent and Failure Sequence. **Paths:** `nepa/llm/client.py`, `nepa/llm/telemetry.py`, `tests/test_llm_budget.py`, `tests/test_llm_telemetry.py`. **Verify:** `uv run pytest tests/test_llm_budget.py tests/test_llm_telemetry.py -q -k 'budget_exhausted_after_response or no_repair or evidence'`.

## 4. Durable Cache

- [x] 4.1 Add the smallest public `RunStore` read path needed to load and optionally hash-verify a confined immutable artifact; test missing files, hash drift, path escape, and unchanged M1-1 publication semantics instead of using `_confined` from the LLM layer. **Requirements:** Deterministic response cache. **Design:** §4.8, §8.4 item 5; `design.md` Decision 7 and Interfaces and Ownership. **Paths:** `nepa/run_store.py`, `tests/test_run_store.py`. **Verify:** `uv run pytest tests/test_run_store.py -q -k 'read_verified or hash or path'`.
- [x] 4.2 Implement canonical cache keys over provider name/kind, model, temperature, max tokens, optional Schema, and exact system/user text, plus immutable successful-response entries under `cache/llm/`; prove every provider-affecting input changes the key, identical replay is idempotent, conflict fails closed, and secrets cannot enter the key/value. **Requirements:** Deterministic response cache; Configured provider routing and credential isolation. **Design:** §8.4 item 5; `design.md` Decision 7. **Paths:** `nepa/llm/cache.py`, `tests/test_llm_cache.py`. **Verify:** `uv run pytest tests/test_llm_cache.py -q -k 'key or immutable or conflict or secret'`.
- [x] 4.3 Integrate cache lookup/publication with logical completion so only final successful responses are cached, a hit makes no provider request or budget charge and returns zero incremental cost with `cached: true`, parameter/output metadata is preserved, failures create no entry, and probes bypass reads and writes. **Requirements:** Deterministic response cache; Price-based usage and M1-1 budget integration; Cache-disabled capability probe record. **Design:** §8.4 items 4-6; `design.md` Decisions 4, 7, and 8. **Paths:** `nepa/llm/client.py`, `nepa/llm/cache.py`, `tests/test_llm_cache.py`, `tests/test_llm_budget.py`. **Verify:** `uv run pytest tests/test_llm_cache.py tests/test_llm_budget.py -q -k 'hit or failed or zero or bypass'`.

## 5. Capability Evidence and Probe

- [x] 5.1 Normalize requested parameter states so current adapters default to `unknown` and only explicit provider-owned metadata can produce `reported_applied`/`reported_ignored` with `provider_report`; add fake-provider tests proving HTTP acceptance, output stability/difference, and model-name assumptions never upgrade the state. **Requirements:** Honest parameter capability evidence. **Design:** §8.4 item 4, §5.5; `design.md` Decision 8. **Paths:** `nepa/llm/client.py`, provider modules as narrowly required, `tests/test_llm_probe.py`. **Verify:** `uv run pytest tests/test_llm_probe.py -q -k 'unknown or provider_report or no_inference'`.
- [x] 5.2 Implement the minimal unstructured cache-disabled probe and its durable typed record for requested value, acceptance, returned model, tokens/cost/latency, sanitized error, state, and evidence kind; cover accepted-only, explicit provider report, failed request, and unchanged M1-1 usage accounting. **Requirements:** Cache-disabled capability probe record; Price-based usage and M1-1 budget integration. **Design:** §8.4 item 4; `design.md` Decisions 4 and 8. **Paths:** `nepa/llm/client.py`, `nepa/llm/telemetry.py`, `tests/test_llm_probe.py`. **Verify:** `uv run pytest tests/test_llm_probe.py -q`.

## 6. Trace and Crash Evidence

- [x] 6.1 Extend `RunStore` with a canonical append method dedicated to `trace/llm_calls.ndjson` and the minimal numeric artifact-sequence inspection needed by telemetry; retain `RunStore` as sole mutator, validate path confinement, and prove existing numeric orphan files force a new id rather than conflicting reuse. **Requirements:** Durable LLM call evidence. **Design:** §5.5; `design.md` Decision 9 and Interfaces and Ownership. **Paths:** `nepa/run_store.py`, `tests/test_run_store.py`, `tests/test_llm_telemetry.py`. **Verify:** `uv run pytest tests/test_run_store.py tests/test_llm_telemetry.py -q -k 'llm_trace or sequence or orphan or canonical'`.
- [x] 6.2 Implement data-first telemetry publication: immutable complete prompt/output transcripts, exact prompt hash, verified paths, and one validated canonical trace row per logical completion with required §5.5 fields, cache/attempt counts, and preserved documented optional caller fields; cover normal, cache-hit, and provider-failure records with secret-redaction assertions. **Requirements:** Durable LLM call evidence; Configured provider routing and credential isolation. **Design:** §5.5, §8.4 item 6; `nepa/llm/telemetry.py`, `nepa/llm/client.py`, `tests/test_llm_telemetry.py`. **Verify:** `uv run pytest tests/test_llm_telemetry.py -q -k 'success or cache_hit or provider_failure or fields or secret'`.
- [x] 6.3 Add repaired/final-invalid transcript coverage and fault injection before and after prompt publication, output publication, and NDJSON append; prove both provider prompts/outputs and aggregate usage survive, trace validation is `repaired`/`fail`, and no committed row references missing evidence or a mismatched prompt hash. **Requirements:** Durable LLM call evidence; Provider-independent structured output. **Design:** §5.5; `design.md` Decision 9 and Persistent and Failure Sequence. **Paths:** `nepa/llm/telemetry.py`, narrowly required hooks in `nepa/run_store.py`, `tests/test_llm_telemetry.py`, `tests/test_llm_structured.py`. **Verify:** `uv run pytest tests/test_llm_telemetry.py tests/test_llm_structured.py -q -k 'repaired or final_invalid or crash or missing or prompt_hash'`.

## 7. Change-Level Acceptance and Scope Audit

- [x] 7.1 Run the complete focused M1-2 suite, full repository suite, all three existing M0 lint commands, and the signed gold-input SHA-256 comparison without making a live provider request or rewriting gold/freeze assets. **Requirements:** All `llm-provider-runtime` requirements. **Design:** §8.1-§8.4, §10.2 M1-2, §10.8; `design.md` Validation Strategy. **Paths:** all M1-2 implementation/tests plus `gold_file/*` and `configs/m0-default-inputs.freeze.yaml` as read-only evidence. **Verify:** `uv run pytest tests/test_llm_client.py tests/test_llm_providers.py tests/test_llm_retry.py tests/test_llm_pricing.py tests/test_llm_structured.py tests/test_llm_cache.py tests/test_llm_budget.py tests/test_llm_probe.py tests/test_llm_telemetry.py -q && uv run pytest tests -q && uv run nepa lint spec gold_file/specIR.json --gold --manifest gold_file/test_bundle.json && uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json && uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json` plus a scripted SHA-256 comparison with `configs/m0-default-inputs.freeze.yaml`.
- [x] 7.2 Inspect the complete diff and remove unrelated refactoring, speculative provider compatibility, guessed prices, live-call tests, duplicate filesystem/budget paths, and any M1-3+ role/prompt/stage behavior; confirm `project_docs/system_design.md`, gold inputs, freeze records, and M1-1 lifecycle semantics are unchanged, then run `git diff --check`, `openspec validate m1-2-llm-layer --strict`, and the entire task 7.1 verification set. Confirm no independent responsible-owner signature is required for M1-2 and do not claim M1 or D1.0-D1.11 completion. **Requirements:** All `llm-provider-runtime` requirements and scope boundaries. **Design:** §10.2 M1-2/M1-3 and D1 gates; `proposal.md` Out of scope; `design.md` Goals / Non-Goals and Risks / Trade-offs. **Paths:** repository-wide diff inspection limited to the paths declared by this change. **Verify:** `git diff --check && openspec validate m1-2-llm-layer --strict` followed by all task 7.1 commands.
