## 1. Structured Contract Foundations
- [x] 1.1 Replace the Draft 7 checks in `nepa/llm/client.py` and `nepa/agents/base.py` with JSON Schema draft 2020-12 metaschema/value validation, preserving current typed errors, pre-I/O ordering, one format repair, cache identity, and telemetry behavior. Covers `llm-provider-runtime` “Provider-independent structured output,” `agent-invocation-runtime` “Agent roles and output contracts are explicit,” and Design §7. Extend `tests/test_llm_structured.py`, `tests/test_llm_client.py`, and `tests/test_agent_rendering.py`; verify with `uv run pytest tests/test_llm_structured.py tests/test_llm_client.py tests/test_agent_rendering.py -q`.
- [x] 1.2 Add closed draft-2020-12 `nepa/schemas/architecture-draft.schema.json` and a generic conforming `nepa/schemas/examples/architecture-draft.example.json` with the decisions/assumptions/contracts/modules/work-packages shape and no task/hash/coverage/state fields. Covers “ArchitectureDraft is a closed production contract” and Design §5. Add positive, forbidden-field, missing-field, and dialect cases to `tests/test_schema_examples.py` and a focused `tests/test_architecture_schema.py`; verify with `uv run pytest tests/test_schema_examples.py tests/test_architecture_schema.py -q`.
- [x] 1.3 Add the minimum closed Schemas/examples for `architecture-validation`, `calibration-lineage`, `calibration-batch`, committed trial request/response indexes, trial validation, and `calibration-report` under `nepa/schemas/` and `nepa/schemas/examples/`. Covers “ARCH_VALIDATE is the single production S4-G2 validator,” “Lineage identity freezes every non-prompt comparison variable,” “Trial artifacts and calibration reports are hash-bound and recomputable,” and Design §§6, 10, 12. Extend `tests/test_schema_examples.py`; verify with `uv run pytest tests/test_schema_examples.py -q`.

## 2. Frozen Inputs, Planning Index, and Delivery Constraints

- [x] 2.1 Implement `PreparedArchitectureInputs`, `prepare_architecture_inputs(...)`, and `build_test_manifest_metadata(...)` in `nepa/speclib/planning.py`, reusing existing Spec/Target/Test Bundle lints, preserving Spec bytes, canonicalizing only the run-style Target copy, requiring canonical Test Bundle bytes, resolving default build variants, and never checking nodeid files. Covers “Calibration planning inputs are frozen with production S4 semantics” and Design §2. Add valid/invalid/hash/nonexistent-test cases in `tests/test_planning_inputs.py`; verify with `uv run pytest tests/test_planning_inputs.py tests/test_test_bundle_lint.py tests/test_target_lint.py -q`.
- [x] 2.2 Implement the closed deterministic `build_planning_index(...)` projection in `nepa/speclib/planning.py`, including all structural dependencies, requirements, reference graph, target, test metadata, and build variants while removing every source quote. Covers “Planning index and architecture Delivery Constraints are deterministic shared artifacts” and Design §3. Add canonical-repeat, ordering, complete-requirement, source-quote exclusion, and unknown-reference cases in `tests/test_planning_index.py`; verify with `uv run pytest tests/test_planning_index.py -q`.
- [x] 2.3 Implement the pure `compile_delivery_constraints(spec, target_profile)` entry point in `nepa/speclib/delivery.py` for target support, naming collision/empty checks, six patterns, resource limits, build variants, safe `none/per_message` file-slot expansion, and required internal-interface constraints, without building a Blueprint. Covers “Planning index and architecture Delivery Constraints are deterministic shared artifacts” and Design §4. Add default gold, unsupported target, path/identifier failure, repeatability, and generic fixture cases in `tests/test_delivery_constraints.py`; verify with `uv run pytest tests/test_delivery_constraints.py tests/test_target_lint.py -q`.
- [x] 2.4 Implement the deterministic ArchitecturePlanner context preflight in `nepa/speclib/planning.py` using rendered UTF-8 byte upper bounds, explicit per-model context-window limits, requested output reserve, and the configured safety margin; fail with `PLAN_CONTEXT_TOO_LARGE` before any client call and never truncate the index. Covers the oversize scenario in “Planning index and architecture Delivery Constraints are deterministic shared artifacts” and Design §9. Add boundary/over-bound/missing-limit cases in `tests/test_planning_index.py`; verify with `uv run pytest tests/test_planning_index.py -q`.

## 3. Production ARCH_VALIDATE

- [x] 3.1 Implement ArchitectureDraft Schema loading, canonical serialization, parent-ref verification, stable issue construction/sorting, and the closed validation-result envelope in `nepa/speclib/architecture.py`. Covers “ArchitectureDraft is a closed production contract,” “ARCH_VALIDATE is the single production S4-G2 validator,” and Design §§5-6. Add deterministic serialization/ref/error-order tests in `tests/test_architecture_validation.py`; verify with `uv run pytest tests/test_architecture_schema.py tests/test_architecture_validation.py -q`.
- [x] 3.2 Implement `arch_01`-`arch_05` in `nepa/speclib/architecture.py`: id/reference integrity, module boundaries, contract readiness/owner/interface conditions, provider/consumer validity, and exact module contract projections. Covers the first five gates of “ARCH_VALIDATE is the single production S4-G2 validator” and Design §6. Add one positive and focused negative case per gate in `tests/test_architecture_validation.py`; verify with `uv run pytest tests/test_architecture_validation.py -q`.
- [x] 3.3 Implement `arch_06`-`arch_09` in `nepa/speclib/architecture.py`: work-package membership/file partition, contract refinement/unique provider package, exact contract-derived dependencies/DAG, complete file-slot partition, and required-interface closure against Delivery Constraints. Covers gates six through nine of “ARCH_VALIDATE is the single production S4-G2 validator” and Design §6. Add partition, free-edge, cycle, S5 ownership, contract-set, and interface-slot failures in `tests/test_architecture_validation.py`; verify with `uv run pytest tests/test_architecture_validation.py -q`.
- [x] 3.4 Implement `arch_10` in `nepa/speclib/architecture.py`: exclude only `DEFINITION` requirements from primary ownership, enforce one primary plus valid supporting assignments, compute stable work-package ancestor/descendant closure, and return `ARCH_TEST_READINESS_UNCLOSED` for a task-gated test with no convergence point. Covers gate ten of “ARCH_VALIDATE is the single production S4-G2 validator” and Design §6. Add primary/supporting/readiness positives and negatives in `tests/test_architecture_validation.py`; verify with `uv run pytest tests/test_architecture_validation.py -q`.
- [x] 3.5 Close the validator end to end with one generic passing ArchitectureDraft and multi-defect fixtures proving all evaluable gates run without short-circuit, issue bytes are stable, and repeated validation is identical. Covers all scenarios in “ARCH_VALIDATE is the single production S4-G2 validator” and Design §6. Complete `tests/test_architecture_validation.py`; verify with `uv run pytest tests/test_architecture_validation.py -q`.

## 4. ArchitecturePlanner Contract Binding

- [x] 4.1 Update only the ArchitecturePlanner registration and `nepa/agents/prompts/architecture_planner.md` Inputs section to require/delimit `repair_context`; initial rendering uses canonical `null`, and repair rendering contains only `previous_candidate` plus the canonical validator issue list. Keep the five-section structure and all other role inputs unchanged. Covers `agent-invocation-runtime` “M1-3 provides the required built-in role skeletons” and Design §8. Update `tests/test_agent_roles.py`, `tests/test_agent_rendering.py`, and `tests/test_agent_protocol_neutrality.py`; verify with `uv run pytest tests/test_agent_roles.py tests/test_agent_rendering.py tests/test_agent_protocol_neutrality.py -q`.
- [x] 4.2 Add an ArchitecturePlanner contract-binding helper in `nepa/calibration/s4_architecture.py` that loads the production Schema/example and delegates each initial or semantic-repair call exactly once to the existing `AgentInvoker`, with no Agent-layer retry, model fallback, stage mutation, or prompt optimization. Covers “ArchitectureDraft is a closed production contract,” “Semantic repair is explicit, fresh, and bounded by the declared protocol,” and Design §§7-8. Add spy/fake-client cases to `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py tests/test_agent_invoker.py -q`.

## 5. Lineage and Calibration Persistence

- [x] 5.1 Implement `build_lineage_manifest(...)` in `nepa/calibration/s4_architecture.py`: hash frozen inputs/artifacts, the Schema/example, serializer, planning/Delivery/validator/Agent-framework/statistics component bytes, referenced provider/pricing plus three model request configs, and context-window declarations; exclude only prompt label/hash from the lineage projection. Covers “Lineage identity freezes every non-prompt comparison variable” and Design §2/§9. Add prompt-only-stability and every-component-invalidation cases in `tests/test_calibration_lineage.py`; verify with `uv run pytest tests/test_calibration_lineage.py -q`.
- [x] 5.2 Publish and reload immutable `lineage.json`, frozen input copies, Test Manifest metadata, planning index, and Delivery Constraints below `runs/_calibration/s4-architecture/<lineage_id>/` using only the existing confined atomic/immutable artifact primitives and no formal Run APIs. Covers “Calibration planning inputs are frozen with production S4 semantics,” “Lineage identity freezes every non-prompt comparison variable,” “Calibration evidence is not a formal run or a downstream planning result,” and Design §§2/10/14. Add identical replay, conflicting replay, hash drift, path confinement, and no-`run.json` cases in `tests/test_calibration_lineage.py`; verify with `uv run pytest tests/test_calibration_lineage.py tests/test_run_store.py -q`.
- [x] 5.3 Implement immutable `batch.json` publication and staged/fsynced/atomic `trial_NNN` directory commits containing `request_ref.json`, `response_ref.json`, and `validation.json`; on restart verify and reuse only complete committed trials while retaining but ignoring orphan trace evidence. Covers “Trial artifacts and calibration reports are hash-bound and recomputable” and Design §10. Add crash hooks before/after trial rename, ref mismatch, replay, and orphan-evidence cases in `tests/test_calibration_artifacts.py`; verify with `uv run pytest tests/test_calibration_artifacts.py -q`.

## 6. Isolated Three-Model Trial Driver

- [x] 6.1 Add the closed calibration batch declaration/model-target types and provider-factory injection in `nepa/calibration/s4_architecture.py`; require exactly `claude`, `qwen`, and `deepseek`, derive a separately validated config with only the ArchitecturePlanner route overridden per target, and build a distinct artifact store/telemetry/client/invoker per model. Covers “Three-model trial execution is isolated and cache-independent” and Design §9. Add missing/extra model, original-config immutability, target/price, distinct-object/root, and fake-provider construction cases in `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py -q`.
- [x] 6.2 Implement exactly three top-level model workers with serial ascending trials per worker, identical lineage/input/schema/validator/raw-template hashes, calibration-qualified trace identity, and `use_cache=False` on every Agent call. Covers “Three-model trial execution is isolated and cache-independent” and Design §11. Add barrier-based overlap tests plus assertions that no cache hit, trace path, call sequence, session, response, or candidate crosses model/trial roots in `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py tests/test_llm_cache.py tests/test_llm_telemetry.py -q`.
- [x] 6.3 Implement the trial loop in `nepa/calibration/s4_architecture.py`: validate the first Schema-legal candidate immediately, stop on pass, otherwise issue at most the declared zero/one/two fresh semantic repairs with unchanged base inputs and exact validator failures; keep M1-2 format repair distinct in trace/index/validation evidence. Covers “Semantic repair is explicit, fresh, and bounded by the declared protocol” and Design §§8/11/12. Add p0, one-repair, two-repair, format-repair, Schema-double-failure, and no-hidden-history cases in `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py tests/test_llm_structured.py -q`.
- [x] 6.4 Classify transport exhaustion without any model response as `infrastructure-invalid`, prohibit replacement sampling or report completion, and require a fresh attempt-qualified batch root for a whole three-model rerun while preserving logical prompt version/hash. Covers the infrastructure failure scenario in “Three-model trial execution is isolated and cache-independent” and Design §§10-12. Add one-model failure, no-cherry-pick, immutable invalid evidence, and rerun-separation cases in `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py tests/test_llm_retry.py -q`.

## 7. Recomputable Calibration Reporting

- [x] 7.1 Implement trial-evidence reload/revalidation in `nepa/calibration/s4_architecture.py`, including every request/response/trace/candidate/validator parent ref and recorded Schema/semantic depth, without consulting model prose or external state. Covers “Trial artifacts and calibration reports are hash-bound and recomputable” and Design §12. Add missing/mutated/duplicate/cross-lineage/cross-prompt/cross-model evidence cases in `tests/test_calibration_reporting.py`; verify with `uv run pytest tests/test_calibration_reporting.py -q`.
- [x] 7.2 Implement `recompute_calibration_report(model_root)` with fixed-N Schema/raw/semantic/p0/p1/p2 rates, unconditional `arch_01`-`arch_10` k/N, failure co-occurrence, repair gains, call/token/cost/latency, finish/truncation, model-version, and parameter-support aggregates; emit a null reason for undeclared semantic depth. Covers “Calibration metrics preserve fixed denominators and failure evidence” and Design §12. Add hand-calculated N fixtures for raw pass, format repair, semantic repair, double Schema failure, and undeclared p2 in `tests/test_calibration_reporting.py`; verify with `uv run pytest tests/test_calibration_reporting.py -q`.
- [x] 7.3 Publish `calibration_report.json` only for a complete non-infrastructure-invalid batch and prove repeated recomputation is byte-identical; reject an incomplete batch or any drift without overwriting existing evidence. Covers “Trial artifacts and calibration reports are hash-bound and recomputable” and “Calibration metrics preserve fixed denominators and failure evidence,” Design §§10/12. Complete `tests/test_calibration_reporting.py`; verify with `uv run pytest tests/test_calibration_reporting.py tests/test_calibration_artifacts.py -q`.

## 8. Protocol Neutrality and Scope Gates

- [x] 8.1 Extend deterministic source scans to the planning/calibration Python, all new Schemas/examples, and ArchitecturePlanner template; forbid embedded target-protocol constants/identity branches everywhere and model/provider branches in the shared prompt while allowing configured model bindings outside prompt source. Covers “Planning and calibration assets remain protocol neutral” and Design §13. Add/extend `tests/test_planning_protocol_neutrality.py` and `tests/test_agent_protocol_neutrality.py`; verify with `uv run pytest tests/test_planning_protocol_neutrality.py tests/test_agent_protocol_neutrality.py -q`.
- [x] 8.2 Add one non-MQTT application-layer server/C99 fixture under `tests/fixtures/` and drive it through input preparation, planning index, Delivery Constraints, a passing ArchitectureDraft/all ten gates, lineage, and prompt rendering; assert every concrete derived identifier/path is attributable to fixture input or documented language/role rules. Covers the fixture scenario in “Planning and calibration assets remain protocol neutral” and Design §13. Verify with `uv run pytest tests/test_planning_protocol_neutrality.py tests/test_delivery_constraints.py tests/test_architecture_validation.py -q`.
- [x] 8.3 Add scope assertions proving a completed calibration batch creates no `run.json`, S4 receipt, Plan, Blueprint, Report v2, eval-run input, TaskPlanner/PlanCritic/FlatPlanBaseline call, or downstream-consumable artifact. Covers “Calibration evidence is not a formal run or a downstream planning result,” proposal non-goals, and Design §14. Add cases to `tests/test_architecture_calibration.py`; verify with `uv run pytest tests/test_architecture_calibration.py tests/test_orchestrator.py tests/test_s9_partial_report.py -q`.
- [x] 8.4 Record M1-4a1 technical acceptance without creating or marking any responsible-owner approval: D1.0, production model/prompt/Schema/validator/call-shape/budget freeze, and owner signature remain explicitly unsatisfied until M1-4a3. Covers the proposal manual-gate boundary and Design Goals/Non-Goals. Verify by inspecting only this change's artifacts and confirming no approval/signature/freeze record was added with `git diff --check && git status --short`.

## 9. Acceptance and Scope Audit

- [x] 9.1 Run all focused M1-4a1 and affected M1-1/M1-2/M1-3 regressions, including Schema examples, input preparation, constraints, all architecture gates, Agent contract/rendering, lineage/artifact commits, three-model isolation, semantic repair, report recomputation, and neutrality. Covers every delta requirement and Design §§1-14. Verify with `uv run pytest tests/test_schema_examples.py tests/test_planning_inputs.py tests/test_planning_index.py tests/test_delivery_constraints.py tests/test_architecture_schema.py tests/test_architecture_validation.py tests/test_agent_roles.py tests/test_agent_rendering.py tests/test_agent_invoker.py tests/test_architecture_calibration.py tests/test_calibration_lineage.py tests/test_calibration_artifacts.py tests/test_calibration_reporting.py tests/test_planning_protocol_neutrality.py tests/test_agent_protocol_neutrality.py tests/test_llm_structured.py tests/test_llm_client.py tests/test_llm_cache.py tests/test_llm_telemetry.py tests/test_llm_retry.py -q`.
- [x] 9.2 Inspect the complete diff and remove unrelated/speculative code, then run `uv run pytest -q` and `openspec validate --all --strict`. Confirm no live V0/V1/V2 optimization, N=20 qualification, B1-B4 decision, production route/signature freeze, full Blueprint/Plan/Plan State/PlanDraftIR/Linker/lint, S4 controller/seal, TaskPlanner/PlanCritic/flat execution, S5/S6 behavior, protocol-specific branch, formal Run/report, or escalation-role decision was implemented; M1-4a2, M1-4a3, M1-4b, and M1-4c remain separate changes.
