## 1. Entry Gates and Closed Contracts

- [x] 1.1 Reverify design 7.1.1 and the M1-4c entry records before code changes: M1-4b/M1-4b2 must remain committed and archived with all machine gates passing, and the recorded M1-4a2 handoff/selection/assessment/bundle/owner-approval references must verify under lineage `ee5a23a8fcbaa5dc273f36c0365707fac5a9684f050463fc32ec7fd6bc3b67a5`, name consumer `m1-4c`, and match the packaged initial/repair prompt bytes. Record this as consumption of the existing approval, not a new signature or quality claim. (Requirements: Production S4 admits only the approved ArchitecturePlanner bundle; all M1-4c requirements. Design: Context, Decisions 2/6; system design §6.4.8.2 and §10.2. Paths: archived M1-4a2/M1-4b changes, `runs/_calibration/s4-architecture/<lineage>/prompt-development/`, packaged ArchitecturePlanner prompts. Verify: scripted ref/hash/Schema comparison; `openspec validate --all --strict`; `git status --short`.)
- [x] 1.2 Add closed draft-2020-12 Schemas and minimal conforming examples for S4 commitment, reference-only checkpoint manifest, PlanCritic result, flat semantic draft, active Plan pointer, minimal initial file ledger, and empty revision ledger; register mutual Schema/example validation without adding task UID/digests, later revision fields, execution state, or M2 test binding. (Requirements: Initial publication artifacts are closed and canonical; Production S4 roles use closed stage-specific contracts. Design: Decisions 3/4/6. Paths: `nepa/schemas/`, `nepa/schemas/examples/`, `tests/test_schema_examples.py`, `tests/test_s4_contracts.py`. Verify: `uv run pytest -q tests/test_schema_examples.py tests/test_s4_contracts.py`.)
- [x] 1.3 Make Run v3 model only `stages.s4.output_refs` as the closed mixed shape of Plan/active-pointer file refs plus Blueprint/config SHA-256 anchors while every other stage retains the existing reference-map behavior; update StageResult/RunStore commit validation without treating hash anchors as paths. (Requirements: The S4 seal carries independent typed anchors. Design: Decision 7; system design §4.8/§6.4.7. Paths: `nepa/schemas/run.schema.json`, `nepa/orchestrator.py`, `nepa/run_store.py`, `tests/test_orchestrator.py`, `tests/test_run_store.py`, `tests/test_s4_contracts.py`. Verify: `uv run pytest -q tests/test_orchestrator.py tests/test_run_store.py tests/test_s4_contracts.py -k 'output or anchor or stage'`.)

## 2. Production Agent and Handoff Bindings

- [x] 2.1 Extend the existing TaskPlanner role/template with the sealed `planning_budget` input and bind its output to the existing state-free task-shard Schema/example; verify missing/extra inputs, forbidden global fields, and complete manifest-only visibility fail before provider I/O. (Requirements: Production S4 roles use closed stage-specific contracts; Production role visibility and strategy remain controller-owned. Design: Decision 4. Paths: `nepa/agents/roles.py`, `nepa/agents/prompts/task_planner.md`, Agent contract binding, `tests/test_agent_roles.py`, `tests/test_s4_agent_contracts.py`. Verify: `uv run pytest -q tests/test_agent_roles.py tests/test_agent_rendering.py tests/test_s4_agent_contracts.py -k task_planner`.)
- [x] 2.2 Bind PlanCritic to the new closed result contract and FlatPlanBaseline to the new state-free semantic draft contract; enforce critic verdict/issue shape and flat-only availability while prohibiting final Plan/Blueprint/hash/state output. (Requirements: Production S4 roles use closed stage-specific contracts; Production role visibility and strategy remain controller-owned. Design: Decisions 4/5. Paths: Agent bindings/Schemas, `nepa/agents/roles.py`, PlanCritic/flat prompts, `tests/test_s4_agent_contracts.py`, `tests/test_s4_strategy.py`. Verify: `uv run pytest -q tests/test_s4_agent_contracts.py tests/test_s4_strategy.py -k 'critic or flat'`.)
- [x] 2.3 Implement the production M1-4a2 handoff verifier over confined lineage-relative refs and packaged prompt bytes, then route admitted ArchitecturePlanner initial/repair calls through the existing AgentInvoker and shared patch/ARCH_VALIDATE binding; reject missing, drifting, wrong-consumer, unsigned, or substituted evidence before provider I/O. (Requirements: Production S4 admits only the approved ArchitecturePlanner bundle; Production and calibration share architecture preparation and validation. Design: Decision 2. Paths: a narrow production handoff helper, existing calibration contract binding, `tests/test_s4_handoff.py`. Verify: `uv run pytest -q tests/test_s4_handoff.py tests/test_architecture_patch.py tests/test_architecture_validation.py`.)

## 3. Commitment and Recoverable Checkpoints

- [x] 3.1 Implement canonical S4a commitment construction from verified frozen refs/config, normative requirement levels, manifest contract metadata, enabled/build variants, planning budgets and layer switches; publish the existing planning index and complete Delivery Constraints beside it and make repeated construction byte-identical. (Requirements: S4 progresses through three closed planning phases; S4 checkpoints are parent-bound and resumable. Design: Decisions 1/3; system design §6.4.3; pipeline design §5.1. Paths: `nepa/stages/s4_planning.py`, existing planning/delivery helpers, `tests/test_s4_commitment.py`. Verify: `uv run pytest -q tests/test_s4_commitment.py tests/test_planning_inputs.py tests/test_delivery_constraints.py`.)
- [x] 3.2 Implement atomically replaced `plan/_s4/s4_state.json` plus reference-only checkpoint publication and ordered recovery for architecture attempts, per-package shard attempts, completed candidates, and critic rounds; persist phase, accepted refs, pending Agent call, attempt/repair counters (including per-package shard redo), and seen issue signatures before the corresponding side effect; reconcile state/checkpoints/durable trace, validate every payload/report ref and exact parent chain, reuse only the longest valid prefix without resetting budgets or duplicating a completed call, and treat conflicting immutable bytes as damage. (Requirements: S4 checkpoints are parent-bound and resumable. Design: Decision 3. Paths: S4 controller/checkpoint helper, `nepa/run_store.py`, `tests/test_s4_checkpoint_resume.py`. Verify: `uv run pytest -q tests/test_s4_checkpoint_resume.py -k 'parent or budget or prefix or conflict or pending'`.)

## 4. S4a and S4b Controller Flow

- [x] 4.1 Add `S4Controller` at the existing StageController boundary and implement PREPARE → DELIVERY_CONSTRAINTS → SELECT_STRATEGY with frozen-input/config verification, S4a checkpointing, context preflight, and no workspace access; register it programmatically without adding the M1-7 CLI. (Requirements: S4 progresses through three closed planning phases; Expected S4 failures are controlled and publish no partial Plan. Design: Decision 1. Paths: `nepa/stages/s4_planning.py`, `nepa/orchestrator.py`, `tests/test_s4_controller.py`. Verify: `uv run pytest -q tests/test_s4_controller.py -k 'prepare or constraints or preflight or workspace'`.)
- [x] 4.2 Implement layered ARCHITECT → ARCH_VALIDATE using the approved initial template, at most one shared semantic patch/correction path under `plan_architecture_repairs`, complete Schema/arch_01～arch_15 revalidation, sealed architecture checkpointing, and controlled failure with no child shards when the budget or validator cannot close. (Requirements: S4 progresses through three closed planning phases; Semantic repair is targeted and bounded; Production and calibration share architecture preparation and validation. Design: Decisions 2/3/5; system design §6.4.4. Paths: S4 controller, existing architecture/calibration bindings, `tests/test_s4_architecture_flow.py`. Verify: `uv run pytest -q tests/test_s4_architecture_flow.py tests/test_architecture_repair_locality.py`.)
- [x] 4.3 Add crash/replay cases after initial architecture, patch application, validation and sealed-architecture publication; prove matching checkpoints suppress duplicate Agent calls, changed parent hashes invalidate children, and completed S4 verification is a read-only no-op. (Requirements: S4 checkpoints are parent-bound and resumable; Initial publication is immutable and replay-safe. Design: Decisions 3/8. Paths: S4 controller, `tests/test_s4_checkpoint_resume.py`, `tests/test_runtime_resume.py`. Verify: `uv run pytest -q tests/test_s4_checkpoint_resume.py tests/test_runtime_resume.py -k 'architecture or s4'`.)

## 5. S4c Strategies, Linking, and Critic Repair

- [x] 5.1 Implement layered work-package expansion in stable work-package-id order with one fresh TaskPlanner call per package, exact context/budget projection, local shard validation, one cumulative semantic redo per package, and checkpoint/resume from the first missing valid shard. (Requirements: S4 progresses through three closed planning phases; Semantic repair is targeted and bounded. Design: Decisions 3/4/5; system design §6.4.4. Paths: S4 controller, task-shard validator, `tests/test_s4_layered.py`, `tests/test_s4_checkpoint_resume.py`. Verify: `uv run pytest -q tests/test_s4_layered.py tests/test_s4_checkpoint_resume.py -k shard`.)
- [x] 5.2 Implement the one shared candidate-completion helper that normalizes either strategy to PlanDraftIR, calls the existing deterministic Linker/Blueprint compiler, runs full S4-G0～S4-G6 lint, and publishes link/Blueprint/candidate/lint evidence only as one valid checkpoint chain; prove equivalent layered/flat drafts produce byte-identical completion outputs. (Requirements: The controller uses one deterministic candidate-completion path; Candidate completion remains state-free and M1-4d-free. Design: Decision 5. Paths: S4 controller/helper, `nepa/speclib/plan.py`, `nepa/speclib/delivery.py`, `tests/test_s4_candidate_completion.py`. Verify: `uv run pytest -q tests/test_s4_candidate_completion.py tests/test_plan_linker.py tests/test_plan_lint.py tests/test_delivery_blueprint.py`.)
- [x] 5.3 Implement fresh PlanCritic invocation, deterministic verdict validation and issue signatures, layered local/global invalidation, complete relink/lint/review after every repair, minor-issue normalization, configured critic/global budgets, and immediate controlled stop on a repeated signature. (Requirements: Semantic repair is targeted and bounded; Critic results are validated and deterministically routed. Design: Decisions 4/5. Paths: S4 controller/critic binding, `tests/test_s4_critic.py`. Verify: `uv run pytest -q tests/test_s4_critic.py -k 'verdict or local or global or repeated or minor or budget'`.)
- [x] 5.4 Implement the explicit flat arm so only FlatPlanBaseline is called, its draft enters the common completion path, every semantic revise invalidates the whole flat draft and consumes both configured budgets, and no layered/flat failure can switch strategy; retain true per-arm usage/trace counts. (Requirements: Layered and flat strategies are isolated; Semantic repair is targeted and bounded. Design: Decisions 4/5. Paths: S4 controller, existing role availability path, `tests/test_s4_strategy.py`. Verify: `uv run pytest -q tests/test_s4_strategy.py -k 'flat or fallback or budget or trace'`.)

## 6. Initial Plan Publication and Seal Recovery

- [x] 6.1 Implement final candidate regeneration and initial publication in the exact order Plan 1.0.0 → file ledger → empty revision ledger → active pointer, using canonical idempotent RunStore writes; require the file-ledger rows to equal the expanded Blueprint paths and keep Plan/ledgers free of all M1-4d and runtime fields. (Requirements: Initial publication artifacts are closed and canonical; Initial Plan publication has one logical commit point; Candidate completion remains state-free and M1-4d-free. Design: Decisions 6/8. Paths: S4 publication helper, new Schemas, `nepa/run_store.py`, `tests/test_s4_publication.py`. Verify: `uv run pytest -q tests/test_s4_publication.py -k 'canonical or ledger or order or m1_4d'`.)
- [x] 6.2 Implement `S4Controller.verify_completed` and Orchestrator integration so Plan/pointer refs, frozen input refs, Blueprint/config hashes, ledger path set and initial pointer fields are semantically rechecked before commit, downstream acceptance or done-stage no-op; preserve generic reference-only behavior for other/fake stage controllers. (Requirements: The S4 seal carries independent typed anchors; Initial publication is immutable and replay-safe. Design: Decisions 7/8. Paths: `nepa/stages/s4_planning.py`, `nepa/orchestrator.py`, `nepa/run_store.py`, `tests/test_s4_publication.py`, `tests/test_orchestrator.py`. Verify: `uv run pytest -q tests/test_s4_publication.py tests/test_orchestrator.py -k 'seal or anchor or verify or noop'`.)
- [x] 6.3 Add fault injection after each immutable Plan/ledger/pointer write, after semantic reread, and before/after the Run done transition; prove partial files are never consumable, identical prefixes resume forward, conflicting Plan/pointer/ledger bytes fail closed, and a committed seal never rewrites published bytes. (Requirements: Initial Plan publication has one logical commit point; Initial publication is immutable and replay-safe; S4 checkpoints are parent-bound and resumable. Design: Decision 8. Paths: S4 controller/publication helper, `tests/test_s4_publication_resume.py`, `tests/test_runtime_resume.py`. Verify: `uv run pytest -q tests/test_s4_publication_resume.py tests/test_runtime_resume.py -k 'publish or seal or crash'`.)

## 7. Controlled Failure, Neutrality, and Final Acceptance

- [x] 7.1 Exercise each expected S4 controlled failure—frozen-input/handoff drift, context overflow, final structured-output failure/truncation, exhausted architecture/shard/critic/global budget, invalid full lint and non-convergence—and distinguish deterministic invariant/tool defects as internal errors; assert no Plan/active pointer or workspace mutation escapes a failed S4. (Requirements: Expected S4 failures are controlled and publish no partial Plan. Design: Decisions 1/3/8 and Risks. Paths: S4 controller tests, existing orchestrator/S9 tests. Verify: `uv run pytest -q tests/test_s4_failures.py tests/test_termination.py tests/test_s9_partial_report.py`.)
- [x] 7.2 Run the complete M1-4c acceptance with scripted Agent responses and no live provider call: layered success/repair/replay, explicit flat comparison/no-fallback, S4-G0～S4-G6 and PlanCritic publication gates, all checkpoint and seal crash windows, canonical byte replay, MQTT/non-MQTT protocol-neutrality and source scans, schema/example mutual validation, full repository tests, four gold lints, strict current/all OpenSpec validation and `git diff --check`. Inspect the complete diff and confirm no `task_uid`/migration digest, M1-4d revision append/migration, M1-4e F2/F3, S5/S6 behavior, M1-7 CLI, M2 test implementation/binding, design edit, dependency, live qualification or unrelated refactor entered the change. (Requirements: all. Design: all decisions/non-goals. Paths: complete change diff. Verify: `uv run pytest -q`; four existing `uv run nepa lint` gold commands; `openspec validate m1-4c-s4-controller-and-initial-plan-publication --strict`; `openspec validate --all --strict`; `git diff --check`; `git diff --stat` and full diff review.)
