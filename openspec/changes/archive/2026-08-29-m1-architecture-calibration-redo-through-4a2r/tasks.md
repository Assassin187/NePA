## 1. Entry gates and bounded implementation brief

- [x] 1.1 Verify the recorded M0 DoD and archived M1-1/M1-2/M1-3 dependency evidence, inventory the current fixed-layout/ten-gate/two-slot implementation and historical calibration roots without modifying them, and write the four-part implementation brief plus supersession inventory to `experiments/m1-architecture-calibration-redo-through-4a2r/00-implementation-brief.md`. (Requirements: The resolved design baseline is an implementation entry control; Development starts only on a complete new free-layout lineage. Design: `system_design.md` §10.2 M0/M1-1～M1-3, §10.8; change `design.md` Decisions 1/10. Paths: `openspec/changes/archive/`, `openspec/changes/m1-4a2-architecture-planner-prompt-optimization/`, `runs/_calibration/s4-architecture/`, `experiments/m1-architecture-calibration-redo-through-4a2r/`. Verify: `python -m pytest -q tests/test_spec_lint.py tests/test_target_lint.py tests/test_test_bundle_lint.py tests/test_run_store.py tests/test_llm_providers.py tests/test_agent_invoker.py` and `git diff --check`.)
- [x] 1.2 Apply the responsible-owner decision to adopt the authorized subdocument's placeholder definition and synchronize `project_docs/system_design.md` 5.2.0: allow only `{message_id}`/`messages` and `{type_id}`/`types`, mapped mechanically to `per_message` and `per_type` with static paths mapped to `none`; record the revision in the main design history and remove the former change-level block. (Requirement: The resolved design baseline is an implementation entry control. Design: `system_design.md` §0.1, §5.6.5.3, §11.3; change `design.md` Decision 1. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, this change's `proposal.md`, `design.md`, planning-infrastructure spec and `tasks.md`. Verify: `rg -n '\{message_id\}|\{type_id\}|per_message|per_type|expand_over' project_docs/system_design.md project_docs/pipeline_design_s4_s9.md` and `git diff --check`.)
- [x] 1.3 Freeze the resolved 5.3.0 main-design and 1.1.0 subdocument path/SHA-256 (the bytes produced by tasks 1.2 and 1.4, not any earlier revision), M0/dependency evidence refs, affected-contract version plan, machine-verifiable DoD and explicit non-scope boundary in the implementation brief, while recording hashes of old experiment reports/roots that must remain historical-only. (Requirements: The resolved design baseline is an implementation entry control; Lineage identity freezes every non-prompt comparison variable. Design: `system_design.md` §9.2, §10.8; change `design.md` Decisions 1/2/7. Paths: `experiments/m1-architecture-calibration-redo-through-4a2r/00-implementation-brief.md`, `project_docs/`, `runs/_calibration/s4-architecture/`. Verify: `python -m pytest -q tests/test_calibration_lineage.py tests/test_prompt_development_scope.py tests/test_prompt_recovery_scope.py` and `git diff --check`.)
- [x] 1.4 Apply the responsible-owner decision that the second layout-gate conflict resolves to the authorized subdocument's wording, and synchronize `system_design.md` 5.3.0 plus `pipeline_design_s4_s9.md` 1.1.0: `arch_13` becomes three-segment closure plus one-artifact-per-`link_source`, unique artifact output paths, exact `delivery_form` `entry_point` count and an acyclic build graph; `arch_15` becomes the whitelist form over `path`/`path_pattern` segments and `purpose` tokens, with the general-responsibility whitelist declared a lineage-bound validator-side implementation shared with D1.11 rather than convention `advisory`/`hard` content. Record in the same revision the narrowed §5.6.5.3 reference deferring the `kind`/`producer` derivation table to M1-4b2, and the §10.2 reassignment of the convention asset, `layout_convention_id` derivation, `architecture.layout` Schema and `arch_11`～`arch_15` from M1-4b2 to M1-4a1. (Requirement: The resolved design baseline is an implementation entry control. Design: `system_design.md` §0.1, §5.6.5.3, §6.4.4, §10.2, §11.3, §12.5; `pipeline_design_s4_s9.md` §5.2.2～§5.2.4, §14; change `design.md` Decision 1. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, this change's `proposal.md`, `design.md`, planning-infrastructure spec and `tasks.md`. Verify: `rg -n 'link_source|entry_point|通用职责白名单|kind|producer' project_docs/system_design.md project_docs/pipeline_design_s4_s9.md` shows one consistent criterion per gate with no `app`-slot wording left in `arch_13`, and `git diff --check`.)
- [x] 1.5 Dispose of the superseded change `m1-4a2-architecture-planner-prompt-optimization` before implementation begins, so the two active changes cannot both add `architecture-prompt-development`: archive it with `openspec archive m1-4a2-architecture-planner-prompt-optimization --skip-specs -y` so its dual-model requirement set never enters the capability baseline, leave its own `tasks.md` bytes and 34/36 state unmodified, and record the supersession reason plus its final hashes in the implementation brief without claiming completion. (Requirements: The resolved design baseline is an implementation entry control; Lineage identity freezes every non-prompt comparison variable. Design: `system_design.md` §10.8, §11.3; change `design.md` Decisions 2/10. Paths: `openspec/changes/m1-4a2-architecture-planner-prompt-optimization/`, `openspec/changes/archive/`, `openspec/specs/`, `experiments/m1-architecture-calibration-redo-through-4a2r/00-implementation-brief.md`. Verify: `openspec list` shows only this change active, `openspec validate --all --strict` passes, and `ls openspec/specs/` still has no `architecture-prompt-development` directory.)

## 2. Free-layout production contracts and deterministic constraints (M1-4a1 redo)

- [x] 2.1 Add the resolved, versioned, protocol-neutral application-layer layout-convention asset and deterministic loader/canonicalizer; make convention id, canonical hash, separated `advisory`/`hard` projections and mechanical bounds explicit, with no protocol/model branch or workspace/network/environment lookup. (Requirements: Planning index and architecture Delivery Constraints are deterministic shared artifacts; Planning and calibration assets remain protocol neutral. Design: `system_design.md` §5.6.5.2～§5.6.5.3, §6.4.8.1; change `design.md` Decision 3. Paths: `nepa/assets/layout_conventions/`, `nepa/speclib/delivery.py`, `tests/test_delivery_constraints.py`, `tests/test_planning_protocol_neutrality.py`. Verify: `python -m pytest -q tests/test_delivery_constraints.py tests/test_planning_protocol_neutrality.py`.)
- [x] 2.2 Migrate `compile_delivery_constraints` and its planning-input callers from the fixed `file_rules/file_slots/internal_interface_slots` projection to the convention-bound free-layout projection, retaining deterministic planning-index content, quote exclusion, context-bound computation and production entry-point identity. (Requirement: Planning index and architecture Delivery Constraints are deterministic shared artifacts. Design: `system_design.md` §6.4.1, §6.4.3, §6.4.8.1; change `design.md` Decision 3. Paths: `nepa/speclib/delivery.py`, `nepa/speclib/planning.py`, `tests/test_delivery_constraints.py`, `tests/test_planning_index.py`, `tests/test_planning_inputs.py`. Verify: `python -m pytest -q tests/test_delivery_constraints.py tests/test_planning_index.py tests/test_planning_inputs.py`.)
- [x] 2.3 Major-version the closed ArchitectureDraft and ArchitectureValidation JSON contracts and examples so `architecture.layout` and exactly `arch_01`～`arch_15` are required, static paths plus the resolved `{message_id}`/`messages` and `{type_id}`/`types` pairs are accepted, mixed/repeated/unknown/mismatched pairs and old fixed-layout shapes fail, downstream-only fields remain forbidden, and canonical examples validate. (Requirements: ArchitectureDraft is a closed production contract; ARCH_VALIDATE is the single production S4-G2 validator; The resolved design baseline is an implementation entry control. Design: `system_design.md` §5.2.1～§5.2.2, §5.6.5.3, §6.4.4; change `design.md` Decisions 1～3. Paths: `nepa/schemas/architecture-draft.schema.json`, `nepa/schemas/architecture-validation.schema.json`, `nepa/schemas/examples/architecture-draft.example.json`, `nepa/schemas/examples/architecture-validation.example.json`, `tests/test_architecture_schema.py`, `tests/test_schema_examples.py`. Verify: `python -m pytest -q tests/test_architecture_schema.py tests/test_schema_examples.py`.)
- [x] 2.4 Adapt `arch_01`～`arch_10` to layout-derived file/contract sets and implement non-short-circuit `arch_11 LAYOUT_SAFETY`, `arch_12 LAYOUT_CLASS`, `arch_13 BUILD_GRAPH`, `arch_14 LAYERING` and `arch_15 PATH_NEUTRALITY` in the existing production `validate_architecture` path with canonical issue ordering, including exact validation of both resolved placeholder/domain pairs. (Requirement: ARCH_VALIDATE is the single production S4-G2 validator. Design: `system_design.md` §5.6.5.3, §6.4.4; `pipeline_design_s4_s9.md` §5.2.2～§5.2.4; change `design.md` Decisions 3/4. Paths: `nepa/speclib/architecture.py`, `tests/test_architecture_validation.py`. Verify: `python -m pytest -q tests/test_architecture_validation.py`.)
- [x] 2.5 Extend table-driven validator and repair-impact-policy coverage for every new gate, multi-defect ordering, layout cross-projections and issue-local/regression-free one-repair behavior without adding an experimental validator. (Requirements: ARCH_VALIDATE is the single production S4-G2 validator; Semantic repair is limited, issue-local and regression-free. Design: `system_design.md` §6.4.4, §6.4.8.2.1; change `design.md` Decisions 4/5. Paths: `nepa/speclib/architecture.py`, `nepa/calibration/s4_architecture.py`, `tests/test_architecture_validation.py`, `tests/test_architecture_repair_locality.py`. Verify: `python -m pytest -q tests/test_architecture_validation.py tests/test_architecture_repair_locality.py`.)
- [x] 2.6 Rewrite the shared five-section ArchitecturePlanner template to construct and reconcile the free layout from the named planning inputs and convention data, keeping repair context exact and excluding embedded protocol facts, convention-owned identifiers, and model/provider branches. (Requirements: ArchitectureDraft is a closed production contract; Planning and calibration assets remain protocol neutral. Design: `system_design.md` §6.4, §8.8; change `design.md` Decision 5. Paths: `nepa/agents/prompts/architecture_planner.md`, `nepa/agents/renderer.py`, `tests/test_agent_rendering.py`, `tests/test_agent_protocol_neutrality.py`. Verify: `python -m pytest -q tests/test_agent_rendering.py tests/test_agent_protocol_neutrality.py tests/test_agent_roles.py`.)
- [x] 2.7 Prove the same Delivery Constraints, Schema, renderer and fifteen-gate validator path on MQTT and the existing non-MQTT application-layer fixture, including context overflow failure before Provider I/O and zero cross-protocol residue. Because validation needs a draft instance and D1.11 now forbids faking one from a fixed slot table, author one version-controlled frozen layout-bearing ArchitectureDraft fixture per protocol (each carrying its own `architecture.layout`) alongside the existing `spec.json`/`target.json`/`test_bundle.json`, and drive both through the identical code path. (Requirements: Planning index and architecture Delivery Constraints are deterministic shared artifacts; Planning and calibration assets remain protocol neutral. Design: `system_design.md` §6.4.8.1, §8.8, D1.11; change `design.md` Decisions 3～5. Paths: `tests/fixtures/non_mqtt_application/`, `gold_file/`, `tests/test_planning_protocol_neutrality.py`, `tests/test_architecture_validation.py`, `tests/test_architecture_calibration.py`. Verify: `python -m pytest -q tests/test_planning_protocol_neutrality.py tests/test_architecture_validation.py tests/test_architecture_calibration.py`.)

## 3. Three-slot calibration lineage and artifact migration (M1-4a1 redo)

- [x] 3.1 Define one ordered `qwen/claude/deepseek` logical-slot contract across internal defaults, live config, context limits and pricing; require complete endpoints/routes/secret-variable names, temperature and `max_tokens=65536`, while never reading or persisting credential values during deterministic tests. (Requirements: Three-model trial execution is isolated and cache-independent; Development starts only on a complete new free-layout lineage. Design: `system_design.md` §4.6, §6.4.8.1, §9.2; change `design.md` Decisions 6/11. Paths: `nepa/config.py`, `configs/default.yaml`, `configs/m1-4a2-live.yaml`, `configs/m1-4a2-context-limits.json`, `tests/test_config.py`, `tests/test_prompt_development_config.py`, `tests/test_llm_pricing.py`. Verify: `python -m pytest -q tests/test_config.py tests/test_prompt_development_config.py tests/test_llm_pricing.py`.)
- [x] 3.2 Major-version the affected lineage, batch, report, attempt, development and recovery Schemas/examples to require all three logical slots and fifteen-gate evidence, and reject dual-slot/old-version artifacts instead of auto-filling them. (Requirements: Lineage identity freezes every non-prompt comparison variable; Trial artifacts and calibration reports are hash-bound and recomputable; Calibration metrics preserve fixed denominators and failure evidence. Design: `system_design.md` §6.4.8.1～§6.4.8.3; change `design.md` Decisions 2/6. Paths: `nepa/schemas/calibration-*.schema.json`, `nepa/schemas/examples/calibration-*.example.json`, `tests/test_calibration_artifacts.py`, `tests/test_prompt_development_schema.py`, `tests/test_prompt_recovery_schema.py`, `tests/test_schema_examples.py`. Verify: `python -m pytest -q tests/test_calibration_artifacts.py tests/test_prompt_development_schema.py tests/test_prompt_recovery_schema.py tests/test_schema_examples.py`.)
- [x] 3.3 Generalize the S4 calibration driver, task declaration, execution loops, report aggregation and storage paths to three isolated slot roots, clients/sessions/caches/traces and no-history/cache-disabled calls; keep directory identity on the logical slot. (Requirements: Three-model trial execution is isolated and cache-independent; Trial artifacts and calibration reports are hash-bound and recomputable. Design: `system_design.md` §4.6, §6.4.8.1, §8.3～§8.4; change `design.md` Decisions 6/7. Paths: `nepa/calibration/s4_architecture.py`, `tests/test_architecture_calibration.py`, `tests/test_calibration_artifacts.py`, `tests/test_calibration_reporting.py`. Verify: `python -m pytest -q tests/test_architecture_calibration.py tests/test_calibration_artifacts.py tests/test_calibration_reporting.py`.)
- [x] 3.4 Rebuild the canonical lineage projection so convention/Schema/validator/slot endpoint-route-parameter/context/statistic controls are bound but configured and returned model strings are observations only; record per-call strings and set/share summaries without splitting, invalidating or naming a root. (Requirements: Lineage identity freezes every non-prompt comparison variable; Calibration metrics preserve fixed denominators and failure evidence. Design: `system_design.md` §9.2 rule 1; change `design.md` Decision 6. Paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, `tests/test_calibration_lineage.py`, `tests/test_calibration_reporting.py`, `tests/test_prompt_development_selection.py`. Verify: `python -m pytest -q tests/test_calibration_lineage.py tests/test_calibration_reporting.py tests/test_prompt_development_selection.py`.)
- [x] 3.5 Enforce append-only coherent three-slot attempt/resume/recompute behavior: declare before calls, commit only complete hash-valid leaves, make one infrastructure-invalid slot invalidate the entire attempt for selection, rerun all three slots, and reject cross-attempt/old-contract splicing. (Requirements: Three-model trial execution is isolated and cache-independent; Trial artifacts and calibration reports are hash-bound and recomputable. Design: `system_design.md` §6.4.8.1～§6.4.8.2; change `design.md` Decision 7. Paths: `nepa/calibration/s4_architecture.py`, `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_attempts.py`, `tests/test_calibration_artifacts.py`, `tests/test_calibration_lineage.py`. Verify: `python -m pytest -q tests/test_prompt_development_attempts.py tests/test_calibration_artifacts.py tests/test_calibration_lineage.py`.)
- [x] 3.6 Run the deterministic M1-4a1 acceptance set and freeze a new lineage only after free layout, all fifteen gates, three-slot configuration, neutrality and recomputation pass; assert historical roots remain byte-unchanged and mechanically ineligible. (Requirements: all `planning-architecture-infrastructure` requirements. Design: `system_design.md` §6.4.8.1, §10.2 M1-4a1, D1.0/D1.11; change `design.md` Migration Plan 2～4. Paths: `nepa/`, `configs/`, `tests/`, `runs/_calibration/s4-architecture/`, `experiments/m1-architecture-calibration-redo-through-4a2r/`. Verify: `python -m pytest -q tests/test_delivery_constraints.py tests/test_architecture_schema.py tests/test_architecture_validation.py tests/test_architecture_calibration.py tests/test_calibration_lineage.py tests/test_calibration_reporting.py tests/test_planning_protocol_neutrality.py` and compare the task-1.3 historical hashes.)

## 4. Bounded prompt-development workflow (M1-4a2 redo)

- [x] 4.1 Encode a typed development protocol with three required slots, N=5, one semantic repair, `p1_threshold=0.80`, zero truncation, valid infrastructure and an assertion that the screening rate stays strictly below the M1-4a3 B1 reference 0.90; keep Schema-after-format, p0, first-pass/per-gate/repeated-failure and model-string values outside the hard-gate object. (Requirement: Development screening is a monotonic p1 gate with diagnostic first-pass metrics. Design: `system_design.md` §6.4.8.2 revision 5.1.0; change `design.md` Decision 8. Paths: `nepa/schemas/calibration-development-protocol.schema.json`, its example, `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_schema.py`, `tests/test_prompt_development_selection.py`. Verify: `python -m pytest -q tests/test_prompt_development_schema.py tests/test_prompt_development_selection.py`.)
- [x] 4.2 Generalize V0/V1/optional V2 admission, evidence-backed revision, early-stop and fixed fallback/tie logic to coherent three-slot assessments; compute the fallback tuple from the minimum across all three slots and remove model-identity/schema-1.00/first-pass hard rejection. (Requirements: One shared protocol-neutral prompt is developed through V0, V1 and optional V2; Fallback and tie resolution use only complete final assessments. Design: `system_design.md` §6.4.8.2; change `design.md` Decisions 6/8. Paths: `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development.py`, `tests/test_prompt_development_revisions.py`, `tests/test_prompt_development_selection.py`. Verify: `python -m pytest -q tests/test_prompt_development.py tests/test_prompt_development_revisions.py tests/test_prompt_development_selection.py`.)
- [x] 4.3 Generalize the evidence-qualified V1/V2 N=10 extension predicates to all three slots, append exactly trials 006～010 under unchanged controls, and reject V0 extension, partial-slot extension or cross-attempt assembly. (Requirement: Every development version uses coherent isolated three-model trials. Design: `system_design.md` §6.4.8.2; change `design.md` Decisions 7/8. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/calibration-development-extension.schema.json`, its example, `tests/test_prompt_development_extension.py`, `tests/test_prompt_development_attempts.py`. Verify: `python -m pytest -q tests/test_prompt_development_extension.py tests/test_prompt_development_attempts.py`.)
- [x] 4.4 Bind admitted prompt snapshots, repository source bytes, rendered requests and revision diffs as one shared protocol-neutral identity for Qwen/Claude/DeepSeek, and fail before Provider I/O on source mismatch, model/provider specialization or non-prompt control changes. (Requirements: One shared protocol-neutral prompt is developed through V0, V1 and optional V2; Development starts only on a complete new free-layout lineage. Design: `system_design.md` §6.4.8.2, §8.8; change `design.md` Decisions 5/7. Paths: `nepa/agents/prompts/architecture_planner.md`, `nepa/calibration/s4_prompt_development.py`, `tests/test_prompt_development_snapshot.py`, `tests/test_prompt_development_neutrality.py`, `tests/test_prompt_development_revisions.py`. Verify: `python -m pytest -q tests/test_prompt_development_snapshot.py tests/test_prompt_development_neutrality.py tests/test_prompt_development_revisions.py`.)
- [x] 4.5 Update development report/recompute/handoff artifacts so all three fixed denominators, fifteen gates, repair gain, truncation/infrastructure, cost/latency, parameter support and model-string shares are machine-derived, with unique selection/tie and an M1-4a3-only handoff or explicit `M1-4a2r=not_triggered`. (Requirement: Development evidence and the new report are recomputable and scope-limited. Design: `system_design.md` §6.4.8, §9.2; change `design.md` Decision 10. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/calibration-development-*.schema.json`, their examples, `tests/test_prompt_development_artifacts.py`, `tests/test_prompt_development_reporting.py`, `tests/test_prompt_development_scope.py`. Verify: `python -m pytest -q tests/test_prompt_development_artifacts.py tests/test_prompt_development_reporting.py tests/test_prompt_development_scope.py`.)
- [x] 4.6 Complete the fake-provider M1-4a2 suite for p1=0.80 pass, one-slot failure, truncation, alias variation, coherent retry, early stop, fallback/tie and old-lineage rejection before permitting any live call. (Requirements: all `architecture-prompt-development` requirements. Design: `system_design.md` §6.4.8.2, D1.0/D1.11; change `design.md` Decisions 6～8. Paths: `tests/test_prompt_development*.py`, `tests/test_architecture_calibration.py`. Verify: `python -m pytest -q tests/test_prompt_development.py tests/test_prompt_development_artifacts.py tests/test_prompt_development_attempts.py tests/test_prompt_development_config.py tests/test_prompt_development_extension.py tests/test_prompt_development_neutrality.py tests/test_prompt_development_reporting.py tests/test_prompt_development_revisions.py tests/test_prompt_development_schema.py tests/test_prompt_development_scope.py tests/test_prompt_development_selection.py tests/test_prompt_development_snapshot.py`.)

## 5. New M1-4a2 live experiment and report

- [x] 5.1 Write and validate `01-preregistration.md` plus non-secret configuration/context provenance for the exact new lineage, V0～V2 bounds, coherent rerun/extension rules, 0.80 gate, fallback/tie decision, cost limits and report fields; check only the three declared environment-variable names for presence immediately before live work and never print/store values. Every credential-requiring command in tasks 5.1～5.6 and 6.8～6.9 SHALL use `./run_m1_calibration.sh`, never `python -m nepa.calibration.s4_prompt_development` directly, so the current `~/.bashrc` exports are loaded before the unchanged module runner starts. (Requirements: Development starts only on a complete new free-layout lineage; Development evidence and the new report are recomputable and scope-limited. Design: `system_design.md` §6.4.8.1～§6.4.8.2, §9.2; change `design.md` Decisions 6/7/10/11. Paths: `experiments/m1-architecture-calibration-redo-through-4a2r/01-preregistration.md`, `configs/m1-4a2-live.yaml`, `configs/m1-4a2-context-limits.json`, `run_m1_calibration.sh`. Verify: `./run_m1_calibration.sh init --config configs/m1-4a2-live.yaml --context-limits configs/m1-4a2-context-limits.json --runs-root runs` succeeds only after deterministic preflight and credential presence, and a secret scan of the tracked experiment diff is clean.)
- [x] 5.2 Declare and run one coherent fresh V0 N=5 attempt for each of Qwen, Claude and DeepSeek under the frozen new lineage; on infrastructure invalidity retain the attempt as audit-only and rerun all three complete batches under a new attempt, with no replacement sampling. (Requirements: Every development version uses coherent isolated three-model trials; Three-model trial execution is isolated and cache-independent. Design: `system_design.md` §4.6, §6.4.8.2, §8.3～§8.4; change `design.md` Decisions 7/11. Paths: `runs/_calibration/s4-architecture/<new-lineage>/development/v0/`, `experiments/m1-architecture-calibration-redo-through-4a2r/`, `run_m1_calibration.sh`. Verify: run the declared `./run_m1_calibration.sh run-version --development-root <new-development-root> --version v0`, then `./run_m1_calibration.sh recompute --development-root <new-development-root> --version v0 --count 5 --require-complete --require-source-match`.)
- [x] 5.3 If V0 fails and its committed diagnostics support one preregistered falsifiable prompt defect, record exactly one V1 revision and run a coherent three-slot N=5 attempt; perform the all-slot trials 006～010 extension only if the typed ambiguity predicate fires, otherwise stop or continue exactly as the state machine directs. (Requirements: One shared protocol-neutral prompt is developed through V0, V1 and optional V2; Every development version uses coherent isolated three-model trials. Design: `system_design.md` §6.4.8.2; change `design.md` Decisions 7/8/11. Paths: `runs/_calibration/s4-architecture/<new-lineage>/development/v1/`, `nepa/agents/prompts/architecture_planner.md`, `run_m1_calibration.sh`. Verify: use `record-revision`, `./run_m1_calibration.sh run-version`, conditional `./run_m1_calibration.sh expand`, and `./run_m1_calibration.sh recompute --require-complete --require-source-match` for V1, then verify the exact revision/evidence refs and either N=5 or N=10 denominator.)
- [x] 5.4 If V1 still fails and complete evidence supports a distinct second hypothesis, record the sole optional V2 revision and run/recompute the same bounded three-slot N=5/conditional-N=10 procedure; reject any V3 or non-prompt change. (Requirements: One shared protocol-neutral prompt is developed through V0, V1 and optional V2; Fallback and tie resolution use only complete final assessments. Design: `system_design.md` §6.4.8.2; change `design.md` Decisions 7/8/11. Paths: `runs/_calibration/s4-architecture/<new-lineage>/development/v2/`, `nepa/agents/prompts/architecture_planner.md`, `run_m1_calibration.sh`. Verify: use `record-revision`, `./run_m1_calibration.sh run-version`, conditional `./run_m1_calibration.sh expand`, and `./run_m1_calibration.sh recompute --require-complete --require-source-match` for V2, then verify the coordinator has no V3 action.)
- [x] 5.5 Recompute and atomically publish the first passing selection or exact fixed-fallback winner/tie, verify repository prompt bytes equal the selected snapshot, and publish only an M1-4a3 admission handoff; if selected, publish `M1-4a2r=not_triggered`, and if tied publish neither selection nor handoff. (Requirements: Fallback and tie resolution use only complete final assessments; Development evidence and the new report are recomputable and scope-limited. Design: `system_design.md` §6.4.8.2; change `design.md` Decisions 8～11. Paths: `runs/_calibration/s4-architecture/<new-lineage>/development/`, `nepa/agents/prompts/architecture_planner.md`, `experiments/m1-architecture-calibration-redo-through-4a2r/results/`, `run_m1_calibration.sh`. Verify: `./run_m1_calibration.sh recompute --development-root <new-development-root> --version <terminal-version> --count <5-or-10> --require-complete --require-source-match` and `python -m pytest -q tests/test_prompt_development_selection.py tests/test_prompt_development_reporting.py tests/test_prompt_development_scope.py`.)
- [x] 5.6 Generate `results/development-summary.json` and `results/development-report.md` exclusively from new-lineage leaves, present each slot and all required diagnostics/limitations separately, validate Markdown values against the machine summary, and make no M1-4a3/B1～B4 or production claim. (Requirement: Development evidence and the new report are recomputable and scope-limited. Design: `system_design.md` §6.4.8, §10.2 M1-4a2; change `design.md` Decisions 10/11. Paths: `experiments/m1-architecture-calibration-redo-through-4a2r/results/development-summary.json`, `experiments/m1-architecture-calibration-redo-through-4a2r/results/development-report.md`, `runs/_calibration/s4-architecture/<new-lineage>/development/`, `run_m1_calibration.sh`. Verify: rerun the implemented report command and `./run_m1_calibration.sh recompute` from leaves, then `python -m pytest -q tests/test_prompt_development_reporting.py tests/test_prompt_development_scope.py`.)

## 6. Conditional recovery workflow and experiment (M1-4a2r redo)

- [x] 6.1 Remove hard-coded historical predecessor/seed/design identities and migrate recovery initialization/attestation/protocol artifacts to accept only a complete new free-layout, fifteen-gate, three-slot development tie; reject historical dual-model roots, selections and copied predecessor trials. (Requirements: Recovery activation requires explicit design authority; A terminal M1-4a2 tie admits only a fresh isolated recovery lineage. Design: `system_design.md` §6.4.8.2～§6.4.8.2.1, D1.0; change `design.md` Decision 9. Paths: `nepa/calibration/s4_prompt_development.py`, `configs/m1-4a2r-authorization.json`, `nepa/schemas/calibration-recovery-*.schema.json`, their examples, `tests/test_prompt_recovery_authorization.py`, `tests/test_prompt_recovery_lineage.py`. Verify: `python -m pytest -q tests/test_prompt_recovery_authorization.py tests/test_prompt_recovery_lineage.py tests/test_prompt_recovery_schema.py`.)
- [x] 6.2 Generalize R0/R1/optional R2, restoration/no-selection and prompt source/snapshot/revision controls to one shared free-layout prompt across all three slots, with no N=10 extension, fallback ranking, R3, historical trial denominator or model/protocol branch. (Requirements: Recovery uses one shared protocol-neutral exact-algorithm prompt; Recovery prompt development is bounded to R0 and two evidence-backed revisions. Design: `system_design.md` §6.4.8.2.1, §8.8; change `design.md` Decisions 5/9. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/agents/prompts/architecture_planner.md`, `tests/test_prompt_recovery.py`, `tests/test_prompt_recovery_revisions.py`, `tests/test_prompt_recovery_snapshot.py`, `tests/test_prompt_recovery_neutrality.py`. Verify: `python -m pytest -q tests/test_prompt_recovery.py tests/test_prompt_recovery_revisions.py tests/test_prompt_recovery_snapshot.py tests/test_prompt_recovery_neutrality.py`.)
- [x] 6.3 Generalize recovery attempts/reports/screening to coherent three-slot N=5, record-only model strings, p1≥0.80 per slot, zero truncation, valid infrastructure, complete fifteen-gate repair locality and full final validation; retain Schema/p0/first-pass/per-gate/repeated-failure/quality diagnostics without extra rate gates. (Requirements: Every recovery version uses one coherent fresh three-model N=5 attempt; Semantic repair is limited, issue-local and regression-free; Recovery screening prioritizes bounded repair success and retains first-pass diagnostics. Design: `system_design.md` §6.4.8.2.1 revision 5.1.0; change `design.md` Decisions 6～9. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/calibration/s4_architecture.py`, `tests/test_prompt_recovery_attempts.py`, `tests/test_prompt_recovery_selection.py`, `tests/test_architecture_repair_locality.py`. Verify: `python -m pytest -q tests/test_prompt_recovery_attempts.py tests/test_prompt_recovery_selection.py tests/test_architecture_repair_locality.py`.)
- [x] 6.4 Recompute the architecture-quality audit, immutable recovery selection/no-selection, prompt restoration/source match and M1-4a3-only handoff from canonical three-slot leaves, keeping quality diagnostics out of validation/screening and rejecting downstream S4/S5/S6 consumption. (Requirements: Architecture-quality audit remains separate from mechanical validation; Recovery selection and handoff are unique, immutable and limited to M1-4a3 admission; Recovery evidence is reproducible, secret-free and non-downstream. Design: `system_design.md` §6.4.8.2.1～§6.4.8.3, §11.3; change `design.md` Decisions 9/10. Paths: `nepa/calibration/s4_prompt_development.py`, `nepa/schemas/calibration-recovery-*.schema.json`, their examples, `tests/test_prompt_recovery_quality.py`, `tests/test_prompt_recovery_reporting.py`, `tests/test_prompt_recovery_scope.py`. Verify: `python -m pytest -q tests/test_prompt_recovery_quality.py tests/test_prompt_recovery_reporting.py tests/test_prompt_recovery_scope.py tests/test_prompt_recovery_selection.py`.)
- [x] 6.5 Complete the full deterministic fake-provider M1-4a2r suite, including old-root rejection, missing/mismatched authorization, p1=0.80 pass, one-slot failure, coherent retry, alias variation, locality failure, first-pass diagnostic non-gating, terminal restoration and `not_triggered`, before any conditional live call. (Requirements: all `architecture-calibration-recovery` requirements. Design: `system_design.md` §6.4.8.2.1, D1.0; change `design.md` Decision 9. Paths: `tests/test_prompt_recovery*.py`, `tests/test_architecture_repair_locality.py`. Verify: `python -m pytest -q tests/test_prompt_recovery.py tests/test_prompt_recovery_artifacts.py tests/test_prompt_recovery_attempts.py tests/test_prompt_recovery_authorization.py tests/test_prompt_recovery_config.py tests/test_prompt_recovery_lineage.py tests/test_prompt_recovery_neutrality.py tests/test_prompt_recovery_quality.py tests/test_prompt_recovery_reporting.py tests/test_prompt_recovery_revisions.py tests/test_prompt_recovery_schema.py tests/test_prompt_recovery_scope.py tests/test_prompt_recovery_selection.py tests/test_prompt_recovery_snapshot.py tests/test_architecture_repair_locality.py`.)
- [x] 6.6 If task 5.5 produced a normal selection, publish/recompute the tracked `results/recovery-status.json` as `not_triggered`, prove no recovery lineage or recovery Provider call was created, and skip tasks 6.7～6.9. (Requirement: Recovery activation requires explicit design authority. Design: `system_design.md` §6.4.8.2～§6.4.8.2.1; change `design.md` Decision 9. Paths: `experiments/m1-architecture-calibration-redo-through-4a2r/results/recovery-status.json`, `runs/_calibration/s4-architecture/<new-lineage>/`. Verify: `python -m pytest -q tests/test_prompt_recovery_authorization.py tests/test_prompt_recovery_scope.py` plus a lineage-root inventory showing no recovery root/call.)
- [ ] 6.7 Only if task 5.5 produced a complete recomputable `PROMPT_SELECTION_TIE`, obtain a separate responsible-owner authorization bound to that exact predecessor hash graph and the then-current approved `project_docs/system_design.md` path/SHA-256; the implementer and this change must not mark the authorization as approved. **Not applicable: task 5.5 produced a normal fallback selection, not a tie; no authorization-only recovery preflight was run.** (Requirement: Recovery activation requires explicit design authority. Design: `system_design.md` §6.4.8.2.1; change `design.md` Decisions 1/9. Paths: owner-supplied authorization record, `project_docs/system_design.md`, `<new-development-root>`, `experiments/m1-architecture-calibration-redo-through-4a2r/results/recovery-status.json`. Verify: `python -m pytest -q tests/test_prompt_recovery_authorization.py` and a successful authorization-only recovery preflight with zero Provider calls.)
- [ ] 6.8 After task 6.7 only, initialize a fresh recovery lineage and run coherent R0 N=5 for all three slots; admit R1 and optional R2 only from complete failing predecessor evidence plus one distinct falsifiable hypothesis each, stop at the first passing version, or restore/hash-check the pre-recovery prompt before `no_selection`. **Not applicable: recovery was not triggered, so no recovery lineage or Provider call was created.** (Requirements: A terminal M1-4a2 tie admits only a fresh isolated recovery lineage; Recovery prompt development is bounded to R0 and two evidence-backed revisions; Every recovery version uses one coherent fresh three-model N=5 attempt. Design: `system_design.md` §6.4.8.2.1; change `design.md` Decisions 7/9/11. Paths: `runs/_calibration/s4-architecture/<new-recovery-lineage>/`, `nepa/agents/prompts/architecture_planner.md`, `run_m1_calibration.sh`. Verify: use `./run_m1_calibration.sh recovery-init`, bounded `./run_m1_calibration.sh recovery-run-version`, conditional `recovery-record-revision`, and `./run_m1_calibration.sh recovery-recompute --require-complete --require-source-match`; verify no R3/N=10/fallback action is available.)
- [ ] 6.9 If recovery ran, generate `results/recovery-summary.json`, `results/recovery-report.md` and terminal status exclusively from its new leaves, presenting each slot, all fifteen gates, locality/quality diagnostics, model-string shares, exact selection/no-selection and M1-4a3-only limitation; do not mix predecessor or historical trials. **Not applicable: recovery did not run; the tracked terminal evidence is `results/recovery-status.json` with `status=not_triggered` and `provider_calls=0`.** (Requirements: Recovery selection and handoff are unique, immutable and limited to M1-4a3 admission; Recovery evidence is reproducible, secret-free and non-downstream. Design: `system_design.md` §6.4.8.2.1～§6.4.8.3; change `design.md` Decisions 10/11. Paths: `experiments/m1-architecture-calibration-redo-through-4a2r/results/recovery-summary.json`, `experiments/m1-architecture-calibration-redo-through-4a2r/results/recovery-report.md`, `<new-recovery-root>`, `run_m1_calibration.sh`. Verify: rerun `./run_m1_calibration.sh recovery-recompute --require-complete --require-source-match` from leaves and `python -m pytest -q tests/test_prompt_recovery_reporting.py tests/test_prompt_recovery_scope.py`.)

## 7. End-to-end acceptance and scope audit

- [x] 7.1 Re-run the focused free-layout, fifteen-gate, three-slot development/recovery suites and validate that all current JSON examples, recompute paths, source matches, neutrality fixtures and old-evidence rejection cases pass without Provider access. (Requirements: all requirements in this change. Design: `system_design.md` §6.4.8, §9.2, D1.0/D1.11; change `design.md` Migration Plan 2～6. Paths: `nepa/`, `configs/`, `tests/`, `experiments/m1-architecture-calibration-redo-through-4a2r/`. Verify: `python -m pytest -q tests/test_delivery_constraints.py tests/test_planning_inputs.py tests/test_planning_protocol_neutrality.py tests/test_architecture_schema.py tests/test_architecture_validation.py tests/test_architecture_calibration.py tests/test_architecture_repair_locality.py tests/test_calibration_artifacts.py tests/test_calibration_lineage.py tests/test_calibration_reporting.py tests/test_prompt_development*.py tests/test_prompt_recovery*.py tests/test_schema_examples.py`.)
- [x] 7.2 Run the full repository test suite, strict OpenSpec validation, design/gold-file lints and diff hygiene; regenerate the machine summaries from raw new-lineage leaves and prove their Markdown reports match. (Requirements: Development evidence and the new report are recomputable and scope-limited; Recovery evidence is reproducible, secret-free and non-downstream. Design: `system_design.md` §10.2 M1-4a1/M1-4a2/M1-4a2r; change `design.md` Decision 10. Paths: repository-wide plus `openspec/changes/m1-architecture-calibration-redo-through-4a2r/` and its experiment directory. Verify: `python -m pytest -q`, `openspec validate m1-architecture-calibration-redo-through-4a2r --strict`, the implemented development/recovery report recompute commands, and `git diff --check`.)
- [x] 7.3 Inspect the complete code/evidence diff and hash graph, remove incidental or compatibility work, verify historical roots/reports are unmodified, confirm secrets are absent, verify the task-1.5 supersession left the archived change's bytes and 34/36 state intact with no dual-model requirement in the capability baseline, and record that M1-4a3 N=10, B1～B4, the M1-4b2 Blueprint transcriber and `kind`/`producer` derivation table, complete S4, S5/S6 and production freeze/signature remain unimplemented; this final task closes only the new M1-4a1/M1-4a2 and conditional M1-4a2r scope. (Requirements: all requirements in this change. Design: `system_design.md` §6.4.8.3, §10.2; change `design.md` Goals/Non-Goals and Migration Plan. Paths: full `git diff`, `runs/_calibration/s4-architecture/`, `experiments/m1-architecture-calibration-redo-through-4a2r/`, prior experiment/change directories. Verify: `git status --short`, `git diff --stat`, `git diff --check`, compare task-1.3 historical hashes, rerun every validation command listed by this change, and review that no non-scope artifact or completion claim was added.)
