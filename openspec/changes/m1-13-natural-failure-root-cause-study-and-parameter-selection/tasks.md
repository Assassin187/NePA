## 1. Entry conditions and implementation brief

- [x] 1.1 Verify the archived M1-12 responsible-owner decision and post-approval acceptance, confirm this M1-13 change is the only active change, confirm `configs/default.yaml` still sets production F2/F3 limits to `0/0`, and inventory `runs/` without treating `_calibration` as formal evidence. Requirements: preregistered real-run admission and responsible-owner gate. Design: Context, Decisions 1/2/7, Migration step 1. Paths: archived M1-12 acceptance record, current OpenSpec status, default config, and run-directory inventory only. Verify: `openspec list --json`, targeted `rg` checks of the acceptance/defaults, and a read-only `find runs -mindepth 1 -maxdepth 1 -type d` record the exact baseline.
- [x] 1.2 Derive `experiments/m1-13-natural-failure-root-cause-study/00-implementation-brief.md` with exactly the §10.8 four sections: input artifacts and Schema references, output artifacts and acceptance commands, intended function signatures, and every governing design section; introduce no decision beyond the authoritative design and this change. Requirements: all revision-root-cause-study requirements. Design: all decisions and Migration step 1. Paths: the new implementation brief only. Verify: manual four-section audit against system design §10.8 and the citations in the delta spec.

## 2. Study protocol and record contracts

- [x] 2.1 Publish `01-preregistration.md` before any formal provider call, fixing one N=5 configuration group, exact frozen input/config hashes, sandbox digest, requested provider route/model slot and request parameters, per-run cache isolation, run-id convention, F2/F3=`0/0`, sample unit, inclusion/exclusion rules, finalized-`internal_error` whole-group invalidation, transient-interruption resume policy, returned-model-identity disclosure policy, and the stopping rule. Requirements: study admits only preregistered real-run evidence. Design: Decision 2. Paths: M1-13 preregistration and referenced frozen input/config files only. Verify: the audit script's protocol check passes before formal run admission and the file records five declared logical run identities without outcome-dependent conditions or an exact returned-model-name gate.
- [x] 2.2 Define the closed canonical shapes in `02-sample-manifest.json` for `real_runs`, `natural_failure_samples`, `excluded_runs`, and `synthetic_evidence`, including stable `(run_id, task_uid)` identities, configuration-group identity, zero/one/many observed returned model identities with call counts, retained interruption/resume evidence, classifications, rationales, trigger observations, and relative SHA-256 evidence references. Requirements: unique evidence-bound samples, tolerant real-run admission, evidence-class separation, and recomputable results. Design: Decisions 1/2/3/5. Paths: M1-13 manifest and audit script constants/validators. Verify: the dedicated M1-13 test module accepts a complete minimal manifest plus absent/aliased/varying model identities and rejects missing/extra normative fields.
- [x] 2.3 Define `05-parameter-recommendation.json` with exactly one row for each listed PQ-1/M1-13 parameter and closed `select`, `retain_current`, or `insufficient_evidence` decisions, optional proposed values, rationale, and result/sample references; do not add a production consumer. Requirements: parameter rationale remains evidence bound and non-enabling. Design: Decision 6. Paths: recommendation record and audit validator. Verify: focused tests reject missing parameters, duplicate parameters, unsupported decisions, uncited values, and any claim that mutates production configuration.

## 3. Narrow audit and recomputation script

- [x] 3.1 Implement the argument/parser and read-only `--check` entry point in `scripts/audit_root_causes.py`, confining `--study-root` and `--runs-root`, loading only the declared protocol/manifest/recommendation, and returning a controlled nonzero result for invalid input without adding a `nepa` CLI command. Requirements: deterministic recomputation and no public interface change. Design: Decision 4. Paths: M1-13 audit script and focused test module. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py -k 'arguments or confinement or malformed'` passes.
- [x] 3.2 Implement run-group admission checks for preregistration membership, finalized real-run identity, frozen requested input/config/route equivalence, F2/F3=`0/0`, observational model-identity disclosure, retained interruption/usage evidence, existing applicable artifact/hash validity, and complete-group invalidation only when a run actually finalizes `internal_error`; calibration, synthetic, injected, unfinalized, and undeclared runs remain outside the natural denominator. Missing, aliased, or varying returned model names must not reject or split an otherwise valid group. Apply the owner-authorized minimal provider correction that records the exact returned identity or absence in metadata without changing the public response contract. Requirements: preregistered real-run evidence and tolerant third-party identity/interruption handling. Design: Decision 2 and authorized implementation-entry correction. Paths: audit script run loader, provider stream adapter, focused provider tests, and temporary focused fixtures. Verify: the focused provider identity tests plus `uv run pytest -q tests/test_m1_13_root_cause_study.py -k 'run_admission or provider_identity or interruption or group_invalidation'` pass positive, absent/identity-variation, resume, exclusion, requested-config drift, finalized-internal-error, and selective-replacement cases.
- [x] 3.3 Implement sample verification over `(run_id, task_uid)`, terminal unsuccessful State, initial/active Plans, complete relevant F0/F1 attempt/lease history, diagnostics, build/smoke refs, workspace commit/tree, and reference hashes; reject duplicates, cross-run refs, successful/nonterminal tasks, and incomplete evidence. Requirements: unique evidence-bound samples. Design: Decision 3. Paths: audit script sample loader and focused fixtures. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py -k 'sample_identity or evidence_binding'` passes repeated-attempt, duplicate, cross-run, missing-ref, and hash-drift cases.
- [x] 3.4 Validate each classification as `planning_defect`, `local_implementation_defect`, or `indeterminate`, require a non-empty bounded rationale and decisive refs, require planning-defect rows to identify the necessary F2/F3-class boundary and applicable machine facts, and never promote Diagnoser prose or exhaustion alone to authority. Requirements: root-cause judgments. Design: Decision 3. Paths: audit script classification validator and focused fixtures. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py -k classification` passes both determinate categories, indeterminate handling, missing rationale, model-only assertion, and exhaustion-only rejection.
- [x] 3.5 Deterministically compute admitted/invalid/excluded run counts, natural/classified/indeterminate task counts, classification coverage, two-category proportions, per-configuration descriptive summaries, observed TR facts, and separate real/synthetic tables with stable ordering and no significance claims. Requirements: deterministic results and evidence separation. Design: Decisions 4/5. Paths: audit script and `03-root-cause-results.json` renderer. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py -k 'aggregate or denominator or synthetic_separation'` passes hand-calculated zero, mixed-category, indeterminate, duplicate, and small-N cases.
- [x] 3.6 Assemble each admitted run's existing M1 metric input package and call `nepa.metrics.compute_m1_metrics` without changing its formulas; preserve existing availability envelopes when evidence is unavailable and bind the returned projections into the machine results. Requirements: deterministic results. Design: Decision 4. Paths: M1-13 audit script and existing `nepa/metrics.py` as a read-only dependency. Verify: focused M1-13 fixtures compare exact expected metric projections; do not modify or run the general `nepa eval runs` surface.
- [x] 3.7 Implement deterministic JSON/Markdown rendering plus `--check`: generation writes canonical `03-root-cause-results.json` and the generated sections of `04-root-cause-report.md`, while check mode writes nothing and fails on any byte drift in results, tables, denominators, recommendation refs, or evidence hashes. Requirements: deterministic recomputation and parameter rationale. Design: Decisions 1/4/6. Paths: audit script and M1-13 results/report/recommendation files. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py -k 'deterministic or check_mode or drift'` proves two generations are byte-identical and check mode leaves the tree unchanged.

## 4. Focused audit test matrix

- [x] 4.1 Add `tests/test_m1_13_root_cause_study.py` with temporary positive fixtures covering a valid N=5 group, absent/aliased/varying returned model names, recoverable interrupted-and-resumed calls with retained usage, zero natural failures, both determinate categories, indeterminate coverage, multiple attempts for one task, separate synthetic evidence, all parameter decision states, and byte-stable regeneration. Requirements: all machine-verifiable capability requirements except the real owner decision and operational monitor. Design: Decisions 2-6. Paths: the dedicated test module and M1-13 audit script only. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py` passes.
- [x] 4.2 Add negative cases for undeclared/mixed requested configuration groups, nonzero revision limits, calibration/injected samples in the natural denominator, selective retention after a finalized `internal_error`, duplicate task identities, broken/cross-run refs, forced semantic classification, handwritten aggregate drift, incomplete parameter coverage, path escape, and a fabricated owner decision. Add an assertion that returned-model-name mismatch alone is never a rejection reason. Requirements: admission, tolerant identity handling, evidence binding, classification, aggregation, recommendation, and owner-gate requirements. Design: Decisions 2-7. Paths: dedicated M1-13 test module and audit script. Verify: `uv run pytest -q tests/test_m1_13_root_cause_study.py` exercises every named rejection/non-rejection and passes without running unrelated tests.

## 5. Preregistered formal real runs

- [x] 5.1 Run the audit preflight against the published preregistration and capture the exact frozen hashes, environment-variable names without secret values, sandbox digest, five logical run identities, requested provider route/model slot, and commands before provider I/O; do not require or predict an exact provider-returned model string. Stop and report unavailable required inputs or a design-contract conflict rather than changing `project_docs/` or runtime code. Requirements: preregistered real-run evidence and tolerant identity handling. Design: Decision 2 and Migration step 3. Paths: preregistration and implementation brief only. Verify: the audit protocol check succeeds and a manual diff confirms the preregistration predates every formal run artifact.
- [x] 5.2 When the primary `gpt-5.6-sol` agent starts the long real-call batch, spawn one `gpt-5.6-luna` subagent if available with no file ownership and an explicit read-only monitoring prompt: observe progress with bounded waits, stay quiet on unchanged state, and notify only on a completed run, persistent interruption, terminal failure, batch completion, or required user action. Do not delegate experiment launch/retry/resume, code work, root-cause analysis, classification, parameter decisions, or acceptance; if Luna is unavailable, continue without a monitor. Requirements: read-only non-authoritative progress monitoring. Design: Decision 8. Paths: no repository-owned path; operational agent coordination only. Verify: the primary agent records the monitor identity/scope in progress notes and independently checks every actionable notification against authoritative Run artifacts.
- [x] 5.3 Execute exactly the preregistered N=5 logical real runs with independent cache directories and production F2/F3 limits `0/0`. On a recoverable third-party disconnect, timeout, rate limit, 5xx, or similar interruption, retain call/usage evidence and resume the same run through the existing persisted path until it finalizes or existing hard budgets end; do not create a new sample or group merely because of the interruption. Record returned model identities exactly when supplied, including absence and multiple aliases, and do not extend the group after observing failures. Requirements: preregistered real-run evidence, tolerant provider handling, and read-only monitoring. Design: Decisions 2/8. Paths: raw evidence under `runs/` and the declared run inventory in the manifest. Verify: five declared logical runs finalize with matching requested inputs/config or remain explicitly pending for later resume; no endpoint interruption or returned-name mismatch alone creates an exclusion/replacement.
- [x] 5.4 If a formal run actually finalizes `internal_error`, record the entire group as invalid and execute a fresh complete N=5 group with the same preregistered requested configuration; otherwise admit the recovered/original group. Never delete the invalid group or splice valid-looking members across attempts. Requirements: whole-group invalidation after finalization. Design: Decision 2. Paths: raw runs plus `real_runs`/`excluded_runs` manifest rows. Verify: audit group admission accepts exactly one complete valid group or reports a persistent external/infrastructure blocker without misclassifying it as a root cause.
- [x] 5.5 Freeze the admitted-run inventory with run/config/returned-model-observation/interruption/artifact hashes and enumerate every terminal unsuccessful F0/F1 task exactly once; list zero samples truthfully if no such task exists. Requirements: admission and unique evidence-bound samples. Design: Decisions 2/3/5. Paths: `02-sample-manifest.json` and referenced raw runs. Verify: `uv run python experiments/m1-13-natural-failure-root-cause-study/scripts/audit_root_causes.py --study-root experiments/m1-13-natural-failure-root-cause-study --runs-root runs --check` passes admission and identity checks once generated outputs are present.

## 6. Classification, report, and parameter rationale

- [x] 6.1 Review every natural failure sample's complete evidence and record one evidence-backed `planning_defect`, `local_implementation_defect`, or `indeterminate` judgment; for planning defects, name the concrete F2/F3-class change and applicable trigger facts, and never classify from exhaustion or Diagnoser opinion alone. Requirements: root-cause judgments. Design: Decision 3. Paths: natural-failure rows in `02-sample-manifest.json` and referenced raw evidence. Verify: classification-focused audit passes and every admitted sample appears exactly once in coverage counts.
- [x] 6.2 Generate `03-root-cause-results.json` and `04-root-cause-report.md` from the frozen manifest, reporting run/task sample sizes, classification coverage, two-category proportions, model identity disclosure, real/synthetic separation, descriptive small-N limits, and every contributing run/sample id. Requirements: deterministic results. Design: Decisions 4/5. Paths: audit script, results, and report. Verify: generation followed by two read-only `--check` executions succeeds with byte-identical outputs.
- [x] 6.3 Complete all ten rows in `05-parameter-recommendation.json` from admitted real evidence, choosing `select`, `retain_current`, or `insufficient_evidence` with exact citations; if evidence is absent or insufficient, retain F2/F3=`0/0` and state explicitly that D1.14 and production enablement are not established. Requirements: evidence-bound non-enabling parameter rationale. Design: Decision 6. Paths: parameter recommendation and report only. Verify: recommendation-focused audit passes and `git diff` shows no change to `configs/default.yaml`, `nepa/config.py`, or another production consumer.

## 7. Focused technical acceptance and owner gate

- [x] 7.1 Run only `uv run pytest -q tests/test_m1_13_root_cause_study.py`, the study audit `--check`, `uv run ruff check` on the M1-13 script/test paths, `uv run mypy` on the M1-13 script, strict validation of this change, and `git diff --check`; record exact commands/results and explicitly state that repository-wide pytest and all-item OpenSpec validation were not run by user direction. Requirements: all machine-verifiable capability requirements. Design: Decision 7 and Migration step 4. Paths: M1-13 study/change/test diff and `acceptance-record.md`. Verify: every listed focused command is green; no unlisted test suite is invoked.
- [ ] 7.2 Obtain and record the responsible owner's explicit dated decision in `06-owner-decision.md` only after the new 65,536-token group is frozen and reported, naming hashes for every preregistration, admitted/excluded inventory, classification, result, report, parameter recommendation, limitation, and requested correction; leave this task unchecked until the real owner acts and do not synthesize approval. Requirements: responsible-owner acceptance. Design: Decisions 7/9. Paths: owner-decision file and M1-13 acceptance record only. Verify: the audit confirms the decision references the exact current reviewed package and the acceptance record describes its true scope.
- [ ] 7.3 After owner review, apply only requested M1-13 corrections, regenerate affected study outputs, and rerun the same focused commands from task 7.1; if reviewed hashes change, obtain a superseding owner decision rather than carrying approval forward. Requirements: responsible-owner acceptance and deterministic results. Design: Decision 7 and Migration step 5. Paths: only owner-requested M1-13 study/change/test files. Verify: focused acceptance is green and the final owner decision matches current hashes.
- [ ] 7.4 Inspect the complete final diff, remove unrelated or speculative work, run the focused validation one final time, and confirm no public CLI/API, dependency, `nepa/metrics.py`, production configuration change beyond `architecture_planner.max_tokens=65536`, exact returned-model-name admission gate, provider change beyond the explicitly authorized identity-observation correction, unbounded retry loop, monitoring-agent mutation/decision artifact, PlanReviser, M1-14/M1-15, TR-5/TR-9 enablement, S7/S8/M2, `project_docs/`, prompt/calibration lineage, archived-change, rewritten historical preregistration, or raw-run commit was added; confirm every requirement/task is closed before completion. Requirements: all change requirements and non-scope. Design: all decisions/risks and authorized corrections. Paths: complete final diff. Verify: final focused transcript, `git diff --check`, scope scans, and change-only strict validation pass; repository-wide pytest and `openspec validate --all --strict` remain intentionally unrun.

## 8. ArchitecturePlanner budget correction and new configuration group

- [x] 8.1 Set only `roles.architecture_planner.max_tokens` to `65536` in `configs/default.yaml` and the matching `_DEFAULT_ROLES` fallback in `nepa/config.py`; leave `tiers.T1.max_tokens=16000` and every other role unchanged. Requirements: bounded ArchitecturePlanner completion budget. Design: Decision 9. Paths: the two production configuration sources and `tests/test_config.py`. Verify: run only the focused config tests proving the ArchitecturePlanner route resolves `65536`, another T1 role resolves `16000`, and the YAML and fallback defaults agree.
- [x] 8.2 Preserve all existing 16K preregistrations, manifests, results, reports, and raw runs unchanged; publish a new study configuration and preregistration with new logical run ids, hashes, `architecture_planner.max_tokens=65536`, T1=`16000`, F2/F3=`0/0`, the existing N=5 stopping rule, and an explicit prohibition on pooling historical 16K outcomes. Requirements: preregistered real-run evidence and bounded ArchitecturePlanner completion budget. Design: Decisions 2/9. Paths: new M1-13 study configuration/preregistration records only. Verify: protocol preflight succeeds before Provider I/O and hashes distinguish the new group from every historical group.
- [ ] 8.3 Execute and freeze exactly the new preregistered N=5 group using the existing bounded resume and read-only Luna monitoring policy, classify any natural F0/F1 failures, and regenerate the manifest, results, report, and parameter recommendation from the new group without mixing 16K evidence into its denominator. Requirements: all study evidence requirements. Design: Decisions 2-9. Paths: new raw runs and M1-13 study outputs. Verify: the study audit admits exactly the new group and deterministically reproduces its outputs.
- [ ] 8.4 Run only the dedicated M1-13 tests, focused config tests, study audit `--check`, Ruff/mypy on changed Python files, strict validation of this change, and `git diff --check`; do not run repository-wide pytest or all-change OpenSpec validation. Requirements: all machine-verifiable requirements. Design: Decisions 7/9. Paths: current M1-13/config diff and acceptance record. Verify: every listed focused command is green and the acceptance record supersedes the pre-budget transcript without claiming owner approval.
