## 1. Entry Gates and Closed Contracts

- [ ] 1.1 Reverify the authoritative inputs before implementation: design 7.1.0, pipeline design 1.2.0, the archived M0/M1-4a1 records, current gold lints, and the existing `compile_delivery_constraints`/`ARCH_VALIDATE` code path; record any unverified premise and stop rather than editing design if a material conflict appears. (Requirements: all. Design: Context, Goals/Non-Goals. Paths: `project_docs/system_design.md`, `project_docs/pipeline_design_s4_s9.md`, `openspec/changes/archive/`, `nepa/speclib/delivery.py`, `nepa/speclib/architecture.py`, `gold_file/`. Verify: `openspec validate --all --strict`; the four existing `uv run nepa lint` gold commands; `git status --short`.)
- [ ] 1.2 Add closed draft-2020-12 Schemas and conforming minimal examples for the Delivery Blueprint, task shard/PlanDraftIR boundary, Plan v4, Plan State v1, and only the typed persisted reports/events actually required by this change; register them in the existing mutual-validation test without inventing future-controller fields or hashes. (Requirements: Plan and task-shard contracts are closed and state-free; Plan State Schema and snapshot lint enforce one unambiguous state; Complete Blueprint fields are deterministic and closed. Design: Decision 7. Paths: `nepa/schemas/`, `nepa/schemas/examples/`, `tests/test_schema_examples.py`. Verify: `uv run pytest -q tests/test_schema_examples.py`.)
- [ ] 1.3 Reconfirm the human-gate boundary as a separate admission record: M1-4b/M1-4b2 has no new owner-signature DoD, the existing M0 freeze and archived M1-4a2 owner approval must remain byte-unchanged, and no implementation agent may manufacture or mark a new approval; any newly discovered design decision requires explicit owner authorization before work continues. (Requirements: all and non-scope. Design: Context, Goals/Non-Goals, Migration Plan. Paths: `configs/m0-default-inputs.freeze.yaml`, `experiments/m1-4a2-patch-calibration-rerun/results/`, `openspec/changes/archive/2026-09-04-m1-4a2-patch-calibration-rerun/`. Verify: `git diff -- configs/m0-default-inputs.freeze.yaml experiments/m1-4a2-patch-calibration-rerun/results openspec/changes/archive/2026-09-04-m1-4a2-patch-calibration-rerun` is empty.)

## 2. M1-4b Delivery Constraints Completion

- [ ] 2.1 Complete the existing Delivery Constraints projection only where design 7.1.0 requires missing C99/server rule data: role support, six naming patterns, collision checks, per-key resource defaults/merge behavior, build variants, layout convention, mechanical input whitelist/template boundary, and protocol-neutral canonical output; preserve the single M1-4a1 compiler entry point and current callers. (Requirements: Planning index and architecture Delivery Constraints are deterministic shared artifacts; Complete Blueprint fields are deterministic and closed. Design: Decisions 1/7. Paths: `nepa/speclib/delivery.py`, applicable constraints Schema/example, `tests/test_delivery_constraints.py`, `tests/test_planning_protocol_neutrality.py`. Verify: `uv run pytest -q tests/test_delivery_constraints.py tests/test_planning_protocol_neutrality.py`.)

## 3. M1-4b2 Free-Layout Blueprint Transcription

- [ ] 3.1 Implement the one-to-one `layout.files[] → file_rules[]` transcriber in the existing Delivery Compiler, including exact field projection and the literal approved eight-row `(render_rule, class, contract_id presence, build_role) → (kind, producer)` table; reject every table-external tuple with no path, suffix, module, or protocol inference. (Requirements: Every declared layout file is transcribed exactly once; File kind and producer follow the approved complete derivation table. Design: Decision 2. Paths: `nepa/speclib/delivery.py`, `tests/test_delivery_blueprint.py`. Verify: `uv run pytest -q tests/test_delivery_blueprint.py -k 'file_rule or derivation_table'`.)
- [ ] 3.2 Implement closed literal/per-message/per-type path expansion using the existing normalized Spec ids, target-role message selection, UTF-8 ordering, and complete safety/uniqueness failures; test both MQTT and the existing non-MQTT application fixture without changing shared sources. (Requirements: Path expansion is closed, safe, and deterministic. Design: Decision 3. Paths: `nepa/speclib/delivery.py`, `tests/test_delivery_blueprint.py`, `tests/fixtures/non_mqtt_application/`, `tests/test_planning_protocol_neutrality.py`. Verify: `uv run pytest -q tests/test_delivery_blueprint.py tests/test_planning_protocol_neutrality.py -k 'expansion or blueprint or neutral'`.)
- [ ] 3.3 Transcribe `layout.build_graph` into deliverables, build artifacts, and link source sets; close entry/link references, artifact outputs, file-rule memberships, layout templates, and one-to-one mechanical generation ownership, then return one canonical complete Blueprint or one controlled error without partial publication. (Requirements: Layout build graph is transcribed into the required three segments; Complete Blueprint fields are deterministic and closed. Design: Decisions 1/3. Paths: `nepa/speclib/delivery.py`, Delivery Blueprint Schema/example, `tests/test_delivery_blueprint.py`. Verify: `uv run pytest -q tests/test_delivery_blueprint.py`; byte-compare repeated canonical outputs.)

## 4. M1-4b Deterministic Plan Linker

- [ ] 4.1 Implement PlanDraftIR normalization from one accepted architecture and exactly one task shard per work package, enforcing local-id/state-free boundaries and exact module/work-package/task file, contract, and responsibility memberships before global linking. (Requirements: Plan and task-shard contracts are closed and state-free; Linker closes contracts, ownership, responsibilities, and DAGs. Design: Decision 4. Paths: `nepa/speclib/plan.py`, task-shard/PlanDraftIR Schema/example, `tests/test_plan_linker.py`. Verify: `uv run pytest -q tests/test_plan_linker.py -k 'draft or membership or contract'`.)
- [ ] 4.2 Resolve unique task-ready contract providers, inject only proven provider/consumer and shard-local dependencies, reject illegal cross-package edges/cycles, and assign final `T-###` ids with the exact stable Kahn ready-queue ordering; rewrite refs, build variants, and requirement context refs deterministically while leaving M1-4d-owned task uid/migration-digest calculation unimplemented. (Requirements: Linker closes contracts, ownership, responsibilities, and DAGs; Final task identifiers use stable topological order. Design: Decision 4. Paths: `nepa/speclib/plan.py`, `tests/test_plan_linker.py`. Verify: `uv run pytest -q tests/test_plan_linker.py -k 'dependency or topology or identifier'`; permutation fixtures produce byte-identical linked graphs.)
- [ ] 4.3 Generate the complete requirement/test coverage matrix from final responsibilities, DAG, Test Manifest, and layer switches; choose each task gate's earliest legal convergence point, retain disabled/static mappings, keep task `acceptance.tests` empty before M2-0, and fail when readiness cannot converge. (Requirements: Coverage is generated from authoritative responsibilities and manifest metadata. Design: Decision 4. Paths: `nepa/speclib/plan.py`, `tests/test_plan_coverage.py`. Verify: `uv run pytest -q tests/test_plan_coverage.py`.)
- [ ] 4.4 Compile the Blueprint from the fully injected final task view, compute its canonical SHA-256 once, inject it and the three controller-supplied frozen refs into the candidate Plan, validate Plan v4, and emit deterministic link evidence without introducing a Blueprint/Plan hash cycle. (Requirements: Final Plan binds the exact Blueprint and frozen inputs without a hash cycle; Complete Blueprint fields are deterministic and closed. Design: Decisions 3/4/7. Paths: `nepa/speclib/plan.py`, `nepa/speclib/delivery.py`, Plan/Blueprint Schemas/examples, `tests/test_plan_linker.py`. Verify: `uv run pytest -q tests/test_plan_linker.py tests/test_delivery_blueprint.py`; repeated complete linking yields byte-identical Plan, Blueprint, and link report.)

## 5. Plan and Plan State Validation

- [ ] 5.1 Implement `plan_lint` basic and full levels with canonically ordered reports: basic closes Schema, refs, DAGs, responsibilities, contracts, coverage, manifest entries, and enabled derivation; full additionally recomputes constraints/Blueprint and enforces file partition, build variants, contract ancestry, readiness, budgets, and faithful layout transcription across S4-G0～S4-G6. (Requirements: Plan lint distinguishes basic shape from full S4 readiness. Design: Decision 5. Paths: `nepa/speclib/plan.py`, `tests/test_plan_lint.py`. Verify: `uv run pytest -q tests/test_plan_lint.py`.)
- [ ] 5.2 Implement side-effect-free Plan State snapshot and closed event-transition validation, including exact task set, attempt/status field table, dependency proof, commit/evidence inputs, and typed revision/lease event validation without implementing their controllers, persistence, locks, or ledger mutation. (Requirements: Plan State Schema and snapshot lint enforce one unambiguous state; State transitions are derived from the closed event table; Validation is deterministic and side-effect free. Design: Decision 6. Paths: `nepa/speclib/plan_state.py`, Plan State/event Schemas/examples, `tests/test_plan_state.py`. Verify: `uv run pytest -q tests/test_plan_state.py -k 'snapshot or transition'`.)
- [ ] 5.3 Implement execution-state lint over explicit read-only workspace/evidence/receipt inputs, checking commit ancestry and trailers, evidence content hashes and task/attempt identity, Plan acceptance, S5 anchors, and deterministic issue ordering without claiming these checks in JSON-only snapshot lint. (Requirements: Execution lint verifies external evidence separately from snapshot shape; Validation is deterministic and side-effect free. Design: Decision 6. Paths: `nepa/speclib/plan_state.py`, `tests/test_plan_state.py`. Verify: `uv run pytest -q tests/test_plan_state.py -k 'execution or evidence or commit'`.)

## 6. CLI, Neutrality, and Final Acceptance

- [ ] 6.1 Extend the existing Typer tree with `nepa lint plan`, explicit companion inputs for basic lint and `--run-dir` reconstruction for full lint; preserve structured report shape and exit codes 0/20/1, and do not add S4 publication or Plan State mutation behavior. (Requirements: Plan lint distinguishes basic shape from full S4 readiness. Design: Decision 5. Paths: `nepa/cli.py`, `tests/test_cli_contract.py`, `tests/test_plan_lint.py`. Verify: `uv run pytest -q tests/test_cli_contract.py tests/test_plan_lint.py -k 'plan or exit'`.)
- [ ] 6.2 Run the complete M1-4b/M1-4b2 acceptance: schema/example mutual validation; all focused compiler/linker/lint/state tests; legal eight-row and table-external mapping coverage; deterministic permutation/replay; MQTT/non-MQTT neutrality and source scan; four gold lints; full repository tests; strict current-change/all-OpenSpec validation; `git diff --check`; then inspect the complete diff and confirm no M1-4c controller, M1-4d/e version/revision infrastructure or task migration-digest calculation, S5/S6 behavior, M2 test binding, design edit, dependency, or unrelated refactor entered the change. (Requirements: all. Design: Risks/Trade-offs and Migration Plan. Paths: complete change diff. Verify: `uv run pytest -q`; the four existing `uv run nepa lint` gold commands; `openspec validate m1-4b-deterministic-plan-compiler-and-layout-transcriber --strict`; `openspec validate --all --strict`; `git diff --check`; `git diff --stat` and full diff review.)
