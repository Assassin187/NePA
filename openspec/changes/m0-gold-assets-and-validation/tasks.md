## 1. M0 foundations: schemas, scope, and deterministic lint boundary

- [x] 1.1 **[M0-1]** Transcribe the Chapter 5 structures into draft 2020-12 schemas for Spec IR, segments, run, spec review, merge decisions, summary, repair log, report, Test Bundle, and Target Profile; add one minimal valid example for every schema. **Requirements:** M0 artifact schemas have mutually valid minimal examples. **Design:** §5, §5.1, §5.2, §5.3, §5.4, §5.6, §5.6.5; §10.1 M0-1/D0.4. **Verify:** `uv run pytest tests -k schema_examples`.
- [ ] 1.2 **[M0-2, D0.5 — manual gate]** Create `configs/scope-mqtt-min.yaml` that exactly records the §7.1 server-only MQTT-min inclusion/exclusion boundary and prepare a scope-freeze record with date and confirmer fields. Obtain the responsible person's signature; do not have an agent sign or mark this gate complete. **Requirements:** Gold scope and input freezes require owner approval; Scope conflicts block gold acceptance without changing the design. **Design:** §7.1; §10.1 M0-2/D0.5. **Verify:** `uv run python -c 'from pathlib import Path; import yaml; yaml.safe_load(Path("configs/scope-mqtt-min.yaml").read_text())'` and responsible-person signature review.
- [ ] 1.3 **[M0-4]** Implement the deterministic `nepa lint spec` and `nepa lint target` commands plus unit tests: Spec IR §5.1.6 rules 1–3 and 5; closed two-field Target Profile validation; C99 language/role support; Spec-role subset when `--spec` is given; and positive/negative cases including the rejected `roles=["client","server"]` profile with `TARGET_ROLE_UNSUPPORTED`. Do not implement `plan_lint` or Blueprint closure checks. **Requirements:** Gold Spec IR and Target Profile are deterministically validated; M0 artifact schemas have mutually valid minimal examples. **Design:** §5.1.6, §5.6.5.1–§5.6.5.2; §10.1 M0-4/D0.3a. **Verify:** `uv run pytest tests -k 'spec_lint or target_lint' && uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json`.

## 2. M0 gold inputs: scope-aligned Spec IR and Target Profile

- [ ] 2.1 **[M0-3 — owner scope decision applied; validation pending]** The designated owner resolved the §7.1 conflict by cropping Will and username/password CONNECT content (`REQ-CONNECT-020`–`REQ-CONNECT-026`) and the related Test Bundle references without widening scope. Verify that every retained requirement has an OASIS section and key original quote and that all §7.1 inclusions are represented. **Requirements:** Scope conflicts block gold acceptance without changing the design; Gold Spec IR and Target Profile are deterministically validated. **Design:** §5.1, §5.1.4, §7.1; §10.1 M0-3. **Verify:** `uv run nepa lint spec gold_file/specIR.json` (must report 0 errors after the validator exists).
- [ ] 2.2 **[M0-3a]** Validate and retain the default Target Profile as exactly `roles=["server"]` and `language={"name":"C","version":"C99"}`, with no schema/version, backend, template, delivery, or test-contract fields; keep the caller-provided bytes unchanged. **Requirements:** Gold Spec IR and Target Profile are deterministically validated; Raw-byte preservation is limited to caller source assets. **Design:** §4.2, §5.6.5.1–§5.6.5.2, §7.1; §10.1 M0-3a/D0.3a. **Verify:** `uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json` and `uv run pytest tests -k target_lint`.

## 3. M0 Test Bundle metadata validation and the C99 sandbox

- [ ] 3.1 **[M0-5]** Implement `nepa lint test-bundle <path> [--spec <path>]` and its tests for the Test Bundle schema, unique nodeid, nodeid/layer agreement, REQ references, gate values, and built-in C99 build variants; make gold-mode Spec lint enforce MUST/MUST NOT coverage only from `gate in {task, s7_only}`. Do not create, collect, or execute the named tests and do not implement runner, oracle, workspace adapter, pytest marker registration, or a public test contract. **Requirements:** Test Bundle validation and canonicalization are M0-only metadata operations; Gold Spec IR and Target Profile are deterministically validated. **Design:** §5.1.6(4), §5.3–§5.3.2, §5.6.5.2; §10.1 M0-5. **Verify:** `uv run pytest tests -k test_bundle_lint` and `uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json`.
- [ ] 3.2 **[M0-5, D0.2]** After the owner has reconciled the scope conflict, rewrite only `gold_file/test_bundle.json` using the §5 canonical serializer (compact, sorted UTF-8, no trailing newline), then re-run its metadata lint. Preserve the raw bytes of `gold_file/specIR.json` and `gold_file/target.json`; do not canonicalize either. **Requirements:** Test Bundle validation and canonicalization are M0-only metadata operations; Raw-byte preservation is limited to caller source assets. **Design:** §5 general conventions, §5.3.2, §5.6.5.5; §10.1 M0-5/D0.2. **Verify:** `uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json`.
- [ ] 3.3 **[M0-7]** Build `docker/sandbox.Dockerfile`, record its immutable image digest, and verify the image supplies gcc, make, sanitizer runtime, Python with pytest, mosquitto/mosquitto-clients, and paho-mqtt with default network disabled. This check is tool availability only, not protocol execution. **Requirements:** Sandbox image supplies the declared M0 tool baseline. **Design:** §8.5, §5.6.5.2; §10.1 M0-7. **Verify:** `docker build --iidfile .m0-sandbox-image-id -f docker/sandbox.Dockerfile -t nepa-sandbox:m0 . && docker run --rm --network=none nepa-sandbox:m0 sh -lc 'gcc --version && make --version && python3 -c "import pytest, paho.mqtt.client" && mosquitto -h'`.

## 4. M0 acceptance validation and default-input freeze

- [ ] 4.1 **[M0-3/M0-4, D0.1]** Run the completed gold Spec IR lint and record its zero-error result. **Requirements:** Gold Spec IR and Target Profile are deterministically validated; Scope conflicts block gold acceptance without changing the design. **Design:** §5.1.6; §10.1 D0.1. **Verify:** `uv run nepa lint spec gold_file/specIR.json`.
- [ ] 4.2 **[M0-5, D0.2]** Confirm the canonical Test Bundle passes Schema, reference, coverage, and build-variant validation, and prove its on-disk bytes are canonical. **Requirements:** Test Bundle validation and canonicalization are M0-only metadata operations. **Design:** §5.3.2; §10.1 D0.2. **Verify:** `uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json`.
- [ ] 4.3 **[M0-4/M0-5, D0.3]** Confirm every gold `MUST`/`MUST NOT` requirement is covered by at least one Test Bundle case with gate `task` or `s7_only`; do not infer coverage from prospective test code. **Requirements:** Test Bundle validation and canonicalization are M0-only metadata operations; Gold Spec IR and Target Profile are deterministically validated. **Design:** §5.1.6(4), §5.3.2; §10.1 D0.3. **Verify:** `uv run nepa lint spec gold_file/specIR.json --gold --manifest gold_file/test_bundle.json`.
- [ ] 4.4 **[M0-3a/M0-4/M0-5, D0.3a]** Confirm the default Target Profile has exactly two fields, its server role is both in the gold Spec and supported by C99, the historical dual-role profile fails with `TARGET_ROLE_UNSUPPORTED`, and each manifest build variant is an exposed C99 variant. **Requirements:** Gold Spec IR and Target Profile are deterministically validated; Test Bundle validation and canonicalization are M0-only metadata operations. **Design:** §5.3.2, §5.6.5.1–§5.6.5.2; §10.1 D0.3a. **Verify:** `uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json && uv run pytest tests -k target_lint && uv run nepa lint test-bundle gold_file/test_bundle.json --spec gold_file/specIR.json`.
- [ ] 4.5 **[M0-1, D0.4]** Validate every M0 schema against its corresponding minimal example and record the result for future M1-8 CI adoption. **Requirements:** M0 artifact schemas have mutually valid minimal examples. **Design:** §5, §5.6.5; §10.1 D0.4. **Verify:** `uv run pytest tests -k schema_examples`.
- [ ] 4.6 **[M0-6, D0.6 — manual gate]** Only after M0-3, M0-3a, M0-5, and D0.1–D0.4 pass, calculate SHA-256 over the three final on-disk gold files and prepare the default-input freeze record (date, confirmer, three hashes, and Target Profile two-field check). Obtain the responsible person's signature; an agent must leave the gate pending and must not claim it complete. **Requirements:** Gold scope and input freezes require owner approval; Raw-byte preservation is limited to caller source assets. **Design:** §4.2, §5 general conventions, §5.3.2, §5.6, §5.6.5; §10.1 M0-6/D0.6. **Verify:** `sha256sum gold_file/specIR.json gold_file/target.json gold_file/test_bundle.json && uv run nepa lint target gold_file/target.json --spec gold_file/specIR.json` and responsible-person signature review.
